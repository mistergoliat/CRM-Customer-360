---
release: ACS-R1-04
title: Customer Identity Resolution + Onboarding
doc_id: release-acs-r1-04-customer-identity-onboarding
status: active
updated_at: 2026-07-13
current_task: ACS-R1-04-T08
next_task: ACS-R1-04-T09
blocked: true
owner: product
source_of_truth_for:
  - ACS-R1-04 release scope
  - ACS-R1-04 task queue
  - ACS-R1-04 definition of done
depends_on:
  - ../ACTIVE_RELEASE.md
  - ../ROADMAP.md
  - ../product/MVP_EXECUTION_MAP.md
  - ../CAPABILITY_MATRIX.md
  - ../product/autonomous-commerce-prd.md
  - ../architecture/adr/ADR-006-autonomous-planning-and-capability-governance.md
  - ../architecture/adr/ADR-008-customer-360-boundary.md
  - ../capabilities/customer-360-read-model.md
  - ../capabilities/customer-service-capability.md
  - ../integrations/customer-service-http-contract.md
  - ../data/customer-360-contract.md
  - ../data/customer-lifecycle-event-contract.md
  - ../data/customer-onboarding-identity-contract.md
  - ../data/customer-creation-linking-authority-contract.md
supersedes:
  - ACS-R1-02
tags:
  - release
  - product
---

# ACS-R1-04 - Customer Identity Resolution + Onboarding

## Objetivo

Permitir que un mensaje entrante de WhatsApp resuelva una identidad existente, conserve una identidad externa no resuelta cuando no exista coincidencia, detecte conflictos sin fusionar customers automaticamente, inicie onboarding minimo, cargue Customer 360 solo cuando la identidad esta resuelta y el proposito de onboarding lo autoriza (ver regla de acceso mas abajo), incorpore contexto historico al ciclo autonomo y persista el estado entre mensajes.

## Resultado esperado

- La identidad externa no resuelta, la identidad existente y el conflicto quedan representados de forma canonica.
- El onboarding minimo puede continuar entre mensajes.
- Customer 360 se usa como contexto, no como Customer Master. Se carga unicamente cuando: existe identidad resuelta; `contextAccess` (`none`/`commercial_history`/`validated_entity`) lo autoriza; no existe conflicto ni bloqueo temporal; y el proposito de onboarding activo habilita contexto comercial (`quote`/`purchase`). Un `customerId` resuelto por si solo nunca es suficiente para autorizar la carga.
- El estado persiste entre turnos sin depender de `n8n_*`.

## No objetivos

- Address Book operativo.
- Confirmacion de direccion.
- Quote.
- Shipping.
- Checkout.
- Voice.
- Nuevas pantallas.
- Merge automatico de customers.
- Redisenio de Customer 360.

## Required reading

- [Autonomous Commerce PRD](../product/autonomous-commerce-prd.md)
- [ROADMAP](../ROADMAP.md)
- [MVP execution map](../product/MVP_EXECUTION_MAP.md)
- [ACTIVE_RELEASE](../ACTIVE_RELEASE.md)
- [Customer onboarding and identity contract](../data/customer-onboarding-identity-contract.md)
- [Customer creation, linking and interest authority contract](../data/customer-creation-linking-authority-contract.md)
- [Customer Service capability](../capabilities/customer-service-capability.md)
- [Customer Service HTTP contract](../integrations/customer-service-http-contract.md)
- [CAPABILITY_MATRIX](../CAPABILITY_MATRIX.md)

## ADRs aplicables

- [ADR-006 - Autonomous planning and capability governance](../architecture/adr/ADR-006-autonomous-planning-and-capability-governance.md)
- [ADR-008 - Customer 360 boundary](../architecture/adr/ADR-008-customer-360-boundary.md)

## Contratos aplicables

- [customer-onboarding-identity-contract](../data/customer-onboarding-identity-contract.md)
- [customer-creation-linking-authority-contract](../data/customer-creation-linking-authority-contract.md)
- [customer-360-contract](../data/customer-360-contract.md)
- [customer-lifecycle-event-contract](../data/customer-lifecycle-event-contract.md)
- [customer-service-capability](../capabilities/customer-service-capability.md)
- [customer-service-http-contract](../integrations/customer-service-http-contract.md)

## Tareas

| ID | Tarea | Estado | Dependencias | Evidencia de cierre |
| -- | ----- | ------ | ------------ | ------------------- |
| ACS-R1-04-T01 | Definir contrato de onboarding e identidad | done | ACTIVE_RELEASE, PRD, CAPABILITY_MATRIX, ADRs y contratos relacionados | [customer-onboarding-identity-contract](../data/customer-onboarding-identity-contract.md) |
| ACS-R1-04-T02 | Implementar resolucion por `wa_id` y telefono normalizado | done | ACS-R1-04-T01 | [lib/domains/customer-identity](../../lib/domains/customer-identity), [tests/domains/customerIdentity.test.ts](../../tests/domains/customerIdentity.test.ts) |
| ACS-R1-04-T02.1 | Corregir clasificacion de input y resolucion telefonica canonica | done | ACS-R1-04-T02 | [lib/domains/customer-identity](../../lib/domains/customer-identity), [lib/integrations/customer-external-identity/repository.ts](../../lib/integrations/customer-external-identity/repository.ts), [tests/domains/customerIdentity.test.ts](../../tests/domains/customerIdentity.test.ts) |
| ACS-R1-04-T03 | Persistir onboarding multi-turno | done | ACS-R1-04-T02.1 | [lib/domains/customer-onboarding](../../lib/domains/customer-onboarding), [migrations/023_crm_customer_onboarding_state.sql](../../migrations/023_crm_customer_onboarding_state.sql), [tests/domains/customerOnboarding.test.ts](../../tests/domains/customerOnboarding.test.ts) |
| ACS-R1-04-T03.1 | Validar migracion canonica y preservar invariantes de persistencia | done | ACS-R1-04-T03 | [migrations/023_crm_customer_onboarding_state.sql](../../migrations/023_crm_customer_onboarding_state.sql), [tests/domains/customerOnboarding.test.ts](../../tests/domains/customerOnboarding.test.ts) (tests 10-11) |
| ACS-R1-04-T04 | Definir reglas de creacion y vinculacion canonica | done | ACS-R1-04-T03.1 | [customer-creation-linking-authority-contract](../data/customer-creation-linking-authority-contract.md) |
| ACS-R1-04-T04.1 | Implementar Customer Service Port y politicas de creacion/vinculacion | done | ACS-R1-04-T04 | [lib/domains/customer-service](../../lib/domains/customer-service), [lib/integrations/customer-service/http-adapter.ts](../../lib/integrations/customer-service/http-adapter.ts), [tests/domains/customerService.test.ts](../../tests/domains/customerService.test.ts), [tests/integrations/customerServiceHttpAdapter.test.ts](../../tests/integrations/customerServiceHttpAdapter.test.ts) |
| ACS-R1-04-T05 | Incorporar Customer 360 al contexto autonomo | done | ACS-R1-04-T04.1 | [lib/brain/commercial/context/autonomousCustomerContext.ts](../../lib/brain/commercial/context/autonomousCustomerContext.ts), [lib/brain/commercial/context/loadAutonomousCustomerContext.ts](../../lib/brain/commercial/context/loadAutonomousCustomerContext.ts), [lib/domains/customer-360/service.ts](../../lib/domains/customer-360/service.ts), [tests/commercial/autonomousCustomerContext.test.ts](../../tests/commercial/autonomousCustomerContext.test.ts), [tests/commercial/loadAutonomousCustomerContext.test.ts](../../tests/commercial/loadAutonomousCustomerContext.test.ts), [tests/commercial/runNativeAutonomousCycleCustomer360.test.ts](../../tests/commercial/runNativeAutonomousCycleCustomer360.test.ts), [tests/commercial/multiRequestCustomer360.test.ts](../../tests/commercial/multiRequestCustomer360.test.ts), [tests/commercial/customer360AutonomousBoundary.test.ts](../../tests/commercial/customer360AutonomousBoundary.test.ts) |
| ACS-R1-04-T06 | Conectar identidad y onboarding al inbound nativo | done | ACS-R1-04-T05 | [lib/brain/commercial/native-cycle/customer-session](../../lib/brain/commercial/native-cycle/customer-session), [lib/brain/commercial/capability-gateway/customerIdentityCapabilities.ts](../../lib/brain/commercial/capability-gateway/customerIdentityCapabilities.ts), [tests/commercial/customerSession.test.ts](../../tests/commercial/customerSession.test.ts), [tests/commercial/customerSessionCustomer360Gate.test.ts](../../tests/commercial/customerSessionCustomer360Gate.test.ts), [tests/commercial/customerIdentityCapabilityGateway.test.ts](../../tests/commercial/customerIdentityCapabilityGateway.test.ts), [tests/commercial/createCustomerCapability.test.ts](../../tests/commercial/createCustomerCapability.test.ts), [tests/commercial/linkExternalIdentityCapability.test.ts](../../tests/commercial/linkExternalIdentityCapability.test.ts), [tests/commercial/customerSessionPrivacy.test.ts](../../tests/commercial/customerSessionPrivacy.test.ts) |
| ACS-R1-04-T06.1 | Completar activacion y captura multi-turno del onboarding | done | ACS-R1-04-T06 | [lib/brain/commercial/native-cycle/customer-session/runCustomerOnboardingPostPlanStage.ts](../../lib/brain/commercial/native-cycle/customer-session/runCustomerOnboardingPostPlanStage.ts), [lib/brain/commercial/native-cycle/customer-session/extractCustomerOnboardingFields.ts](../../lib/brain/commercial/native-cycle/customer-session/extractCustomerOnboardingFields.ts), [lib/brain/commercial/native-cycle/buildOnboardingGroundedMessage.ts](../../lib/brain/commercial/native-cycle/buildOnboardingGroundedMessage.ts), [tests/commercial/customerOnboardingPostPlanStage.test.ts](../../tests/commercial/customerOnboardingPostPlanStage.test.ts), [tests/commercial/extractCustomerOnboardingFields.test.ts](../../tests/commercial/extractCustomerOnboardingFields.test.ts), [tests/commercial/customerOnboardingPostPlanRuntime.test.ts](../../tests/commercial/customerOnboardingPostPlanRuntime.test.ts), [tests/commercial/customerOnboardingPostPlanPrivacy.test.ts](../../tests/commercial/customerOnboardingPostPlanPrivacy.test.ts) |
| ACS-R1-04-T06.2 | Reconciliar el inbound de identidad posterior a PR #43 | done | ACS-R1-04-T06.1 | [lib/brain/native-whatsapp/service.ts](../../lib/brain/native-whatsapp/service.ts) (`resolveOrPersistNativeExternalIdentity`), [migrations/024_reconcile_unresolved_customer_external_identity.sql](../../migrations/024_reconcile_unresolved_customer_external_identity.sql), [scripts/migration-manifest.ts](../../scripts/migration-manifest.ts), [tests/native/identity-conflict.test.ts](../../tests/native/identity-conflict.test.ts), [tests/native/nativeInboundIdentityBoundary.test.ts](../../tests/native/nativeInboundIdentityBoundary.test.ts), [tests/scripts/migrationManifest.test.ts](../../tests/scripts/migrationManifest.test.ts) |
| ACS-R1-04-T07 | Persistir executions, outcomes y advertencias | done | ACS-R1-04-T06.2 | [lib/brain/commercial/events](../../lib/brain/commercial/events) (4 nuevos `CommercialEventType`), [lib/brain/commercial/capability-gateway/identityCapabilityOutcome.ts](../../lib/brain/commercial/capability-gateway/identityCapabilityOutcome.ts), [lib/brain/commercial/native-cycle/customer-session/identityAuditEvents.ts](../../lib/brain/commercial/native-cycle/customer-session/identityAuditEvents.ts), [tests/commercial/customerIdentityAuditEvents.test.ts](../../tests/commercial/customerIdentityAuditEvents.test.ts), [tests/commercial/identityCapabilityGatewaySummaries.test.ts](../../tests/commercial/identityCapabilityGatewaySummaries.test.ts) |
| ACS-R1-04-T08 | Ejecutar pruebas end-to-end: nuevo, antiguo y conflicto | blocked (E2E en verde; falta verificacion operacional real - gap estructural resuelto en T08.1) | ACS-R1-04-T07 | [tests/e2e/customerIdentityOnboarding.e2e.test.ts](../../tests/e2e/customerIdentityOnboarding.e2e.test.ts) (13 tests), [tests/domains/customerIdentity.test.ts](../../tests/domains/customerIdentity.test.ts) (regresion), [lib/domains/customer-identity/local-adapter.ts](../../lib/domains/customer-identity/local-adapter.ts) (bug fix) |
| ACS-R1-04-T08.1 | Reconciliar `customerMasterId` entre Customer Service y ACS, incl. recuperacion runtime | done | ACS-R1-04-T08 | [lib/domains/customer-service/customerMasterProjection.ts](../../lib/domains/customer-service/customerMasterProjection.ts), [lib/brain/commercial/native-cycle/customer-session/onboardingTransitions.ts](../../lib/brain/commercial/native-cycle/customer-session/onboardingTransitions.ts) (`completeOnboardingWithVerifiedCustomer`, `verifyCustomerMasterProjection`, `ensureResolving`), [lib/brain/commercial/native-cycle/customer-session/resolveNativeCustomerSession.ts](../../lib/brain/commercial/native-cycle/customer-session/resolveNativeCustomerSession.ts), [lib/integrations/customer-service/http-adapter.ts](../../lib/integrations/customer-service/http-adapter.ts), [lib/brain/commercial/events/types.ts](../../lib/brain/commercial/events/types.ts) (`retry_resolution`), [tests/commercial/customerMasterProjectionGate.test.ts](../../tests/commercial/customerMasterProjectionGate.test.ts), [tests/e2e/customerIdentityOnboarding.e2e.test.ts](../../tests/e2e/customerIdentityOnboarding.e2e.test.ts) ("T08-A6", "T08-A7") |
| ACS-R1-04-T09 | Auditoria de aceptacion y cierre | ready (no iniciada) | ACS-R1-04-T08 | pending |

## Tarea actual

`ACS-R1-04-T08`

## Definition of Done de la tarea actual

`ACS-R1-04-T08` debe ejecutar pruebas end-to-end reales del flujo de identidad/onboarding (`T06`/`T06.1`, instrumentado por `T07`) para los tres escenarios canonicos: cliente nuevo (sin match, onboarding completo, `create_customer`), cliente antiguo (resuelto por `wa_id`/telefono, sin duplicar), y conflicto (senales contradictorias, sin auto-seleccionar). No reabre autoridad ni la frontera de Customer 360.

## Siguiente tarea

`ACS-R1-04-T09` (no iniciada - `ACS-R1-04-T08` no quedo completamente aceptada, ver Bloqueos; `ACS-R1-04-T08.1` cerro el gap estructural que bloqueaba una parte de T08, pero el bloqueo operacional - smoke contra Customer Service desplegado - permanece)

## Bloqueos

- `ACS-R1-04-T08` queda `blocked: true`: no existe un Customer Service desplegado (productivo ni sandbox) contra el cual ejecutar el smoke operacional que exige la Definition of Done de esta tarea - `CUSTOMER_SERVICE_BASE_URL`/`CUSTOMER_SERVICE_API_KEY` estan vacios en `.env.example`, sin configuracion real en el repo ni en el entorno. La suite E2E completa (`tests/e2e/customerIdentityOnboarding.e2e.test.ts`, 14 tests) corre en verde recorriendo la cadena real (`processNativeWhatsAppInbound` -> `runNativeAutonomousCycle` -> `resolveNativeCustomerSession` -> `CustomerOnboardingService` -> Capability Gateway -> adapter HTTP -> `commercial_event` T07) contra un servidor HTTP local controlado que implementa el contrato real (nunca un mock de `executeGovernedCapability`) y contra la cadena de migraciones completa (`001`-`024`) sobre una base `crm_test` desechable, verificada dos veces (cadena completa, luego `skip` completo). Eso certifica integracion, no operacion real: `resolve_customer`/`create_customer`/`link_external_identity` permanecen `operational: not_verified` en `CAPABILITY_MATRIX.md`. El gap arquitectonico que T08 encontro (reconciliacion de IDs entre Customer Service y `master_customer`) **ya quedo resuelto en `ACS-R1-04-T08.1`** (ver entrada abajo), incluyendo la recuperacion runtime real de un onboarding `temporarily_unavailable` en un turno posterior - el unico bloqueo restante de T08 es la falta de un Customer Service desplegado para el smoke operacional real. Ver "Deudas fuera del incremento", entradas `ACS-R1-04-T08` y `ACS-R1-04-T08.1`, para el detalle completo. T08 tambien encontro y corrigio un bug real (identidad externa no resuelta tratada como candidato valido). El bloqueo de `PR #43`/commit `3222003` sobre la ruta de inbound de identidad se resolvio en `ACS-R1-04-T06.2` (ver "Deudas fuera del incremento").

## Deudas fuera del incremento

- `ACS-R1-01` conserva deuda de hardening del capability gateway.
- `ACS-R1-03` conserva deuda de acceptance formal y cierre de auditoria.
- Address Book, Quote, Policy, Shipping, Checkout y Voice quedan fuera de este incremento.
- `ACS-R1-04-T02.1` corrigio el alcance de `CustomerIdentityResolutionService`: la resolucion por `wa_id` (`provider + external_id` exacto) sigue scoped a `provider = channel`; la resolucion por telefono ahora busca en `customer_external_identity` **sin** filtrar por provider (telefono historico registrado por cualquier canal), via `findDistinctCustomersByNormalizedValueAcrossProviders`. Fuentes de telefono revisadas y descartadas conscientemente: `customer_addresses.recipient_phone` (es contacto de despacho, no identidad verificada del titular) y `ps_customer` de Prestashop (no existe bridge verificado entre `ps_customer.id_customer` y `master_customer.id` en este repo - ningun writer crea filas `customer_external_identity` con `provider = 'prestashop'`). Un input invalido ahora es `invalid_input`, distinto de `identification_required`. Sigue sin estar conectado al inbound nativo, al Gateway ni a Customer 360 (eso es T04-T06).
- `ACS-R1-04-T03` agrego `crm_customer_onboarding_state` (migration 023) y el dominio `lib/domains/customer-onboarding` como persistencia canonica de `CustomerOnboardingState` (contrato seccion 11), con maquina de estados, normalizacion y optimistic locking por `version`. El contrato **si** permite persistir `firstName`, `lastName`, `email` y `orderReference` - son exactamente los campos de `CustomerOnboardingCollectedData` (seccion 11) - y esta tabla los guarda dentro de `collected_json`, tal como el dominio los normaliza (`lib/domains/customer-onboarding/service.ts`). La tabla legacy `crm_customer_onboarding` (P1M/local-ai-sdr, migration 007) se revisó y se descarto como fuente canonica por incompatibilidades reales: usa `conversation_case_id` (no `conversation.id`), un enum de estado incompatible, no tiene `purpose`, no tiene `version` para optimistic locking, y ademas de exponer `firstname`/`lastname`/`email` como columnas planas de nivel superior (en vez de `collected_json`), persiste `last_response_text`, `context_json` y `warnings_json` - datos que el contrato si prohibe en esta tabla (mensajes, prompts, payloads arbitrarios; seccion 12). Se dejo intacta, sin dual-write ni fallback. T03 no conecta el dominio al inbound, al LLM, al Gateway, a Customer 360 ni a operaciones de escritura sobre customers (eso sigue siendo T04-T06); no expone un `saveAnyState` generico, solo las transiciones explicitas del contrato.
- `ACS-R1-04-T03.1` corrigio la FK de `customer_id` en `crm_customer_onboarding_state` de `ON DELETE SET NULL` a `ON DELETE RESTRICT`: la invariante de `completed` (contrato seccion 11 y 14) exige un `customerId` valido, y `SET NULL` permitia que borrar el `master_customer` referenciado dejara una fila `completed` con `customer_id = NULL`, violando esa invariante silenciosamente. Confirmado con un test DB-backed que demuestra que el `DELETE` es rechazado (`tests/domains/customerOnboarding.test.ts`, "integration 10"). Tambien se intento un `CHECK (status <> 'completed' OR customer_id IS NOT NULL)`, pero MariaDB 11.4 lo rechaza con error 1901 ("Function or expression 'customer_id' cannot be used in the CHECK clause") porque `customer_id` ya participa en un FOREIGN KEY - confirmado por reproduccion directa (un CHECK aislado sobre esa columna funciona; agregar el FK, en el mismo `CREATE TABLE` o en un `ALTER TABLE` posterior, hace fallar el mismo CHECK). La invariante queda protegida por el FK (`RESTRICT`, arriba) mas la capa de dominio (`completeOnboarding` en `lib/domains/customer-onboarding/service.ts` es el unico camino que fija `status = 'completed'` y exige `customerId` no vacio). Detalle completo en el comentario de cabecera de [migrations/023_crm_customer_onboarding_state.sql](../../migrations/023_crm_customer_onboarding_state.sql).
- `ACS-R1-04-T03.1` valido la cadena canonica `001 -> 023` completa desde una base MariaDB 11.4 genuinamente vacia (contenedor Docker nuevo, sin volumen previo, inicializado con `infra/mariadb/init/*` sin modificar). Hallazgo: el comando documentado `npm run db:migrate -- --database=dev`, ejecutado tal cual contra esa base vacia, **falla** en el primer `CREATE TABLE IF NOT EXISTS schema_migrations` con `CREATE command denied to user 'crm_app'`. La causa no es el SQL de ninguna migracion (011 incluida): es un bug de precedencia de alias en `lib/database-config.ts` (`resolveWithAlias`), que arma la lista de candidatos como `[...aliases, key]` y por lo tanto revisa el alias generico (`DB_USER`) *antes* que la clave especifica (`MIGRATION_DATABASE_USER`); como `infra/.env` define ambos, `DB_USER=crm_app` (solo DML) siempre gana sobre `MIGRATION_DATABASE_USER=crm_dev_admin` (con DDL) para las conexiones `migration`/`test`/`legacy`. Confirmado con un diagnostico directo (`resolveNamedDatabaseConnection("migration")` devuelve `user: "crm_app"` pese a que `process.env.MIGRATION_DATABASE_USER` es `crm_dev_admin`). Esto solo se manifiesta contra una base realmente nueva: el contenedor de desarrollo compartido (`crm-customer-360-mariadb`) tenia un grant de `crm_app` sobre `main_management` mas amplio que el que otorgan hoy los scripts de init (heredado de una version anterior, ya que `docker-entrypoint-initdb.d` solo corre una vez), lo que enmascaraba el bug. Usando las credenciales correctas (sin modificar ninguna migracion ni el runner - solo un script de diagnostico desechable que invoca la misma logica de aplicacion/checksum con credenciales explicitas), la cadena `001 -> 023` completa (22 archivos; no existe `012`) aplica en orden, sin conflictos de checksum, crea `crm_customer_onboarding_state` con sus FKs/indices/unique esperados, y una repeticion de la corrida resulta en `[skip]` para los 22 archivos. La suite completa corrida contra esa base recien migrada paso **799/800** (unico fallo: `tests/commercial/capabilityGateway.test.ts`, "search_products executes over HTTP...", que referencia IDs de fixture hardcodeados (`opportunityId: 7`) inexistentes en una base sin seed - no relacionado con el dominio de onboarding ni con ninguna migracion de este release). El contenedor de desarrollo compartido se dejo exactamente como estaba (se detuvo y se reinicio sin tocar su volumen); sigue con `schema_migrations` detenido en `011_commercial_event.sql` por el mismo problema de base contaminada documentado originalmente en T03. Fix sugerido (no aplicado, fuera de alcance de T03.1): en `resolveWithAlias` (`lib/database-config.ts`), revisar `key` antes que `aliases` (candidatos `[key, ...aliases]`), para que las claves especificas (`MIGRATION_DATABASE_USER`, `TEST_DATABASE_USER`, etc.) tengan precedencia sobre los alias genericos (`DB_USER`) en vez de ser eclipsadas por ellos.
- **[RESUELTO en `ACS-R1-04-T06.2`, ver entrada debajo]** El commit `3222003` (PR #43, rama `feature/customer-identity-onboarding`, mergeado a `develop`) aterrizo fuera de la secuencia `ACS-R1-04-T01`-`T09` (sin ID de tarea, no aparece en la tabla de Tareas) y **rompio en runtime la ruta de primer contacto del inbound nativo de WhatsApp**, verificado por ejecucion real, no solo por lectura de codigo:
  1. `migrations/022_customer_identity_onboarding.sql` colisiona de version con `migrations/022_crm_capability_executions.sql` (ambos extraen version `"022"` del prefijo numerico; `schema_migrations` tiene `UNIQUE KEY uq_schema_migrations_version`). Confirmado corriendo `npm run db:migrate -- --database=dev`: falla con `Duplicate entry '022' for key 'uq_schema_migrations_version'` **despues** de que el DDL del archivo ya se ejecuto (MariaDB autocommitea cada sentencia) - la migracion queda con efecto pero sin registrar en `schema_migrations`, y reintentara (y volvera a fallar, esta vez por columnas duplicadas) en cada corrida futura de `db:migrate` hasta que se renumere.
  2. El doc [customer-identity-onboarding](../product/customer-identity-onboarding.md) documenta `BRAIN_IDENTITY_ONBOARDING_ENABLED=false` como flag temporal de rollout. **No existe tal lectura en ningun `.ts` del repo** (grep completo sin resultados) - no hay flag real, el comportamiento nuevo corre siempre.
  3. `resolveOrCreateNativeCustomer` (`lib/brain/native-whatsapp/service.ts`), que T06 declaro explicitamente "sin tocar", fue modificado: ya no crea `master_customer` provisional para un remitente nuevo sin match (objetivo correcto, ver [provisional-customer-remediation](../product/provisional-customer-remediation.md)), pero ahora llama sin condicion a `persistCustomerOnboardingState` (`lib/brain/commercial/customer-onboarding/state.ts`), que escribe en la tabla **legacy** `crm_customer_onboarding` (migration 007, P1M/local-ai-sdr) que el propio debt de `T03` (mas abajo en este documento) dijo dejar intacta "sin dual-write ni fallback". Hoy se escribe en paralelo a la tabla canonica `crm_customer_onboarding_state` (T03, migration 023) en cada turno.
  4. Con el schema sin parchar (estado tipico de una DB que corrio `db:migrate` antes de este commit), el nuevo codigo revienta `processNativeWhatsAppInbound` con `ER_BAD_NULL_ERROR: Column 'customer_id' cannot be null` para **cualquier contacto de WhatsApp nuevo sin identidad previa** - el caso mas comun del inbound. Con el schema parchado (columna `customer_id` de `customer_external_identity` ya nullable), la suite dirigida de `T06`/`T06.1` (`tests/native/identity-conflict.test.ts`, `tests/commercial/customerSession*.test.ts`, `tests/commercial/customerOnboardingPostPlan*.test.ts`) pasa de los ~10 fallos documentados y ya conocidos a **37 fallos / 138 pass**, la mayoria con `"seeding must resolve a real customerId"` - los fixtures de esas tareas asumen que la resolucion de un remitente nuevo sigue creando/vinculando un customer real, y ya no es asi.
  - Pendiente antes de escribir cualquier codigo de `T07` (que instrumenta exactamente esta ruta): decidir si el nuevo comportamiento de `resolveOrCreateNativeCustomer` es el correcto (en cuyo caso hay que actualizar los tests y la documentacion de `T06`/`T06.1` que asumian lo viejo) o si necesita revisión; renumerar `022_customer_identity_onboarding.sql` a un numero libre (`024`, ya que `023` esta tomado) y reconciliar `schema_migrations` en los entornos donde el DDL ya corrio a medias; y decidir si `crm_customer_onboarding` (legacy) sigue recibiendo writes del inbound nativo o si eso fue un error de integracion.
- `ACS-R1-04-T06.2` resolvio el bloqueo de arriba conservando el fix correcto de PR #43 (ningun WhatsApp desconocido crea `master_customer`) y eliminando la arquitectura paralela que lo acompañaba. Decision tomada: el nuevo comportamiento de la funcion (renombrada `resolveOrPersistNativeExternalIdentity`) es el correcto y pasa a ser la version fijada del contrato; los tests/fixtures de `T06`/`T06.1` que asumian creacion automatica de customer se corrigieron para sembrar el customer explicitamente (como ya hacia `tests/native/identity-conflict.test.ts`), en vez de relajar la invariante nueva.
  Cambios de codigo: `lib/brain/native-whatsapp/service.ts` - `resolveOrCreateNativeCustomer` renombrada a `resolveOrPersistNativeExternalIdentity`, devuelve `status: "identified" | "unresolved" | "conflict" | "temporarily_unavailable"` en vez de un booleano `created` (nunca `customerCreated`, solo `externalIdentityCreated`); se elimino el import y la llamada a `persistCustomerOnboardingState` dentro de `processNativeWhatsAppInbound` (dual-write a la tabla legacy `crm_customer_onboarding`) - el inbound ya no inicia, persiste ni completa onboarding, eso sigue siendo exclusivo de `resolveNativeCustomerSession`/`CustomerOnboardingService` dentro de `runNativeAutonomousCycle`.
  Se elimino por completo el dominio paralelo `lib/domains/customer-identity-onboarding/**` (611+76+2 lineas: `findMasterCustomerByEmail`, `getIdentityStatus`, `linkExternalIdentity`, `recordCustomerCreationConsent`, `createCustomerFromAuthorizedOnboarding`) junto con sus consumidores nuevos: las read capabilities `find_customer_by_email`/`get_identity_status` y el archivo `lib/brain/commercial/capabilities/mutations.ts` (contratos de mutacion paralelos a `create_customer`/`link_external_identity`/`record_customer_interest` ya gobernados por el Capability Gateway), los 4 `request-definitions` nuevos (`customer_identification`, `customer_registration`, `delivery_address_selection`, `delivery_address_confirmation` - los dos ultimos son Address Book, fuera de alcance de este release) y sus `REQUEST_EVENT_TYPES`. Los archivos legacy que PR #43 habia rewireado hacia ese dominio (`lib/brain/tools/customers/create-customer.ts`, `lookup-customer-by-email.ts`, y el orquestador/estado de `lib/brain/commercial/customer-onboarding/` que sirve al subsistema P1M/local-ai-sdr, no a ACS) se restauraron a su implementacion previa a PR #43 - siguen funcionando, sin depender del dominio eliminado. El guard de email artificial que PR #43 agrego a `lib/integrations/customer-master/customer-repository.ts` (`createMasterCustomer`/`findMasterCustomerByEmail`) tambien se revirtio: bloqueaba cualquier email `.invalid`, no solo el patron `wa-<phone>@local.invalid` del inbound, y rompia fixtures de test preexistentes de `T02.1`/`T03` que usan `.invalid` legitimamente (RFC 2606); la invariante real ("el inbound no fabrica ese email") ya queda garantizada en la fuente, dentro de `resolveOrPersistNativeExternalIdentity`, sin necesidad de un guard generico en el repositorio compartido (`lib/domains/customers/email.ts` y la validacion de payload HTTP en `lib/domains/customers/validation.ts`, ambas en la frontera correcta, se mantuvieron).
  Migracion: `migrations/022_customer_identity_onboarding.sql` se elimino y se reemplazo por `migrations/024_reconcile_unresolved_customer_external_identity.sql` (`023` ya estaba tomado). Se agrego `scripts/migration-manifest.ts` (`validateMigrationManifest`, con test unitario sin DB) y se lo conecto a `scripts/db-migrate.ts` para que el runner valide el manifiesto completo (sin version numerica duplicada) antes de tocar cualquier base de datos - no solo despues de que el DDL de un archivo colisionado ya corrio. La migracion 024 es fisicamente convergente: aplica limpio en una base nueva (`crm_test`) y tambien en la base local que ya tenia el DDL original parcialmente aplicado (columna `customer_id` de `customer_external_identity` ya nullable, columnas de `crm_customer_onboarding` ya agregadas) via `ADD COLUMN IF NOT EXISTS`/`CREATE INDEX IF NOT EXISTS`; esas columnas de `crm_customer_onboarding` quedan documentadas como compatibilidad inerte, sin writer de ACS, no reemplazan `crm_customer_onboarding_state`.
  Se elimino el feature flag `BRAIN_IDENTITY_ONBOARDING_ENABLED` de `.env.example` (no tenia ninguna lectura en `.ts`, no era un flag real) - la invariante de no crear customers provisionales no es opcional.
  Tests nuevos: `tests/scripts/migrationManifest.test.ts` (6, unitarios sin DB), `tests/native/nativeInboundIdentityBoundary.test.ts` (5, DB-backed: sin escritura a `crm_customer_onboarding`, sin `master_customer` nuevo, sin ejecucion de `create_customer`/`link_external_identity`, sin email `.invalid`, y una invariante estructural de imports); mas un test nuevo en `tests/native/identity-conflict.test.ts` (identidad no resuelta persiste entre turnos sin duplicar fila ni fabricar customer). Se corrigieron tres fixtures `seedConversation` (`tests/commercial/customerSessionCustomer360Gate.test.ts`, `customerOnboardingPostPlanRuntime.test.ts`, `customerOnboardingPostPlanPrivacy.test.ts`) que asumian creacion automatica de customer por el inbound - ahora siembran el customer explicitamente antes de enviar el inbound.
  No implementado en `T06.2` (fuera de alcance, permanece para tareas posteriores): persistencia de executions/outcomes/warnings de identidad (`ACS-R1-04-T07`, sin tocar); un test de fallo de infraestructura forzado para `resolveOrPersistNativeExternalIdentity` no se agrego - la funcion no tiene dependency injection (igual que antes de PR #43) y forzarlo habria requerido agregar DI, fuera del alcance de esta tarea; un fallo real de escritura en `upsertExternalIdentity` hoy se propaga como excepcion (fail-closed: no fabrica un customer ni una fila de identidad falsa), no como un `status: "temporarily_unavailable"` gracioso salvo en la rama de re-link tras `findExternalIdentityByNormalizedValue`.
- `ACS-R1-04-T04` es exclusivamente documental: define [customer-creation-linking-authority-contract](../data/customer-creation-linking-authority-contract.md), el contrato canonico de autoridad para `create_customer`, `link_external_identity` y `record_customer_interest` (inputs/outcomes conceptuales, datos minimos, idempotencia, deduplicacion, consentimiento, manejo de conflictos y fallos del Customer Service). No agrega codigo productivo ni reduce la autonomia estrategica de la IA (seccion 3 del contrato: la IA sigue decidiendo cuando explorar, preguntar, recomendar o proponer una accion; la policy solo gobierna la autoridad para ejecutar capabilities con efecto secundario). Extiende, sin modificar, la separacion `resolve_customer != create_customer != link_external_identity` ya declarada en [customer-onboarding-identity-contract](../data/customer-onboarding-identity-contract.md) (seccion 6), agregando `record_customer_interest` como cuarta operacion. La implementacion real (Customer Service Port, policies, capabilities) queda para `ACS-R1-04-T04.1`.
- `ACS-R1-04-T04.1` implemento el `CustomerServicePort` (`lib/domains/customer-service/ports.ts`), los tipos de datos v1.0.2 (`types.ts`), las tres policies puras de autoridad (`authority-policy.ts`: `evaluateCreateCustomerAuthority`, `evaluateLinkExternalIdentityAuthority`, `evaluateCustomerInterestAuthority`) y la service layer (`service.ts`) que normaliza inputs, evalua policy antes de llamar al port, y genera el `idempotencyKey` (`customer-service:<op>:<capabilityExecutionId>`) — la API publica del agente no permite elegirlo. El adapter HTTP (`lib/integrations/customer-service/http-adapter.ts`) es fail-closed (sin `CUSTOMER_SERVICE_BASE_URL`/`CUSTOMER_SERVICE_API_KEY` responde `temporarily_unavailable`, nunca `no_match`), sin retry interno (el Capability Gateway sera el unico propietario cuando se conecte) y sin fallback hacia `master_customer`, PrestaShop, SAP, POS o `customer_external_identity`. Contrato de transporte documentado en [customer-service-http-contract](../integrations/customer-service-http-contract.md); contrato de datos bump a `1.0.2` (`invalid_input`/`failed` explicitos en `CreateCustomerResult`/`LinkExternalIdentityResult`). 50 tests nuevos: 31 de policy/service, 19 del adapter HTTP. No se toco `CustomerIdentityResolutionService` (T02/T02.1), no hay dual-read ni fallback entre ambos resolutores. No se conecto al inbound, al LLM, al Capability Gateway, a Customer 360 ni se persistio ningun interes (`record_customer_interest` es solo policy + tipos) — eso sigue en `ACS-R1-04-T05`/`T06`. Ver [docs/capabilities/customer-service-capability.md](../capabilities/customer-service-capability.md).
- `ACS-R1-04-T05` incorporo Customer 360 al ciclo autonomo exclusivamente como historia consolidada, nunca como resolucion de identidad: recibe `input.customerMasterId` ya resuelto, nunca resuelve por `wa_id`/telefono/email, nunca llama `resolveCustomer` ni Customer Service, nunca escribe `master_customer`/onboarding/direcciones. Extension aditiva en `lib/domains/customer-360/service.ts`: `Customer360LoadResult` (`found`/`not_found`/`unavailable`) y `loadByCustomerId()` distinguen lo que `getByCustomerId()` colapsaba en `null`; `getByCustomerId()` sigue igual (delega al metodo nuevo), sin romper la ruta `/api/customers/[id]/360` existente (se acoto su dependencia de tipos a `Pick<Customer360QueryService, "getByCustomerId">` para no forzar el stub de test a implementar el metodo nuevo). Proyector `lib/brain/commercial/context/autonomousCustomerContext.ts` (`AutonomousCustomerContext`, allowlist campo por campo, nunca denylist): perfil minimo, resumen de relacion, hasta 3 oportunidades/perfiles de necesidad/cotizaciones recientes en orden newest-first deterministico con desempate estable, calidad de dato — nunca email completo, telefono, `wa_id`, identidades vinculadas, `customerKey`, direcciones, referencias de pedido, invoice number, total pagado, cuerpos ni previews de mensaje, draft/final messages, provider message ids, metadata libre ni el snapshot completo; nunca muta el snapshot de entrada. Loader `lib/brain/commercial/context/loadAutonomousCustomerContext.ts` es el unico punto de carga: `customerId` nulo produce cero llamadas (`not_requested`); una excepcion nunca se propaga cruda, siempre se mapea a `unavailable` con el warning estructurado `customer_360_unavailable`. `runNativeAutonomousCycle` determina primero si algun runtime esta habilitado (si ninguno lo esta, no carga Customer 360), carga una sola vez, y recien despues bifurca entre multi-request y el pipeline legacy (mutuamente excluyentes, sin cambios en esa exclusividad ya existente). `MultiRequestCycleInput`/`PlanTurnInput`/`TurnPlannerProviderInput` (multi-request) y `CommercialContextSnapshot`/`buildNativeBrainContextShim`/`NormalizedCommercialBrainContext`/`SalesAgentInput` (legacy) se extendieron con campos tipados explicitos `customerContext`/`customerContextState` o `customer360`/`customer360State` - nunca dentro de `metadata: Record<string, unknown>`. El contexto reducido entra al `inputHash` del turn plan (`buildTurnPlanInputHash`), sin `lastRefreshedAt` ni timestamps generados durante la lectura; el retry del mismo inbound sigue reutilizando el plan persistido sin invocar el planner de nuevo (una sola invocacion por turno, sin cambios). 49 tests nuevos: `Customer360QueryService.loadByCustomerId` (5), proyector (15), loader (8), ciclo nativo con Customer 360 inyectado por DI (7), multi-request/hash/retry (6), boundary de invariantes duraderas por dependency injection (8) - sin depender del historial Git ni congelar la ausencia temporal de `ACS-R1-04-T06`. T05 no conecto la resolucion de identidad, el onboarding ni el `CustomerServicePort` al inbound nativo, al planner/LLM o al Capability Gateway; esas integraciones permanecen en `ACS-R1-04-T06`. Customer 360 si quedo incorporado, como contexto historico read-only y minimizado, a las entradas tipadas de los runtimes multi-request y legacy.
- `ACS-R1-04-T06` conecto identidad y onboarding al inbound nativo mediante un orquestador de sesion unico, `resolveNativeCustomerSession` (`lib/brain/commercial/native-cycle/customer-session/`: `types.ts`, `warnings.ts`, `onboardingPurposeMapping.ts`, `onboardingTransitions.ts`, `consentEvidence.ts`, `resolveNativeCustomerSession.ts`), invocado exactamente una vez por turno desde `runNativeAutonomousCycle`, despues de que el inbound nativo persiste el mensaje y antes de bifurcar entre runtimes y de cargar Customer 360 (no cambia validacion de firma Meta, idempotencia, dedup de mensajes, persistencia de conversacion/mensaje, webhook de entrega ni el outbox worker). Resuelve identidad local exactamente una vez via `CustomerIdentityResolutionService` (T02/T02.1, sin SQL directo, orden externalId exacto → telefono normalizado cross-provider); reconcilia contra `conversation.customer_id` del resolver nativo preexistente (`resolveOrCreateNativeCustomer`, sin tocar) — una contradiccion produce `conflict`, nunca una seleccion automatica. Intenta `resolve_customer` externo a lo sumo una vez por turno, solo cuando la resolucion local no encontro coincidencia y el onboarding activo lo requiere (`required`/`collecting`) — nunca para consulta publica sin onboarding, nunca tras un conflicto local, nunca como fallback de un fallo tecnico local, nunca cuando la identidad ya esta resuelta — siempre a traves del Capability Gateway (`executeGovernedCapability("resolve_customer", ...)`), nunca llamando al adapter HTTP directamente.
  `resolve_customer`, `create_customer` y `link_external_identity` quedaron registrados en el Gateway (`lib/brain/commercial/capability-gateway/customerIdentityCapabilities.ts`, sumados a `CAPABILITY_GATEWAY_REGISTRY`) con nombres canonicos snake_case, gobernanza real (`read_only/autonomous/low` para `resolve_customer`; `mutating/autonomous/medium` para las otras dos - el campo binario `authority` del Gateway es sobre aprobacion de operador, no sobre la policy real que corre dentro de `execute()`, documentado inline) y `maxRetries: 0` (el Gateway es el unico propietario de reintentos; el HTTP adapter de T04.1 ya no reintenta internamente). `createCustomer`/`linkExternalIdentity` (camelCase) se agregaron a `BRAIN_TOOL_NAMES`/`BRAIN_TOOL_REGISTRY` y al alias table centralizado (`toolAliases.ts`) para que el runtime legacy los pueda proponer como herramientas via `runCapabilityExecutionStage` (etapa generica preexistente, sin cambios en su logica); `resolve_customer` deliberadamente no tiene alias - nunca es una herramienta que el LLM proponga, solo lo invoca el orquestador de sesion directamente. `record_customer_interest` sigue sin registrar (sin persistencia operacional todavia, seccion 22).
  `create_customer`/`link_external_identity` ensamblan su input completamente en el servidor desde `NativeCustomerSessionExecutionContext` (`context.trustedCustomerSession`) - el input de la herramienta que propone el LLM (`_input`) se ignora por completo, nunca se lee; `create_customer` exige proposito de onboarding permitido (mapeado desde un unico modulo centralizado, `onboardingPurposeMapping.ts`), evidencia fresca de `resolve_customer = no_match` de este mismo turno (una evidencia ausente o de otro tipo nunca equivale a `no_match` - `evaluateCreateCustomerAuthority`, T04.1, sin cambios), datos minimos validos y consentimiento explicito de este turno; `link_external_identity` exige un `customerId` ya consistente en la sesion, el mismo `wa_id` que el canal inbound verifico (nunca otro, nunca elegido por el modelo) y consentimiento explicito separado. Ninguna ejecuta a la otra como efecto lateral - `link_external_identity` es siempre una llamada posterior y separada. Ambas aplican la tabla de transiciones de onboarding (creado/existente-emparejado → `completeOnboarding` con el `customerId`; informacion faltante → `collectFields` con los campos requeridos reportados; conflicto → `markConflict` (sin Customer 360, sin link); denegado/no disponible/fallido → sin cambio de estado) usando exclusivamente las transiciones publicas de `CustomerOnboardingService` (T03), nunca escritura directa a `crm_customer_onboarding_state`; esa logica compartida vive en `onboardingTransitions.ts` (dependency-free respecto al Capability Gateway, para evitar un ciclo de import de valor entre `resolveNativeCustomerSession` → Gateway → `customerIdentityCapabilities` → de vuelta a `resolveNativeCustomerSession`). `resolveNativeCustomerSession` tambien completa el onboarding cuando la identidad se resuelve solo localmente (sin pasar por `resolve_customer` externo) - no unicamente en la rama externa, gap real detectado y corregido durante esta tarea.
  El acceso a Customer 360 (T05) quedo detras de una compuerta explicita, `contextAccess: none | commercial_history | validated_entity` (seccion 11 del contrato): identidad identificada ya no autoriza por si sola la carga - se exige ademas onboarding activo con proposito `quote`/`purchase`, sin conflicto ni bloqueo temporal; `validated_entity` nunca se otorga en T06 (no existe validacion de propiedad de entidad historica - un `orderReference` presente no alcanza). `runNativeAutonomousCycle` entrega al loader de T05 el `customerId` solo cuando `contextAccess !== "none"`, nunca el `wa_id`/telefono/email crudos.
  El contexto que reciben ambos runtimes (`CustomerSessionDecisionContext`, `schemaVersion: "1.0.0"`) es la unica representacion que llega al planner/LLM: nunca `customerId`, email, telefono, `wa_id`, `externalId`, referencia de pedido, nombres capturados, `messageId`, texto de consentimiento, timestamps de resolucion, ids/conteos de candidatos, codigos internos de customer, errores SQL, stack traces, HTTP crudo, estado completo de onboarding, payloads ni metadata libre - solo enums y booleanos (`identity.status`, `hasResolvedCustomer`, `onboarding.status/purpose/pendingFields`, `collected.*Available` como booleanos, `contextAccess`, `operations.can*`). La sesion de ejecucion completa (`NativeCustomerSessionExecutionContext`) es exclusivamente server-side, nunca serializada entera hacia el planner/LLM/response provider. `MultiRequestCycleInput`/`PlanTurnInput`/`TurnPlannerProviderInput` y `CommercialContextSnapshot`/`buildNativeBrainContextShim`/`NormalizedCommercialBrainContext`/`SalesAgentInput` ganaron un campo `customerSession: CustomerSessionDecisionContext | null` tipado explicito (nunca dentro de un `metadata: Record<string, unknown>` generico); entra al `inputHash` del turn plan junto al resto del contexto reducido. El consentimiento (`ConsentEvidence`) se parsea de forma deterministica y conservadora desde el mensaje del turno actual (`consentEvidence.ts`) - un "si"/"ok"/"dale" sin verbo/objeto explicito nunca alcanza; nunca se reutiliza entre turnos ni entre `create_customer`/`link_external_identity`.
  Escribiendo las 90 pruebas de esta tarea se detectaron y corrigieron tres fallas reales en el codigo recien escrito, antes de cerrar la tarea: (1) `create_customer`/`link_external_identity` enviaban `consent: {createCustomer: true}`/`{granted: true}` de forma incondicional a la policy de autoridad, sin importar si el turno realmente traia evidencia de consentimiento parseada - el gate de consentimiento explicito quedaba efectivamente deshabilitado; (2) la finalizacion de onboarding por resolucion local (sin `resolve_customer` externo) no existia - un onboarding `required`/`collecting` quedaba indefinidamente sin cerrar si el cliente ya estaba vinculado localmente; (3) una rama local de `conflict` no emitia el warning estructurado `customer_identity_conflict` (solo lo hacian los conflictos detectados externamente o por contradiccion con la conversacion previa).
  90 tests nuevos en 6 archivos, todos por dependency injection (sin depender de historial Git ni de servidores externos reales): `tests/commercial/customerSession.test.ts` (42 - sesion e identidad local, resolucion externa, onboarding, contexto para el modelo), `tests/commercial/customerSessionCustomer360Gate.test.ts` (6 - compuerta de Customer 360 sobre `runNativeAutonomousCycle` real con DB), `tests/commercial/customerIdentityCapabilityGateway.test.ts` (8 - registro/gobernanza/alias), `tests/commercial/createCustomerCapability.test.ts` (14) y `tests/commercial/linkExternalIdentityCapability.test.ts` (10 - ejecucion de cada capability contra un servidor HTTP local, mismo patron que T04.1), `tests/commercial/customerSessionPrivacy.test.ts` (10 - retry/idempotencia, vocabulario de warnings, privacidad de punta a punta). Ademas, 7 pruebas T05 existentes (`tests/commercial/customer360AutonomousBoundary.test.ts`, `tests/commercial/runNativeAutonomousCycleCustomer360.test.ts`) asumian el comportamiento pre-T06 (`customerMasterId` resuelto → Customer 360 automatico) y se actualizaron para exigir explicitamente la nueva compuerta de acceso (inyectando una sesion `commercial_history` via DI) en vez de modificar el codigo de T06 para preservar el comportamiento viejo. Suite dirigida (identidad/onboarding/Customer Service/Gateway/multi-request/turn plan/grounded response/native-whatsapp): 147 tests, 137 pass, 10 fallos - los mismos ya documentados de `customerOnboarding.test.ts` (deuda `schema_migrations`, sin relacion con T06). Suite completa del repo: 989 tests, 979 pass, exactamente los mismos 10 fallos preexistentes, cero fallos nuevos. `npx tsc --noEmit` y `npm run lint` limpios (35 warnings preexistentes sin cambios, 0 errores).
  No implementado en T06 (queda para `ACS-R1-04-T07` u otra tarea posterior): ninguna persistencia nueva de executions/outcomes/warnings especifica de identidad mas alla de lo que el Gateway ya audita hoy via `insertCapabilityExecution`; ningun event store, dashboard, replay o auditoria nueva. `record_customer_interest` sigue `contract: implemented, policy: implemented, persistence: not_implemented, gateway: not_connected, runtime: not_connected` - no se registro un no-op ni se le dio efecto operacional. Las pruebas de `create_customer`/`link_external_identity` corrieron contra un servidor HTTP local en memoria (mismo patron que T04.1), no contra un Customer Service real desplegado - no hay validacion end-to-end real todavia (eso es `ACS-R1-04-T08`). `validated_entity` no se implemento: ninguna operacion historica (pedido, garantia, devolucion) queda validada por propiedad real de la entidad; un `orderReference` capturado nunca autoriza por si solo mas acceso. Multi-request recibe `customerSession` solo a nivel de tipos e input hash (mismo patron de deuda ya documentado para Customer 360 en T05) - la ejecucion real de `create_customer`/`link_external_identity` como herramientas propuestas por el LLM esta conectada unicamente en el runtime legacy, via `runCapabilityExecutionStage`.
- `ACS-R1-04-T06.1` completo la conexion del inbound nativo con dos piezas que T06 dejaba pendientes: activacion real de onboarding desde una intencion operacional estructurada, y captura multi-turno de campos desde el mensaje del cliente - de forma que la secuencia completa (identificar → capturar → crear/vincular) funcione naturalmente, no solo cuando el LLM decide proponer `createCustomer`/`linkExternalIdentity` como herramienta.
  Arquitectura de dos fases dentro de `runNativeAutonomousCycle`: la Fase 3 (pre-plan, `resolveNativeCustomerSession`, sin cambios de T06) sigue resolviendo identidad local, intentando `resolve_customer` externo cuando corresponde, reconciliando y calculando el gate de Customer 360, exactamente como antes. Despues corre el Sales Agent/operational loop (el planner canonico, sin cambios). Una Fase 3.6 nueva (post-plan, `runCustomerOnboardingPostPlanStage` en `lib/brain/commercial/native-cycle/customer-session/runCustomerOnboardingPostPlanStage.ts`) corre exclusivamente en el runtime legacy, despues del loop y de la etapa de Capability Gateway generica, reutilizando `session.execution` de la Fase 3 sin recargar onboarding, sin re-resolver identidad local y sin volver a cargar Customer 360.
  La activacion de onboarding usa como unica senal estructurada el `type` de `loop.selectedNextAction` (`CommercialOperationalLoopNextActionType`, producido por `selectNextCommercialAction.ts`) - nunca busqueda libre de keywords en el mensaje - mapeado via el mismo modulo centralizado ya creado en T06, `onboardingPurposeMapping.ts`. Se agrego la clave real y efectivamente alcanzable `prepare_quote → quote` (el unico next-action-type hoy que implica una relacion comercial nueva); se confirmo por inspeccion de codigo que las claves preexistentes `request_quote_draft`/`create_quote_draft`/`request_order_lookup` (vocabulario de `salesAgentTypes.ts`) nunca las produce el runtime real (ni `selectNextCommercialAction.ts` ni `runCommercialOperationalLoop.ts` las referencian) - se dejaron documentadas como muertas, no se removieron. Los propositos `order_inquiry`/`complaint`/`warranty`/`return` del contrato siguen sin ser alcanzables desde el runtime real hoy: no existe un next-action-type ni una tool-alias que los exprese - deuda documentada explicitamente, no resuelta en `T06.1` (extender el vocabulario del operational loop es una pieza mayor, fuera de alcance).
  Extraccion conservadora de campos, `lib/brain/commercial/native-cycle/customer-session/extractCustomerOnboardingFields.ts`, solo desde el mensaje actual (nunca Customer 360, nunca turnos previos, nunca candidatos propuestos por el LLM): email valido se reconoce por forma (`normalizeEmail` de `lib/customer-identity/normalize.ts` para trim/lowercase, `isValidEmail` recien exportado de `lib/domains/customers/validation.ts` para el formato - validador ya existente, reutilizado en vez de duplicar la regex); nombre solo por declaracion explicita (`me llamo`/`mi nombre es`/`soy`/`nombre:`) con una lista de bloqueo de continuaciones no-nombre (cliente, de, el, encargado, necesito, ...) que prioriza falsos negativos sobre falsos positivos, rechaza digitos y patrones de URL/email; referencia de pedido solo con etiqueta explicita (pedido/orden/compra/referencia) seguida de un token alfanumerico con al menos un digito, nunca un numero aislado. La persistencia usa exclusivamente `CustomerOnboardingService.collectFields` (nunca escritura directa a `crm_customer_onboarding_state`); un conflicto de version optimista recarga el estado una sola vez, revalida, y nunca sobrescribe ni entra en loop.
  `create_customer`/`link_external_identity` perdieron su alias de herramienta del sales agent en `toolAliases.ts` (`createCustomer`/`linkExternalIdentity` ya no son propuestas por el LLM) - el post-plan stage quedo como el unico punto de decision y ejecucion para ambas, igual que `resolve_customer` ya lo era desde T06 (invocado directamente, nunca vía tool request). Esto evita que una segunda via LLM-propuesta hacia la misma capability arriesgue una ejecucion duplicada en el mismo turno (contrato seccion 20); el `capabilityExecutionId` sigue siendo `<correlationId>:create_customer`/`<correlationId>:link_external_identity`, por lo que un retry del mismo inbound envia la misma `Idempotency-Key` a Customer Service, confirmado con tests dedicados.
  Escribiendo las pruebas de esta tarea se detecto y corrigio un error real de alcance en la propia logica de `T06.1`, antes de cerrarla: `mapLocalResolution` (T06, sin cambios) solo reporta el estado `identification_required` cuando el onboarding YA estaba activo antes del turno actual; una conversacion nueva sin onboarding previo siempre reporta `anonymous`, incluso en el turno donde una intencion de cotizacion va a activar identificacion por primera vez. El gate de activacion original de `T06.1` solo comprobaba `identity.status === "identification_required"`, por lo que nunca se disparaba en el primer turno real de una conversacion nueva (unicamente en turnos posteriores, una vez que el onboarding ya existia por otra via) - un caso que las pruebas de secuencia completa (`turno 1 inicia onboarding`, `turno unico con todos los datos`) expusieron de inmediato. Se corrigio con un chequeo unico, `identityUnresolved = identity.status === "anonymous" || identity.status === "identification_required"`, reutilizado tanto en la activacion como en el gate de `create_customer`.
  Multi-request no gano ninguna via de ejecucion de capabilities de identidad: sigue recibiendo `customerSession` solo a nivel de tipos e input hash para razonamiento, sin acceso al Capability Gateway para `create_customer`/`link_external_identity` - confirmado explicitamente con tests que verifican cero llamadas HTTP a Customer Service bajo el runtime multi-request, incluso con mensajes que en el runtime legacy si dispararian create/link. Customer 360 sigue cargandose una sola vez, exactamente en el punto ya fijado por T05 dentro de la Fase 3 - nunca dentro del post-plan, incluso cuando este crea un customer en el mismo turno (el `customerId` recien creado no se usa para una segunda carga hasta el siguiente turno).
  60 tests nuevos en 4 archivos, todos por dependency injection: `tests/commercial/extractCustomerOnboardingFields.test.ts` (10 - extraccion pura, sin DB), `tests/commercial/customerOnboardingPostPlanStage.test.ts` (30 - fase post-plan, persistencia, secuencias de customer nuevo y link, contra un servidor HTTP local igual que T06), `tests/commercial/customerOnboardingPostPlanRuntime.test.ts` (10 - compuerta de Customer 360 y aislamiento de runtime con DB real), `tests/commercial/customerOnboardingPostPlanPrivacy.test.ts` (10 - privacidad e idempotencia de punta a punta). Ademas, 3 tests de T06 (`tests/commercial/customerIdentityCapabilityGateway.test.ts`, tests 52/54/55) asumian el alias de herramienta que T06.1 elimino y se actualizaron para reflejar que ninguna de las tres capabilities de identidad tiene ya alias de sales agent.
  Suite dirigida (identidad/onboarding/Customer Service/Gateway/multi-request/turn plan/grounded response/native-whatsapp/operational loop): 162 tests, 152 pass, 10 fallos - los mismos ya documentados de `customerOnboarding.test.ts`. Suite completa del repo: 1049 tests, 1039 pass, exactamente los mismos 10 fallos preexistentes, cero fallos nuevos. `npx tsc --noEmit` y `npm run lint` limpios (35 warnings preexistentes sin cambios, 0 errores).
  No implementado en `T06.1` (permanece para tareas posteriores): ninguna persistencia nueva de executions/outcomes/warnings especifica de identidad mas alla de lo que el Gateway ya audita hoy via `insertCapabilityExecution` (`ACS-R1-04-T07`); validacion end-to-end contra un Customer Service real desplegado - las pruebas de `create_customer`/`link_external_identity` siguen corriendo contra un servidor HTTP local en memoria (`ACS-R1-04-T08`); `validated_entity` (ninguna operacion historica queda validada por propiedad real de la entidad); efecto operacional de `record_customer_interest` (`contract: implemented, policy: implemented, persistence: not_implemented, gateway: not_connected, runtime: not_connected`, sin cambios); el vocabulario de next-action-types para propositos historicos (order_inquiry/complaint/warranty/return).
- `ACS-R1-04-T08` (rama `acs-r1-04-t08-identity-onboarding-e2e`) ejecuto pruebas end-to-end reales de los tres escenarios canonicos (`tests/e2e/customerIdentityOnboarding.e2e.test.ts`, 12 tests) recorriendo la cadena completa - nunca solo funciones puras aisladas, nunca un mock de `executeGovernedCapability` - sobre una base MariaDB `crm_test` desechable (drop + create + `npm run db:migrate -- --database=test` x2: primera corrida aplica los 23 archivos `001`-`024`, segunda corrida resulta en 23 `skip`, cero conflictos de checksum).
  Escenario A (cliente nuevo, T08-A1..A5): turno inicial con intencion de cotizacion deja `customer_external_identity.customer_id = NULL`, `conversation.customer_id = NULL`, cero `master_customer` provisional, cero email `@local.invalid`, onboarding iniciado via `CustomerOnboardingService` (`crm_customer_onboarding_state`), cero writes en `crm_customer_onboarding` legacy; captura multi-turno de `firstName`/`lastName`/`email` via `collectFields`; creacion via `create_customer` con `resolve_customer = no_match` fresco del mismo turno (maximo una llamada por inbound), `crm_capability_executions` con summaries allowlisted, evento `customer_identity_capability_outcome_recorded` con `gatewayStatus: completed` / `businessOutcome: created`; retry del mismo `providerMessageId` reconocido como duplicado, cero segunda ejecucion, cero segundo evento, cero segundo customer; vinculacion en un turno separado con consentimiento propio (`link_external_identity`, nunca reutiliza el consentimiento de creacion), resolucion local posterior via `source: onboarding_state`; caso adicional de conflicto de negocio en `link_external_identity` (Customer Service ya vinculado a otro customer) confirmando `gatewayStatus: completed` / `businessOutcome: conflict` como estados compatibles (release spec T07, seccion de negocio vs tecnico).
  Escenario B (cliente existente, T08-B1..B3): resolucion exacta por `provider + external_id` y resolucion cross-provider por telefono normalizado (`findDistinctCustomersByNormalizedValueAcrossProviders`, T02.1) ambas con cero llamadas a Customer Service (`resolve_customer` externo nunca se ejecuta cuando la resolucion local basta) y cero duplicacion de `master_customer`/`customer_external_identity`; confirmado que la resolucion cross-provider deja `conversation.customer_id = NULL` (T06.2 esta scoped a `provider = whatsapp`, deuda ya documentada en T02.1) mientras la sesion (`resolveNativeCustomerSession`, T02 real) SI reconoce al cliente - las dos capas resuelven con alcance distinto por diseño. Customer 360 gate: un `customerId` identificado aislado (sin onboarding activo) nunca autoriza la carga (`contextAccess: none`); sembrando un onboarding `completed`/`quote` para la conversacion via las transiciones publicas reales del servicio (nunca SQL directo), el siguiente inbound de cotizacion obtiene `contextAccess: commercial_history` y exactamente una carga de Customer 360.
  Escenario C (conflicto, T08-C1/C2): `divergent_identity_links` (mismo telefono normalizado, dos `customerId`) produce `identity.status: conflict` tanto en la resolucion T06.2 (persistencia/inbound) como en la resolucion independiente de `resolveNativeCustomerSession` (la ambiguedad vive en el lookup de telefono cross-provider, que ambas capas consultan), cero seleccion automatica, cero `create_customer`/`link_external_identity`, cero `resolve_customer` externo como fallback, Customer 360 nunca cargado, warning estructurado `customer_identity_conflict` persistido sin duplicar (mismo `dedupe_key`), `hub_audit_log` con evidencia investigable (`customer.identity_conflict`), `conversation.customer_id` nunca sobrescrito. `customer_conversation_mismatch` (segundo caso, opcional segun el brief de T08) se probo en la capa donde el T06.2 ya lo detecta y protege correctamente (`processNativeWhatsAppInbound`: `identityConflict.type`, `conversation.customer_id` preservado, `hub_audit_log`) - **gap real descubierto, documentado, no corregido**: la reconciliacion propia de `resolveNativeCustomerSession` (`priorConversationCustomerId`) nunca ve este conflicto especifico, porque `processNativeWhatsAppInbound` le pasa `result.customerId` (ya anulado por el propio T06.2 al detectar el mismatch) en vez del `customer_id` previo de la conversacion que el comentario del tipo `ResolveNativeCustomerSessionInput.priorConversationCustomerId` describe como entrada esperada. En los flujos hoy alcanzables esto no permite un efecto lateral inseguro (`create_customer`/`link_external_identity` siguen bloqueados por sus propias precondiciones - identidad ya "identified", no "unresolved"), pero si puede reportar `identity.status: identified` hacia el planner/LLM para un turno que el nivel de conversacion ya sabe que es ambiguo. No se corrigio en T08 porque tocar la propagacion de `customerMasterId` esta a un paso de "resolucion local"/"semantica de conflicto" (fronteras que esta tarea no puede cambiar) y el sub-caso es explicitamente opcional en el brief; queda como debt para revision arquitectonica (candidato natural para T09 o una ADR dedicada).
  Privacidad (T08-privacy, agregada sobre las 3 escenarios): `payload_json`/`metadata_json` de los cuatro tipos de evento T07 y `request_summary_json`/`response_summary_json` de `crm_capability_executions` escaneados contra los valores crudos reales de cada turno (`wa_id`, telefono, email, nombre, apellido, texto de consentimiento) - ninguna coincidencia. Idempotencia/dedupe (T08-dedupe): dos senales de conflicto en el mismo turno (mismo `messageId`+`phase`+`warningCode`) nunca duplican fila, `dedupe_key` estable.
  **Bug real encontrado y corregido, con regresion**: `lib/domains/customer-identity/local-adapter.ts#findCustomerByExternalIdentity` trataba una fila de `customer_external_identity` no resuelta (`customer_id IS NULL`, la que `resolveOrPersistNativeExternalIdentity`/T06.2 persiste para un remitente nuevo sin match) como un candidato valido: `String(null) === "null"` se contaba como `candidateCustomerIds: ["null"]`, haciendo que `CustomerIdentityResolutionService.resolveIdentity` reportara a cualquier contacto nuevo como `status: "identified"` con un `customerId` literal `"null"` en cuanto su propia fila no resuelta existiera - en la practica, desde el primer turno real de cualquier cliente nuevo con el ciclo autonomo activo. Ningun test previo lo detecto porque las pruebas de T06/T06.1 que ejercitan el flujo de cliente nuevo siempre siembran el link ya resuelto o inyectan un `identityService` fake (nunca la resolucion real contra una fila `customer_id = NULL` recien creada en el mismo turno) - T08 es la primera suite que combina la persistencia real de T06.2 con la resolucion real de T02 para un remitente genuinamente nuevo. Corregido con un guard (`result.row.customer_id !== null`); regresion agregada en `tests/domains/customerIdentity.test.ts` ("integration (regression, ACS-R1-04-T08): a not-yet-resolved external identity row (customer_id NULL) must never count as a match").
  **[RESUELTO en `ACS-R1-04-T08.1`, ver entrada debajo] Gap arquitectonico real encontrado**: `create_customer`/`link_external_identity` nunca escriben `master_customer` (invariante explicita del contrato: "ACS nunca escribe directamente en las fuentes maestras") - el `customerId` que devuelve Customer Service se persistia tal cual, via `CustomerOnboardingService.completeOnboarding`, en `crm_customer_onboarding_state.customer_id`, columna con FK real (`ON DELETE RESTRICT`) contra `master_customer.id` (migracion 023). Nada en la implementacion de T08 reconciliaba el espacio de IDs de un Customer Service genuinamente externo con los ids autoincrementales de la tabla local `master_customer`: contra cualquier Customer Service real y verdaderamente independiente, ese `UPDATE` habria fallado por violacion de FK en la primera creacion de cliente. Ningun test previo lo detecto porque T06/T06.1 siempre usan un `CustomerOnboardingService` fake en memoria (`setOnboardingServiceForTests`, sin SQL, sin FK) para el camino de creacion - T08 fue la primera suite que combino un servidor HTTP real (con id propio) con el servicio de onboarding real (con FK real). Era, ademas, la razon principal por la que un smoke real contra un Customer Service desplegado habria fallado en el primer intento de creacion.
- `ACS-R1-04-T08.1` (rama `acs-r1-04-t08-1-customer-master-projection`, base `f96b577`) resolvio el gap anterior sin eliminar la FK, sin que ACS inserte/actualice `master_customer`, sin tabla de mapeo y sin cambiar la autoridad de creacion/vinculacion (Customer Service sigue siendo la unica autoridad).
  Contrato HTTP renombrado (breaking, solo en los resultados exitosos): `ResolveCustomerResult`/`CreateCustomerResult`/`LinkExternalIdentityResult` (`lib/domains/customer-service/types.ts`) reemplazan el campo `customerId` por `customerMasterId` unicamente en `resolved`/`created`/`matched_existing`/`completed`/`already_linked` - sin dual-field, sin renombrar `customerId` en ningun otro tipo (`LinkExternalIdentityInput.customerId`, `CustomerOnboardingState.customerId`, `ResolveCustomerIdentityResult.customerId` de T02, `identity.customerId` de la sesion nativa y la columna `commercial_event.customer_id` quedaron exactamente igual - renombrar esos habria excedido el alcance). [customer-service-http-contract](../integrations/customer-service-http-contract.md) subio de `1.0.0` a `2.0.0`; [customer-creation-linking-authority-contract](../data/customer-creation-linking-authority-contract.md) subio de `1.0.2` a `2.0.0` (nueva seccion 1.1); [customer-onboarding-identity-contract](../data/customer-onboarding-identity-contract.md) subio de `1.0.0` a `1.1.0` (nota, sin romper la seccion 5 de resolucion).
  Adapter fail-closed (`lib/integrations/customer-service/http-adapter.ts`): `asCustomerMasterId()` valida formato (`CUSTOMER_MASTER_ID_PATTERN = /^[1-9]\d*$/`, compatible con `master_customer.id` BIGINT UNSIGNED autoincremental) y `hasConflictingErrorEnvelope()` detecta un body que declara exito junto a un envelope de error - cualquier respuesta exitosa sin `customerMasterId`, con formato invalido, o con campos incompatibles nunca se convierte en `no_match`: se mapea a `invalid_response`/`failed`/`temporarily_unavailable` segun el vocabulario existente del adapter, sin filtrar HTTP crudo/payloads/headers/API keys hacia el planner, el usuario ni los eventos.
  Proyeccion local de solo lectura: puerto nuevo `CustomerMasterProjectionReader` (`lib/domains/customer-service/customerMasterProjection.ts`, `exists(customerMasterId): Promise<boolean>`), implementado sobre el repositorio ya existente `getMasterCustomerById` (`lib/integrations/customer-master/customer-repository.ts`, reutilizado, sin SQL nuevo) - sin INSERT, sin UPDATE, sin fallback a PrestaShop ni a Customer 360, inyectable en tests via un seam de dependency injection (`projectionReader` en `ResolveNativeCustomerSessionDependencies`/`CustomerOnboardingPostPlanDependencies`) mas un setter de modulo para tests que no usan DI directa (`setCustomerMasterProjectionReaderForTests`).
  Compuerta centralizada (`lib/brain/commercial/native-cycle/customer-session/onboardingTransitions.ts`): `verifyCustomerMasterProjection` (pura, solo consulta) y `completeOnboardingWithVerifiedCustomer` (envuelve la verificacion mas la transicion real de onboarding) - un unico punto de logica, aplicado en los tres sitios donde una capability de Customer Service puede introducir un customer: la rama "resolved" de `resolveNativeCustomerSession.ts` (pre-plan), la rama "resolved" del paso 3 de `runCustomerOnboardingPostPlanStage.ts` (post-plan, `resolve_customer` fresco) y las ramas "created"/"matched_existing"/"completed"/"already_linked" dentro de `customerIdentityCapabilities.ts` (`createCustomerCapability`/`linkExternalIdentityCapability`). No aplica a una resolucion puramente local ya sourced desde `master_customer` (esa ya es la fuente de verdad).
  Proyeccion no disponible: el `gatewayStatus`/`businessOutcome` de la capability no cambian (la llamada a Customer Service si tuvo exito) - `CapabilityGatewayResult`/`CapabilityExecutionOutcome` ganaron un campo `warnings?: string[]` (aditivo, `search_products`/`get_product_details` sin cambios) para transportar el aviso sin tocar el resultado de negocio. ACS no llama `completeOnboarding`, no lanza excepcion de FK, no fabrica una fila `master_customer`, no carga Customer 360 ni vincula la conversacion a ese id; el onboarding aterriza en `temporarily_unavailable` via la transicion publica ya existente (`landOnboardingInTerminalState`, sin nueva maquina de estados) y se persiste el warning estructurado nuevo `customer_master_projection_unavailable` (agregado a `NATIVE_SESSION_WARNINGS`, mismo mecanismo de auditoria de T07). Un fallo tecnico al consultar la proyeccion es fail-closed, distinto (`customer_master_projection_check_failed`), nunca reintenta Customer Service en el mismo turno y nunca expone SQL/stack trace en el trail de auditoria.
  Reintento honesto (primer incremento): al cerrar el incremento inicial, `temporarily_unavailable` seguia sin un camino de reintento automatico en el runtime (`CustomerOnboardingService.retryResolution` sin ningun caller productivo, mismo hallazgo ya documentado para `customer_service_unavailable`) - se documento como deuda heredada, no corregida en ese momento porque hacerlo excedia el alcance original "reconciliar `customerMasterId`". **Esta deuda se cerro en el segundo incremento de `ACS-R1-04-T08.1`** (ver parrafo de recuperacion runtime, mas abajo) mediante una tarea de seguimiento explicita, no silenciosa.
  E2E (`tests/e2e/customerIdentityOnboarding.e2e.test.ts`, ahora 14 tests): el doble HTTP controlado de Customer Service crea/asegura su propia fila `master_customer` dentro de `handleCreate` (representando la responsabilidad real de Customer Service, no del cuerpo del test) - el test ya no pre-inserta el customer antes de invocar la capability. "T08-A6" agrega el escenario negativo del brief: Customer Service responde `created` con un `customerMasterId` que no existe localmente - cero excepcion de FK, onboarding nunca `completed`, warning persistido, Customer 360 nunca cargado, cero escritura de ACS en `master_customer` - y ahora ademas demuestra la recuperacion runtime real en un turno N+1 (ver abajo). "T08-A7" es la regresion: la proyeccion sigue sin aparecer en el turno de reintento.
  **Segundo incremento (`ACS-R1-04-T08.1`, "recuperacion runtime de `customerMasterId` pendiente de proyeccion") - commit `102fc5e`**: cierra la deuda anterior con una recuperacion real, no simulada. Causa raiz: `resolveNativeCustomerSession.ts`'s `onboardingNeedsIdentity` (el gate que decide si vale la pena intentar `resolve_customer` externo este turno) solo consideraba `onboarding.status === "required" || "collecting"` - un onboarding ya aterrizado en `temporarily_unavailable` nunca volvia a intentar resolucion en ningun turno posterior, por diseño original de T06, aunque la identidad seguia sin resolver localmente. Correccion minima en dos piezas: (1) `onboardingNeedsIdentity` ahora tambien es verdadero cuando `onboarding.status === "temporarily_unavailable"` (`resolveNativeCustomerSession.ts`), permitiendo exactamente un intento fresco de `resolve_customer` por turno, igual que para `required`/`collecting`; (2) un helper centralizado nuevo, `ensureResolving` (`onboardingTransitions.ts`, privado al modulo), usado internamente tanto por `landOnboardingInTerminalState` como por `completeOnboardingWithCustomer` (y por lo tanto tambien por `completeOnboardingWithVerifiedCustomer`, que delega en ambos) - ademas de mover `required`/`collecting` a `resolving` via `markResolving` (comportamiento previo sin cambios), ahora tambien mueve `temporarily_unavailable` a `resolving` via `onboardingService.retryResolution` (metodo del dominio existente desde `ACS-R1-04-T03`, con su propia guarda de transicion `RETRY_FROM = ["temporarily_unavailable"]`, pero sin ningun caller en el runtime hasta este commit). Se registra como una nueva operacion de transicion T07, `retry_resolution` (agregada a `CUSTOMER_ONBOARDING_TRANSITION_OPERATIONS`, `lib/brain/commercial/events/types.ts`) - sin nueva tabla, reutilizando `customer_onboarding_transition_recorded` existente. Comportamiento resultante, sin tocar `runCustomerOnboardingPostPlanStage.ts`: en el turno de recuperacion, `resolve_customer` corre una vez desde pre-plan; si retorna `resolved` + proyeccion verificada, `completeOnboardingWithVerifiedCustomer` completa el onboarding (nunca un segundo `create_customer`, porque el paso 3 del post-plan sigue excluyendo cualquier status en `TERMINAL_STATUSES`, que incluye `temporarily_unavailable`); si la proyeccion sigue sin existir o Customer Service sigue fallando, el mismo helper vuelve a aterrizar en `temporarily_unavailable` (reintento seguro, idempotente, nunca un estado `resolving` colgado); si retorna `no_match` o `invalid_input`, el onboarding queda intacto en `temporarily_unavailable` sin transicion alguna (nunca dispara `create_customer` automaticamente como "recuperacion"). Verificado con un turno real end-to-end (nunca llamando `verifyCustomerMasterProjection`/`completeOnboardingWithVerifiedCustomer` directamente) en `tests/e2e/customerIdentityOnboarding.e2e.test.ts`: "T08-A6" (la proyeccion aparece entre turnos - insertada por el fixture que representa a Customer Service, nunca por el cuerpo del test ni por ACS - `resolve_customer` exactamente una vez, `create_customer` cero veces, onboarding `completed`, `customerId` igual al `customerMasterId` original, cero segundo customer) y "T08-A7" (la proyeccion sigue ausente - mismo comportamiento seguro, warning `customer_master_projection_unavailable` sin duplicar). Unico archivo de test modificado en este segundo incremento: `tests/e2e/customerIdentityOnboarding.e2e.test.ts` (rewrite de "T08-A6" + nuevo "T08-A7") - ningun archivo de test unitario/directo requirio cambios (`customerMasterProjectionGate.test.ts` sigue con sus 14 tests originales sin modificar, cubriendo el gate en si; `customerOnboardingPostPlanStage.test.ts`, `customerSession.test.ts`, `createCustomerCapability.test.ts`, `linkExternalIdentityCapability.test.ts` y el resto de la suite directa del primer incremento pasan sin cambios). No se toco `runCustomerOnboardingPostPlanStage.ts`, `customerIdentityCapabilities.ts`, `TERMINAL_STATUSES`, la autoridad de `create_customer`, las reglas de consentimiento, el runtime multi-request, ni se implemento `ACS-R1-04-T09`. Validado sobre una `crm_test` desechable nueva (`001`-`024` aplicadas, segunda corrida 23/23 `[skip]`, cero conflictos de checksum): `npx tsc --noEmit` y `npm run lint` limpios (35 warnings preexistentes, 0 errores), `npm run docs:validate` limpio, suite dirigida (identidad/onboarding/Customer Service/Gateway/E2E) 280/281 con el unico fallo el drift preexistente de `shared_environment_drift` sobre `main_management` (no `crm_test`, documentado desde T03.1, ajeno a este cambio), suite completa del repo 1138/1139 con exactamente el mismo unico fallo preexistente.
  14 tests nuevos en `tests/commercial/customerMasterProjectionGate.test.ts` (compuerta con proyeccion existente/inexistente/fallo de consulta, formato invalido, no-mutacion en el camino de fallo, mas 2 tests estructurales que confirman por codigo fuente que la compuerta nunca ejecuta `INSERT INTO`/`UPDATE master_customer`/`DELETE FROM`). Archivos de test existentes actualizados para el rename y para inyectar un `projectionReader` siempre-verificado donde la prueba no ejercita la compuerta en si (documentado inline en cada archivo): `createCustomerCapability.test.ts`, `linkExternalIdentityCapability.test.ts`, `customerOnboardingPostPlanStage.test.ts`, `customerOnboardingPostPlanRuntime.test.ts`, `customerOnboardingPostPlanPrivacy.test.ts`, `customerIdentityAuditEvents.test.ts`, `identityCapabilityGatewaySummaries.test.ts`, `customerSession.test.ts` (const de modulo `ALWAYS_VERIFIED_PROJECTION_READER` mezclado en `baseInput`, sin DB/HTTP en este archivo), `customerServiceHttpAdapter.test.ts` (+3), `customerService.test.ts`.
  No tocado (confirmado): la FK de `crm_customer_onboarding_state.customer_id` sigue `ON DELETE RESTRICT`; cero tablas o migraciones nuevas; `crm_customer_onboarding` legacy sigue en cero escrituras; autoridad de creacion/vinculacion sin cambios; reglas de consentimiento sin cambios; freshness de `no_match` sin cambios; orden pre-plan/post-plan sin cambios; runtime multi-request sin nuevo acceso al Gateway de identidad; `record_customer_interest` sigue no-operacional; el gap de `customer_conversation_mismatch` documentado en la entrada de T08 (arriba) queda intacto, sin modificar en este incremento; ninguna capability paso a `operational: verified`; `ACS-R1-04-T09` no se implemento.
- `ACS-R1-04-T07` (commit `cd9317e`) persistio una familia de cuatro eventos nuevos sobre `commercial_event` existente (`lib/brain/commercial/events`, `CommercialEventType`), evidencia descriptiva de auditoria - nunca autoridad, nunca una segunda ejecucion tecnica junto a `crm_capability_executions` (T06/T06.1).
  `customer_identity_resolution_recorded`: `resolver: "local" | "customer_service"`, `phase: "pre_plan" | "post_plan"`, `outcome` (`identified`/`no_match`/`conflict`/`invalid_input`/`temporarily_unavailable`, mapeo exhaustivo desde `CustomerIdentityResolutionStatus`/`ResolveCustomerResult` con `assertNever`), `matchedBy`, `hasResolvedCustomer`. Se registra en `resolveNativeCustomerSession.ts` (resolucion local siempre `pre_plan`; resolucion externa segun el llamador) y en `runCustomerOnboardingPostPlanStage.ts` (fase `post_plan`, solo cuando el turno realmente ejecuta un `resolve_customer` fresco - nunca cuando reutiliza la evidencia de pre-plan).
  `customer_onboarding_transition_recorded`: `operation`, `previousStatus`/`previousVersion` (`null` para `start`), `nextStatus`/`nextVersion`, `pendingFields`, `collectedAvailability` (booleanos por campo, nunca los valores), `hasResolvedCustomer`. Se registra solo cuando `CustomerOnboardingMutationResult.status` es `"created"`/`"updated"` real - nunca para `"unchanged"` ni para un resultado `!ok` (test 15/42).
  Transiciones actualmente emitidas (con sitio de llamada real en el runtime): `start`, `collect_fields`, `mark_resolving`, `complete`, `mark_conflict`, `mark_temporarily_unavailable`. `markResolving`/`markConflict`/`markTemporarilyUnavailable`/`completeOnboarding` se instrumentaron una sola vez, dentro de los dos helpers compartidos de `onboardingTransitions.ts` (`landOnboardingInTerminalState`/`completeOnboardingWithCustomer`), cubriendo automaticamente a los tres llamadores existentes (`resolveNativeCustomerSession.ts`, `runCustomerOnboardingPostPlanStage.ts`, `customerIdentityCapabilities.ts`) sin duplicar logica ni requerir un wrapper de servicio; `start`/`collect_fields` se instrumentaron en linea en sus 3 sitios de llamada reales (uno en `runCustomerOnboardingPostPlanStage.ts`, dos en `customerIdentityCapabilities.ts`).
  Mapping reservado, todavia no emitido: `record_verification_failure` esta incluido en `CustomerOnboardingTransitionOperation` (el tipo TypeScript del payload) para que la funcion de mapeo sea exhaustiva desde el dia uno, pero no tiene ningun sitio de llamada real hoy (`CustomerOnboardingService.recordVerificationFailure`, igual que `retryResolution`, no es invocado por ningun caller productivo - confirmado por grep). No debe interpretarse como comportamiento runtime conectado: ningun turno del ciclo autonomo emite hoy un evento con `operation: "record_verification_failure"`. Queda mapeado para cuando esa operacion se conecte a un caller real, sin instrumentacion muerta que ejecutar mientras tanto.
  `customer_identity_capability_outcome_recorded`: aplica a `resolve_customer`/`create_customer`/`link_external_identity`. Separa `gatewayStatus` (el `CapabilityGatewayExecutionStatus` tecnico) de `businessOutcome` (String derivado por `deriveIdentityCapabilityBusinessOutcome`, `lib/brain/commercial/capability-gateway/identityCapabilityOutcome.ts`): cuando el Gateway reporta `completed`, se lee el resultado de dominio anidado (`ResolveCustomerResult`/`CreateCustomerResult`/`LinkExternalIdentityResult`) mediante un switch exhaustivo con `assertNever` - confirmando en tests el caso explicito del contrato ("Gateway status: completed / Business outcome: conflict") para `create_customer` y `link_external_identity`; para el resto de los `gatewayStatus` (sin resultado de dominio que inspeccionar, p.ej. denegado por policy antes de llamar a Customer Service) se usa un mapeo de respaldo tambien exhaustivo sobre `CapabilityGatewayExecutionStatus`. Se registra en el unico punto compartido para `resolve_customer` (`defaultResolveCustomerExternal`, cubre pre-plan y post-plan sin duplicar) y en los dos sitios de `executeGovernedCapability` para `create_customer`/`link_external_identity` dentro de `runCustomerOnboardingPostPlanStage.ts`. Se omite por completo cuando `executionPublicId` es `null` - nunca fabrica un id para correlacionar.
  `customer_session_warning_recorded`: persiste unicamente codigos de `NativeSessionWarning` (`warnings.ts`, filtrados con `isNativeSessionWarning` antes de escribir - nunca texto libre), con `phase` y `executionPublicId` opcional. Se registra al final de `resolveNativeCustomerSession` y en cada punto de salida de `runCustomerOnboardingPostPlanStage` (los tres `return`), sobre la lista ya deduplicada por `mergeWarnings` - dos codigos distintos producen dos eventos, el mismo codigo en el mismo turno (mismo `messageId`+`phase`) nunca duplica.
  Dedupe (todas las familias, sin PII ni timestamps en la clave): `identity:<messageId>:<phase>:<resolver>:<outcome>`, `onboarding:<conversationId>:<nextVersion>:<operation>`, `identity-capability:<executionPublicId>:<businessOutcome>`, `identity-warning:<messageId>:<phase>:<warningCode>` (`lib/brain/commercial/events/dedupe.ts`).
  Correlacion: `correlationId`/`conversationId`/`opportunityId`/`customerId` se propagan cuando estan disponibles; `decisionId` (sin columna propia en `CommercialEventV1`) viaja en `metadata.decisionId` solo cuando existe. La resolucion pre-plan nunca tiene `opportunityId`/`decisionId` (aun no existen en esa fase, sin cambios de T06). Para que la correlacion post-plan sea real cuando el turno ya genero una oportunidad/decision, `CustomerOnboardingPostPlanInput` gano dos campos opcionales (`opportunityId`, `decisionId`) que `runNativeAutonomousCycle.ts` llena desde `groundedLoop.resultingState?.opportunityId`/`groundedLoop.decisionRecord?.decisionId` (valores ya calculados un paso antes para la etapa de catalogo, nunca re-derivados) - sin tocar el orden pre-plan/post-plan ni la autoridad de `create_customer`/`link_external_identity` (esos dos capabilities se siguen invocando con el mismo `CapabilityGatewayContext` de siempre; los campos nuevos solo alimentan los eventos de T07).
  Privacidad (contrato seccion 7): el Gateway persistia hasta ahora `request_summary_json`/`response_summary_json` crudos (`input`/`outcome.data` verbatim) para toda capability, incluido `resolve_customer` - cuyo input real es `{externalId, phoneNumber, email}`. Se agregaron dos hooks opcionales a `CapabilityGatewayDefinition` (`buildRequestSummary`/`buildResponseSummary`, `types.ts`) que `executeCapability.ts` usa solo si el definition los provee - `search_products`/`get_product_details` siguen exactamente igual (regresion cubierta por su test existente). Las tres capabilities de identidad (`customerIdentityCapabilities.ts`) implementan summaries allowlisted: request (`channel`, `phoneAvailable`, `emailAvailable`, `consentPresent`, `purpose`, `hasResolvedCustomer`, `hasExternalIdentity`) y response (`businessOutcome`, `gatewayStatus`, `retryable`, `stableErrorCode`, `hasResolvedCustomer`, `hasExternalIdentity`) - nunca email/telefono/wa_id/nombres/order reference/candidatos, confirmado con tests que fuerzan esos valores en el fixture y verifican su ausencia en la fila persistida.
  Fail-safe (seccion 10): cada funcion exportada de `identityAuditEvents.ts` envuelve su propia llamada a `recordCommercialEvent` en `try/catch` - un fallo de auditoria nunca se propaga como excepcion ni altera el valor de retorno de `resolveNativeCustomerSession`/`runCustomerOnboardingPostPlanStage`/las capabilities de identidad (confirmado con un test dedicado); `recordCommercialEvent` en si ya era fail-safe desde antes (nunca lanza, siempre devuelve `{ok:false}`). No se abrio ninguna segunda transaccion distribuida - la escritura del evento es una operacion independiente, best-effort, posterior a la transicion real ya confirmada.
  44 tests nuevos en 2 archivos: `tests/commercial/customerIdentityAuditEvents.test.ts` (39 - resolucion local/externa, transiciones de onboarding, business outcomes, warnings, privacidad, idempotencia, fronteras, mas 3 tests de integracion end-to-end contra los orquestadores reales) y `tests/commercial/identityCapabilityGatewaySummaries.test.ts` (5 - summaries del Gateway via `executeGovernedCapability` real contra un servidor HTTP local, mismo patron que T04.1/T06). Ningun test existente se modifico. Suite dirigida dependiente (eventos comerciales, capability gateway, customer session, customer identity capability gateway, create/link customer capability, customer onboarding, customer onboarding post-plan, customer session privacy, native inbound identity boundary, identity conflict): 158 + 39 + 5 = 202 tests, 202 pass, 0 fallos nuevos. Suite completa del repo: 1105 tests, 1104 pass, exactamente 1 fallo preexistente (`tests/domains/customerOnboarding.test.ts`, deuda de `schema_migrations` documentada desde T03.1, sin relacion con T07). `npx tsc --noEmit`, `npm run lint` (35 warnings preexistentes, 0 errores) y `npm run docs:validate` limpios.
  No implementado en `T07` (queda para tareas posteriores): validacion end-to-end contra un Customer Service real desplegado (`ACS-R1-04-T08`); `record_customer_interest` sigue sin persistencia operacional (sin cambios); multi-request sigue sin ejecutar capabilities de identidad (sin cambios, confirmado por test estructural); ningun cambio a `crm_customer_onboarding` legacy, a Customer 360, ni a la autoridad de `create_customer`/`link_external_identity`/`resolve_customer`.

## Regla de actualizacion

Cuando la tarea actual termine:

1. Cambiar su estado a `done`.
2. Mover la siguiente tarea a `in_progress`.
3. Actualizar `ACTIVE_RELEASE.md`.
4. Actualizar `current_task` y `next_task`.
5. Agregar evidencia de cierre.
6. Actualizar `CAPABILITY_MATRIX.md` si cambio la implementacion real.
7. No crear una auditoria por cada tarea.
8. Crear auditoria solo al cerrar una release.
