---
release: ACS-R1-05
title: Autonomous Follow-up Runtime
doc_id: release-acs-r1-05-autonomous-follow-up-runtime
status: parallel_in_progress
updated_at: 2026-07-17
current_task: ACS-R1-05-T05
next_task: ACS-R1-05-T07
blocked: false
owner: product
source_of_truth_for:
  - ACS-R1-05 release scope
  - ACS-R1-05 task queue
  - ACS-R1-05 definition of done
depends_on:
  - ../ACTIVE_RELEASE.md
  - ../ROADMAP.md
  - ../product/MVP_EXECUTION_MAP.md
  - ../audits/follow-up-runtime-reconciliation.md
  - ../product/ai-sdr-follow-up-planner.md
  - ../product/follow-up-decision-policy.md
supersedes: []
tags:
  - release
  - product
---

# ACS-R1-05 - Autonomous Follow-up Runtime

## Estado

`parallel_in_progress`: workstream autorizado a avanzar en paralelo a `ACS-R1-04` porque no depende del Customer Service externo (`PAUSED_EXTERNAL`, ver `ROADMAP.md`). No es una segunda release "activa" en el sentido secuencial de `AGENTS.md` - es una excepcion explicita y acotada al follow-up, documentada aqui y en `ROADMAP.md`/`ACTIVE_RELEASE.md`.

## Objetivo

Consolidar y endurecer el runtime de follow-up ya existente, reutilizando planner, `crm_agent_actions`, worker, ciclo autonomo, outbox y Meta, sin crear un runtime paralelo nuevo.

## Alcance y Definition of Done: derivados de la auditoria

Esta release no redefine hallazgos: el alcance completo, la matriz de clasificacion de componentes, la ruta canonica reconstruida, las hipotesis verificadas (persistencia incompleta, worker, contact policy, cancelacion, outbox/envio, shadow flags, seguridad, idempotencia) y los gaps priorizados (P0-P3) viven en [Follow-up runtime reconciliation](../audits/follow-up-runtime-reconciliation.md) - unica fuente de esa evidencia. Cada tarea de esta release cierra exactamente uno o mas gaps de esa auditoria; ninguna tarea puede reabrir alcance que la auditoria no encontro.

## No objetivos

- Construir un runtime de follow-up paralelo o alternativo al ya conectado (`sales-consultative` -> `crm_agent_actions` -> `autonomous-followup-worker` -> `runNativeAutonomousCycle` -> outbox -> Meta).
- Marketing automation, campanas o contacto masivo (eso es `marketing_contact_policy`, fuera de esta release).
- Address Book, Quote, Shipping, Checkout, Voice.
- Declarar follow-up `operational: verified` en `CAPABILITY_MATRIX.md`.
- Cerrar o desbloquear `ACS-R1-04`.

## Required reading

- [Follow-up runtime reconciliation](../audits/follow-up-runtime-reconciliation.md)
- [ROADMAP](../ROADMAP.md)
- [MVP execution map](../product/MVP_EXECUTION_MAP.md)
- [ACTIVE_RELEASE](../ACTIVE_RELEASE.md)
- [AI SDR follow-up planner](../product/ai-sdr-follow-up-planner.md)
- [Follow-up decision policy](../product/follow-up-decision-policy.md)
- [AI SDR agent action queue](../product/ai-sdr-agent-action-queue.md)
- [AI SDR action lifecycle contract](../product/ai-sdr-action-lifecycle-contract.md)

## Tareas

| ID | Tarea | Estado | Dependencias | Gap(s) de la auditoria que cierra |
| -- | ----- | ------ | ------------ | ---------------------------------- |
| ACS-R1-05-T01 | Consolidar planner y persistencia | done | [Follow-up runtime reconciliation](../audits/follow-up-runtime-reconciliation.md) | P0-1 (hardcodes `attempt_number`/`max_attempts`/`policy_status`, idempotency key sin scope temporal); consolida `follow-up-planner/planFollowUp.ts` como fuente de calculo para `sales-consultative/repository.ts` |
| ACS-R1-05-T02 | Aplicar follow-up dispatch policy | done | ACS-R1-05-T01 | P0-4 (opt-out/quiet-hours/identity-conflict shadow-only, nunca gatean el write real); conecta `follow_up_dispatch_policy` (`evaluateCommercialPolicy`) como gate obligatorio antes de `upsertActionRow` |
| ACS-R1-05-T03 | Hardening del worker | done | ACS-R1-05-T01 | P0-2 (sin stale-lock recovery), P0-3 (sin retry/enforcement de `max_attempts`), P1-1 (`cancelFollowUp` sin precondicion de status) |
| ACS-R1-05-T04 | Consolidar outbox y delivery outcomes | done | ACS-R1-05-T01 | P1-3 (delivery outcomes no llegan a `crm_opportunities`), P1-4 (dos escritores divergentes de `brain_message_outbox`) |
| ACS-R1-05-T05 | Aislar runtimes paralelos o muertos | ready | ACS-R1-05-T01 | P2-1 (5 planners paralelos), P2-2 (`multi-request/requestFollowups.ts` muerto), P2-5 (modulo `outbox-worker/` hyphenated duplicado) |
| ACS-R1-05-T06 | Seguridad y configuracion operacional | done | ACS-R1-05-T01 | P1-2 (`failure_reason` sin redactar), P1-5 (flags auto-escalados en silencio por los dos workers) |
| ACS-R1-05-T06.2 | Continuidad del turno de venta reactivo | done | ACS-R1-05-T06 | Incidente de piloto real (ver "Evidencia de cierre - ACS-R1-05-T06.2" abajo): resultado del ciclo reactivo descartado, censura lexical comercial en el sandbox, autoridad de policy divergente entre decision/action, `recentCustomerReply` autobloqueando el propio turno, acciones bloqueadas sin terminalizar, y sin batch/ranking por presupuesto en la recomendacion de catalogo |
| ACS-R1-05-T07 | E2E productivo y restart recovery | ready | ACS-R1-05-T01 a T06.2 | Cierra la release: verifica end-to-end (patron del harness existente de identity onboarding) que el runtime consolidado sostiene reinicio/retry sin duplicar ni perder envios |

## Tarea actual

`ACS-R1-05-T05`

## Definition of Done de la tarea actual

`ACS-R1-05-T05` debe aislar o eliminar el codigo muerto/paralelo identificado por la auditoria: `multi-request/requestFollowups.ts` (cero callers productivos), la familia `autonomous-loop` relativa a produccion, y el modulo `outbox-worker/` hyphenated (alcanzable solo desde el simulador `/dev`); y reconciliar `ai-sdr-follow-up-planner.md`/`follow-up-decision-policy.md` con la implementacion real (`sales-consultative`).

## Siguiente tarea

`ACS-R1-05-T07` (`ready`, no iniciada - su fila de dependencias exige T01 a T06.2, lo que incluye T05; no puede aceptarse formalmente hasta que T05 cierre, ver nota operativa)

## Nota operativa - diferimiento de ACS-R1-05-T05

`ACS-R1-05-T05` se diferio temporalmente (de `in_progress` a `ready`, sin codigo tocado - no existia rama ni commit propios en el momento del diferimiento) para priorizar `ACS-R1-05-T06` y acelerar el piloto controlado. `T05` sigue siendo obligatoria antes de la aceptacion formal de `ACS-R1-05-T07` (su fila de dependencias exige `T01 a T06.2`, rango que incluye `T05`). `ACS-R1-05-T06.2` (ver evidencia de cierre abajo) corrigio gaps reales encontrados en el piloto controlado, sin tocar `T05`. El smoke E2E controlado que `T06`/`T06.1`/`T06.2` dejan preparado (allowlist no vacia, workers fail-closed, sin auto-escalacion de flags, continuidad del turno reactivo garantizada) **no equivale** a `ACS-R1-05-T07` ni habilita declarar `operational: verified` en `CAPABILITY_MATRIX.md` - es unicamente la condicion de seguridad y continuidad operacional para poder ejecutarlo de forma controlada.

## Evidencia de cierre - ACS-R1-05-T06.2

Corrigio un incidente real de piloto controlado: un cliente indico "Necesito una jaula de potencia para entrenar en casa, mi presupuesto maximo es $800.000, ¿que modelos me recomiendan?"; el sistema no ejecuto la busqueda/recomendacion, y en el turno siguiente ("No estoy seguro.") el sandbox bloqueo una respuesta legitima del modelo por contener la palabra "precios" (censura lexical), dejando la accion en `crm_agent_actions.status = "proposed"` sin fila en `brain_message_outbox` y al cliente sin respuesta. La auditoria del incidente encontro ademas: el resultado crudo del Sales Agent podia seguir siendo usado despues de policy; `recentCustomerReply` podia activar revision en el mismo ciclo reactivo que la genera; `human_owner_active` podia aparecer como warning aunque el flag real fuera falso; CRM solo hidrataba con precio un producto (no batch); y el flujo no volvia a ejecutar tras las tools.

- Estado: `done`.
- Rama: `acs-r1-05-t06-2-sales-turn-continuity` (sin push, sin PR).
- Archivos funcionales nuevos: `lib/brain/commercial/continuity/` (`salesTurnDisposition.ts`, `ensureAutonomousSalesTurnContinuity.ts`, `terminalizeBlockedAgentAction.ts`, `dispatchFallbackAction.ts`, `buildContinuityFallbackMessage.ts`, `index.ts`), `lib/brain/commercial/native-cycle/rankCatalogCandidatesByBudget.ts`.
- Archivos funcionales modificados: `lib/brain/native-whatsapp/service.ts` (captura el resultado del ciclo via el nuevo servicio de continuidad, ya no lo descarta), `lib/brain/commercial/native-cycle/runNativeAutonomousCycle.ts` (expone `commercialNeed` en el resultado, sin segunda lectura a DB), `lib/brain/commercial/native-cycle/buildCatalogGroundedMessage.ts` (reescrito: search -> batch (una sola llamada) -> ranking por presupuesto -> composicion grounded), `lib/brain/commercial/operational-loop/runCommercialOperationalLoop.ts` (`buildDefaultInputContext` prioriza `commercialPolicyResult.governedResult` sobre el resultado crudo), `lib/brain/commercial/policy/evaluateCommercialPolicy.ts` + `policyTypes.ts` + `policyConstants.ts` (`recentCustomerReply` ya no participa en `channelReview`; `quiet_hours_active`/`manual_approval_required` se reportan con su propio codigo, nunca como `human_owner_active`), `lib/brain/commercial/action-queue/buildAgentAction.ts` (`policy_status` propaga la autoridad real de policy en vez de recalcularla), `lib/brain/commercial/autonomy-sandbox/validateAutonomousReplyCandidate.ts` (elimina la regla lexica `precio/stock/descuento/...`), `lib/catalog/types.ts` + `httpCatalogAdapter.ts` (agrega `batchGetProducts`, `POST /v1/products/batch`), `lib/brain/commercial/capability-gateway/registry.ts` (registra `batch_get_products`, capability interna no aliasada al LLM), `lib/brain/commercial/events/*` (dos eventos nuevos: `autonomous_turn_disposition`, `autonomous_turn_continuity_failed`, sobre `commercial_event` existente).
- **A. Continuidad**: `ensureAutonomousSalesTurnContinuity` envuelve `runNativeAutonomousCycle` y es el unico punto que `processNativeWhatsAppInbound` invoca ahora. Construye un `SalesTurnDisposition` (`terminalOutcome`/`commercialObjective`/`responseOwner`/`responsePlanned`/`opportunityAdvanced`/`nextBestActionDefined`/`fallbackUsed`/`followUpEligible`/`followUpReason`) para todo turno reactivo. Cuando el bridge no logra colocar una respuesta para un `nextAction.type` que exige respuesta al cliente (`respond`/`ask_clarifying_question`) y el motivo no es real ownership humano ni un motivo excluido explicitamente (`recipient_not_whitelisted`/`unsupported_channel`/`case_closed`), terminaliza la accion original a `status = "blocked"` via CAS (`UPDATE ... WHERE status IN ('proposed','requires_review','approved') AND outbox_message_id IS NULL`, nunca sobrescribe `planned`/`executing`/`executed`/`cancelled`/`failed`) y despacha una accion de fallback idempotente (`continuity-fallback:<conversationId>:<inboundMessageId>:<fallbackClass>`) por el mismo pipeline real (`persistAgentAction` -> `evaluateAgentActionForSandbox` -> `executeActionThroughGate`/`SqlExecutionUnitOfWork`, nunca un INSERT directo a `brain_message_outbox`). Ownership humano real (`resultingState.humanOwnerActive`/`aiBlocked`) nunca recibe un fallback de IA - queda `responseOwner: "human"`/`human_response_required`. Pilotos fuera de allowlist (`wa_id_not_authorized_for_pilot`/flags apagados, T06.1) quedan fuera del contrato de continuidad por diseño (cero side effects ya garantizado por T06.1) y no persisten evento. Un fallo tecnico real antes de tener contexto (p. ej. `conversation_not_found`) persiste `autonomous_turn_continuity_failed` en vez de simular continuidad.
- **B. Autoridad de policy**: `runCommercialOperationalLoop.ts#buildDefaultInputContext` ahora usa `commercialPolicyResult.governedResult` como fuente de `salesAgentResult` para el resto del loop (reduccion de estado, seleccion de next-action) en vez del resultado crudo del shadow/runtime - el crudo queda disponible solo para auditoria via `commercialPolicyResult.originalResultReference`. `buildAgentAction.ts` ya no recalcula `policy_status` de forma independiente (perdiendo `allowed_with_restrictions`/`failed_safe`) sino que propaga `commercialPolicyResult.status` (via `loop.decisionRecord.policyStatus`, la misma fuente que ya escribe `crm_agent_decisions.policy_status`), cerrando la divergencia decision/action.
- **C. recentCustomerReply y warnings precisos**: `evaluateCommercialPolicy.ts#computeChannelSignals` ya no incluye `recentCustomerReply` en `channelReview` - el mensaje que el ciclo reactivo esta procesando ya no puede bloquear su propia respuesta. La señal sigue disponible en `channelContext` para `evaluateCommercialActions.ts`, que la sigue usando (sin cambios) para bloquear una accion `follow_up` cuando el cliente ya respondio. `human_owner_active`/`quiet_hours_active`/`manual_approval_required` ahora se reportan cada uno con su propio codigo real (`COMMERCIAL_POLICY_ISSUE_CODES`/`COMMERCIAL_POLICY_RULE_IDS` ampliados con `quiet_hours_active`/`manual_approval_required`, `POLICY-OUTBOUND-QUIET-HOURS`/`POLICY-OUTBOUND-MANUAL-APPROVAL`) - nunca colapsados bajo `human_owner_active` cuando ese flag especifico es falso.
- **D. Sandbox sin censura lexical (corregido - ver seccion H)**: `validateAutonomousReplyCandidate.ts` elimino `hasPromiseOrSensitiveCommercialClaim` (la lista `precio/stock/descuento/entrega/despacho/garantia/reclamo/queja/reembolso/devolucion` + verbos de promesa `promet/garantiz/asegur/confirm/...`). El primer cierre de T06.2 dejo esto sin reemplazo alguno - un hallazgo P1 real de la auditoria posterior (ninguna capa impedia que el Sales Agent enviara un compromiso comercial no respaldado del tipo "te garantizo stock"). La seccion H documenta la correccion de dos capas que lo cierra. Se conservan intactos todos los controles tecnicos/operacionales preexistentes: mensaje vacio, limite de longitud, placeholders sin resolver, credenciales/Bearer/API keys, JSON crudo, allowlist de destinatario, canal, tipo/estado de accion, idempotency key, duplicados, `human_owner_active`/`ai_blocked`/`case_closed` reales, riesgo/aprobacion.
- **E. Catalogo: search -> batch -> ranking por presupuesto -> composicion grounded**: `lib/catalog/types.ts`/`httpCatalogAdapter.ts` agregan `batchGetProducts` (contrato real confirmado contra el repositorio `MS-pesaschile-catalog-service`: `POST /v1/products/batch`, maximo 20 items, respuesta `{items: [{ok, input, product|error}]}}` - no se modifico ese repositorio, el contrato ya soportaba esto). `capability-gateway/registry.ts` registra `batch_get_products` como capability interna (nunca aliasada al LLM - el Sales Agent solo pide `searchProducts`; C5 del contrato de la tarea). `buildCatalogGroundedMessage.ts` reescrito: tras un `search_products` exitoso, hidrata TODOS los candidatos (hasta 20) en una sola llamada batch, filtra solo items `ok:true`, y `rankCatalogCandidatesByBudget.ts` (nuevo) selecciona economy/near_budget/stretch (o hasta 3 alternativas relevantes cuando no hay presupuesto, o las mas cercanas cuando todo excede el presupuesto) - nunca inventa precio, stock o atributos, nunca reglas especificas por categoria. El mensaje compuesto nombra solo productos hidratados exitosamente, incluye motivo por tier (economico/cercano al presupuesto/superior) y un siguiente paso. `commercialNeed` (productQuery/usage/budgetMax/currency) se lee de `crm_sales_need_profiles` (via `snapshot.needProfile`, ya cargado) y de la propia query de busqueda - sin segunda lectura a DB, sin inventar el numero de categoria.
- **F. Fallback classes (A7)**: `buildContinuityFallbackMessage.ts` implementa las 5 clases del contrato (`catalog_unavailable`, `model_unavailable`, `invalid_model_result`, `unsafe_primary_draft`, `handoff_acknowledgement`), cada una preservando `productQuery`/`usage`/`budgetMax` cuando se conocen (nunca inventando producto/precio/plazo), con texto distinto por clase - nunca un placeholder administrativo vacio.
- **G. Observabilidad**: dos eventos nuevos sobre `commercial_event` existente (sin tabla nueva): `autonomous_turn_disposition` (dedupe `autonomous-turn-disposition:<inboundMessageId>`, payload con `responseOwner`/`commercialObjective`/`primaryActionId`/`primaryDisposition`/`primaryBlockReasons`/`fallbackActionId`/`outboxId`/`opportunityAdvanced`/`nextBestAction`/`followUpEligible`/`followUpReason`/`terminalOutcome`) y `autonomous_turn_continuity_failed` (cuando no hay responsable ni salida valida).
- **No objetivo tocado**: `crm-followup` no se activo, ningun envio real fuera del `wa_id` autorizado, `T05` no se toco, flags fail-closed sin cambios, sin migraciones nuevas (cero cambios de schema), `MS-pesaschile-catalog-service` no se modifico (contrato ya soportaba batch).
- **Migraciones**: cero. Todos los cambios reutilizan `crm_agent_actions`, `crm_agent_decisions`, `crm_opportunities`, `crm_sales_need_profiles`, `commercial_event` y `crm_capability_executions` existentes.
- **Tests**: nuevos - `tests/commercial/rankCatalogCandidatesByBudget.test.ts` (7/7, puro), `tests/catalog/httpCatalogAdapter.test.ts` (+3 tests de batch, 15/15 el archivo completo), `tests/commercial/buildContinuityFallbackMessage.test.ts` (5/5, puro), `tests/commercial/autonomySandbox.test.ts` (actualizado: nuevas aserciones de frases legitimas permitidas + bloqueos tecnicos reales, 31/31), `tests/commercial/evaluateCommercialPolicy.test.ts` (actualizado + 3 tests nuevos, 38/38), `tests/commercial/capabilityGatewayHardening.test.ts` (9/9, sin cambios de codigo, verificado en verde). DB-backed (no ejecutables en el entorno de implementacion - ver informe final): `tests/commercial/continuityFallback.test.ts`, `tests/commercial/buildCatalogGroundedMessageBudget.test.ts`, `tests/native/ensureAutonomousSalesTurnContinuity.test.ts`, mas actualizaciones a `tests/native/catalogCapabilityCycle.test.ts` y `tests/native/catalogConversationFlow.test.ts` (sus fakes HTTP ahora responden tambien `POST /v1/products/batch`). Ademas se corrigieron 5 tests preexistentes cuyas fixtures horneaban el bug de autobloqueo (`recentCustomerReply`) o la censura lexica ya removida: `tests/commercial/executionGate.test.ts`, `tests/commercial/aiSdrReviewSurface.test.ts`, `tests/commercial/buildCommercialShadowReview.test.ts`, `tests/commercial/runCommercialShadowEvaluation.test.ts` (2 tests).
- **Regresion**: comparacion exacta contra `develop` (`git stash` + suite completa en ambos lados, mismo commando): baseline 1293 tests/959 pass/334 fail; esta rama 1324 tests/984 pass/340 fail. Los 334 fallos de baseline persisten identicos (mismos nombres, no relacionados - conexion a MariaDB no disponible en el entorno de implementacion); los 6 fallos nuevos son exclusivamente los tests DB-backed nuevos de esta tarea (mismo motivo de conexion). Cero tests previamente verdes pasaron a fallar.
- **Limitacion del entorno de implementacion**: Docker Desktop no pudo mantener el daemon estable en este entorno (multiples intentos, `docker info` nunca establecio conexion sostenida), por lo que ningun test contra MariaDB real pudo ejecutarse aqui - incluye los tests DB-backed nuevos de esta tarea y la suite DB-backed preexistente. `npx tsc --noEmit` y `npm run build` se ejecutaron limpios en cada etapa. Recomendacion: ejecutar la suite completa contra MariaDB real antes de aceptar formalmente esta tarea.

### Correccion posterior (segunda pasada) - hallazgos P1 y P2 de la auditoria

Una auditoria independiente del cierre anterior (evidencia `file:line` contra el codigo real, no contra esta documentacion) encontro un hallazgo **P1** real: la seccion D elimino por completo `hasPromiseOrSensitiveCommercialClaim` sin ningun reemplazo, dejando el sistema sin ningun control que impidiera un compromiso comercial no respaldado ("Te garantizo stock.", "El despacho esta asegurado.") - la afirmacion de la seccion D de que la truthfulness comercial estaba "gobernada aguas arriba por el contrato del Sales Agent, Commercial Policy, el Capability Gateway y el grounding de catalogo" no estaba respaldada por ningun mecanismo real verificable. Encontro ademas dos hallazgos **P2**: `rankCatalogCandidatesByBudget.ts#hasUsablePrice` no excluia precios negativos explicitamente, y los tests de concurrencia de continuidad (`continuityFallback.test.ts`) solo probaban replay secuencial, nunca `Promise.all` real.

- Estado: `implementation complete, DB verification pending, formal acceptance blocked` (sin cambios respecto del estado previo a esta correccion - sigue bloqueado unicamente por falta de MariaDB real en el entorno de implementacion, nunca por hallazgos abiertos).
- Rama: `acs-r1-05-t06-2-sales-turn-continuity` (misma rama, commits nuevos encima de `865b411`; sin push, sin PR).
- **H. Correccion P1 - dos capas, ninguna blacklist amplia**: la correccion distingue tres cosas (pregunta / accion pendiente / afirmacion) mediante un clasificador de oraciones deterministico compartido (`lib/brain/commercial/commercialSentenceClassifier.ts`: `isQuestionSentence`, `isPendingActionSentence`) usado por ambas capas para que nunca diverjan sobre que es una pregunta o una accion pendiente:
  - **Capa 1 (sandbox, independiente de evidencia)**: `lib/brain/commercial/autonomy-sandbox/detectUnsupportedCommercialCommitment.ts` bloquea unicamente compromisos/certeza absoluta con patrones especificos de forma conjugada - nunca un stem amplio como el eliminado `garantiz`/`asegur`/`confirm` (eso es exactamente lo que causaba que "no puedo garantizarte" o "antes de confirmar el precio" se bloquearan antes). Patrones: verbo de promesa dirigido (`te/le garantizo/aseguro/confirmo/prometo/mantengo`), verbo de promesa en plural (`garantizamos/aseguramos/prometemos/confirmamos`), certeza pasiva con copula real (`esta/queda/sera asegurado/garantizado/confirmado` - deliberadamente exige la copula para que "Hay stock asegurado para hoy." (adjetivo sin copula, ya probado como legitimo) siga permitido mientras "El despacho esta asegurado." se bloquea), marcadores de certeza absoluta (`con toda seguridad`, `sin ningun problema`, `sin duda`, `cien por ciento`) y cobertura absoluta (`cubrira/resolvera cualquier`). Una oracion que es pregunta o accion pendiente nunca llega a evaluarse. Nunca requiere ni inspecciona evidencia - un compromiso de certeza futura es inseguro independientemente de cualquier evidencia presente, porque la evidencia solo describe el estado actual conocido.
  - **Capa 2 (policy, con evidencia real)**: `lib/brain/commercial/policy/evaluateCommercialCommitmentGrounding.ts` (nuevo evaluador, mismo patron que `evaluateCommercialClaims.ts`/`evaluateCommercialActions.ts`) escanea `salesAgentResult.responseProposal.draftText` (el texto crudo del Sales Agent, nunca el mensaje compuesto por `buildCatalogGroundedMessage.ts` - ese se compone despues de que policy ya corrio, ver `applyCatalogGroundingToNextAction.ts`) en busca de afirmaciones declarativas sobre un topico sensible (precio/stock/despacho/entrega/plazo/garantia/descuento/disponibilidad/pedido) sin un claim `type` correspondiente cuyo `status === "allowed"` en `claimEvaluation.assessments` (verificado, fresco, fuente fuerte). Esto cierra un gap que `evaluateCommercialClaims.ts` no podia cerrar solo: ese modulo unicamente gobierna claims que el Sales Agent efectivamente declaro en `responseProposal.claims[]`; nada impedia antes que el modelo escribiera un hecho comercial directamente en el texto libre sin declarar el claim estructurado correspondiente. Cuando encuentra una afirmacion sin respaldo, agrega un issue (`evidence_missing`, regla nueva `POLICY-DRAFT-STATEMENT-EVIDENCE`) y fuerza `requires_review` (nunca `blocked` - el mensaje se retiene para revision humana, no se descarta) via el mismo mecanismo ya usado por `channelReview`. Wireado en `evaluateCommercialPolicy.ts` inmediatamente despues de `claimEvaluation`.
  - Nueva regla `POLICY-DRAFT-STATEMENT-EVIDENCE` agregada a `COMMERCIAL_POLICY_RULE_IDS` en `policyConstants.ts` y `policyTypes.ts` (ambos archivos se mantienen sincronizados, patron preexistente). Nuevo block reason `unsupported_commercial_commitment` agregado a `SANDBOX_AUTONOMY_BLOCK_REASONS` (`autonomy-sandbox/types.ts`), mapeado a `unsafe_message` a nivel de execution gate (`evaluateExecutionGate.ts#mapSandboxBlockReasons`, mismo mapeo que `unsafe_payload`) - ninguna via privilegiada nueva, pasa por sandbox y execution gate exactamente igual que cualquier otro bloqueo.
  - Ambas capas verificadas contra el catalogo de frases exigido por la tarea de correccion, incluyendo variantes de conjugacion (`garantizamos`/`aseguramos`/`confirmamos`/`prometemos`) para que la implementacion no pase solo por reconocer frases exactas, y contra las 6 frases legitimas preexistentes de `autonomySandbox.test.ts` (incluida "Hay stock asegurado para hoy.", el caso limite copula-vs-adjetivo).
- **P2 - precios invalidos**: `rankCatalogCandidatesByBudget.ts#hasUsablePrice` ahora exige `Number.isFinite(amount) && amount >= 0`. Regla elegida tras revisar el dominio: `price >= 0`, no `price > 0` - el contrato de catalogo (`lib/catalog/types.ts`, ADR-005) documenta que un precio desconocido se representa como `amount: null`, nunca `0`; por lo tanto `0` es un precio real informado por el catalogo (ej. un accesorio incluido/promocional), y excluirlo seria ocultar un dato real del catalogo, no una correccion de seguridad. Se excluyen: `null`, `undefined`, `NaN`, `Infinity`/`-Infinity`, negativos y strings no numericos (los ultimos tres solo alcanzables si el adapter HTTP entregara un valor fuera de su propio contrato TypeScript - defensa en profundidad).
- **P2 - concurrencia real**: `tests/commercial/continuityConcurrency.test.ts` (nuevo) agrega tres escenarios con `Promise.all` real contra el pool `mysql2` real (no una fake in-memory ni una conexion unica serializada - `connectionLimit` por defecto de `mysql.createPool` permite conexiones concurrentes genuinas): (1) dos `dispatchFallbackAction` concurrentes para el mismo `idempotencyKey` -> exactamente una fila de accion, a lo sumo una fila de outbox; (2) dos `terminalizeBlockedAgentAction` concurrentes sobre la misma accion -> exactamente un ganador CAS, estado final `blocked` aplicado una sola vez; (3) dos `ensureAutonomousSalesTurnContinuity` concurrentes para el mismo inbound ya persistido -> exactamente una fila de outbox, exactamente un evento `autonomous_turn_disposition` (dedupe por `inboundMessageId`).
- **Tests nuevos**: `tests/commercial/detectUnsupportedCommercialCommitment.test.ts` (29 casos puros, clasificador aislado), `tests/commercial/autonomySandbox.test.ts` (+2 tests: 10 frases permitidas + 14 frases/variantes bloqueadas via el pipeline completo de sandbox), `tests/commercial/evaluateCommercialPolicy.test.ts` (+3 tests: misma frase declarativa con/sin claim verificado, preguntas/acciones pendientes nunca requieren grounding), `tests/commercial/rankCatalogCandidatesByBudget.test.ts` (+9 tests: negativo, NaN, Infinity, -Infinity, undefined, null, string no numerico, precio cero valido, mezcla invalida/valida nunca se mezcla en un tier), `tests/commercial/continuityConcurrency.test.ts` (3 tests, DB-backed, **DB verification pending** - ver limitacion de entorno abajo).
- **Regresion**: comparacion exacta contra `develop` (mismo comando, mismo entorno): baseline 1293 tests/959 pass/334 fail (sin cambios); esta rama tras la correccion 1373 tests/1030 pass/343 fail. Los 334 fallos de baseline persisten identicos. De los 9 fallos nuevos frente a baseline, los 6 preexistentes (DB-backed de la primera pasada de T06.2) mas los 3 nuevos de esta correccion (`continuityConcurrency.test.ts`) fallan exclusivamente por `ECONNREFUSED 127.0.0.1:3306` (uno de los tres, el de `dispatchFallbackAction` concurrente, falla primero en una aserción `attempted === true` porque `persistAgentAction` captura el error de conexion internamente en vez de relanzarlo - misma causa raiz, distinta superficie de fallo, confirmado leyendo el stack). Cero tests previamente verdes pasaron a fallar. `npx tsc --noEmit` y `npm run build` limpios.
- **Limitacion del entorno de implementacion (sin cambios respecto de la primera pasada)**: Docker Desktop siguio sin poder mantener el daemon estable en este entorno (`docker info` devuelve `500 Internal Server Error`), sin servicio MySQL local en el puerto 3306. El unico `DATABASE_URL` real configurado en `.env` apunta a un RDS de produccion (`pesas-productiva`) - no se uso para tests; todos los tests DB-backed (incluidos los tres nuevos de concurrencia) usan credenciales fijas a `127.0.0.1:3306` y fallan limpio contra localhost, sin riesgo de tocar produccion. Los tres tests de concurrencia nuevos quedan escritos y listos pero **no verificados** - no se declara su resultado como validado.

### Correccion posterior (tercera pasada) - dudas pendientes de la auditoria P1/P2

Una auditoria de solo lectura sobre la segunda pasada (evidencia `file:line`, sin modificar codigo) encontro que el detector de compromisos detectaba patrones sintacticos especificos, no significado: de 39 frases probadas manualmente, solo 8-9 quedaban bloqueadas por el sandbox; el resto evadia por formas no cubiertas (adjetivo sin copula, reflexivo/impersonal "se garantiza", plural sin "te" en "mantengo/mantenemos", copula elidida, falta la palabra exacta "ningun", modal+infinitivo+clitico "podemos garantizarte"). Encontro ademas que el grounding de `evaluateCommercialCommitmentGrounding.ts` operaba por `claim.type`, nunca por instancia concreta (un claim de precio verificado sobre un producto podia respaldar cualquier afirmacion de precio sobre cualquier producto), que `requires_review` nunca detenia realmente el envio (siempre terminaba en un mensaje autonomo de IA via `escalate_to_operator`/`handoff_acknowledgement`, con `responseOwner: "ai"` incluso cuando la resolucion real dependia de un humano), que `persistAgentAction.ts` no manejaba una colision real de `idempotency_key` bajo concurrencia (el `UNIQUE KEY` existe desde la migracion 005, pero el `INSERT` plano sin `ON DUPLICATE`/captura de `ER_DUP_ENTRY` hacia que la ejecucion perdedora retornara `status: "failed"`), y que la familia "reservado"/"apartado" evadia ambas capas por completo (sin palabra clave de topico ni participio rastreado).

- Estado: `implementation complete, DB verification pending, formal acceptance blocked` (sin cambios de bloqueo - los hallazgos de la auditoria quedan corregidos con evidencia y tests; sigue pendiente unicamente MariaDB real).
- Rama: `acs-r1-05-t06-2-sales-turn-continuity` (misma rama, commits nuevos; sin push, sin PR).
- **Grounding por instancia concreta**: `evaluateCommercialCommitmentGrounding.ts` ahora recibe `{type, value}` de cada claim (no solo `type`) y compara el numero normalizado (y, si es identificable, la moneda) declarado en la oracion contra el `value` del claim - nunca solo el tipo. Extrae montos con `extractNormalizedAmount` (formato chileno, separador de miles `.`) y moneda con `extractCurrencyToken` (`CLP`/`USD`/`EUR`/`ARS`, un `$` sin codigo explicito se asume `CLP`, la moneda del retailer). El contrato `SalesAgentClaim` no tiene `productId`/`entityId` estructurado - MVP deliberado, sin inventar contrato nuevo (seccion 5 de la tarea): dos productos distintos casi siempre tienen precios distintos, asi que el numero concreto es un proxy honesto de "misma instancia" sin pretender resolver identidad de entidad que el contrato no expone. `TOPIC_KEYWORDS` se amplio para incluir `cuesta(n)`/`vale(n)` ("La jaula A cuesta $500.000.") ademas del sustantivo `precio` - un bug real encontrado durante la implementacion: sin este ajuste, los propios ejemplos de la tarea nunca activaban el grounding en absoluto.
- **Coherencia de requires_review**: `commitmentGrounding.requiresReview` ahora participa tambien en el calculo de `requiresApproval` (antes solo en `status`), evitando la incoherencia `policy_status: "requires_review"` junto a `approval_requirement: "none"` en `crm_agent_decisions`/`crm_agent_actions`.
- **Handoff con ownership humano real**: `SalesTurnDisposition` agrega `acknowledgementSender`, `waitingFor` (`"customer_response" | "human_response" | "none"`) y `handoffCreated` (tambien persistidos en el payload de `autonomous_turn_disposition`). `ensureAutonomousSalesTurnContinuity.ts` distingue ahora `fallbackClass === "unsafe_primary_draft"` (problema de contenido - compromiso no respaldado o afirmacion sin grounding, necesita juicio humano) de los otros tres (`catalog_unavailable`/`model_unavailable`/`invalid_model_result`, fallas de infraestructura que la IA puede reintentar legitimamente sola): solo en el primer caso `responseOwner` pasa a `"human"` (nunca `"ai"`, aunque `acknowledgementSender: "ai"` refleje que la IA redacto el acknowledgement) y `waitingFor: "human_response"`. Mismo tratamiento en la rama `escalate_to_operator`. No se crea tabla ni capability de handoff nueva - el registro durable es la accion original terminalizada a `blocked` mas el evento `autonomous_turn_disposition` ya existente.
- **Reservas tratadas como compromiso no respaldado, nunca como capability**: `detectUnsupportedCommercialCommitment.ts` agrega `reservad[oa]s?`/`apartad[oa]s?` a la lista cerrada de participios de `PASSIVE_CERTAINTY_PATTERN`, mas el verbo copulativo `dejar` (`dejamos reservado`) junto a `estar`/`quedar`/`ser` - "Esto es solo una defensa. No es una capability de reserva" (seccion 7 de la tarea): cero estados, tablas o campos de inventory-hold agregados en ningun punto del sistema.
- **Idempotencia real de `persistAgentAction`**: el `INSERT` ahora esta envuelto en su propio `try/catch`; un `ER_DUP_ENTRY` (detectado via `error.code`, nunca por mensaje de texto) se recupera re-seleccionando la fila ganadora por `idempotency_key` y resolviendo a `"duplicate_ignored"` con esa fila - nunca a `"failed"`. Cualquier otro error de persistencia sigue propagandose sin cambios. `dispatchFallbackAction`/`ensureAutonomousSalesTurnContinuity.ts` ya trataban `"duplicate_ignored"` como resultado usable (`attempted: true`) desde la correccion anterior. Se encontro y corrigio ademas un caso residual: si la fila ganadora aun no habia terminado de escribir su outbox en el instante exacto de la re-seleccion, la ejecucion perdedora podia persistir un evento `autonomous_turn_continuity_failed` espurio - `ensureAutonomousSalesTurnContinuity.ts` ahora reconoce `actionPersistence.status === "duplicate_ignored"` y omite ese evento en ese caso especifico (la ejecucion hermana ya escribe, o escribira, su propia disposicion).
- **Precio cero excluido (revision de la regla)**: `rankCatalogCandidatesByBudget.ts#hasUsablePrice` cambia de `amount >= 0` a `amount > 0`. La regla anterior (segunda pasada) se apoyaba en que ADR-005 distingue `unknown != zero` a nivel de contrato abstracto, pero eso no confirma que el Catalog Service real alguna vez entregue `effectiveUnitPrice: 0` para un producto vendible - ningun fixture ni documentacion de contrato HTTP lo muestra, y un catalogo respaldado por PrestaShop tipicamente usa `0` para un producto sin precio configurado (una brecha de datos), no para un articulo gratuito. T06.2 no implementa productos gratuitos ni precios especiales - un `0` ahora se trata igual que cualquier otro precio invalido, nunca presentado como "0 CLP" ni "opcion economica".
- **Quiet hours - investigado y revertido**: la correccion anterior removio `quietHoursActive` del gate reactivo de `evaluateCommercialPolicy.ts` sobre la teoria de que podia bloquear indebidamente el turno reactivo. La auditoria de esta pasada encontro que esa teoria era incorrecta: `shadow/runCommercialShadowEvaluation.ts#buildChannelContext` (la fuente real de `channelContext` para el turno reactivo) ya hardcodea `quietHoursActive: false` incondicionalmente desde antes de T06.2 - el turno reactivo nunca recibe una senal real de quiet hours en ese punto. El unico caller que si pasa un `quietHoursActive` real es `sales-consultative/followUpDispatchPolicy.ts` (el gate de despacho de follow-up proactivo), y remover la senal rompio ese gate (`followUpDispatchPolicy.test.ts`, caso `[7]`) sin corregir nada real para el turno reactivo. Revertido integramente: `evaluateCommercialPolicy.ts#computeChannelSignals` y `evaluateCommercialActions.ts` vuelven a su forma exacta previa a la segunda pasada.
- **Tests nuevos/actualizados**: `evaluateCommercialPolicy.test.ts` (+6: mismo valor grounded, distinto valor via el ejemplo "jaula A/B" de la tarea, distinta moneda, claim no verificado, claim con evidencia debil, claim de tipo no relacionado; mas `requiresApproval` verificado como `operator_review` coherente), `detectUnsupportedCommercialCommitment.test.ts` (+7 frases: lista MVP de la seccion 6 mas la familia "reservado"), `autonomySandbox.test.ts` (+2 tests: reservas bloqueadas end-to-end, lista adicional de frases permitidas), `rankCatalogCandidatesByBudget.test.ts` (test de precio cero invertido de "valido" a "excluido"), `tests/native/ensureAutonomousSalesTurnContinuity.test.ts` (actualizado: `responseOwner`/`acknowledgementSender`/`waitingFor`/`handoffCreated` en el escenario de placeholder no resuelto y en el de human ownership real; +1 test nuevo: afirmacion de precio sin grounding -> `escalate_to_operator` -> `handoff_acknowledgement_planned` -> `responseOwner: "human"`, el precio no grounded nunca llega al outbox), `continuityConcurrency.test.ts` (barrera de sincronizacion explicita vía el punto de inyeccion del `provider` ya existente - ambas ejecuciones concurrentes deben llegar a la invocacion del LLM antes de que cualquiera continue, en vez de depender solo de `Promise.all`; aserciones actualizadas para exigir `attempted: true`/`actionPersistence.status !== "failed"` en ambas llamadas y para verificar que ninguna carrera perdida contra `persistAgentAction` produzca un evento `autonomous_turn_continuity_failed` espurio).
- **Regresion**: comparacion exacta contra `develop` (mismo comando, mismo entorno): baseline sin cambios (1293/959/334). Esta rama: 1394 tests/1050 pass/344 fail. Los 334 fallos de baseline persisten identicos. De los fallos nuevos frente a baseline, exactamente 10 son DB-backed (los 6 de la primera pasada + los 3 de concurrencia de la segunda pasada + 1 nuevo de esta pasada, el escenario de precio sin grounding), todos `ECONNREFUSED 127.0.0.1:3306`. Cero tests previamente verdes rotos, cero regresiones no relacionadas. `npx tsc --noEmit` y `npm run build` limpios en cada etapa.
- **Limitacion del entorno de implementacion (sin cambios)**: MariaDB sigue sin estar disponible (Docker Desktop no mantiene el daemon, sin servicio local en 3306). El unico `DATABASE_URL` real en `.env` sigue apuntando al RDS de produccion (`pesas-productiva`) y no se uso. Los tests DB-backed (incluidos los nuevos y actualizados de esta pasada) quedan escritos, correctos por inspeccion de codigo, y **no verificados** contra MariaDB real.

## Evidencia de cierre - ACS-R1-05-T06

Primer cierre (SHA funcional `b992ff9`, doc `1ed997e`) fue corregido por `ACS-R1-05-T06.1` (SHA funcional `c2953e7`): el primer intento construyo el lector fail-closed y elimino la auto-escalacion de `process.env`, pero `BRAIN_AUTONOMOUS_TEST_WA_IDS` en si mismo nunca se comparaba contra un `wa_id` real en ningun punto del ciclo autonomo, del follow-up worker ni del outbox worker - la unica enforcement real preexistente era la defensa final en `metaClient.ts`. `ACS-R1-05-T06.1` cierra ese gap sin tocar `ACS-R1-05-T05` (sigue `ready`) ni `ACS-R1-05-T07`, sin ejecutar envios reales a Meta y sin declarar el runtime `operational: verified`. Estado final: `done`.

- Estado: `done`.
- SHA funcional: `c2953e7` (correccion T06.1); `b992ff9` fue el primer intento. Rama `acs-r1-05-t06-operational-safety`.
- Archivos funcionales (T06 + T06.1): `lib/brain/runtime/autonomousRuntimeConfig.ts` (lector canonico y fail-closed; T06.1 anade `loadAutonomousPilotAllowlist`/`isWaIdAuthorizedForPilot` con normalizacion de digitos compartida con `metaClient.ts`, el contrato de 5 flags del follow-up worker y el reporte de preflight puro), `lib/brain/commercial/redactErrorMessage.ts` (T06.1 anade redaccion de email/telefono), `lib/brain/commercial/native-cycle/runNativeAutonomousCycle.ts` (T06.1 - gate temprano, nuevo en esta correccion), `lib/brain/commercial/followup/runFollowupTick.ts` (T06.1 anade el gate pre-claim), `lib/brain/messaging/autonomousOutboxTick.ts` (T06.1 - consolida el guard de allowlist existente sobre el comparador canonico y redacta los dos escritores de error restantes), `lib/brain/commercial/action-queue/persistActionOutcome.ts` (`markActionFailed`), `scripts/autonomous-followup-worker.ts` (T06.1 anade el gate de arranque `assertFollowUpWorkerRuntimeConfigIsSafe`), `scripts/autonomous-outbox-worker.ts`, `scripts/commercial-live-autonomy-run.ts`, `scripts/autonomous-runtime-preflight.ts` (nuevo en T06.1).
- **Diagnostico previo T06 (P1-5/P1-2)**: se inspeccionaron los dos workers productivos, el endpoint HTTP del outbox worker, el ciclo autonomo inbound, execution gate/bridge, el outbox writer, `metaClient.ts` y todos los usos de los cuatro flags nombrados. Hallazgo central: `lib/brain/commercial/config/commercialCycleConfig.ts#readEnvFlag` y los lectores de `outboxWorker.ts`/`metaClient.ts` YA eran fail-closed (`trim().toLowerCase() === "true"`, sin auto-escalacion) - el gap P1-5 real no estaba en el parseo de booleanos sino en tres scripts CLI (`scripts/autonomous-followup-worker.ts`, `scripts/autonomous-outbox-worker.ts`, `scripts/commercial-live-autonomy-run.ts`) que escribian `process.env[key] = "true"` para cada flag ausente antes de que cualquier lector corriera. Para P1-2 se confirmaron exactamente 3 escritores reales de `crm_agent_actions.failure_reason`: `runFollowupTick.ts:172` (constante fija, ya segura), `runFollowupTick.ts:350` y `persistActionOutcome.ts#markActionFailed` (ambos con `error.message`/`errorMessage` crudo, sin redactar).
- **P1-5 (fail-closed, sin auto-escalacion, T06)**: los tres scripts ya no escriben a `process.env` - solo cargan `.env`/`.env.local`/`.env` con `loadEnvFile` (sin mutar valores). Un flag ausente permanece `false`/deshabilitado, igual que cualquier otro proceso; el operador debe habilitar explicitamente cada flag en su archivo de entorno. Los dos workers imprimen ahora un resumen de solo lectura de los flags relevantes al arrancar (`logAutonomousCycleStatus`/`logLiveAutonomyStatus`), nunca los modifican.
- **Contrato del outbox worker (seccion 6 de la tarea, T06)**: `loadOutboxWorkerRuntimeConfig` acepta unicamente `"true"`/`"false"` (trim + lowercase) para los tres flags de habilitacion; ausente -> `false`; cualquier otro valor lanza `AutonomousRuntimeConfigError` en vez de degradar en silencio. `assertOutboxWorkerRuntimeConfigIsSafe`: si los tres flags de real-send estan en `true` Y `BRAIN_AUTONOMOUS_TEST_WA_IDS` esta vacio, el worker rechaza arrancar (excepcion clara, `scripts/autonomous-outbox-worker.ts#main` la deja propagar antes de tocar la DB o el loop de polling).
- **Gap de T06.1 (enforcement real de la allowlist en 4 capas)**: el diagnostico previo a T06.1 encontro que `BRAIN_AUTONOMOUS_TEST_WA_IDS` no se comparaba contra ningun `wa_id` en `runNativeAutonomousCycle.ts` (el ciclo llamaba al LLM, cargaba Customer 360 y creaba acciones/decisiones para cualquier numero) ni en `runFollowupTick.ts` (reclamaba y consumia `attempt_number` de cualquier fila due, sin mirar `wa_id`). El unico guard real preexistente vivia en `autonomousOutboxTick.ts` (con un bug: `candidate.wa_id &&` dejaba pasar filas con `wa_id` nulo) y en la defensa final de `metaClient.ts` (`getAllowedRecipients`, ya fail-closed). T06.1 agrega exactamente cuatro capas independientes, cada una verificable por separado:
  1. **Inbound autonomo** (`runNativeAutonomousCycle.ts`, Step 0, antes de cualquier otro paso): un `wa_id` fuera de la allowlist retorna `{ ran: false, reason: "wa_id_not_authorized_for_pilot", shadow: null, loop: null, bridge: null }` sin llamar `resolveNativeCustomerSession` (evita tambien una llamada innecesaria al Customer Service externo), sin cargar Customer 360, sin invocar el proveedor LLM.
  2. **Follow-up worker** (`runFollowupTick.ts`, antes de `claimFollowUpCandidate`): un candidato con `wa_id` no autorizado se salta por completo (`result.skippedUnauthorized`), sin ningun `UPDATE` sobre la fila - `status`/`attempt_number`/`cancel_reason` quedan exactamente iguales, a diferencia de una cancelacion (que si escribe `cancel_reason`).
  3. **Outbox worker** (`autonomousOutboxTick.ts`): el guard existente ahora usa `isWaIdAuthorizedForPilot` (normalizacion de digitos compartida, corrige el bug de `wa_id` nulo) en vez de un `.includes()` crudo.
  4. **Defensa final** (`metaClient.ts`, sin cambios de codigo - ya era correcta): `getAllowedRecipients` bloquea con `blocked_by_policy` cuando la allowlist esta vacia O cuando el `wa_id` no esta en ella, incluso si `BRAIN_META_SEND_ENABLED=true` - verificado con dos tests nuevos que fijan ese comportamiento explicitamente.
  Las cuatro comparten el mismo normalizador (`normalizeWhatsAppRecipientDigits`, ya usado por la capa 4) via `isWaIdAuthorizedForPilot`/`loadAutonomousPilotAllowlist` - una entrada de allowlist como `"+56 9 1234 5678"` compara igual que `"56912345678"` en las cuatro capas. Una allowlist vacia significa "sin restriccion configurada" en las capas 1-3 (preserva el comportamiento de todo caller existente que nunca configura `BRAIN_AUTONOMOUS_TEST_WA_IDS` - cero regresion), mientras la capa 4 sigue siendo fail-closed siempre (invariante preexistente, no tocado).
- **P1-2 (redaccion, T06 + T06.1)**: `redactErrorMessage.ts` centraliza el mismo patron de regex ya usado en 4 archivos hermanos (no tocados) y T06.1 le agrega redaccion de email (`[redacted-email]`) y de secuencias de 8+ digitos tipo telefono (`[redacted-phone]`, un conteo de digitos evita falsos positivos sobre codigos HTTP o IPs cortas). T06 la conecto en `runFollowupTick.ts` y `persistActionOutcome.ts#markActionFailed`; T06.1 la conecta ademas en los dos escritores restantes de `autonomousOutboxTick.ts` (la rama de excepcion del `sendFn` y el `error_message` devuelto por el proveedor), documentados en T06 como deuda fuera de alcance y ahora cerrados - un error de Meta que reflejara el telefono/email del destinatario o un `Authorization: Bearer` ya no llega crudo a `brain_message_outbox.error_message` ni `crm_action_executions.error_message`.
- **Validacion de configuracion completa (T06.1, seccion 4 de la tarea)**: ningun entrypoint (los tres scripts CLI) muta `process.env`. `scripts/autonomous-outbox-worker.ts#main` sigue llamando `assertOutboxWorkerRuntimeConfigIsSafe` antes del loop de polling (T06). `scripts/autonomous-followup-worker.ts#main` ahora tambien llama `assertFollowUpWorkerRuntimeConfigIsSafe` antes de arrancar: sus cinco flags estructurales (`BRAIN_SALES_AGENT_ENABLED`, `BRAIN_COMMERCIAL_OPERATIONAL_LOOP_ENABLED`, `BRAIN_AGENT_ACTION_QUEUE_ENABLED`, `BRAIN_EXECUTION_GATE_ENABLED`, `BRAIN_OUTBOX_BRIDGE_ENABLED`) deben estar todos en `true` o todos en `false` - una mezcla parcial (algunos true, otros false) deja el ciclo corriendo el LLM sin nunca llegar al outbox, sin ningun error visible, asi que el worker rechaza arrancar en ese caso.
- **Preflight operacional (T06.1, seccion 5 de la tarea)**: `scripts/autonomous-runtime-preflight.ts` (`npm run preflight:autonomous`) es un wrapper delgado sobre `buildAutonomousRuntimePreflightReport` (funcion pura, sin DB, sin Meta, sin arrancar ningun worker) que valida ambos contratos y reporta booleanos/conteos en JSON; exit code `0` si valido, `1` si invalido o si algun flag esta malformado. Verificado manualmente contra el `.env`/`.env.local` real de este entorno (`ok: true`, `pilotAllowlistCount: 0`, ambos workers en estado consistente) y contra una configuracion invalida forzada por variables de entorno inline (`ok: false`, exit code `1`, mensaje exacto de `assertOutboxWorkerRuntimeConfigIsSafe`) - nunca se invoco el worker real con flags de real-send activos, siguiendo la instruccion de no ejecutar el smoke ni llamadas reales a Meta.
- **Invariantes preservadas**: ningun worker habilita otro worker, Meta, execution gate, execution bridge, outbox write ni el autonomous loop - `execution-gate/**`/`execution-bridge/**` ya recibian configuracion tipada inyectada (cero `process.env` propio) y no se tocaron. `outboxWorker.ts`, `metaClient.ts` y `app/api/brain/outbox/worker/route.ts` no se modificaron (sus lectores ya eran fail-closed) - quedan documentados como deuda de consolidacion futura.
- **Deuda explicitamente fuera de alcance** (no arreglada, documentada para no perderla): `metaClient.ts#sanitizeResponseBody` no redacta pese a su nombre (P3-3, prioridad menor); `scripts/commercial-live-autonomy-run.ts` no tiene su propio gate de arranque tipo `assertFollowUpWorkerRuntimeConfigIsSafe` (es un CLI manual de un solo turno, no un worker persistente, fuera del alcance explicito de T06.1 seccion 1); la ruta legacy `processInbound.ts` (n8n/`/api/ai/orchestrate`) no tiene el gate de pilot isolation - el webhook real de WhatsApp (`app/api/integrations/whatsapp/webhook/route.ts`) solo usa `processNativeWhatsAppInbound`/`runNativeAutonomousCycle`, que si esta cubierto.
- Tests nuevos T06.1: 22 tests - `tests/brain/autonomousRuntimeConfig.test.ts` (+8: normalizacion de allowlist, `isWaIdAuthorizedForPilot`, contrato de 5 flags del follow-up worker, reporte de preflight valido/invalido/malformado), `tests/commercial/redactErrorMessage.test.ts` (+4: email, telefono, digitos cortos intactos, combinacion Bearer+email+telefono), `tests/commercial/outbound-guards.test.ts` (+2: defensa final con allowlist vacia y con `wa_id` fuera de la allowlist), `tests/commercial/runNativeAutonomousCyclePilotIsolation.test.ts` (nuevo, 2 tests: short-circuit sin DB/LLM, normalizacion de formato autoriza correctamente), `tests/commercial/runFollowupTick.test.ts` (+3: fila/`attempt_number` intactos, batch mixto, allowlist vacia sin regresion), `tests/native/outbox-pilot-isolation.test.ts` (nuevo, 3 tests: fila no reclamada, batch mixto, redaccion de error persistido).
- Validacion T06.1: `npx tsc --noEmit` limpio; 33 tests puros nuevos/existentes en verde (config + redaccion + outbound-guards + pilot isolation); suites DB-backed que ejercitan el codigo modificado en verde (`runFollowupTick.test.ts` 34/34, `tests/native/outbox-pilot-isolation.test.ts` + `outbox-ownership.test.ts` 10/10, `deliveryOutcomeProjection.test.ts` + `canonicalOutboxWriter.test.ts` + `followUpDispatchPolicy.test.ts` + `followUpPlanAdapter.test.ts` + `followUpPlanner.test.ts` + `salesConsultativeFollowUpRepository.test.ts` 108/108); suite completa (`npm test`, 1293 tests, +22 sobre T06) con 17 fallos preexistentes (uno menos que el baseline de 18 de T06, confirmado no relacionado - `requestsView.test.ts`, no tocado por esta tarea) en las mismas areas ya documentadas en T06 (scenario simulator de transporte/retry, capabilities de identidad de cliente, checksum de migraciones, E2E `T08-A6`/`T08-A7` de `ACS-R1-04`) - cero fallos nuevos causados por T06.1.
- No objetivo de T06/T06.1 tocado: `ACS-R1-05-T05` (codigo muerto/paralelo, sigue `ready`), `ACS-R1-05-T07` (E2E productivo), `ACS-R1-04`; ningun envio real a Meta ejecutado, ningun worker real arrancado con flags de real-send activos; el runtime de follow-up sigue sin `operational: verified` en `CAPABILITY_MATRIX.md`; cero tablas o migraciones nuevas.

## Evidencia de cierre - ACS-R1-05-T04

Primer cierre (SHA funcional `df6c41a`, doc `47a846f`) fue corregido por `ACS-R1-05-T04.1` (SHA funcional `468fe7d`): el primer intento consolido el SQL de insercion en un unico writer pero dejo dos esquemas de `dedupe_key` divergentes entre adapters (P1-4 incompleto), y la proyeccion de oportunidad usaba un CAS de lectura-mas-`UPDATE` que no protegia contra dos escrituras concurrentes de estado distinto para el mismo mensaje (P1-3 incompleto) - la misma revision encontro ademas que `conversation_message`/`brain_message_outbox` seguian con el mismo patron read-then-write no atomico, nunca corregido en el primer intento. Estado final: `done`.

- Estado: `done`.
- SHA funcional: `468fe7d` (correccion T04.1); `df6c41a` fue el primer intento. Rama `acs-r1-05-t04-outbox-delivery-outcomes`.
- Archivos funcionales: `lib/brain/messaging/canonicalOutboxWriter.ts` (unico writer de `brain_message_outbox`; T04.1 anade `buildCanonicalOutboxDedupeKey`), `lib/brain/messaging/outbox.ts` (`createOutboxPlannedRecord` delega, sin SQL propio; T04.1 usa el builder canonico), `lib/brain/commercial/execution-gate/buildOutboxCommand.ts` (T04.1 - `idempotencyKey` ahora es el hash canonico, `commandId` queda como referencia legible separada), `lib/brain/commercial/action-queue/persistActionOutcome.ts` (dedupe key deterministico + insercion idempotente), `lib/brain/commercial/delivery/deliveryStatusProjection.ts` (`shouldProjectDeliveryStatus` centralizado; T04.1 lo reexpresa como ranking total: `sent < failed < delivered < read`), `lib/brain/native-whatsapp/service.ts` (`applyMetaDeliveryStatus` reestructurado; T04.1 hace atomicas las tres proyecciones), `lib/brain/commercial/sales-consultative/repository.ts` (`queueCustomerMessageRecord` envia `opportunityId`), `lib/brain/messaging/autonomousOutboxTick.ts` (outcome `sent`/`failed` con dedupe key y atribucion de experimento), `migrations/025_action_outcome_idempotency_and_opportunity_delivery_projection.sql`.
- **P1-4 (writer canonico y dedupe compartido, corregido en T04.1)**: `createOutboxPlannedRecord` (`outbox.ts`) y `SqlOutboxRepository.insertCommand` (`sqlExecutionUnitOfWork.ts`) delegan ambos a `writeCanonicalOutboxMessage` - una sola sentencia `INSERT IGNORE`, una sola normalizacion de columnas, una sola resolucion de `phone_number_id`. Ambos adapters computan ademas su `dedupe_key` con la MISMA funcion compartida (`buildCanonicalOutboxDedupeKey(channel, actionId, idempotencyKey, recipient, content)`): el adapter legacy (`outbox.ts`) usa `sourceRequestId` como `actionId` y `actionType` como `idempotencyKey` (no existe fila `crm_agent_actions` detras de un envio inmediato); el adapter de execution-gate usa el `actionId`/`idempotencyKey` reales de la accion (`buildOutboxCommand.ts` ya no descarta el `idempotencyKey` original al construir `commandId` - ese string legible se mantiene solo para logging/metadata, nunca para dedupe). Mismo `actionId`+`idempotencyKey`+`recipient`+`contenido`+`canal` produce la misma `dedupe_key` sin importar el adapter, verificado con un test que invoca ambos adapters (secuencial y concurrente) con el mismo comando logico y confirma una sola fila y el mismo `rowId` reutilizado. Verificado por `rg -n "INSERT( IGNORE)? INTO.*BRAIN_MESSAGE_OUTBOX_TABLE" lib scripts app`: una sola coincidencia productiva.
- **P1-3 (proyeccion hasta opportunity, concurrencia corregida en T04.1)**: `applyMetaDeliveryStatus` reestructurado en los pasos del contrato. La oportunidad se resuelve por prioridad: `meta_payload_json.opportunity_id` del outbox y, si falta, `crm_agent_actions.opportunity_id` via el `action_id` enlazado - nunca "la oportunidad activa actual de la conversacion". Las tres proyecciones (`conversation_message.status`, `brain_message_outbox.provider_status`, `crm_opportunities.last_outbound_*`) usan ahora el mismo patron: un `UPDATE` unico cuyo `WHERE` es la unica autoridad de si el escritor puede aplicar, con un ranking total (`sent=1 < failed=2 < delivered=3 < read=4`) expresado como `CASE` SQL identico al de `deliveryStatusProjection.ts` - nunca una decision previa en TypeScript contra un estado que un escritor concurrente puede cambiar antes de que este confirme. Para `crm_opportunities` el `WHERE` combina ademas el orden entre mensajes (`last_outbound_outbox_message_id IS NULL` o `< incoming` siempre reemplaza; `= incoming` exige mayor rango; `> incoming` nunca aplica). `failed` ahora puede proyectarse desde `sent` y tambien puede ser superado por `delivered`/`read` (antes era terminal sin excepcion) - unico cambio de comportamiento respecto del primer cierre, necesario para que `failed`/`delivered` concurrentes desde `sent` siempre resuelvan a `delivered` sin importar cual gane la carrera por el lock de fila. `crm_opportunities` nunca toca `status`/`stage`/`temperature`/`priority`/`waiting_for`/`next_action_type`/`next_action_due_at`.
- **Idempotencia de outcomes**: `crm_action_outcomes.outcome_dedupe_key` (VARCHAR(64), UNIQUE, nullable - migracion 025) es `sha256("delivery|meta|<provider_message_id>|<outcome_type>")`, usado tanto por el camino de exito HTTP como por el webhook - un webhook `sent` posterior para el mismo mensaje resuelve a la misma fila via `INSERT IGNORE` + re-seleccion, nunca una segunda fila. Outcomes sin `provider_message_id` se insertan con `outcome_dedupe_key = NULL`, permitido por el indice unico nullable.
- **Historial append-only vs proyeccion monotonica**: el registro del outcome no esta condicionado a que la proyeccion avance: un evento `delivered` tardio se registra como outcome aunque la proyeccion ya este en `read`. Verificado con pares concurrentes (`read`/`delivered`, `read`/`sent`, `failed`/`delivered`): las dos filas de outcome distintas quedan en `crm_action_outcomes` sin importar cual gano la proyeccion visible, y un evento rechazado por la proyeccion nunca modifica `last_outbound_delivery_status_at`.
- **Manejo de fallos**: la persistencia del outcome es el paso principal - una falla real de DB hace fallar toda la llamada, reemplazando el `.catch(() => void 0)` previo que la silenciaba. Las proyecciones de mensaje/outbox/oportunidad son secundarias y best-effort, cada una reportada en el resultado estructurado (`messageProjectionApplied`, `outboxProjectionApplied`, `opportunityProjectionApplied`, `opportunityId`, `warning`).
- **Trazabilidad de A/B testing**: el writer canonico acepta `experiment` opcional, plegado en `meta_payload_json.experiment`; al registrar cualquier outcome, esa atribucion se copia a `crm_action_outcomes.metadata_json.experiment`. No se implemento asignador de variantes, calculo estadistico ni UI.
- No reabre el hardening del worker cerrado en T03; no toca `ACS-R1-04`; no declara el runtime `operational: verified`.
- Tests: 36 tests nuevos con MariaDB real (`tests/commercial/canonicalOutboxWriter.test.ts` 12/12, `tests/commercial/deliveryOutcomeProjection.test.ts` 21/21) mas 289/289 en regresion combinada. Concurrencia verificada ademas con 30/30 iteraciones de un stress test SQL aislado (fuera del repo, solo para diagnostico) sin ninguna falla. `npm run e2e:autonomous` corrido multiples veces contra el mismo commit base sin T04/T04.1 (via `git stash`) y contra el codigo final: el escenario `[H]` falla de forma identica en ambos (preexistente); los escenarios `[A]`/`[B]` (dependientes de un LLM real, `mode: live LLM cycle`) variaron entre `PASS`/`PARTIAL`/`FAIL` en corridas repetidas del MISMO commit en ambos lados (T04-only y T04.1), confirmando no-determinismo del proveedor LLM externo, no una regresion causada por T04/T04.1 - ningun otro escenario (`C`-`G`, `I`, `J`) vario nunca.

## Evidencia de cierre - ACS-R1-05-T03

Primer cierre (SHA `6558f29`) fue corregido por `ACS-R1-05-T03.1` (SHA funcional `59a536c`): el primer intento dejaba la recuperacion de stale-lock sin consumir intento, sin terminalizacion para filas `executing` agotadas, y revalidaba antes del claim en vez de despues para los origenes `planned`/`failed`. `ACS-R1-05-T03.2` (SHA funcional `af0718f`) corrigio ademas una mezcla de fuente horaria: los CAS de stale recovery/terminalizacion escribian `updated_at` con `CURRENT_TIMESTAMP(3)` (reloj de sesion) pero calculaban el cutoff stale con `UTC_TIMESTAMP()` (reloj siempre-UTC, independiente de la sesion) - dependian por accidente de que la sesion MariaDB corriera en UTC. Estado final: `done`.

- Estado: `done`.
- SHA funcional: `af0718f` (correccion de fuente horaria; `59a536c` corrigio la semantica de recuperacion/terminalizacion/revalidacion; `6558f29` fue el primer intento). Rama `acs-r1-05-t03-followup-worker-hardening`.
- Archivos funcionales: `lib/brain/commercial/followup/runFollowupTick.ts` (reescrito - candidatos, claim CAS, terminalizacion y cancelacion endurecidos; T03.2 unifico la fuente horaria de los tres CAS de staleness), `lib/brain/commercial/followup/followUpWorkerPolicy.ts` (constantes y funciones puras, sin acceso a DB; `FOLLOW_UP_STALE_EXECUTION_EXHAUSTED_REASON` nuevo en T03.1, sin cambios en T03.2).
- **Seleccion de candidatas** (`selectDueFollowUps`): ademas de `status = 'planned' AND scheduled_for <= UTC_TIMESTAMP()`, ahora tambien selecciona **toda** fila `status = 'executing' AND updated_at < DATE_SUB(CURRENT_TIMESTAMP(3), INTERVAL <FOLLOW_UP_STALE_EXECUTING_LOCK_SECONDS> SECOND)` (P0-2, stale-locked, con o sin intentos restantes - la correccion de T03.1 elimino el filtro `attempt_number < max_attempts` de esta rama, que antes dejaba invisibles para siempre las filas agotadas) y `status = 'failed' AND attempt_number < max_attempts` (P0-3), siempre acotado a `action_type = 'schedule_followup'` y a `expires_at`. El cutoff de la rama `executing` usa `CURRENT_TIMESTAMP(3)` (T03.2), la misma funcion que escribe `updated_at` en cada CAS de esta seccion - nunca `UTC_TIMESTAMP()`, que es un reloj distinto e independiente de la sesion. Las ramas `planned`/`expires_at` si siguen comparando contra `UTC_TIMESTAMP()` porque `scheduled_for`/`expires_at` los escribe la aplicacion (JS, siempre UTC via `toMysqlDateTime`), no `CURRENT_TIMESTAMP()` SQL - no hay mezcla ahi, y T03.2 no las toco. `FOLLOW_UP_STALE_EXECUTING_LOCK_SECONDS = 300` (`followUpWorkerPolicy.ts`) es una constante nueva y explicita, nombrada, no un literal repetido. No hay transicion global `executing -> planned` en ningun punto; la recuperacion y la terminalizacion siempre pasan por su propio CAS.
- **Claim CAS por origen** (`claimPlannedFollowUp`, `claimStaleExecutingFollowUp`, `claimFailedFollowUpRetry`, despachadas por `claimFollowUpCandidate`; mas `terminalizeExhaustedStaleFollowUp`, que no es un claim): las cuatro son `UPDATE ... WHERE action_id = ? AND action_type = 'schedule_followup' AND <precondicion exacta>`, cada una verificada por `affectedRows`, el mismo patron CAS ya usado por el claim `planned -> executing` original. `claimStaleExecutingFollowUp` re-verifica staleness y `attempt_number < max_attempts` en el propio `UPDATE` (no solo en la seleccion previa), por lo que dos recuperaciones concurrentes sobre la misma fila nunca pueden ganar ambas - la segunda relee `updated_at` ya refrescado por la primera y su condicion deja de cumplirse. `claimFailedFollowUpRetry` incrementa `attempt_number = attempt_number + 1` atomicamente dentro del mismo `UPDATE` que gana el CAS, nunca en un paso separado. `terminalizeExhaustedStaleFollowUp` exige `status = 'executing' AND attempt_number >= max_attempts AND updated_at < DATE_SUB(CURRENT_TIMESTAMP(3), INTERVAL ? SECOND)` (T03.2: mismo reloj de sesion que la escritura, nunca `UTC_TIMESTAMP()`), escribe `status = 'failed', failure_reason = 'follow_up_stale_execution_exhausted'` sin tocar `attempt_number`, y es idempotente por construccion (tras el primer exito la fila ya no esta en `executing`, asi que una segunda llamada no afecta filas).
- **Semantica de recuperacion vs retry (corregida en T03.1)**: recuperar una fila `executing` stale **SI** incrementa `attempt_number` exactamente una vez, atomicamente dentro del mismo `UPDATE` que gana el CAS - es un intento comercial nuevo, no la continuacion silenciosa del anterior (no hay confirmacion de que el intento previo se haya completado o fallado, asi que se cuenta como su propio intento). Reclamar una fila `failed` tambien incrementa `attempt_number` exactamente una vez, con el mismo mecanismo. Una fila `executing` stale que ya alcanzo `max_attempts` **nunca se reclama**: se terminaliza a `failed` con `failure_reason = 'follow_up_stale_execution_exhausted'` (codigo fijo, corto, sin datos sensibles), sin ejecutar `cycleRunner`, sin crear otra accion, sin incrementar intentos. Verificado con tests dedicados: incremento exacto tras cada recuperacion, recuperaciones sucesivas hasta agotar `max_attempts`, terminalizacion con el motivo exacto, segundo tick que nunca vuelve a tocar la fila terminalizada, y dos terminalizaciones concurrentes donde solo una modifica la fila.
- **Revalidacion post-claim uniforme (corregida en T03.1)**: la secuencia es siempre seleccionar -> claim CAS -> revalidar (`shouldCancelFollowUp` + `wa_id`) -> abortar (`abortClaimedFollowUp`, `UPDATE ... WHERE status = 'executing'`) si corresponde -> solo entonces `cycleRunner`. El primer cierre revalidaba **antes** del claim para origen `planned`/`failed` y solo revalidaba despues del claim para la recuperacion de `executing` - la correccion elimina esa asimetria: ahora ningun origen ejecuta `cycleRunner` sin haber revalidado el estado comercial ya con la fila reclamada (`executing`) bajo su propio CAS. `cancelFollowUp` (standalone, precondicion `status IN ('planned', 'failed')`) ya no se llama desde el bucle del tick en ningun caso - queda como punto de entrada externo (p. ej. una futura cancelacion desde operador), probado directamente; nunca sobrescribe `executing`/`executed`/`cancelled`/`requires_review`. La carrera cancelacion-vs-claim (contra `claimPlannedFollowUp`) se sigue resolviendo por CAS: exactamente una gana, la otra ve `affectedRows = 0` y lo reporta en vez de sobrescribir. Verificado con un hook de sincronizacion explicita nuevo (`onAfterClaim`, invocado justo despues del claim exitoso, antes de la revalidacion) que permite a los tests insertar la respuesta del cliente / oportunidad terminal / toma de control humana exactamente en la ventana entre claim y revalidacion, sin sleeps arbitrarios, para los tres origenes (`planned`, `failed` con retry, `executing` recuperado).
- **Invariantes preservadas sin reabrirlas**: el planner T01 (`follow-up-planner/planFollowUp.ts`) y la idempotencia T01.1 no se tocan; `follow_up_dispatch_policy` (T02, `sales-consultative/followUpDispatchPolicy.ts`) no se copia ni se referencia dentro del worker; `requires_review` nunca es seleccionado por `selectDueFollowUps` y por tanto nunca se ejecuta; la cancelacion por respuesta del cliente, propietario humano, IA pausada, conversacion cerrada y oportunidad terminal (`shouldCancelFollowUp`, sin cambios de logica) se aplica ahora de forma identica a los tres origenes de claim; la reentrada via `runNativeAutonomousCycle` (inyectable, `cycleRunner`) no cambia; cero envio directo desde el worker.
- **Fallo del ciclo** (bloque `catch`, sin cambios de mecanismo): `UPDATE ... SET status = 'failed', failure_reason = ? WHERE status = 'executing'` - retryable vs terminal para este caso se sigue decidiendo enteramente en el proximo intento de claim (`claimFailedFollowUpRetry`'s precondicion `attempt_number < max_attempts`), a diferencia de la terminalizacion de stale-lock agotado (mecanismo nuevo y distinto, ver arriba) que si transiciona a `failed` en el momento mismo de la deteccion, sin pasar por un `cycleRunner` fallido. `failure_reason` del catch sigue sin redaccion (P1-2, deuda explicita de T06, no tocada aqui).
- **Fuente horaria unificada (T03.2)**: `selectDueFollowUps` (rama `executing`), `claimStaleExecutingFollowUp` y `terminalizeExhaustedStaleFollowUp` calculaban el cutoff stale con `UTC_TIMESTAMP()` mientras escribian `updated_at = CURRENT_TIMESTAMP(3)` - como `updated_at` es un `DATETIME` sin zona horaria propia, esto dependia por accidente de que la sesion MariaDB corriera en UTC (si no, un `CURRENT_TIMESTAMP(3)` recien escrito bajo una sesion no-UTC podia leerse como ya vencido, o nunca vencido, frente a un cutoff `UTC_TIMESTAMP()`). Las tres consultas ahora comparan `updated_at < DATE_SUB(CURRENT_TIMESTAMP(3), INTERVAL <segundos> SECOND)` - el mismo reloj de sesion en ambos lados. No se toco ningun otro dominio (`outbox`/`messaging` tiene su propio `isStaleLockedTimestamp`, en JS puro sobre valores ya leidos, sin este patron). Verificado con un test dedicado que fuerza `SET time_zone = '-04:00'` en una conexion unica (via `withConnection`, nunca liberada al pool compartido con el override activo) y confirma, sobre esa misma sesion: una fila `executing` reciente no se reclama; una fila stale se recupera; el intento se incrementa exactamente una vez; una fila stale agotada se terminaliza; una fila recien reclamada no vuelve a parecer stale de inmediato; la zona horaria original se restaura en `finally` antes de devolver la conexion al pool. Todos los timestamps de la prueba se construyen con el reloj SQL de la propia sesion (`DATE_SUB(CURRENT_TIMESTAMP(3), INTERVAL ... SECOND)`), nunca con aritmetica de `Date` en JS.
- Tests: 31/31 verde en `tests/commercial/runFollowupTick.test.ts` contra MariaDB real (`main_management`) - 15 tests de T03, 11 tests de la correccion T03.1, mas 1 test nuevo de T03.2 (consistencia bajo sesion no-UTC), mas 4 aserciones corregidas en tests preexistentes de T03 durante T03.1 (recuperacion ahora espera `attempt_number` incrementado; la prueba de "nunca recuperada" fue reemplazada por "terminalizada"; la prueba de concurrencia de recuperacion ahora tambien verifica el intento final).
- Invariantes T01/T02 verificadas en verde en la misma corrida: `followUpPlanner.test.ts`, `followUpPlanAdapter.test.ts`, `followUpDispatchPolicy.test.ts`, `salesConsultativeFollowUpRepository.test.ts` - 75/75, cero aserciones modificadas en esos archivos. E2E (`npm run e2e:autonomous`, escenario E, follow-up) sigue en `PASS`; los otros dos fallos del harness (`B`, `H`) y el `PARTIAL` (`A`) son preexistentes, ya verificados identicos contra el commit base `9d6b50a` durante T03 - no son causados por esta correccion.
- No objetivo de T03/T03.1/T03.2 tocado: planner T01, dispatch policy T02, `failure_reason` sin redactar (P1-2), outbox/`applyMetaDeliveryStatus`/`crm_opportunities` (T04), planners paralelos/codigo muerto (T05), flags auto-escalados (T06), Customer Service, Address Book, Voice, frontend, EC2/Nginx/Meta deployment; cero tablas o migraciones nuevas; cero cambio de comportamiento en `send_whatsapp_reply`/`prepare_quote_draft`/`take_over_case`/`pause_ai`/`mark_lost_candidate`/`create_internal_task`.

## Evidencia de cierre - ACS-R1-05-T02

- Estado: `done`.
- SHA funcional: ver seccion de commits de `ACTIVE_RELEASE.md`. Rama `acs-r1-05-t02-followup-dispatch-policy`.
- Archivos funcionales: `lib/brain/commercial/sales-consultative/followUpDispatchPolicy.ts` (nuevo - adapter puro `follow_up_dispatch_policy` sobre `policy/evaluateCommercialPolicy.ts`), `lib/brain/commercial/sales-consultative/repository.ts` (gate conectado en `upsertFollowUpActionRow`, justo antes del `INSERT INTO crm_agent_actions`), `lib/brain/commercial/sales-consultative/followUpPlanAdapter.ts` (`FOLLOW_UP_TIMEZONE` exportado para reuso, sin cambio de comportamiento).
- **Arquitectura**: `sales-consultative/engine.ts` (trigger, sin cambios) -> `planCommercialFollowUp` (T01, sin cambios) -> `follow_up_dispatch_policy` (`evaluateFollowUpDispatchPolicy`, nuevo) -> `upsertFollowUpActionRow` (INSERT en `crm_agent_actions`). El gate es la unica via que puede convertir un plan `recommended`/`requires_operator_review` en una fila ejecutable; ningun caller puede saltarselo porque esta dentro de la misma funcion que hace el INSERT, no en un paso opcional externo.
- **Boundary de politica**: `evaluateFollowUpDispatchPolicy` llama directamente a `policy/evaluateCommercialPolicy.ts` con un `SalesAgentResult` intencionalmente vacio (`proposedActions`/`toolRequests`/`entityProposals: []`, `responseProposal: null`) - el gate de canal del evaluador (`computeChannelSignals`: opt-out/ai-blocked/identity-conflict fuerzan `blocked`; human-owner/quiet-hours/manual-approval fuerzan `requires_review`) es independiente de esos arrays, asi que este uso ejercita exactamente el "boundary puro" sin duplicar ninguna regla de bloqueo dentro de `sales-consultative`. No se llama `runCommercialShadowEvaluation` en ningun punto del path nuevo.
- **Resultado de dispatch explicito**: `FollowUpDispatchDecision = "allow" | "require_review" | "deny" | "failed_safe"`, mapeado desde `CommercialPolicyStatus` por una funcion pura e independientemente testeable (`mapCommercialPolicyStatusToDispatchDecision`): `allowed`/`allowed_with_restrictions -> allow`; `requires_review -> require_review`; `blocked -> deny`; `failed_safe -> failed_safe`. Nunca se usa un booleano `blocked` suelto.
- **Fuentes reales de senales** (`loadFollowUpDispatchChannelSignals`):
  - `optOut`: `crm_opportunities.signals_json` (array estructurado ya cargado en la oportunidad, nunca el texto libre del ultimo mensaje). Ningun escritor real del repositorio popula hoy una entrada `"opt_out"` en ese array (no existe capa de opt-out capture ni tabla de Customer Preference en este repo - T02 no la agrega, section 6 regla 7 de la tarea), asi que el gate evalua `false` en la practica hasta que exista ese escritor; el mecanismo esta real y conectado, no es una constante hardcodeada.
  - `quietHoursActive`: `computeQuietHoursActive(currentTime, timezone)`, hora y timezone explicitos (`FOLLOW_UP_TIMEZONE = "America/Santiago"`, reexportado de `followUpPlanAdapter.ts`), nunca la timezone del servidor. Ventana 21:00-09:00 local, nueva constante nombrada (`FOLLOW_UP_QUIET_HOURS_START_HOUR`/`END_HOUR`) porque `follow-up-decision-policy.md` deja la ventana "configurable by context" sin un valor canonico previo. Un `currentTime` no parseable degrada fail-closed (tratado como dentro de quiet hours).
  - `humanOwnerActive` / `aiBlocked`: `conversation.human_owner_active` / `conversation.ai_enabled` (migraciones 008/010), las mismas columnas que `updateOpportunityHandoffState` (`native-whatsapp/service.ts`) escribe de forma confiable - a diferencia de `crm_opportunities.human_owner_active`/`ai_blocked`, que no se usan aqui porque ese camino de escritura re-persiste el valor previo al turno (ver comentario en el codigo). Sourced como dos booleanos independientes, no combinados: `evaluateCommercialPolicy` ya les da severidad distinta (`aiBlocked` es parte del bloqueo duro; `humanOwnerActive` solo exige revision) - combinarlos habria hecho inalcanzable el resultado "revision" cuando solo hay dueno humano activo sin IA explicitamente deshabilitada.
  - `identityConflict`: `crm_customer_onboarding_state.status = 'conflict'` (migracion 023, el estado nativo real de resolucion de identidad de ACS-R1-04), nunca el `resolver_identity.identity_type` legacy que usa el runtime shadow.
  - `conversation status`/disponibilidad: `conversation.status = 'open'` alimenta `available`/`outboundAllowed`.
  - Un `conversationId` sin fila real en `conversation` (incluida ausencia total de `conversationId`) degrada a "sin senal adicional" (no bloquea, no es un fallo) - preserva el invariante T01 de "fallback seguro sin contexto completo". Solo una excepcion tecnica real de `safeQueryRows` (`ok:false`) hace fallar cerrado el gate completo.
- **Configuracion**: `BRAIN_COMMERCIAL_POLICY_ENABLED` (leido con el mecanismo tipado existente `readEnvFlag`, `config/commercialCycleConfig.ts`), default `false` ya documentado en `.env.example`. Sin dependencia de `BRAIN_COMMERCIAL_SHADOW_ENABLED` ni `BRAIN_COMMERCIAL_SHADOW_ALLOW_REAL_PROVIDER` en ningun punto del nuevo codigo (verificado con un test dedicado que lee el codigo fuente y confirma ausencia de esos imports/flags). Politica deshabilitada -> `failed_safe`, nunca `allowed` por omision.
- **Mapeo a persistencia**: `allow` conserva `action.status`/`policy_status` calculados por el plan (`planned`/`allowed` o `requires_review`/`requires_review`, sin cambio de T01). `require_review` fuerza `action.status = "requires_review"`/`policy_status = "requires_review"` aunque el plan hubiera propuesto `planned` - nunca degrada a `planned` (cubre quiet hours y human-owner-active). `deny`/`failed_safe` no insertan fila (mismo patron que T01's `follow_up_plan_not_persisted:<status>`): retornan `follow_up_dispatch_deny:<reasonCode>` / `follow_up_dispatch_failed_safe:<reasonCode>`, sin sobrescribir una fila activa previa. `approval_requirement` se eleva a `"operator_review"` solo cuando el plan tenia `"none"` y el dispatch exige revision.
- **Persistencia sin PII**: solo se agregan codigos cortos y fijos (`opt_out_active`, `quiet_hours_active`, `identity_conflict`, `ai_blocked`, `human_owner_active`, `conversation_unavailable`) a `policy_notes_json`, nunca el `CommercialPolicyResult` completo, contexto comercial, telefonos, emails, mensajes ni stack traces. `block_reasons_json` sigue siendo exclusivamente el de T01 (`plan.blockReasons`).
- **Invariantes T01.1 preservadas**: el gate corre unicamente en el tramo final antes del INSERT (despues de retry-exacto, `active_followup_exists` y `existing_action_reused`), nunca altera `loadFollowUpActionHistory`, nunca recalcula `attemptNumber` por su cuenta, nunca cancela ni sobrescribe una fila activa. Los 19 tests de T01.1 corren sin modificacion de aserciones (solo se fijo un `currentTime` deterministico y se activo `BRAIN_COMMERCIAL_POLICY_ENABLED=true` en el harness, ver seccion de tests) y siguen en verde.
- Tests nuevos: `tests/commercial/followUpDispatchPolicy.test.ts` (14 tests puros, sin DB, cubre los 12 casos pedidos mas 2 adicionales de `computeQuietHoursActive`), mas 11 tests nuevos en `tests/commercial/salesConsultativeFollowUpRepository.test.ts` (`[T02-1]`..`[T02-10]`, MariaDB real contra `crm_test`, sin mocks del repositorio). `crm_test` reseteada y migrada dos veces (23 migraciones, cero checksum conflicts).
- No objetivo de T02 tocado: `runFollowupTick.ts`, `autonomous-outbox-worker.ts`, `metaClient.ts`, `applyMetaDeliveryStatus`, `multi-request/requestFollowups.ts`, `autonomous-loop`, sanitizacion de `failure_reason`, recuperacion de stale-lock, retry de `failed`, frequency cap, Customer Service, Address Book, Voice; cero tablas o migraciones nuevas; cancelacion eager por inbound no implementada.

## Evidencia de cierre - ACS-R1-05-T01

Primer cierre (SHA `ef9c5ca`/`f8c17fe`) fue rechazado por semantica incorrecta de historial, intentos e idempotencia. `ACS-R1-05-T01.1` (SHA funcional `d3b07ca`) corrige los cinco defectos encontrados en la revision antes de aceptar la tarea. Estado final: `done`.

- Estado: `done`.
- SHA funcional: `d3b07ca` (correccion; `ef9c5ca` fue el primer intento, rechazado). Rama `acs-r1-05-t01-followup-planner-persistence`.
- Archivos funcionales: `lib/brain/commercial/follow-up-planner/constants.ts` (nueva constante canonica `COMMERCIAL_FOLLOW_UP_DEFAULT_MAX_ATTEMPTS`), `lib/brain/commercial/follow-up-planner/planFollowUp.ts` (`buildSignature` ya no incluye `scheduledFor`), `lib/brain/commercial/sales-consultative/followUpPlanAdapter.ts` (adapter puro de contexto + clasificacion explicita de estados + mapeos separados), `lib/brain/commercial/sales-consultative/repository.ts` (scope estricto de historial, tres resultados distintos, mapeo separado de `policy_status`).
- Planner canonico: `follow-up-planner/planFollowUp.ts` (`planCommercialFollowUp`) - unica fuente de calculo de `intent`/`scheduledFor`/`attemptNumber`/`maxAttempts`/`status`/`riskLevel`/`approvalRequirement`/`policyNotes`/`blockReasons`/`idempotencyKey`. Su unico cambio de logica en esta correccion es que `buildSignature`/`finalizePlan` ya no incluyen `scheduledFor` en el hash de `planId`/`idempotencyKey` - `scheduledFor = plusHours(createdAt, defaultDelayHours)` cambiaba en cada llamada por el simple paso del tiempo, haciendo que dos llamadas por el mismo plan logico en momentos distintos nunca coincidieran. `policy.defaultDelayHours` si permanece en la firma: sales-consultative traduce su cadencia (`dueAt`) a ese campo, y una diferencia real de cadencia (24h vs 30h de espera) si distingue dos planes.
- Disparador: `sales-consultative/engine.ts`, sin modificar.
- **Scope del historial** (`loadFollowUpActionHistory`, `repository.ts`): con `opportunity_id` conocido, la consulta se limita exclusivamente a ese `opportunity_id` - nunca cae a `wa_id`. Sin `opportunity_id`, prioriza `conversation_case_id` exacto (`AND opportunity_id IS NULL AND conversation_case_id = ?`); solo usa `wa_id` para filas que tampoco tienen `opportunity_id`. Siempre acotado a `action_type = 'schedule_followup'`. Verificado con una oportunidad A y B compartiendo `wa_id`: el intento consumido de A no afecta el `attemptNumber` calculado para B.
- **Estados activos explicitos** (`FOLLOW_UP_ACTIVE_ACTION_STATUSES`, `followUpPlanAdapter.ts`): unicamente `planned`, `requires_review`, `executing`. Cualquier otro estado (incluido uno desconocido/futuro) degrada de forma segura a "no activo".
- **Estados que consumen intento** (`FOLLOW_UP_ATTEMPT_CONSUMING_STATUSES`): unicamente `executing`, `executed`, `failed`. `rejected`/`blocked`/`cancelled`/`expired` no consumen - un intento nunca real (nunca reclamado por el worker) no agota `maxAttempts`. Formula: `attemptNumber = max(attempt_number de filas consumidoras) + 1`, o `1` si no hay ninguna.
- **Tres resultados distintos** (`upsertFollowUpActionRow`): (1) retry exacto - mismo `planId`, mismo `intent`, mismo `attemptNumber` que la fila activa -> reutiliza esa fila, `existing_action_reused`, sin insertar ni sobrescribir; (2) accion activa distinta - la fila activa existe pero el plan recalculado difiere en `planId`/`intent`/`attemptNumber` -> `active_followup_exists`, sin insertar ni sobrescribir, sin supersession ni cancelacion automatica (fuera de alcance de T01); (3) sin accion activa -> el planner evalua y, si el estado lo permite, persiste el siguiente intento legitimo.
- Idempotencia: `plan.idempotencyKey` sigue siendo la base canonica para la columna `idempotency_key`, ahora genuinamente estable para el mismo plan logico (ver el fix de `buildSignature` arriba). La deteccion de retry exacto vs conflicto usa comparacion semantica explicita (`planId`+`intent`+`attemptNumber`), no solo igualdad de key.
- **`maxAttempts` canonico**: `COMMERCIAL_FOLLOW_UP_DEFAULT_MAX_ATTEMPTS = 3` (`follow-up-planner/constants.ts`), importado por el adapter, inyectable en tests (`buildFollowUpPlanningInput({ maxAttempts: ... })`). Distinto de `COMMERCIAL_FOLLOW_UP_MAX_ATTEMPT_NUMBER = 999` (techo interno del planner sobre `attemptNumber`, no un default de politica).
- **Mapeo de estado separado**: `plan.status` nunca se persiste crudo como `policy_status`. `recommended -> action.status=planned, policy_status=allowed`; `requires_operator_review -> action.status=requires_review, policy_status=requires_review`; `blocked`/`not_needed`/`cancelled`/`expired`/`invalid` no crean fila ejecutable. Dos funciones independientes y testeadas por separado: `mapFollowUpPlanStatusToActionStatus`, `mapFollowUpPlanStatusToPolicyStatus`.
- Bugs pre-existentes corregidos de paso en el primer cierre (confirmados en `develop` HEAD `bee047a`, invisibles bajo la suite mockeada, no reintroducidos por esta correccion): `INSERT INTO crm_agent_actions` con 39 columnas y solo 38 placeholders `?`; `scheduled_for` recibiendo un ISO string crudo contra una columna `DATETIME`. Ambos con test de regresion dedicado.
- Tests nuevos/corregidos: `tests/commercial/followUpPlanAdapter.test.ts` (12 tests puros, sin DB), `tests/commercial/salesConsultativeFollowUpRepository.test.ts` (19 tests, MariaDB real contra `crm_test`), `tests/commercial/followUpPlanner.test.ts` (+1 test de regresion de estabilidad de `idempotencyKey`/`planId` frente a `now` variable).
- `crm_test` reseteada (drop+recreate solo de esa base, via `connectAsRoot`/`dropDatabase`/`ensureDatabaseExists`) y migrada dos veces: primera corrida desde vacio aplico las 23 migraciones sin conflictos de checksum; segunda corrida, todo `[skip]`.
- No objetivo de T01 tocado en esta correccion: `evaluateCommercialPolicy` sigue sin conectarse como gate (T02); `runFollowupTick.ts`, outbox, Meta sender, `autonomous-loop`, shadow flags y `failure_reason` sanitization no se modificaron; cero tablas o migraciones nuevas.

## Bloqueos

Ninguno propio de esta release. No depende de Customer Service (`PAUSED_EXTERNAL`, ver `ROADMAP.md`) ni de Address Book/Voice (`DEFERRED`). No depende de que `ACS-R1-04-T08`/`T09` cierren.

## Deudas fuera del incremento

- Frequency cap por customer: no existe en ningun path (planner, policy o persistencia). No es alcance de `ACS-R1-05-T01`..`T07`; registrar como tarea futura si el negocio lo requiere.
- `metaSendAdapter.ts` (envio con guards de politica) permanece sin usar por ningun worker productivo (P3-1 de la auditoria); no es bloqueante para esta release.
- El `correlationId` de follow-up no se persiste como columna propia (P3-2); reconstruir la traza sigue requiriendo joins por `decision_id`/`action_id`.
