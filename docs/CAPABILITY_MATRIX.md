---
title: CAPABILITY_MATRIX
doc_id: product-capability-matrix
status: active
version: "1.15.0"
owner: architecture
last_reviewed: 2026-08-10
source_of_truth_for:
  - capability inventory
  - domain implementation status
  - port status
  - adapter status
  - gateway registration
  - runtime connection
  - operational verification
depends_on:
  - ./ACTIVE_RELEASE.md
  - ./releases/ACS-R1-04-customer-identity-onboarding.md
  - ./releases/ACS-R1-03-customer-360.md
  - ./audits/acs-r1-01-capability-gateway-evidence.md
  - ./audits/autonomous-commerce-current-state-audit.md
  - ./audits/autonomous-commerce-transactional-closure-audit.md
  - ./audits/acs-r1-03-customer-360-acceptance.md
  - ./capabilities/customer-360-read-model.md
  - ./capabilities/customer-addresses.md
  - ./data/customer-360-contract.md
  - ./data/customer-address-contract.md
  - ./data/customer-lifecycle-event-contract.md
  - ./data/customer-onboarding-identity-contract.md
  - ./data/customer-creation-linking-authority-contract.md
  - ./integrations/customer-service-http-contract.md
  - ./capabilities/customer-service-capability.md
supersedes: []
tags:
  - capability
  - product
  - release
---

# CAPABILITY_MATRIX

La matriz representa estado tecnico real, no intencion de roadmap.

## Leyenda

- `Type`: `tool`, `read_model`, `service`, `command`, `policy`, `channel`, `domain_state`
- `Domain`: `implemented`, `implemented_partial`, `active_development`, `planned`
- `Port`: `implemented`, `partial`, `planned`, `not_applicable`
- `Adapter`: `implemented`, `partial`, `planned`, `not_applicable`
- `Gateway`: `registered`, `not_registered`, `not_applicable`
- `Runtime`: `connected`, `internal_context_only`, `multi_request_only`, `not_connected`
- `Operational`: `verified`, `pending_smoke_test`, `connected`, `planned`, `not_verified`
- `State`: `accepted_with_debt`, `active_development`, `implemented_partial`, `planned`

## Catalog

| Capability | Type | Domain | Port | Adapter | Gateway | Runtime | Operational | State | Debt |
| ---------- | ---- | ------ | ---- | ------- | ------- | ------- | ----------- | ----- | ---- |
| `search_products` | `tool` | `implemented` | `implemented` | `implemented` | `registered` | `connected` | `pending_smoke_test` | `accepted_with_debt` | ACS-R1-01.1 closed: single HTTP call per invocation (gateway owns retry), governance-derived approval (not LLM `blocking`), real smoke test (`scripts/manual-test/catalog-service-smoke.ts`) - see `audits/acs-r1-01-1-capability-gateway-hardening-evidence.md`. **SALES-AGENT-R2-A11.2-C**: backend switched from the legacy `GET /v1/products/search` (naive substring/stopword-broken retrieval) to `POST /api/v2/catalog/resolve-product-intent` (T12 - synonyms, unit normalization, explicit-constraint ranking, structured `resolved/clarification_required/no_match`) via a new `CatalogPort.resolveProductIntent()` method; `CatalogPort.searchProducts()` itself is unchanged and still backs the admin catalog console directly. `buildCommercialWorkProjection.ts` now reads `productIntent.resolution.status` instead of counting candidates. Verified live against the real Catalog Service (read-only RDS replica) with the A11.2-B stopword fix present - see `docs/releases/SALES-AGENT-R2-A11.2-C-catalog-t12-wiring.md`. `Operational` downgraded to `pending_smoke_test`: the live verification used a local dev instance of the Catalog Service; A11.2-B is committed and pushed to `origin/main` of `MS-pesaschile-catalog-service` (`b60b624`, verified 2026-08-24) but a real owner-only WhatsApp smoke against the **deployed** Catalog Service (main@b60b624 or later, in production) is still pending - being on `main` does not mean deployed. Remaining: multi-request's own catalog registry (`lib/brain/commercial/capabilities/registry.ts`) still reads PrestaShop SQL directly, not this port; `propose_followup` in the canonical loop is not wired to the real follow-up scheduling worker |
| `get_product_details` | `tool` | `implemented` | `implemented` | `implemented` | `registered` | `connected` | `verified` | `accepted_with_debt` | ACS-R1-01.1 closed: single HTTP call, governance-derived approval, real smoke test - see `audits/acs-r1-01-1-capability-gateway-hardening-evidence.md`. Only reachable via the deterministic ranker (`rankCatalogSearchResults.ts`) after a search - no direct LLM tool alias yet |
| `batch_get_products` | `command` | `implemented` | `implemented` | `implemented` | `registered` | `connected` | `not_verified` | `accepted_with_debt` | ACS-R1-05-T06.2: hydrates up to 20 search candidates in one `POST /v1/products/batch` call via the Capability Gateway, then `rankCatalogCandidatesByBudget.ts` selects economy/near_budget/stretch tiers. Internal enrichment only - never aliased as an LLM-facing tool (Sales Agent only ever requests `searchProducts`; the runtime chains search -> batch automatically). Unit-tested (adapter + ranking, pure); the full search -> batch -> grounded-message pipeline's integration test (`tests/commercial/buildCatalogGroundedMessageBudget.test.ts`) ran against real MariaDB (`crm_test`, ACS-R1-05-T07) - 3/3 pass. `State` corrected from `active_development` to `accepted_with_debt` (ACS-R1-05-T07 documentation reconciliation): every other row in this matrix with `operational: not_verified` (`resolve_customer`, `create_customer`, `link_external_identity`, `customer_onboarding_state`, `autonomous_customer_context`) uses `accepted_with_debt`, matching the legend's definition ("usable and documented, but still carrying explicit debt", not "still being built") - this capability is fully implemented/wired/tested end-to-end against a real DB, so `active_development` understated its status. Still `not_verified` on the operational axis, unchanged: the DB-backed run used a fake catalog HTTP double, never the real Catalog Service, which is what `operational: verified` requires per this matrix's own contract - that gap is the explicit debt `accepted_with_debt` carries. |
| `search_company_knowledge` | `tool` | `implemented_partial` | `implemented` | `not_applicable` | `registered` | `connected` | `not_verified` | `designed_partial` | ACS-R1-05.1-T02.1: simple lexical fixture search (no embeddings, no vector DB, no external service - `lib/brain/commercial/capability-gateway/companyKnowledgeCapability.ts`), registered in the Capability Gateway and callable by the native agent tool loop (`runAgentToolLoop.ts`). `Domain: implemented_partial` because the fixture content (`companyKnowledgeFixtures.ts`) is explicitly non-productive placeholder copy - no verified, published PesasChile business content (hours, payment methods, coverage) exists anywhere in this repo. Must not be declared `operational: verified` until each fixture entry is replaced with business-confirmed content. |
| `explore_catalog` | `tool` | `implemented` | `implemented` | `implemented` | `registered` | `connected` | `verified` | `accepted_with_debt` | **Operational verification (dated update, see below for the historical defect this supersedes)**: real WhatsApp flow exercised end to end - turn 1 ("¿Cual es el producto mas caro de la pagina?") -> `explore_catalog` -> `execution_status=completed` -> `terminalReason=responded` (Camara Hiperbarica ST801 1.5ATA \| O2Life, $8.599.990 CLP); turn 2 ("¿Me puedes dar el link?") -> `RecentCatalogContext` resolves the anaphoric reference -> `get_product_details` -> `terminalReason=responded` with the real canonical URL. Full chain confirmed: global ranking -> product identification -> anaphoric reference next turn -> rehydration via `get_product_details` -> canonical URL. Reported by the operator from the EC2 deployment (this session has no direct EC2 network access - see the vantage-point note below); not independently re-run in this implementation session. `State` corrected from `active_development` to `accepted_with_debt`: operationally verified end to end, but still carrying explicit debt (Catalog Service `price.max < price.min` hang incident, the legacy sort alias as temporary bridge debt, warnings not persisted to `crm_capability_executions` - all documented below). ACS-R1-05.1-T02.6: extremes (cheapest/most expensive), top-N, rankings and filtered/sorted browse (`POST /v1/products/explore`, own request/response vocabulary confirmed against `mistergoliat/MS-pesaschile-catalog-service` `main@147794b`/`feature@efc2f7e` - never unified with `CatalogAvailabilityStatus`; `availability` defaults to `available` unless the model asks otherwise). Added to `AGENT_LOOP_TOOL_POOL` alongside the three existing tools (none removed). Client-side guards reject `price.max < price.min` and missing `sort`/`limit` before any HTTP call - a request builder allowlists exactly the known fields, never forwarding raw model arguments. `lib/brain/commercial/agent-loop/recentCatalogContext.ts` recognizes `explore_catalog` as a third valid `sourceTool` (alongside `search_products`/`get_product_details`, same `crm_capability_executions` audit trail, no parallel memory, no new table) - a conversational reference to an `explore_catalog` result (e.g. "el enlace de la segunda") now resolves via `get_product_details`, same as a prior `search_products` result; only `position`/`productId`/`name` are kept (never `price`/`stock`/`availability`, consistent with that context's existing identity-only invariant). This integration also added cross-interaction `productId` deduplication to `recentCatalogContext.ts`, which the codebase did not have before this task (audited and confirmed absent). Unit-tested (adapter parse/error-mapping/allowlisted-body, Capability Gateway execution/validation, ToolObservation projection capped at 10 items, prompt rule lines for search_products/explore_catalog/get_product_details differentiation and `exhaustiveForScope` ranking language, RecentCatalogContext capture/ranking/dedup/limits) against a local HTTP double or injected `dataAccess` - never the real Catalog Service (no MariaDB/Docker available in this implementation environment, same limitation documented across every prior task in this release). Historical vantage-point note (context only, superseded by the operational verification at the top of this row): `127.0.0.1` in this local implementation session is not `127.0.0.1` on EC2 - this local session never had direct network access to the EC2 services, so an earlier probe from here timing out reflected that lack of access, not service health. On EC2, MariaDB is available via the `crm-customer-360-mariadb` container, published on `127.0.0.1:3306`, and the Catalog Service is available on `127.0.0.1:4010`. The operator ran the real smoke directly there and it succeeded: `searchProducts` OK, `getProductDetails` OK, `exploreCatalog` OK - `totalMatched=833`, `exhaustiveForScope=true`, `productId=1532`. `T02.6.1` has since been deployed to EC2 and the real WhatsApp inbound flow through the Agent Tool Loop has been exercised end to end to `terminalReason=responded` there (full evidence at the top of this row). DB-backed tests (`tests/commercial/capabilityGateway.test.ts`, 8 of 12) did not run in this local session (no local MariaDB) and must not be run against EC2 either while that file hardcodes local dev credentials (`DB_PASSWORD=una_clave_local`) pointed at `main_management` - running it as-is against EC2 would write real audit rows into the real database. **ACS-R1-05.1-T02.6.1** (real production smoke found this first): `crm_capability_executions` recorded `execution_status=invalid_arguments`/`error_code=sort_and_limit_required` because the model sent `{orderBy, orderDirection}` instead of the canonical `{sort:{by, direction}}` - root cause was structural, not a one-off model mistake: `CapabilityGatewayDefinition` had no `inputSchema` field, so `buildToolDescriptions()`/the prompt only ever showed `{name, description}`, never the argument shape (`httpAgentLoopProvider.ts` also confirmed to send no native OpenAI-compatible `tools`/`function_call` - JSON-mode text only). Fixed structurally: `CapabilityGatewayDefinition.inputSchema` (JSON Schema, canonical, single source) now exists for `search_products`/`get_product_details`/`search_company_knowledge`/`explore_catalog` (never `batch_get_products`, still excluded from the Agent Tool Loop pool), read by `buildToolDescriptions()` and rendered verbatim in the prompt (`buildAgentStepPromptPackage.ts`). A narrowly-scoped, temporary alias (`orderBy`/`orderDirection` -> nested `sort`, only when `sort` is entirely absent, only for `explore_catalog`, warning `explore_catalog_legacy_sort_alias_used`) bridges the one already-observed production shape without weakening the canonical contract, and never forwards `orderBy`/`orderDirection` to the adapter (verified by test - `exploreInput` is built key-by-key, allowlisted). `limit` above the schema's declared maximum (10) is rejected client-side (`limit_out_of_range`) as a real runtime guard, not just a test assumption. `runAgentToolLoop.ts` no longer counts an `invalid_arguments` rejection toward `maxToolExecutions` (only a call that actually reached the Catalog Service does) while it still counts toward `maxDecisions` (bounded, no infinite loop) and still gets a `crm_capability_executions` audit row and a `ToolObservation` exactly like any other outcome. The capability's own sanitized warnings (e.g. `explore_catalog_legacy_sort_alias_used`) are never silently dropped: surfaced on the `ToolObservation` (model-facing) and folded into the turn's own warning list (internal observability) - not persisted into `crm_capability_executions` (migration 022 has no `warnings_json` column, and this task adds no migrations), a known, explicit limitation, not an oversight. 18 new tests total (schema shape, alias normalization incl. the exact incident as a regression test, additional-properties never forwarded, prompt schema rendering, invalid_arguments budget/retry/no-infinite-loop, warning visibility on completed/blocked observations) - all DB-free, all green. **ACS-R1-05.1-T02.6.2** (commercial presentation rules, found necessary by the same real smoke that verified `T02.6`/`T02.6.1`): a stock ranking exposed non-commercial rows (`Servicio vendedor Pesas Chile`, `Costo logistico`) - audited and confirmed CRM receives no stable signal (`active`/`visibility`/`commerciallySellable`/`productType` etc. are all absent from `CatalogExploreItem`) to exclude these client-side; exclusion belongs in the Catalog Service, before ranking/`totalMatched`/top-N/`exhaustiveForScope` are computed there - not fixed in this repo. Evidence and a separate handoff prompt are in `docs/handoffs/catalog-service-exclude-non-commercial-products.md` (a durable file in this repo, not a temp path) - real implementation is its own task/branch inside `MS-pesaschile-catalog-service`, never this one. Added `lib/brain/commercial/agent-loop/stockDisclosurePolicy.ts` (pure, tested `describeStockDisclosure()`: <=0 / =1 singular / 2-19 plural / 20-100 / >100 buckets, never the raw number at 20 or above) and two new immutable prompt rule blocks in `buildAgentStepPromptPackage.ts` (stock disclosure; commercial closing offer for the product link with eight explicit exceptions - deliberately not including publicLink availability, since explore_catalog/search_products never return publicLink at all, only get_product_details does; the offer is gated on a resolvable productId only, and both publicLink outcomes - available or not - are handled only after the customer accepts/asks, never invented) - presentation-only, zero change to `ToolObservation`/`crm_capability_executions`/the real `stockQuantity`. Explicit, undisguised limit: both rules are prompt instructions only - the runtime does not validate or deterministically rewrite what the LLM actually says; a real WhatsApp smoke is required before either rule can be declared verified, not just implemented. 17 new tests, all DB-free, all green. `implemented_pending_real_smoke` for this specific increment (no dedicated WhatsApp smoke yet for the new rules) - does not change `explore_catalog`'s own `verified` status above. |

## Customer Identity

| Capability | Type | Domain | Port | Adapter | Gateway | Runtime | Operational | State | Debt |
| ---------- | ---- | ------ | ---- | ------- | ------- | ------- | ----------- | ----- | ---- |
| `resolve_customer` | `service` | `implemented` | `implemented` | `implemented` | `registered` | `connected` | `not_verified` | `accepted_with_debt` | ACS-R1-04-T06/T06.1: registered in the Capability Gateway (`lib/brain/commercial/capability-gateway/customerIdentityCapabilities.ts`, `read_only/autonomous/low`, `maxRetries: 0`). Invoked only by trusted server-side orchestration - never a sales-agent tool alias, never model-selectable: pre-plan via `resolveNativeCustomerSession`, or post-plan via `runCustomerOnboardingPostPlanStage` when `create_customer` needs fresh `no_match` evidence and pre-plan didn't already attempt resolution this turn. Fires at most once per inbound (the post-plan path reuses pre-plan's result instead of calling twice), only when local resolution (T02/T02.1) found no match and an active onboarding requires identity; never for a public query, never after a local conflict, never as a fallback from a local technical failure. ACS-R1-04-T08 exercised this end to end (real inbound, real `resolveNativeCustomerSession`, real Capability Gateway, local HTTP double implementing the real contract, real `crm_test` DB) across all three canonical scenarios (`tests/e2e/customerIdentityOnboarding.e2e.test.ts`) - still `not_verified` because no real Customer Service deployment exists to smoke-test against (`CUSTOMER_SERVICE_BASE_URL`/`CUSTOMER_SERVICE_API_KEY` empty in `.env.example`). ACS-R1-04-T07 added a durable audit trail on top: `customer_identity_resolution_recorded` (local/external outcome) and `customer_identity_capability_outcome_recorded` (Gateway status vs business outcome) on `commercial_event`, plus an allowlisted, PII-free `request_summary_json`/`response_summary_json` in `crm_capability_executions` (`buildRequestSummary`/`buildResponseSummary` on the capability definition) - the raw `phoneNumber`/`email`/`externalId` this capability's own input carries is no longer persisted verbatim. ACS-R1-04-T08.1: a successful `resolved` result now returns `customerMasterId` (v2.0.0, breaking rename from the ambiguous `customerId`) and is gated by `completeOnboardingWithVerifiedCustomer`/`verifyCustomerMasterProjection` (`onboardingTransitions.ts`) before identity becomes `identified` - see `create_customer` row for the full gap this closes. **ACS-R1-04-T08.1 (second increment) added real runtime recovery**: once onboarding lands `temporarily_unavailable` because the projection was not yet available, a later inbound with identity still unresolved gets exactly one fresh `resolve_customer` attempt again - `resolveNativeCustomerSession`'s trigger condition now also covers `temporarily_unavailable`, not only `required`/`collecting`. This wires `CustomerOnboardingService.retryResolution` (`temporarily_unavailable -> resolving`), previously dead code with zero runtime callers, into the same centralized gate via a shared `ensureResolving` helper (`onboardingTransitions.ts`) - never a second `create_customer`, never a fabricated `master_customer` row. Verified with a real inbound end to end, never by calling the gate functions directly, in `tests/e2e/customerIdentityOnboarding.e2e.test.ts` ("T08-A6" success path, "T08-A7" regression when the projection is still missing) |
| `get_customer` | `service` | `planned` | `planned` | `planned` | `not_registered` | `not_connected` | `planned` | `planned` | depends on Customer Service boundary and onboarding state |
| `create_customer` | `command` | `implemented` | `implemented` | `implemented` | `registered` | `connected` | `not_verified` | `accepted_with_debt` | ACS-R1-04-T06: registered (`mutating/autonomous/medium` - the Gateway's binary `authority` field is about operator pre-approval, not the real policy gate inside `execute()`; `maxRetries: 0`). Input is assembled entirely server-side from the trusted session (`NativeCustomerSessionExecutionContext`) - no LLM-supplied input is ever read. Applies the onboarding outcome table (created/matched_existing -> `completeOnboarding`; missing_information -> `collectFields`; conflict -> `markConflict`, never Customer 360, never link). A consent-bypass bug (`consent.createCustomer` was hardcoded `true` regardless of actual evidence) was found and fixed while writing T06's tests. ACS-R1-04-T06.1 removed its sales-agent tool alias and made `runCustomerOnboardingPostPlanStage` (legacy runtime only) the sole, deterministic caller - never LLM-tool-proposed anymore, avoiding a possible duplicate execution in the same turn. ACS-R1-04-T08 exercised the full "created" path end to end for the first time against a real, DB-backed `CustomerOnboardingService` (every prior test faked the onboarding service for this path) and found a real architecture gap: `completeOnboarding` wrote Customer Service's returned id straight into `crm_customer_onboarding_state.customer_id`, which carries a real FK to `master_customer.id` (migration 023), with nothing reconciling Customer Service's own id space with the local `master_customer` table. **ACS-R1-04-T08.1 closed this gap**: the result field is renamed `customerMasterId` (v2.0.0, breaking; `docs/integrations/customer-service-http-contract.md`), the HTTP adapter fail-closed-rejects any success response missing it or with an invalid (non-numeric) format, and a centralized gate (`completeOnboardingWithVerifiedCustomer`, `lib/brain/commercial/native-cycle/customer-session/onboardingTransitions.ts`, backed by a new read-only `CustomerMasterProjectionReader`, `lib/domains/customer-service/customerMasterProjection.ts`) verifies the local `master_customer` projection exists before completing onboarding - never inserts/updates `master_customer` itself, never fabricates a row, never propagates a raw FK exception. When the projection is not yet available, the business outcome (`created`) is preserved unchanged, onboarding lands `temporarily_unavailable`, and the structured warning `customer_master_projection_unavailable` is persisted via the existing T07 event family. Still tested against a local HTTP double, not a real Customer Service deployment - no operational smoke test yet (`operational: not_verified` unchanged; this was an architecture/FK-safety fix, not an operational verification). See `tests/commercial/customerMasterProjectionGate.test.ts` and `tests/e2e/customerIdentityOnboarding.e2e.test.ts` for full coverage. A `temporarily_unavailable` landing is no longer a permanent dead end either: see `resolve_customer`'s row for the runtime recovery path a later inbound now takes - `create_customer` itself is never re-executed as part of that recovery (only a fresh `resolve_customer` is). ACS-R1-04-T07 added `customer_identity_capability_outcome_recorded` (`commercial_event`) distinguishing Gateway status `completed` from business outcome `conflict`/`created`/`matched_existing`/`missing_information`, plus PII-free request/response summaries in `crm_capability_executions` (never `onboarding.collected` values) |
| `update_customer` | `command` | `planned` | `planned` | `planned` | `not_registered` | `not_connected` | `planned` | `planned` | canonical update rules not yet approved |
| `link_external_identity` | `command` | `implemented` | `implemented` | `implemented` | `registered` | `connected` | `not_verified` | `accepted_with_debt` | ACS-R1-04-T06: registered (`mutating/autonomous/medium`, `maxRetries: 0`). Always a separate, later execution from `create_customer`, never an automatic side effect. Policy requires the linked wa_id to match the one the current inbound channel verified (never model-controlled) and explicit consent; the same consent-bypass bug found in `create_customer` was fixed here too. ACS-R1-04-T06.1 removed its sales-agent tool alias too - `runCustomerOnboardingPostPlanStage` is the sole caller now, firing only for an already-identified customer whose wa_id isn't the confirmed match yet, with explicit link consent from the current turn. ACS-R1-04-T08 exercised this end to end in a separate turn from creation (own consent, never reused) and confirmed Gateway status `completed` / business outcome `conflict` are compatible when Customer Service reports the wa_id already linked elsewhere. ACS-R1-04-T08.1: the echoed-back `customerMasterId` (renamed from `customerId`, v2.0.0) is now verified against the local `master_customer` projection and cross-checked for consistency with the customer already known locally this turn before onboarding is touched, via the same centralized gate as `create_customer` - see that row for the full detail. Tested against a local HTTP server, not a real Customer Service deployment. ACS-R1-04-T07 added the same business-outcome-vs-Gateway-status audit trail and PII-free summaries as `create_customer` |
| `record_customer_interest` | `command` | `implemented_partial` | `not_applicable` | `not_applicable` | `not_registered` | `not_connected` | `planned` | `designed_partial` | ACS-R1-04-T04.1: contract types (`RecordCustomerInterestInput`) and pure policy (`evaluateCustomerInterestAuthority`) implemented - distinguishes `operational_context` (always allowed, no customer needed), `persistent_customer_interest` (requires `customerId` + `consent.storeInterest`) and `proactive_followup` (requires a separate `consent.allowFollowUp`). No persistence, no follow-up scheduling, no customer creation as a side effect - policy/types only |

## Customer Onboarding

| Capability | Type | Domain | Port | Adapter | Gateway | Runtime | Operational | State | Debt |
| ---------- | ---- | ------ | ---- | ------- | ------- | ------- | ----------- | ----- | ---- |
| `customer_onboarding_state` | `domain_state` | `implemented` | `implemented` | `implemented` | `not_applicable` | `connected` | `not_verified` | `accepted_with_debt` | canonical multi-turn persistence for `CustomerOnboardingState` (`lib/domains/customer-onboarding`, ACS-R1-04-T03): state machine over `crm_customer_onboarding_state` (migration 023) with optimistic locking (`version`) and normalization per the contract. Not a callable tool - it is domain state, not a capability an agent invokes. Legacy `crm_customer_onboarding` (P1M) reviewed and intentionally not reused (incompatible key, status enum and privacy columns) and left untouched. ACS-R1-04-T06 connected it to the native inbound via `resolveNativeCustomerSession` and the `create_customer`/`link_external_identity` capabilities, using only its existing public transitions - no direct writes. ACS-R1-04-T06.1 added activation from the canonical planner's own structured next-action (`runCustomerOnboardingPostPlanStage`, legacy runtime only) and conservative multi-turn field extraction from the current message (name/email/order reference) - still through `collectFields` only, never a direct write. Purposes beyond `quote` (order_inquiry/complaint/warranty/return) remain unreachable from the live runtime - no structured next-action or tool signal maps to them yet. ACS-R1-04-T07 added `customer_onboarding_transition_recorded` (`commercial_event`) for every effective transition (`start`/`collect_fields`/`mark_resolving`/`complete`/`mark_conflict`/`mark_temporarily_unavailable`) - never for a no-op mutation, never persisting collected values (booleans only). ACS-R1-04-T08's end-to-end suite surfaced that `customer_id`'s FK to `master_customer.id` (migration 023) had no reconciliation with an independently-id'd external Customer Service. **ACS-R1-04-T08.1 closed this gap** with a centralized projection-verification gate before any `completeOnboarding` call sourced from Customer Service (see the `create_customer`/`resolve_customer`/`link_external_identity` rows above) - the FK itself is unchanged (never dropped, never made nullable-on-delete), no new table or migration was added, and a not-yet-projected customer now lands onboarding in `temporarily_unavailable` with a structured warning instead of throwing a raw FK violation. **ACS-R1-04-T08.1 (second increment)** wired the domain's own `retryResolution` transition (`temporarily_unavailable -> resolving`, present since ACS-R1-04-T03 but never called by any runtime path until now) into a shared `ensureResolving` helper used by both `landOnboardingInTerminalState` and `completeOnboardingWithCustomer` - a `temporarily_unavailable` row can now resume resolution on a later turn instead of being stuck forever; recorded as a new `retry_resolution` transition operation on `customer_onboarding_transition_recorded` (T07) |

## Customer 360

| Capability | Type | Domain | Port | Adapter | Gateway | Runtime | Operational | State | Debt |
| ---------- | ---- | ------ | ---- | ------- | ------- | ------- | ----------- | ----- | ---- |
| `get_customer_context` | `read_model` | `implemented` | `implemented` | `implemented` | `not_applicable` | `internal_context_only` | `connected` | `accepted_with_debt` | Customer 360 exists, full-snapshot Hub API; the autonomous cycle now consumes a separate reduced projection instead (see `autonomous_customer_context` below) |
| `get_customer_addresses` | `read_model` | `implemented` | `implemented` | `implemented` | `not_applicable` | `internal_context_only` | `connected` | `accepted_with_debt` | read model exists; operational write/confirmation flow still planned |
| `create_customer_address` | `command` | `planned` | `planned` | `planned` | `not_registered` | `not_connected` | `planned` | `planned` | Address Book operational capability not active yet |
| `autonomous_customer_context` | `read_model` | `implemented` | `implemented` | `not_applicable` | `not_applicable` | `connected` | `not_verified` | `accepted_with_debt` | ACS-R1-04-T05: `AutonomousCustomerContext` (`lib/brain/commercial/context/autonomousCustomerContext.ts`), an allowlisted (never denylisted), history-only projection of `Customer360Snapshot` - max 3 recent opportunities/need profiles/quotes, newest-first, no PII (no email/phone/wa_id/linked identities/addresses/order refs/invoice numbers/message bodies/provider ids/full snapshot). `loadAutonomousCustomerContext` (`loadAutonomousCustomerContext.ts`) is the single load point: `customerId` null makes zero calls, a thrown/failed load degrades to `unavailable` and never stops the cycle. Wired into both `runNativeAutonomousCycle` runtimes (multi-request and legacy, mutually exclusive as before) via typed fields on `MultiRequestCycleInput`/`PlanTurnInput`/`TurnPlannerProviderInput` and `CommercialContextSnapshot`/`SalesAgentInput` - never inside a generic `metadata` bag. Customer 360 never resolves identity, creates customers, links identities or confirms addresses. ACS-R1-04-T06 connected `customerMasterId` to the real identity resolver (`resolveNativeCustomerSession`, T02/T02.1/T04.1) and put the load itself behind an explicit access gate (`contextAccess: none/commercial_history/validated_entity`) - an identified customer alone no longer authorizes a load; it also requires an active `quote`/`purchase` onboarding with no conflict. `validated_entity` is never granted yet (no historical-entity-ownership validation). No operational smoke test yet |

## Shipping

| Capability | Type | Domain | Port | Adapter | Gateway | Runtime | Operational | State | Debt |
| ---------- | ---- | ------ | ---- | ------- | ------- | ------- | ----------- | ----- | ---- |
| `set_shipping_destination` | `command` | `implemented` | `not_applicable` | `not_applicable` | `registered` | `connected` | `not_verified` | `accepted_with_debt` | CRM-R1-T13D: resolves a customer-stated destination commune (free text only, never a `communeId` - the model never proposes the id) via `CommuneResolver` (T13C, over `pc_pos.comuna`) and persists it as the current opportunity's durable, authoritative shipping destination. Persistence reuses the existing `crm_request_facts` lifecycle (`inferred/confirmed/verified/rejected/superseded`, migration 017) unmodified - no new table, no new migration - anchored by `opportunity:<id>` (a plain string key with no FK, verified in `migrations/017_crm_request_facts.sql`) rather than `crm_conversation_requests.request_id`, since that runtime is the non-canonical multi-request path the Native Agent Tool Loop never populates (T13B audit section 14). An explicit, unambiguous resolution is persisted `status: "confirmed"` directly - no redundant second confirmation - while `needs_clarification`/`not_found`/resolver or persistence failures never persist anything (fail-closed). Idempotent on a repeated identical destination (no duplicate version); a different destination supersedes the prior fact inside the same transaction `upsertRequestFact` already provides, leaving exactly one active fact per opportunity (DB-enforced via the existing `uq_request_fact_active` unique key). Added to `AGENT_LOOP_TOOL_POOL` (`runAgentToolLoop.ts`) alongside the four existing tools (none removed) with its own `inputSchema` (`{destination: string}`, `additionalProperties:false` - no `communeId` property exists in the schema at all). `CommercialContextSnapshot.shippingDestination` rehydrates the active destination from `crm_request_facts` on every context build (`buildNativeCommercialContext.ts`, injectable `loadShippingDestination` for tests) - durable state, never reconstructed from conversation text or an ephemeral tool observation - and is summarized (communeId/canonicalName only) into the prompt payload so the agent does not re-ask once a destination is already confirmed. Deliberately does not implement: shipping rate/cost, carrier selection or coverage, summing `weightKg`, checkout, or any stored-address-to-destination auto-derivation (a saved `customer_addresses`/`delivery_address_id` entry is never silently promoted to an authoritative destination by this capability - out of scope, left as a future, explicitly-confirmed derivation per T13D section 9/20). 24 new tests (`tests/domains/shippingDestination.test.ts` 13, `tests/commercial/shippingDestinationCapability.test.ts` 9, `tests/commercial/buildNativeCommercialContext.test.ts` +2) against real MariaDB (`main_management`, same pattern as `tests/commercial/requestFacts.test.ts`), plus `npx tsc --noEmit`, `npm run lint` and `npm run build` clean. `operational: not_verified` because no real WhatsApp smoke has been run yet (same axis/reasoning as `search_company_knowledge`/`resolve_customer`) - `Domain`/`Gateway`/`Runtime` reflect that the capability, persistence and prompt wiring are real and DB-tested, not a smoke-test claim. |
| `select_products` | `command` | `implemented` | `not_applicable` | `not_applicable` | `registered` | `connected` | `not_verified` | `accepted_with_debt` | CRM-R1-T13E.2: records the customer's confirmed product selection (`productId`/`combinationId`/`quantity`) as the current opportunity's durable, authoritative commercial line items - the missing piece T13E.1 identified as blocking any real `calculate_shipping` wiring. Persisted the same way as `set_shipping_destination` - `crm_request_facts`, fact key `commercial_line_items`, anchor `opportunity:<id>`, `status:"confirmed"` directly, full replacement semantics (each call supplies the complete desired selection, never a delta; duplicate `(productId, combinationId)` pairs within one call are merged by summing quantity). Evidence-grounded: `runAgentToolLoop.ts`'s `processUseToolStep` validates every item's `(productId, combinationId)` against this conversation's own observed evidence (`search_products`/`get_product_details`/`explore_catalog`) **before** the Gateway is ever called, reusing `resolveObservedRecommendationSourceProduct` (the same machinery `recommend_catalog_products`' `sourceProduct` already uses) - a fabricated or merely-discussed-but-never-observed product is rejected pre-Gateway, never persisted. Real bug found and fixed while wiring this: that function requires a numeric `productId` (`recommend_catalog_products`' own schema types it `number`), while `select_products`' schema - like every other catalog tool - types it `string`; without a local `Number()` conversion at the call site, every real call would have been blocked regardless of evidence (caught by an integration test through the real loop, not by the capability-level unit tests alone). `CommercialContextSnapshot.commercialLineItems` rehydrates the active selection the same way `shippingDestination` does, summarized into the prompt payload so the agent does not re-ask once a selection is already confirmed. Explicitly out of scope: no free-text reference resolution ("la segunda") inside this capability - the model resolves the reference itself from context already visible to it (position/name in `RecentCatalogContext`) and supplies concrete ids, which this capability only verifies were actually observed. 25 new tests (`tests/domains/commercialLineItems.test.ts` 15, `tests/commercial/selectProductsCapability.test.ts` 10) against real MariaDB, plus 2 integration tests in `tests/agent-loop/runAgentToolLoop.test.ts` proving the evidence gate blocks an unobserved item and clears an observed one through the real loop. `operational: not_verified` - no real WhatsApp smoke yet. |

## Commercial Execution

| Capability | Type | Domain | Port | Adapter | Gateway | Runtime | Operational | State | Debt |
| ---------- | ---- | ------ | ---- | ------- | ------- | ------- | ----------- | ----- | ---- |
| `prepare_quote` | `command` | `planned` | `planned` | `planned` | `not_registered` | `not_connected` | `planned` | `planned` | quote flow not productively wired yet; policy alignment pending |
| `business_policy` | `policy` | `planned` | `planned` | `planned` | `not_registered` | `not_connected` | `planned` | `planned` | policy boundary not yet productized |
| `calculate_shipping` | `command` | `implemented` | `implemented` | `implemented` | `registered` | `connected` | `not_verified` | `accepted_with_debt` | CRM-R1-T13E.2: **Carrier MS (`http://ms.pesaschile.cl`) is the sole authority over coverage, carriers and rates** - the T13E.1 architecture (CRM computing coverage from `pc_pos.carrier_coverage` and, eventually, rate from `pc_pos.carrier_rangos_dd`) was superseded and its coverage adapter removed (see `## Shipping` note below); CRM's job is now request assembly + response normalization only. Real contract captured live (not designed from hypothesis): `GET /api/pc-carrier/carrier/v1/all?destino=...&alto=1&ancho=1&largo=1&kilos=...&total_boleta=...`, no auth, success is any 2xx (observed `202`) with `{"options":[{carrier_name,service_type,total_cost,estimated_delivery}]}` (an empty array is a valid "no carrier serves this destination" business answer, not an error), 4xx is `{"error":"..."}`. Executor pipeline, takes **no model-supplied arguments** (`inputSchema: {type:"object",properties:{},additionalProperties:false}`): `context.opportunityId` -> `getActiveShippingDestinationForOpportunity` (T13D) -> `getActiveCommercialLineItemsForOpportunity` (T13E.2, `select_products` above) -> `CatalogPort.batchGetProducts` (price + `weightKg`) -> `validateAndSumWeightKg`/`validateAndSumTotalBoleta` (both pure, reused/added in `lib/domains/shipping-calculation/`, integer-scaled arithmetic, fail closed on any `null` weight or price) -> `CarrierService.quoteAll()` -> normalized `{status, destination, totalWeightKg, totalBoleta, options[]}`. `total_boleta` is fed exclusively by Catalog Service's `price.amount`, passed through exactly as reported (no separate net/gross field exists to choose between - documented, not fabricated). Dimensions (`alto=1/ancho=1/largo=1`) are contractual constants hardcoded in the HTTP adapter only (`CARRIER_DEFAULT_HEIGHT/WIDTH/LENGTH`) - never derived, never stored, never model- or Catalog-supplied. Typed fail-closed states for every precondition gap: `no_active_opportunity` (denied), `shipping_destination_required`/`commercial_items_required`/`catalog_product_unavailable`/`weight_unavailable`/`price_unavailable`/`no_shipping_options` (completed, business-shaped), `catalog_unavailable`/`carrier_service_unavailable`/`carrier_service_timeout` (temporarily_blocked, retryable), `carrier_invalid_response` (failed, not retryable - a malformed response is fail-closed, never partially trusted). Live-verified: a manual smoke run (`scripts/manual-test/shipping-calculation-smoke.ts --cases`) against the real Carrier MS across all 6 `carrier_rangos_dd` subtotal boundaries plus weight/destination variation **confirmed empirically** that Pesas Chile's despacho-directo price is a pure function of `total_boleta` (identical price at 0kg through 500kg for a fixed subtotal) - closing T13E.1's `RATE_SOURCE_SEMANTICS_UNCONFIRMED` finding for that carrier for good, without CRM ever computing the price itself. 26 new tests (`tests/integrations/httpCarrierServiceAdapter.test.ts` 13 incl. exact query encoding/dimensions/timeout/malformed-response fail-closed; `tests/commercial/calculateShippingCapability.test.ts` 16 covering every failure state plus the full aggregation pipeline against real DB-persisted destination/selection with faked Catalog/Carrier ports) plus `tests/domains/shippingCalculationSubtotal.test.ts` 10. `operational: not_verified` - no real WhatsApp smoke yet (the manual script above exercised Carrier MS directly, not through a live conversation). See `docs/releases/CRM-R1-T13E-shipping-calculation.md`. |
| `create_checkout_link` | `command` | `planned` | `planned` | `planned` | `not_registered` | `not_connected` | `planned` | `planned` | checkout handoff not wired |
| `find_order` | `read_model` | `implemented_partial` | `partial` | `partial` | `not_registered` | `multi_request_only` | `connected` | `implemented_partial` | order projection exists, but no independent orders boundary yet |
| `get_order_status` | `read_model` | `implemented_partial` | `partial` | `partial` | `not_registered` | `multi_request_only` | `connected` | `implemented_partial` | order projection exists, but no independent orders boundary yet |
| `place_sales_call` | `channel` | `planned` | `planned` | `planned` | `not_registered` | `not_connected` | `planned` | `planned` | voice capability not productized |

## Notes

- `Customer 360` is represented as a read model, capability contract and evidence, not as an agent tool.
- `get_customer_context` and `get_customer_addresses` are read models, not gateway tools.
- `resolve_customer` is the active identity workstream.
- `domain_state` rows (e.g. `customer_onboarding_state`) are persisted state machines, not agent-callable tools; they have no `gateway` registration by design (`not_applicable`).
- `ACS-R1-04` is the active sequential release; `ACS-R1-05` (Autonomous Follow-up Runtime) is an authorized parallel workstream (see `ACTIVE_RELEASE.md`) that adds no new Gateway-registered capability rows here - `schedule_followup` is an internal `crm_agent_actions` action type persisted by `sales-consultative`, not an agent-callable tool. `ACS-R1-05-T02` connected `policy/evaluateCommercialPolicy.ts` as a mandatory `follow_up_dispatch_policy` gate before that internal write (opt-out/quiet-hours/identity-conflict/ai-blocked); this does not change any row above and does not mark `schedule_followup` `operational: verified`. `ACS-R1-05-T03` hardened the follow-up worker itself (`lib/brain/commercial/followup/runFollowupTick.ts`): stale-lock recovery for abandoned `executing` rows, real `max_attempts` enforcement with retry of `failed` rows, and a status-preconditioned `cancelFollowUp`; still an internal worker on `crm_agent_actions`, not a Gateway-registered capability - no row above changes and `schedule_followup` remains outside `operational: verified`. `ACS-R1-05-T04` consolidated the two divergent `brain_message_outbox` writers into one canonical writer (`lib/brain/messaging/canonicalOutboxWriter.ts`) and extended delivery-outcome projection through to `crm_opportunities` (idempotent `crm_action_outcomes` via `outcome_dedupe_key`, monotonic CAS projection, never touching commercial state columns); still transport/outcome plumbing under `crm_agent_actions`/`brain_message_outbox`, not a Gateway-registered capability - no row above changes and `schedule_followup` remains outside `operational: verified`. `ACS-R1-05-T06` closed the auditoria's P1-2/P1-5 gaps: a new canonical, fail-closed reader (`lib/brain/runtime/autonomousRuntimeConfig.ts`) for the outbox worker's own start-up contract (real send requires `BRAIN_OUTBOX_WORKER_ENABLED`/`BRAIN_META_SEND_ENABLED`/`BRAIN_OUTBOX_WORKER_ALLOW_REAL_SEND` all `true`, malformed values are a configuration error, and real send with an empty `BRAIN_AUTONOMOUS_TEST_WA_IDS` refuses to start), plus removing silent `process.env` auto-escalation from the three CLI entry points that re-enter the autonomous cycle or send real Meta messages, plus consistent redaction of `crm_agent_actions.failure_reason` where it previously persisted raw `error.message`. Same scope boundary as T02-T04: internal worker/config hardening, not a Gateway-registered capability - no row above changes and `schedule_followup` remains outside `operational: verified`; no real Meta send was executed as part of this task. `ACS-R1-05-T06.1` closed a gap the first T06 attempt left open: `BRAIN_AUTONOMOUS_TEST_WA_IDS` was never actually compared against a `wa_id` anywhere except the already-fail-closed final defense in `metaClient.ts` - the autonomous cycle called the LLM and `runFollowupTick.ts` claimed/consumed attempts for any number regardless of the allowlist. Adds an early gate in `runNativeAutonomousCycle.ts` (before any LLM call or Customer Service resolution) and a pre-claim gate in `runFollowupTick.ts` (row/`attempt_number` left untouched for an unauthorized number), consolidates the outbox worker's existing guard onto the same digit-normalized comparison as the final defense, and extends the shared redactor with email/phone patterns wired into `autonomousOutboxTick.ts`'s two remaining raw `error.message` writes. Same scope boundary as before - no row above changes, no real Meta send was executed. `ACS-R1-05-T07` (`done, accepted`, closes `ACS-R1-05`) validated the whole consolidated runtime end-to-end against real MariaDB (`crm_test`) with fake external boundaries (catalog HTTP double, fake WhatsApp sender) - proves the productive runtime (persistence, concurrency, restart recovery, idempotency), never the real external operation of Meta/Catalog Service/Customer Service. No row above changes and `schedule_followup`/`batch_get_products` remain outside `operational: verified` - that axis requires a real external smoke this task explicitly did not (and was not required to) perform. See `docs/releases/ACS-R1-05-autonomous-follow-up-runtime.md`, "Evidencia de cierre - ACS-R1-05-T07", for full detail.
- `multi_request_only` means the capability exists only in a non-canonical helper path.
- `accepted_with_debt` means usable and documented, but still carrying explicit debt.
- Planned rows are roadmap only; they are not productively wired just because docs exist.
- CRM-R1-T13E.2 removed `lib/integrations/logistics/shipping-coverage-adapter.ts` (T13E.1's direct `pc_pos.carriers`/`carrier_coverage` read adapter, confirmed to have zero consumers) once Carrier MS was confirmed as the real, reachable shipping authority - `pc_pos.comuna` (T13C, via `set_shipping_destination`) remains CRM's only reason to read `pc_pos` at all. See `docs/releases/CRM-R1-T13E-shipping-calculation.md`.
