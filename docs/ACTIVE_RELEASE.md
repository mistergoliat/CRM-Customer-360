---
release: ACS-R1-04
title: Customer Identity Resolution + Onboarding
status: active_blocked_external
updated_at: 2026-07-19
current_task: ACS-R1-04-T08
next_task: ACS-R1-04-T09
blocked: true
last_accepted_commit: 102fc5e
t06_1_sha: 0c51419
t06_2_sha: 72cb9c3
t07_sha: cd9317e
t08_1_sha: 102fc5e
doc_id: release-active
source_of_truth_for:
  - active release
  - current task
  - next task
  - blocked state
  - last accepted commit
depends_on:
  - ./ROADMAP.md
  - ./releases/README.md
  - ./releases/ACS-R1-04-customer-identity-onboarding.md
  - ./releases/ACS-R1-05-autonomous-follow-up-runtime.md
  - ./releases/ACS-R1-05.1-persistent-commercial-memory-controlled-whatsapp-pilot.md
  - ./product/MVP_EXECUTION_MAP.md
  - ./CAPABILITY_MATRIX.md
tags:
  - release
  - product
---

# ACTIVE_RELEASE

Este documento es un puntero operativo breve. El alcance, la tabla de tareas, la Definition of Done y la deuda detallada viven en la release spec (unica fuente de esos datos).

## Release activa

- `ACS-R1-04`

## Tarea actual

- `ACS-R1-04-T08`

## Siguiente tarea

- `ACS-R1-04-T09`

## Bloqueos

- `ACS-R1-04-T08` (pruebas end-to-end de identidad/onboarding) permanece bloqueada UNICAMENTE por la falta de un Customer Service desplegado (ni productivo ni sandbox) contra el cual ejecutar el smoke operacional que exige su Definition of Done - `CUSTOMER_SERVICE_BASE_URL`/`CUSTOMER_SERVICE_API_KEY` estan vacios en `.env.example` y no hay configuracion real en el repo. El gap estructural que T08 encontro (`ACS-R1-04-T08.1`, ver abajo) ya quedo resuelto: `resolve_customer`/`create_customer`/`link_external_identity` retornan `customerMasterId` (v2.0.0, breaking) y ACS verifica la proyeccion local `master_customer` antes de completar onboarding, via un gate centralizado que no inserta/actualiza `master_customer` ni elimina la FK de `crm_customer_onboarding_state.customer_id`. `ACS-R1-04-T08.1` tambien cerro, en un segundo incremento (commit `102fc5e`), la deuda de recuperacion runtime: un onboarding que aterriza en `temporarily_unavailable` por proyeccion no disponible ya no es un callejon sin salida - un inbound real posterior obtiene exactamente un nuevo intento de `resolve_customer` y completa sin un segundo `create_customer` en cuanto la proyeccion aparece. La suite E2E completa (14 tests, `tests/e2e/customerIdentityOnboarding.e2e.test.ts`, incluye el escenario negativo de proyeccion no disponible y la recuperacion runtime real en un turno N+1) corre en verde contra un servidor HTTP local controlado que implementa el contrato real (nunca un mock de `executeGovernedCapability`) y contra la cadena de migraciones completa sobre una base `crm_test` desechable, pero eso sigue siendo evidencia de integracion, no de verificacion operacional real - `create_customer`/`link_external_identity`/`resolve_customer` permanecen `operational: not_verified` en `CAPABILITY_MATRIX.md` hasta que exista un smoke contra un Customer Service real desplegado. Ver la release spec, seccion "Deudas fuera del incremento", entradas `ACS-R1-04-T08` y `ACS-R1-04-T08.1`, para el detalle completo (incluye un segundo bug real ya corregido con regresion en T08: `lib/domains/customer-identity/local-adapter.ts` trataba una fila de identidad externa no resuelta como un candidato valido con id literal `"null"`). `ACS-R1-04-T06.2` reconcilio el inbound de identidad nativo tras `PR #43`/commit `3222003` - ver la release spec, seccion "Deudas fuera del incremento", entrada `ACS-R1-04-T06.2`. `ACS-R1-04-T07` persistio executions/outcomes/warnings de identidad sobre `commercial_event` existente, sin nueva tabla ni cambio de autoridad - ver evidencia de cierre en la release spec.

## Dependencias externas en pausa

Fuente normativa: [ROADMAP](ROADMAP.md#dependencias-externas-y-capacidades-en-pausa). Resumen: Customer Service `PAUSED_EXTERNAL` (motivo ya descrito en el bloqueo de arriba); Address Book y Voice `DEFERRED`.

## Workstream paralelo autorizado

- `ACS-R1-05` - Autonomous Follow-up Runtime
- current task: none (`ACS-R1-05-T07` closed the release)
- status: `accepted`, mergeada en `develop` (PR #57, commit de merge `a2754e2`, confirmado con `git rev-parse develop`/`origin/develop` y `git log --oneline --decorate --graph`). Al momento en que `ACS-R1-05-T07` cerro funcionalmente (ver "Evidencia de cierre - ACS-R1-05-T07" en la release spec) esa rama no estaba mergeada todavia - el merge via PR #57 ocurrio en un paso posterior, fuera de alcance de T07; ese texto historico no se reescribe aqui.

- `ACS-R1-05.1` - Persistent Commercial Memory + Controlled WhatsApp Pilot
- status: `parallel_in_progress`
- critical_path: `true`
- current_task: `ACS-R1-05.1-T02.1` (Native Read-Only Agent Tool Loop)
- current_task_status: `implemented_pending_real_smoke` (contrato `AgentStep`, loop nativo, `search_company_knowledge` y wiring detras de `BRAIN_AGENT_TOOL_LOOP_ENABLED` completos y probados; smoke real contra `wa_id` allowlisted + LLM real + Catalog Service real pendiente por falta de credenciales/red en este entorno)
- ver [ACS-R1-05.1 spec](releases/ACS-R1-05.1-persistent-commercial-memory-controlled-whatsapp-pilot.md) para alcance, tareas y Definition of Done completos

`ACS-R1-05.1-T01` (Single Commercial Runtime Authority, `accepted` - veredicto `single_commercial_runtime_authority_accepted`; rama `feat/acs-r1-05-1-t01-single-commercial-runtime-authority`, commits `b08e4d0`/`f2d1531`/`439d1a3`/`a8bdf14`) deja el runtime nativo (`processNativeWhatsAppInbound -> runNativeAutonomousCycle -> operational-loop -> persistCommercialState`) como unica autoridad comercial habilitada por defecto: el motor legacy `sales-consultative` (`runSalesConsultativeService`) queda deshabilitado por defecto (fail-closed) en sus dos vias productivas restantes (`process-inbound` y `native-whatsapp/service.ts#processSalesInbound`) detras del flag `BRAIN_LEGACY_SALES_CONSULTATIVE_ENABLED` (default `false`, unico lector `commercialCycleConfig.ts`). `process-inbound` conserva su contrato (`ok:true`, warning `legacy_sales_consultative_disabled`, cero 500 causado por el gate); `processSalesInbound` falla antes de DB con un error de dominio nombrado. Verificado con un test arquitectonico de callers sobre todo el arbol de produccion, MariaDB E2E real (`crm_test`, cero regresiones nuevas respecto de `develop`), replay y same-inbound concurrency a nivel de persistencia. Deuda registrada, no bloqueante: checksum drift preexistente en la migracion 025 contra `main_management`; `processNativeWhatsAppInbound` sin seam de proveedor para pruebas ad-hoc; la senal `duplicate` de aplicacion no es fiable bajo concurrencia real aunque la persistencia final si lo es. Detalle completo en "Evidencia de cierre - ACS-R1-05.1-T01" de la release spec.

`ACS-R1-05.1-T02` (Stable Opportunity Continuity, `accepted` - veredicto `stable_opportunity_continuity_accepted`; rama `feat/acs-r1-05-1-t02-stable-opportunity-continuity`, commits `4a35c41`/`8055584`/`db49246`/`a88ead2`/`6e60345` mas el commit de aceptacion) garantiza continuidad de `opportunity_id`/`opportunity_key` a traves de un intent que cambia turno a turno dentro de la misma familia comercial (venta-venta), ambiguedad fail-closed (sin `activeState`/`selectedState` arbitrario en las tres ubicaciones donde existia ese patron, sin tercera oportunidad, sin mutacion), terminales sin auto-reopen, y separacion venta/postventa implementada y probada en el resolver (`resolveOpportunityIdentity.ts`, clasificacion `sales`/`service`/`neutral` grounded en el routing legado real `postventa_queue`/`mantenciones_queue`). Esa separacion cross-domain **no esta todavia demostrada en el camino nativo real de WhatsApp**: `buildNativeBrainContextShim` sigue fijando `primaryIntent = "unknown"` en todo turno real, asi que la propagacion/clasificacion de intent confiable queda pendiente (deuda explicita, no resuelta con un clasificador improvisado - se abordara junto con `T02.1`/`T03`). T02 tampoco garantiza need profile, memoria comercial completa, distincion de necesidades independientes dentro de la MISMA familia/identidad, vinculo explicito follow-up/accion, catalogo/grounding, ni una aclaracion conversacional real ante ambiguedad (hoy es handoff a humano generico). Detalle completo en "Evidencia de cierre - ACS-R1-05.1-T02" de la release spec.

`ACS-R1-05.1-T02.3A` (`completed` - Sales Agent Configuration domain - dominio tipado, validacion de formato, hash canonico, persistencia, versionado y publicacion transaccional, resolver en cascada; tarea no listada en la tabla de tareas de la release spec al momento de esta implementacion, ejecutada bajo instruccion explicita en paralelo a `T02.1`/`T03`, sin reabrir ni modificar su alcance) implementa exclusivamente `migrations/026_sales_agent_configurations.sql` (tabla `sales_agent_configurations`, columna generada `published_scope_key` con `UNIQUE KEY` que impide dos filas `published` para el mismo `scope_key`, `CHECK` de `status`, verificado contra MariaDB 11.4 real) y el modulo `lib/brain/commercial/sales-agent-configuration/` (`types.ts`, `constants.ts`, `defaults.ts`, `validation.ts`, `hash.ts`, `repository.ts`, `publish.ts`, `resolver.ts`, `index.ts`). Scope unico y hardcodeado `pesas_chile` (`SALES_AGENT_CONFIGURATION_SCOPE`), sin resolver multi-tenant, sin mapping por `phoneNumberId`/`channelAccountId`. Validacion acepta exclusivamente los seis campos MVP (`agentName`, `companyName`, `role`, `companyDescription`, `customInstructions`, `prohibitedPhrases`), rechaza campos desconocidos/faltantes/fuera de limite, normaliza espacios y deduplica frases prohibidas (exact-match tras normalizar, nunca semantica de negocio). Hash SHA-256 sobre `schemaVersion` + configuracion canonica (claves ordenadas, `prohibitedPhrases` ordenado alfabeticamente antes del hash - decision explicita, probada), excluye id/scope/version/status/timestamps/createdBy/parentId. Creacion de draft usa conexion dedicada con `GET_LOCK`/`RELEASE_LOCK` (`sales_agent_configurations:pesas_chile`, timeout 10s) antes de `BEGIN`, calcula `MAX(version)+1` dentro de la transaccion; publicacion usa la misma estrategia de lock + `SELECT ... FOR UPDATE` + re-validacion/recalculo de hash (nunca confia ciegamente en el hash almacenado) + archivar publicacion previa + publicar + auditoria, todo en una sola transaccion con rollback completo ante cualquier fallo. `lib/audit.ts` se extendio con un parametro `connection` opcional (minimo, retrocompatible - todo caller existente sigue sin pasarlo) para que `sales_agent_configuration.created/updated/published/archived` queden dentro de la misma transaccion del escritor en vez de ser un segundo writer independiente; un fallo del insert de auditoria en modo transaccional ya no se silencia (a diferencia del camino no transaccional existente, que si lo hace por diseno). El resolver (`published -> deployment default -> safe default`) nunca envuelve la lectura de `published` en try/catch: solo "no existe fila publicada" habilita el fallback, cualquier fallo real de DB se propaga. No se conecta a `buildAgentStepPromptPackage.ts`, ningun provider, parametros de modelo, Hub UI, follow-up, simulador ni Capability Gateway - explicitamente fuera de alcance de esta tarea. Rama `feat/acs-r1-05-1-t02-3a-agent-config-domain`, base `develop` en `7ddab7f`. Probado con 33 tests nuevos contra MariaDB real (`tests/commercial/salesAgentConfiguration.test.ts`, `crm_test`, incluye concurrencia real de creacion de drafts (5 en paralelo, versiones distintas) y de publish (4 en paralelo, exactamente una publicada al final), y verificacion directa del `CHECK` constraint y de la unicidad de `published_scope_key` contra la base real), mas `npx tsc --noEmit` y `npm run build` limpios. Suite completa de `tests/commercial` sin regresiones nuevas: 1202 tests, 1187 pass/15 fail, los mismos 15 fallos confirmados identicos contra un baseline sin el cambio de `lib/audit.ts` (via `git stash`) - ninguno de los 15 toca este modulo ni `lib/audit.ts`. No modifica `current_task`/`current_task_status` de esta seccion (siguen en `ACS-R1-05.1-T02.1`, sin cerrar por falta del smoke real ya documentado) - esta tarea es paralela, no una continuacion secuencial de `T02.1`/`T03`. Deuda explicita: esta tarea no aparecia en la tabla de tareas ni en el cuerpo de `docs/releases/ACS-R1-05.1-persistent-commercial-memory-controlled-whatsapp-pilot.md` antes de esta implementacion; esta entrada no modifica esa release spec (el alcance explicito de la tarea limito los archivos de documentacion a este archivo unicamente) - la reconciliacion de la tabla de tareas, la seccion "Deuda no cerrada" y `docs/CAPABILITY_MATRIX.md` (sin cambios de capability real, no aplica) queda pendiente para un paso documental posterior.

`ACS-R1-05.1-T02.3B` (`completed` - Sales Agent Configuration connected to the Native Agent Tool Loop runtime; misma condicion de `T02.3A`: tarea no listada en la tabla de tareas de la release spec, ejecutada en paralelo a `T02.1`/`T03`, sin reabrir ni modificar su alcance) conecta la configuracion versionada de `T02.3A` al runtime real. Un unico punto de integracion en el prompt builder (`buildAgentStepPromptPackage.ts` via `renderSalesAgentIdentityPrompt.ts`, capa compartida entre las fases gathering/finalization) reemplaza el hardcode "You are the Sales Agent for PesasChile AI Hub" del motor generico; la identidad (`agentName`/`companyName`/`role`/`companyDescription`/`customInstructions`/`prohibitedPhrases`) llega siempre resuelta por `resolveSalesAgentConfiguration()` (`published -> deployment default -> safe default`), nunca opcional. El dominio se extendio con dos secciones opcionales del documento (`modelConfiguration`, `loopConfiguration`: modelo, temperatura, limite opcional de tokens de salida, timeout, reintentos tecnicos, pasos maximos por turno, tool calls maximos por turno), siempre v2-compatibles con una fila v1 sin runtime config. `httpAgentLoopProvider.ts` distingue reintentos tecnicos HTTP (bounded backoff, solo 429/500/502/503/504, nunca 400/401/403) de decisiones del modelo y de intentos de finalizacion (conceptos ya existentes en `runAgentToolLoop.ts`, nunca mezclados). El evento `agent_tool_loop_completed` se extendio con `configurationSource`/`configurationRecordId`/`configurationVersion`/`configurationHash` y los parametros efectivos ya clampeados (`effectiveModel`, `effectiveTemperature`, `effectiveMaxOutputSize`, `effectiveTimeoutMs`, `effectiveMaxAgentStepsPerTurn`, `effectiveMaxToolCallsPerTurn`) - nunca el prompt, `customInstructions` ni ningun secreto. **Dos correcciones posteriores, misma rama**: (1, commit `09a5d5e`) un fallo real de `resolveSalesAgentConfiguration()` (error de DB/repositorio, distinto de "nada publicado", que el resolver ya resuelve solo) dejo de tratarse como licencia para invocar el modelo con un fallback hardcodeado (`HARD_SAFE_SALES_AGENT_CONFIGURATION_FALLBACK`, eliminado) - ahora produce un handoff tecnico controlado (`runNativeAgentToolLoopCycleConfigurationFailure`, mismo pipeline de despacho que cualquier otro handoff terminal, mensaje neutro al cliente, causa tecnica preservada solo en warnings internos); auditoria de cierre posterior confirmo, con tests adicionales (`[E4]`/`[E5]` en `salesAgentConfigurationRuntime.test.ts`) contra MariaDB real, que una fila `published` con `configuration_json` estructuralmente invalido (no solo fuera de rango) tambien lanza en vez de degradar en silencio a `safe_default`. (2, commit `447c8a0`) ocho correcciones de una revision posterior del diff: precedencia de modelo restaurada (`publicado (si pasa el allowlist vigente) -> BRAIN_MODEL_NAME -> fallback generico` - el resolver antes devolvia siempre un modelo resuelto, nunca dejaba pasar `BRAIN_MODEL_NAME`); `maxModelRetries` default corregido a `0` (el provider pre-T02.3B nunca reintentaba solo); `max_tokens` ya no se envia automaticamente sin configuracion publicada (se omite del request, nunca se inventa `1024`); `computeSalesAgentConfigurationHash` toma `schemaVersion` explicito del registro real (nunca inferido por la presencia de `modelConfiguration`/`loopConfiguration` - cada escritura nueva se estampa con la version actual sin importar el contenido, la inferencia por forma desalineaba el hash contra lo realmente almacenado); allowlist real de modelos (`BRAIN_MODEL_NAME` + fallback generico, nunca un catalogo inventado), exigida en create/update y tolerada en lectura (mismo criterio que los limites numericos); regla inmutable final agregada tras el bloque editable del prompt (la configuracion no puede contradecir AgentStep, evidencia, tools, side effects ni politicas de plataforma); retries corregidos para no iniciar un intento nuevo si el deadline ya vencio (se elimino el `Math.max(1, ...)` que forzaba uno igual) y backoff cancelable por el `AbortSignal` externo; fallo al escribir `agent_tool_loop_completed` ya no se descarta en silencio, ahora agrega un warning observable. No toca Hub UI, follow-up configurable, simulador, multi-tenant ni Capability Gateway/catalogo/tools - explicitamente fuera de alcance. Rama `feat/acs-r1-05-1-t02-3b-agent-config-runtime` (continua directamente sobre los commits de `T02.3A`, nunca mergeados a `develop` por separado), commits `1777457`/`4955842`/`208c046`/`09a5d5e`/`447c8a0`, PR #66 abierta contra `develop` (sin mergear al momento de esta entrada). Probado con el suite completo de `sales-agent-configuration`/`agent-loop`/`events` (136/136 verde, incluye tests nuevos de precedencia de modelo/allowlist `[E6]`/`[E7]`/`[M6]`-`[M8]`, hash con `schemaVersion` explicito `[H11]`/`[H12]` con hash historico golden, retries `[HP13b]`/`[HP18]`/`[HP19]`, regla inmutable del prompt `[PR11]`, warning de auditoria `[AE28]`), mas `npx tsc --noEmit` y `npm run build` limpios. Cero regresiones nuevas verificado contra el baseline pre-correccion via `git stash` + `npm test` completo (1592 vs 1606 tests, mismo conjunto de fallos preexistentes no relacionados). No modifica `current_task`/`current_task_status` de esta seccion (siguen en `ACS-R1-05.1-T02.1`) - misma logica de `T02.3A`: tarea paralela.

`ACS-R1-05.1-T02.3C` (`completed` - Hub "Configura al agente"; misma condicion de `T02.3A`/`T02.3B`: tarea no listada en la tabla de tareas de la release spec, ejecutada en paralelo a `T02.1`/`T03`) agrega la primera superficie visual sobre el dominio de `T02.3A`/`T02.3B` (antes solo editable via SQL/scripts): ruta `/agents/sales/configuration`, entrada de nav nueva ("Configura al agente", grupo `intelligence`) - el modulo legado `/agents` sigue oculto y sin tocar. API con 7 Route Handlers en `app/api/brain/agents/sales/configuration/**` (listar/crear/validar/leer/actualizar/publicar/clonar/archivar), todas detras de `requireOperator` + `isDbWriteEnabled`. Dominio extendido con lo minimo que una UI de edicion necesitaba: `SALES_AGENT_CONFIGURATION_HUB_ACTOR` (actor fijo para `createdBy`, limitacion temporal documentada hasta que exista identidad real de operador), optimistic concurrency real en `updateDraftConfiguration` (`expectedUpdatedAt` comparado en el mismo `UPDATE`, nunca lectura-y-luego-escritura), `archiveDraftConfiguration` (UPDATE atomico `WHERE status='draft'` - el Hub nunca puede archivar una fila `published`), y `parentConfigurationId` en el payload de auditoria para trazar clones. `modelConfiguration`/`loopConfiguration` se editan como toggles todo-o-nada para mantener "sin limite configurado" honesto.

Rama `feat/acs-r1-05-1-t02-3c-agent-config-hub`, base `develop@e8b62ff`, commits `5e5c0a4`/`ef94a57`/`d2a7722`/`e7737b7`/`da31fd1`/`d89bdd6` mas correcciones de revision (atomicidad de archive, guard de dirty-state en publicar/archivar desde la tabla de versiones, columna Parent resuelta a version real, validacion de `?limit`, mensaje de error generico en `page.tsx`) - sin push, sin PR. Probado con tests puros + MariaDB real (dominio y Route Handlers) mas `npx tsc --noEmit`, `npm run lint` y `npm run build` limpios; verificado tambien contra un dev server real (`curl` autenticado, flujo completo crear/validar/actualizar con conflicto/clonar/publicar/archivar-rechazado, mas 401 real sin sesion). `npm test` completo sin regresiones nuevas atribuibles a esta rama (fallas preexistentes documentadas de aislamiento entre archivos de test que comparten `crm_test`). No modifica `current_task`/`current_task_status` de esta seccion (siguen en `ACS-R1-05.1-T02.1`) - tarea paralela.

`ACS-R1-05.1-T02.3D` (`completed` - Follow-up configurable del Sales Agent; misma condicion de `T02.3A`/`T02.3B`/`T02.3C`: tarea no listada en la tabla de tareas de la release spec, ejecutada bajo instruccion explicita en paralelo a `T02.1`/`T03`, aprobada tras una fase de auditoria previa con 15 decisiones explicitas) corrige un defecto real del runtime nativo (el camino de agendamiento de `schedule_followup` persistia siempre `scheduled_for = NULL` y `max_attempts = 1` hardcodeado - `runFollowupTick.selectDueFollowUps` requiere `scheduled_for <= UTC_TIMESTAMP()`, y `NULL <= cualquier_cosa` nunca es verdadero en SQL, asi que todo follow-up nativo era permanentemente inalcanzable por el worker) y agrega, sobre esa correccion, un cuarto schema del dominio de `T02.3A` (`sales_agent_configuration.v3`, superset estructural de v1/v2, sin migracion de filas existentes) con la seccion opcional `followUpConfiguration` (`enabled`, `maxAttempts`, `attemptDelaysMinutes`, `allowedWindow` con `timezone` fijo `America/Santiago`, `maxOpportunityAgeDays`). Tres grupos de commit, sin push ni PR:

**(1) Scheduling nativo**: `migrations/027_crm_agent_actions_followup_scheduling.sql` agrega a `crm_agent_actions` las columnas `followup_sequence_key`/`followup_configuration_source`/`followup_configuration_id`/`followup_configuration_version`/`followup_configuration_hash`, una columna generada `active_followup_sequence_key` (mismo patron que `published_scope_key` de la migracion 026) con `UNIQUE KEY` que impide dos filas `schedule_followup` simultaneamente activas para la misma secuencia, y una reconciliacion DML idempotente que marca `failed` (nunca borra, nunca inventa un `scheduled_for`) las filas `planned` preexistentes sin schedule. `lib/brain/commercial/followup/computeFollowUpSchedule.ts` (nuevo) porta - sin importar codigo muerto - el algoritmo de ventana horaria/DST-safe ya probado en `follow-up-scheduling/calculateNextSchedule.ts` (nunca conectado a produccion): intento 1 medido desde la decision inicial, intento N>1 medido desde el `scheduled_for` propio del intento anterior (nunca desde "ahora", para que una recuperacion tardia del worker no comprima el intervalo), y un objetivo fuera de la ventana permitida siempre se mueve hacia adelante, nunca se descarta ni se adelanta. `lib/brain/commercial/followup/loadFollowUpAttemptHistory.ts` (nuevo) resuelve la continuidad de intentos por `opportunity_id` primero y `conversation_case_id` como fallback (nunca `wa_id`), vía una clave de secuencia deterministica y sin timestamp. `execution-bridge/runCommercialExecutionBridge.ts` (funcion exportada `resolveFollowUpSchedulingContext`) conecta todo esto: resuelve la configuracion vigente, computa un `scheduled_for` real o explica por que no puede (deshabilitado, intentos agotados, oportunidad muy antigua, ventana inalcanzable, configuracion no disponible) via `additionalBlockReasons`, nunca fabrica una fecha. `action-queue/persistAgentAction.ts` gana un segundo chequeo de deduplicacion por secuencia activa (ademas del existente por `idempotencyKey`). El test estatico `followUpRuntimeAuthority.test.ts` se actualizo para reconocer `persistAgentAction.ts` como segundo persister legitimo de `schedule_followup` junto al legacy `sales-consultative/repository.ts` (que se preserva intacto, deshabilitado, solo como referencia) - cualquier tercer archivo que lo haga sigue siendo detectado como regresion.

**(2) Configuracion v3 + Hub**: extension de `types.ts`/`constants.ts`/`defaults.ts`/`validation.ts`/`hash.ts`/`resolver.ts`/`index.ts` del dominio de `T02.3A`/`T02.3B` con la seccion `followUpConfiguration` (limites de plataforma explicitos: 1-5 intentos, 5-43200 minutos de demora por intento, 1-180 dias de edad maxima, hora 0-23; invariantes estructurales como `attemptDelaysMinutes.length === maxAttempts` y `endHour > startHour` siempre exigidos, incluso en lectura). Default seguro deliberadamente conservador (`enabled: false` - un deployment sin publicacion nunca empieza a agendar follow-ups solo). Nueva pestaña "Seguimiento" en `SalesAgentConfigurationWorkspace.tsx` (mismo patron toggle todo-o-nada que `modelConfiguration`/`loopConfiguration`), mapeo puro en `lib/domains/sales-agent-config/form.ts` (`parseAttemptDelaysMinutesInput`/`formatAttemptDelaysMinutesInput` nuevos), y `effectiveFollowUpConfiguration` agregado a la respuesta de `GET /api/brain/agents/sales/configuration/effective`.

**(3) Worker + revalidacion + opt-out**: `migrations/028_crm_customer_opt_outs.sql` (tabla `crm_customer_opt_outs`, una fila por `wa_id`+`channel`) y `lib/brain/commercial/optOutStore.ts` (nuevo - `recordCustomerOptOut` idempotente, `isCustomerOptedOut`, `detectExplicitOptOutCommand` con allowlist EXACTA de comandos de baja, deliberadamente sin coincidencia difusa para que objeciones comerciales ordinarias como "no"/"no gracias"/"no me interesa" nunca se confundan con un opt-out). Conectado como "Step 0.5" en `runNativeAutonomousCycle.ts` (mismo lugar/patron que el Step 0 de pilot allowlist existente): un opt-out ya registrado corta el turno antes de cualquier llamada al modelo; un comando explicito en el mensaje actual se registra y corta el mismo turno. `followup/runFollowupTick.ts` gana `revalidateFollowUpConfiguration` (decision 13): revalida cada fila reclamada contra la configuracion PUBLICADA VIGENTE (nunca el snapshot grabado al agendar) - deshabilitada o intentos/edad fuera de rango cancelan; fuera de la ventana horaria vigente reprograma hacia adelante (nunca cancela, nunca ejecuta fuera de horario); un fallo real de resolucion de configuracion se trata como falla TECNICA (reintentable via el mismo camino `status='failed'` que ya usa una excepcion del cycle runner), nunca como cancelacion de negocio. Esta revalidacion solo aplica a filas con `followup_configuration_source` no nulo (es decir, filas que realmente pasaron por el pipeline de agendamiento consciente de configuracion) - una fila legacy o sembrada fuera de ese pipeline conserva exactamente el comportamiento anterior a esta tarea; sin este limite, cualquier deployment sin una configuracion publicada (`safe_default.enabled=false`) habria cancelado en el siguiente tick cada follow-up legitimamente agendado por el camino legacy.

Rama `feat/acs-r1-05-1-t02-3d-agent-followup-config`, base `develop@e8b62ff`, tres commits (scheduling nativo / configuracion v3+Hub / worker+revalidacion+opt-out) - sin push, sin PR. Validado con MariaDB real (`crm_test`), nunca mocks: 64 tests nuevos (`computeFollowUpSchedule.test.ts` 12, `followUpSequenceContinuity.test.ts` 10, extensiones `[FU1]`-`[FU10]`/`[D5]`-`[D7]`/`[H4]`-`[H6]`/`[R4]`/`[E8]`-`[E9]` en `salesAgentConfigurationRuntime.test.ts` 19, extensiones `[F16]`-`[F21]` en `salesAgentConfigForm.test.ts` 6, `followUpRevalidationAndOptOut.test.ts` 14, `runNativeAutonomousCycleOptOut.test.ts` 3), mas `npx tsc --noEmit` y `npm run build` limpios. Suite dirigida completa (209 tests) verde quando se serializa (`--test-concurrency=1`) para evitar la contencion ya documentada del scope compartido `pesas_chile` en `crm_test` entre archivos de test concurrentes (no es una regresion de esta tarea). Verificacion adicional contra la base "dev" real (`main_management`): las migraciones 027 y 028 no estaban aplicadas alli (solo en `crm_test`) y se aplicaron directamente antes de correr la suite de 83 tests que ejercitan `runNativeAutonomousCycle` contra esa base, confirmando que el nuevo chequeo Step 0.5 no rompe ningun caller existente.

Deuda/riesgos explicitos, no bloqueantes: (a) `npm run db:migrate` esta bloqueado por un drift de checksum preexistente - migracion 026 contra `crm_test` (aplicada 2026-07-22, contenido en disco distinto al aplicado) y migracion 025 contra `main_management` (drift aun mas temprano) - ninguno de los dos causado por esta tarea; ademas se confirmo que la migracion 026 (`sales_agent_configurations`, de `T02.3A`) nunca habia llegado a `main_management` en absoluto (la tabla no existia). Las migraciones 026/027/028 se aplicaron a `main_management` con un script ad-hoc sobre la conexion de migracion (privilegios DDL reales, nunca `crm_app`), mismo patron ya usado para la migracion 027 en la sesion previa contra `crm_test`; la reparacion formal del runner queda pendiente como tarea separada. (b) Dos tests preexistentes de `customerIdentityOnboarding.e2e.test.ts` (`T08-A6`/`T08-A7`, simulacion de proyeccion local retrasada de Customer Service) fallan identicamente contra el arbol limpio (confirmado con `git stash`) - no relacionados con esta tarea, no tocados. (c) El registro de opt-out no tiene superficie visible en el Hub todavia (solo lectura/escritura a nivel de dominio) - un operador no puede hoy ver ni revertir un opt-out desde la UI. (d) Hallazgo de entorno, no de codigo: `.env` (raiz del repo, el mismo que carga `npm run dev`) tiene `DATABASE_PASSWORD` todavia en el valor placeholder `change_me` (nunca reemplazado por la credencial real de `crm_app`) - esto bloquea CUALQUIER ruta del Hub que lea la base de datos al correr `npm run dev` localmente (error generico `internal_error`, causa real solo visible conectando directo a MariaDB), no es especifico de esta tarea. Confirmado probando la ruta `GET /api/brain/agents/sales/configuration/effective` contra un dev server real: fallo con esa credencial, exito inmediato llamando el mismo resolver directamente con la credencial correcta (`una_clave_local`, la misma que usan todos los archivos de test). No se modifico `.env` (fuera de alcance, archivo de secretos del entorno del usuario) - la validacion "runtime real" de esta tarea se completo en su lugar contra `crm_test` (209 tests, suite dirigida) y contra `main_management` corriendo `npm run worker:followup --dry-run` de verdad (credenciales correctas inyectadas solo por variable de entorno de proceso, sin tocar `.env`) - confirmado que el worker conecta, lee las columnas nuevas de la migracion 027 sin error SQL, y aplica correctamente el gate de pilot allowlist sobre filas reales preexistentes. (e) No se toco follow-up legacy, simulador, Capability Gateway, catalogo, tools, provider ni prompt renderer, tal como exigian las decisiones aprobadas. No modifica `current_task`/`current_task_status` de esta seccion (siguen en `ACS-R1-05.1-T02.1`) - misma logica de `T02.3A`/`T02.3B`/`T02.3C`: tarea paralela, sin rama con push ni PR al momento de esta entrada.

**Correccion posterior (review, misma rama, sin commit al momento de esta entrada)**: cinco correcciones sobre los tres grupos de commit originales de `T02.3D`, aplicadas directamente sobre el arbol de trabajo de `feat/acs-r1-05-1-t02-3d-agent-followup-config`:

(1) `recordCustomerOptOut`/`recordCustomerOptIn` (`optOutStore.ts`) eran escrituras independientes contra el pool (insert, update de cancelacion, ambas auditorias, cada una su propia conexion) - un fallo de la cancelacion de follow-ups pendientes despues de un insert ya exitoso devolvia `{ok:true, cancelledFollowUps:0}`, indistinguible de "nada que cancelar", mientras el follow-up seguia agendado y alcanzable por el worker. Ambas funciones ahora corren en una sola transaccion (`lib/db.ts#withTransaction`): insert/delete, ambas auditorias (via el parametro `connection` ya existente de `lib/audit.ts`) y el `UPDATE` de cancelacion comparten conexion; cualquier fallo en cualquier paso hace rollback completo (incluida la fila de opt-out) y devuelve `{ok:false}` - nunca un `ok:true` parcial.

(2) `loadOpportunityCreatedAtForCandidate` (`runFollowupTick.ts`) colapsaba "no encontrado", "ambiguo" (multiples oportunidades via `conversation_case_id`) y "fallo real de lectura" en el mismo `null`, lo que hacia que `revalidateFollowUpConfiguration` saltara el chequeo de `maxOpportunityAgeDays` en silencio ante un error transitorio de DB - fail-open exactamente cuando el chequeo no podia confiarse. Ahora devuelve un resultado tipado (`found`/`not_found`/`ambiguous`/`unavailable`); solo `unavailable` (fallo real de lectura) produce `{outcome: "technical_failure"}`, que nunca ejecuta el follow-up ese tick (mismo backoff tecnico acotado que un fallo del resolver de configuracion); `not_found`/`ambiguous` preservan el comportamiento previo (sin relacion canonica inequivoca, chequeo omitido).

(3) `applyTechnicalFailureBackoff` (`runFollowupTick.ts`) descartaba el resultado de su propio `safeExecute` - un `UPDATE` fallido, o que no afectara ninguna fila (p. ej. otro proceso ya movio la fila fuera de `executing`), igual reportaba el `scheduledFor` calculado como si se hubiera persistido. Ahora exige `affectedRows === 1` exactamente, devuelve un resultado tipado (`{persisted:true, scheduledFor} | {persisted:false, scheduledFor:null}`), registra explicitamente el fallo de persistencia via el `log` del tick, y deja la fila en `executing` para la recuperacion stale ya existente (`claimStaleExecutingFollowUp`/`terminalizeExhaustedStaleFollowUp`) en vez de fabricar una fecha que nunca se escribio. `FollowupTickResult.technicalFailures` gana `persisted: boolean` y `scheduledFor: string | null`.

(4) Verificacion de mojibake solicitada sobre los tests de opt-out: hallazgo negativo. `tests/commercial/followUpRevalidationAndOptOut.test.ts` ya tenia UTF-8 real verificado a nivel de bytes (`Cancelar Suscripción` = `43 61 6e 63 65 6c 61 72 20 53 75 73 63 72 69 70 63 69 c3 b3 6e`, `¡No quiero más mensajes!`, `bórrame de la lista` - las tres con las secuencias UTF-8 correctas para `ó`/`á`/`¡`); un escaneo exhaustivo (`file`, decode UTF-8 forzado, patrones `Ã`/`Â`/`â€`) sobre TODOS los `.ts`/`.tsx` de `tests/` no encontro una sola ocurrencia de mojibake. Sin cambios de codigo.

(5) `tests/e2e/followUpDirectRuntimeValidation.e2e.test.ts`: la asercion final de estado aceptaba `["planned", "requires_review", "proposed"]`, lo que podia enmascarar el mismo defecto de agendamiento que esta tarea corrigio en su primer commit - ahora exige exactamente `status === "planned"`. Ademas, la ventana `ALWAYS_OPEN_CONFIG` (`startHour:0, endHour:23`) excluye la hora local 23 (`computeFollowUpSchedule.ts#isWithinAllowedWindow`, `hour < endHour`) - el chequeo de ventana de `revalidateFollowUpConfiguration` corria contra el reloj real del proceso, asi que el test podia fallar ~1 hora de cada 24 (23:00-23:59 `America/Santiago`) sin ningun defecto real de codigo. `FollowupTickOptions` gana un `now?: string` opcional (default el reloj real - sin cambio de comportamiento para el worker productivo, `worker:followup`); el test lo fija a un instante UTC constante (`2026-01-15T16:00:00.000Z`, siempre 12-13h hora de Santiago segun DST, verificado) para eliminar por completo la dependencia del reloj real en esta asercion.

Validado tras las cinco correcciones: E2E (`followUpDirectRuntimeValidation.e2e.test.ts`) corrido 3 veces consecutivas, 3/3 en verde; suite dirigida serializada (`--test-concurrency=1`, 12 archivos - opt-out/revalidacion, ciclo nativo con opt-out, scheduling, continuidad de secuencia, `runFollowupTick`, runtime/API/dominio/formulario de Sales Agent Configuration, autoridad de runtime de follow-up, ambos E2E de follow-up) 221/221 tests en verde contra MariaDB real (`crm_test`, migraciones 026-028 aplicadas de nuevo en esta sesion - no estaban aplicadas en el Docker local al empezar); `npx tsc --noEmit` limpio; `npm run build` limpio; `git --no-pager diff --check` sin errores de espacio en blanco. Working tree sin commit al momento de esta entrada (rama `feat/acs-r1-05-1-t02-3d-agent-followup-config`, ya pusheada a `origin` con los tres commits originales; estas cinco correcciones son posteriores al ultimo push, sin commit propio todavia).

`ACS-R1-05.1-T02.3E`: **superseded/absorbido en `T02.3D`**. Nunca tuvo alcance formalmente definido en este repositorio (ver "Deuda de reconciliacion" abajo). La validacion de runtime real end-to-end que un `T02.3E` hipotetico habria cubierto ya existe dentro de `T02.3D` (`tests/e2e/followUpDirectRuntimeValidation.e2e.test.ts`, "decision 5" de la revision: inbound real -> `runNativeAutonomousCycle` real -> fila `crm_agent_actions` real -> `runFollowupTick` real -> `runNativeAutonomousCycle` real de nuevo -> outbox real, sin atajos por `resolveFollowUpSchedulingContext`/`revalidateFollowUpConfiguration` en aislamiento). No queda trabajo pendiente bajo el nombre `T02.3E`.

**Deuda de reconciliacion (Sales Agent Configuration, `T02.3A`-`T02.3E`)**: ninguna de las cinco subtareas (`T02.3A` a `T02.3E`) aparece hoy en la tabla de tareas ni en el cuerpo de [ACS-R1-05.1 spec](releases/ACS-R1-05.1-persistent-commercial-memory-controlled-whatsapp-pilot.md) - toda esta linea de trabajo se ejecuto bajo instruccion explicita en paralelo a `T02.1`/`T03`, documentada unicamente aqui. La reconciliacion formal (agregar las cinco entradas a la tabla de tareas de la release spec, con su Definition of Done y dependencias explicitas) queda pendiente; hasta entonces, este documento es la unica fuente de verdad sobre el estado real de `T02.3A`/`T02.3B`/`T02.3C`/`T02.3D` (`completed`, incluidas las cinco correcciones de review de arriba) y `T02.3E` (`superseded`, absorbido en `T02.3D`, ver parrafo de arriba).

`ACS-R1-05.1-T02.4` (`completed` - Observabilidad operativa de follow-ups; misma condicion de `T02.3A`-`T02.3D`: tarea no listada en la tabla de tareas de la release spec, ejecutada bajo instruccion explicita en paralelo a `T02.1`/`T03`, aprobada tras una fase de auditoria previa con 10 decisiones explicitas) agrega la primera superficie de observabilidad read-only sobre `crm_agent_actions` (`action_type = 'schedule_followup'`) para que un operador entienda el comportamiento real del follow-up sin SQL - sin crear una nueva fuente de verdad, sin tabla ni migracion nueva (decision 1), y sin tocar scheduling, worker, Agent Tool Loop, opt-out, configuration runtime u outbox worker.

Dominio nuevo, exclusivamente de lectura (`lib/domains/follow-up-observability/`): `constants.ts` (reexporta `FOLLOW_UP_STALE_EXECUTING_LOCK_SECONDS` del worker real - nunca una segunda copia del umbral de 300s, decision 5; define el umbral de "planned vencido" en 15 minutos, decision 4), `maskWaId.ts` (enmascarado server-side fijo `********1234`, decision 3 - el `wa_id` completo nunca sale de una funcion de dominio hacia una respuesta), `criticality.ts` (deteccion pura de las cinco senales criticas mas un builder de SQL parametrizado por prefijo de columna, reutilizado sin duplicar texto entre `summaryService` sin join y `listService` con join a `crm_opportunities`), `reasonLabels.ts` (etiquetas fijas en espanol para los codigos cortos reales que el runtime ya escribe - nunca traduce `policy_notes_json`, que el dominio ni siquiera selecciona en ninguna query; copy exacto aprobado para `requires_review`, decision 8, y para el badge "Sin configuracion asociada", decision 7), `rowMapper.ts`, `summaryService.ts`, `listService.ts` y `detailService.ts`.

Correlacion con `brain_message_outbox` (decision 9, best-effort, nunca una FK nueva): reconstruye el camino real que deja una ejecucion de follow-up (`runFollowupTick.ts` re-entra al ciclo con `correlationId = followup:<actionId>:<ms>`, persistido tal cual en `crm_agent_decisions.correlation_id`) - `detailService.ts#resolveOutboxCorrelation` busca acciones `send_whatsapp_reply` cuyo `decision_id` provenga de una decision con ese prefijo de correlacion y devuelve exactamente uno de tres resultados (`correlated`/`none`/`ambiguous`), nunca adivina ante 2+ candidatos ni trata la ausencia de correlacion como error.

Gap de historial tecnico aceptado explicitamente (decision 2, coherente con no crear tabla ni migracion nueva de la decision 1): `applyTechnicalFailureBackoff`/`rescheduleClaimedFollowUp` (runtime existente de `T02.3D`, no tocado por esta tarea) devuelven una fila a `planned` sin persistir motivo ni timestamp en ninguna columna consultable - la UI muestra "Historial tecnico detallado no disponible" en vez de inventar un timeline (`FOLLOW_UP_TECHNICAL_HISTORY_UNAVAILABLE_MESSAGE`, usado por `detailService.ts` y expuesto en el detalle de la pagina y de la API).

API (3 Route Handlers nuevos, todas `requireOperator` + cero escrituras): `GET /api/brain/agents/sales/follow-ups/summary`, `GET /api/brain/agents/sales/follow-ups`, `GET /api/brain/agents/sales/follow-ups/[actionId]`. Validacion estricta de query params (`_lib/httpHelpers.ts`): rango fijo `24h|7d|30d`, `status`/`criticality` contra enums fijos, `opportunityId`/`conversationCaseId` enteros positivos, `page`/`limit` con limite maximo fijo de 100 - rechaza entrada malformada, clampa solo lo meramente excesivo (mismo criterio que `parseListLimit` de la Sales Agent Configuration API, `T02.3C`). Nunca expone `policy_notes_json` crudo (el dominio no lo selecciona en ninguna query), prompts, `customInstructions`, secretos, `wa_id` completo ni errores DB crudos (`sanitizeDbError` solo para log interno del servidor; el cliente siempre recibe un codigo de error fijo, nunca el mensaje).

Hub: ruta nueva `/agents/sales/follow-ups` (grupo de navegacion `intelligence`, junto a "Configura al agente" - no existe un grupo separado "Sales Agent" en `Sidebar.tsx`/`lib/modules.ts`, se interpreto la instruccion como la agrupacion visual mas cercana a esa intencion), visible desde el inicio (`navVisible: true`, decision 10). Server component que llama los servicios de dominio directamente (mismo patron que `/actions` y `/agents/sales/configuration`, sin round-trip HTTP a su propia API), con resumen superior (11 tiles: planned/planned vencido/executing/executing stale/executed/cancelled/failed/blocked/requires review/sin scheduled_for/sin configuracion asociada), tabla filtrable (rango, estado, motivo, oportunidad, conversacion, action_id, criticidad) y panel de detalle inline por `?actionId=`. No reutiliza la UI generica `/actions` (responsabilidad y grano distintos - `/actions` sigue mostrando todos los `action_type` sin enmascarar `wa_id`) pero si sus atomos visuales (`PageHeader`, `StatCard`, `StatusChip`, `SectionCard`, `InfoGrid`, `EmptyState`, `ErrorState`, `SurfaceBadge`).

Probado: 22 tests puros (`tests/domains/followUpObservability.test.ts` - masking, las cinco senales criticas contra los umbrales reales, labels/copy exacto aprobado, mapeo de fila sin `policy_notes_json`, validacion estricta de query params) mas 20 tests con MariaDB real (`tests/commercial/salesFollowUpsApi.test.ts` - auth, summary, paginacion/limite maximo, filtros, masking server-side end-to-end sobre la respuesta serializada completa, planned vencido/executing stale/missing schedule/missing configuration/requires_review, detalle 404, injection-safety con caracteres SQL-meta, correlacion outbox correlacionada/ausente/ambigua, cero escrituras). `npx tsc --noEmit` y `npm run build` limpios (build incluye las 3 rutas API y la pagina nueva en su salida, sin warnings nuevos atribuibles a esta tarea). `npm run docs:validate` sin hallazgos nuevos (29 warnings preexistentes de vocabulario legacy, no relacionados). `git --no-pager diff --check` sin errores de espacio en blanco.

**Correccion posterior (misma tarea, una vez disponible MariaDB real para correr `tests/commercial/salesFollowUpsApi.test.ts`)**: la primera corrida contra `crm_test` encontro 4 fallos reales, dos causas raiz distintas, ninguna en el dominio nuevo:

(1) `FUA1`/`FUA2`/`FUA3` (los tres GET sin sesion devolvian 500 en vez de 401): `requireOperator()` (`lib/auth.ts`, compartido por todas las rutas del Hub, no especifico de esta tarea) envolvia `getAdminBypassToken()`/`getSessionSecret()` (fallo real de configuracion del servidor) y `await cookies()` (lectura de la cookie de sesion) en el mismo `try/catch` - cualquier excepcion de cualquiera de las tres se degradaba a 500 con `error: error.message`. Invocar un Route Handler directamente (sin el runtime completo de Next.js detras) hace que `cookies()` lance `` `cookies` was called outside a request scope ``, que caia en esa misma rama y se filtraba crudo al cliente. Corregido separando ambos casos: `getAdminBypassToken()`/`getSessionSecret()` conservan su propio `try/catch` (500 solo ante una configuracion de servidor realmente ausente); la lectura de `cookies()`/verificacion de sesion tiene su propio `try/catch` cuyo `catch` nunca filtra el motivo y cae al mismo 401 `unauthorized` final que ya existia para "sin sesion". Mismo contrato `{ok, response}` para todo caller existente - ningun otro archivo cambio. El error de autenticacion sigue resolviendose antes de cualquier consulta a DB (mismo `if (!auth.ok) return auth.response` de siempre, primera linea de cada handler).

(2) `FUW1` (cero escrituras): `snapshotFollowUpActionsState()` comparaba `MAX(updated_at)` de dos SELECT distintos con `assert.equal` - bajo `node:assert/strict`, `equal` es `strictEqual` (`===`), y mysql2 devuelve `DATETIME` como instancias de `Date` nunca reference-iguales entre si aunque representen el mismo instante. `snapshotFollowUpActionsState()` ahora normaliza `max_updated_at` a ISO string (`toComparableTimestamp`) antes de comparar - comparacion por valor, no por referencia, sin tocar la aserticion de conteo de filas ni la logica de cero escrituras.

Validado tras ambas correcciones: `salesFollowUpsApi.test.ts` solo, serializado (`--test-concurrency=1`), 20/20; suite combinada (`followUpObservability.test.ts` + `salesFollowUpsApi.test.ts`), 42/42; `npx tsc --noEmit` limpio; `npm run build` limpio; `npm run docs:validate` sin hallazgos nuevos; `git --no-pager diff --check` sin errores. Archivos tocados por esta correccion: `lib/auth.ts` (fix compartido, usado por toda ruta con `requireOperator`, sin cambiar su contrato ni ningun caller) y `tests/commercial/salesFollowUpsApi.test.ts` (normalizacion de timestamp en el propio test, la aserticion de cero escrituras se mantiene). No se toco scheduling, worker, Agent Tool Loop, opt-out, configuration runtime, outbox worker ni se agrego migracion.

No se creo tabla ni migracion nueva (decision 1). No se modifico scheduling, worker, Agent Tool Loop, opt-out, configuration runtime, outbox worker ni la UI generica `/actions`. Sin push, sin PR.

`ACS-R1-05.1-T02.5` (`completed` - LLM Provider Error Observability en el Agent Tool Loop; misma condicion de `T02.3A`-`T02.4`: tarea no listada en la tabla de tareas de la release spec, ejecutada bajo instruccion explicita como auditoria y mejora acotada, exclusivamente de observabilidad) cierra un gap real ya confirmado en produccion: `commercial_event.agent_tool_loop_completed` registraba `terminalReason = provider_unavailable` (a veces con `decisionCount = 0`/`toolExecutionCount = 0`, es decir, el primer intento del modelo ya fallaba) sin ninguna causa tecnica - PM2 tampoco la mostraba, y `crm_agent_actions.failure_reason` queda `NULL` en ese fallback (gap de `dispatchAgentLoopResponse.ts`, confirmado pero explicitamente fuera de alcance de esta tarea, ver deuda abajo).

Punto exacto donde se perdia la causa: `lib/brain/commercial/agent-loop/runAgentToolLoop.ts`, los dos sitios (fase gathering y fase finalization) donde `invokeProviderWithDeadline()` devuelve `{kind: "error", error}` - el codigo reducia `error` a un string de warning (`agent_loop_provider_error:<error.message>`) y llamaba `finalize("provider_unavailable")`, un `AgentLoopResult` sin ningun campo capaz de portar la causa real. Ahi es tambien donde `httpAgentLoopProvider.ts` (unico provider productivo) lanzaba errores genericos (`new Error(...)`) sin status HTTP, codigo ni clasificacion adjunta.

Cambio minimo, sin tocar retries/timeout/tools/prompts/fallback (verificado: `RETRYABLE_HTTP_STATUSES`, `maxModelRetries`, `computeBackoffMs`, los puntos exactos donde se lanza/reintenta, y `dispatchAgentLoopResponse.ts` quedan bit-a-bit identicos, solo se anota metadata adicional sobre el mismo `Error` antes de lanzarlo): (1) `lib/brain/commercial/agent-loop/providers/providerFailureClassification.ts` (nuevo) - clasificador puro sin estado de proceso mas alla de un `WeakMap` interno (`markAgentLoopProviderFailure`/`classifyAgentLoopProviderFailure`) que asocia cada `Error` lanzado con su causa saneada (`httpStatus`, `errorCode`, `errorClass`, `normalizedReason`, `retryable`, mas `model`/`attemptCount`/`maxAttempts` cuando el provider los conoce), nunca el mensaje crudo, headers, prompt ni respuesta del proveedor; incluye el fallback generico para AbortError/timeout y fallas de red (ECONNRESET, `fetch failed`) para cualquier provider que no marque explicitamente su error. (2) `httpAgentLoopProvider.ts` marca sus 6 puntos de fallo reales (config faltante, fetch fallido, status HTTP no-ok, `response.json()` invalido, contenido vacio, `parseModelJson` invalido) con la causa saneada correspondiente - los dos ultimos son saneamiento nuevo real: antes de esta tarea, una respuesta 200 con body no-JSON o contenido no-JSON propagaba el `SyntaxError` nativo de `JSON.parse`/`response.json()` sin envolver, con riesgo de fragmento del body en el mensaje. (3) `runAgentToolLoop.ts` agrega `AgentLoopResult.providerFailure` (opcional, `agentStepTypes.ts`), lo captura en los dos puntos de fallo via `captureProviderFailure()` (clasifica + `elapsedMs` medido en `invokeProviderWithDeadline` + `provider.name` + log estructurado sanitizado en stderr, `console.error("agent_tool_loop_provider_failure", JSON.stringify(...))`) y sustituye el warning con mensaje crudo por uno con `normalizedReason` unicamente. (4) `events/types.ts`/`events/normalize.ts` agregan `AgentToolLoopCompletedRecordedPayload.providerFailure` (objeto opcional, mismo patron de union local espejada sin cross-import que ya usa todo el archivo) - presente en `payload_json` solo cuando el loop realmente capturo una causa, ausente (no `null`) en cualquier otro terminalReason; el sanitizador compartido (`SENSITIVE_KEY_PATTERN`) sigue recorriendolo. (5) `runNativeAgentToolLoopCycle.ts` pasa `loop.providerFailure` al evento sin remapeo (misma forma estructural). `terminalReason = "provider_unavailable"` se mantiene sin cambios por compatibilidad.

Ejemplo de `payload_json.providerFailure` real (429 tras agotar reintentos): `{"provider":"http-agent-loop-provider","model":"deepseek-v4-flash","attemptCount":2,"maxAttempts":2,"httpStatus":429,"errorCode":"http_429","errorClass":"HttpStatusError","normalizedReason":"rate_limited","retryable":true,"elapsedMs":842}`.

Probado: 23 tests en `tests/agent-loop/httpAgentLoopProvider.test.ts` (`[PF1]`-`[PF10]`, contra un servidor HTTP local real, nunca mocks - 401/403/429/5xx/AbortError-timeout/ECONNRESET-fetch-failed/respuesta 200 no-JSON/contenido de modelo no-JSON/config faltante/ausencia de secretos en la causa clasificada); 2 tests nuevos en `tests/agent-loop/runAgentToolLoop.test.ts` (`[PF11]`/`[PF12]` - la causa sobrevive en `result.providerFailure` en fase gathering y en finalization, warnings sin el mensaje crudo); 4 tests nuevos en `tests/commercial/agentToolLoopCompletedEventConfig.test.ts` (`[AE29]`-`[AE32]` - presente verbatim, ausente para terminalReason normal, el sanitizador comun rechaza una clave prohibida inyectada en el objeto anidado, allowlist exacta de claves); 1 test de persistencia real en `tests/commercial/commercial-events.test.ts` (`[AE33]` - `recordCommercialEvent`/lectura de `payload_json`/`loadCommercialEventByDedupeKey` round-trip contra `commercial_event` real, mas verificacion de ausencia de secretos en el JSON persistido). `npx tsc --noEmit` limpio, `npm run build` limpio, `npm run lint` limpio sobre los archivos tocados, `git --no-pager diff --check` sin errores. Suite dirigida (agent-loop completo + `agentToolLoopCompletedEventConfig.test.ts`, 125 tests) 125/125 en verde.

Deuda/riesgos explicitos, no bloqueantes: (a) `crm_agent_actions.failure_reason` sigue `NULL` en el fallback de `provider_unavailable` - `dispatchAgentLoopResponse.ts#buildAgentLoopAction` hardcodea `failureReason: null`; confirmado como el mismo gap que describe el reporte de produccion, pero tocar el pipeline de fallback estaba explicitamente fuera de alcance de esta tarea (auditoria pidio "no modificar... fallback"); queda como candidato natural para una tarea siguiente que conecte `providerFailure.normalizedReason` a esa columna. (b) `[AE33]` (persistencia real contra `commercial_event`) no pudo ejecutarse en esta sesion por falta de Docker/MariaDB disponible en este entorno (mismo tipo de limitacion ya documentada en `ACS-R1-05-T05`/T02.3A-D) - el resto de la suite (30 tests puros + HTTP local real) si corrio en verde; el test queda escrito y listo para correr contra `crm_test` en cuanto haya DB disponible. (c) La rama `input.provider === null` de `runAgentToolLoop.ts` (nunca alcanzada en produccion - `runNativeAutonomousCycle.ts` siempre construye `createHttpAgentLoopProvider(...)`) sigue devolviendo `provider_unavailable` sin `providerFailure` (correcto: no hay ninguna excepcion real que clasificar ahi, y fabricar una causa habria sido inventar informacion).

`ACS-R1-05.1-T02.6` (`implemented_pending_real_smoke` - nueva capability `explore_catalog` del Sales Agent; misma condicion de `T02.3A`-`T02.5`: tarea no listada en la tabla de tareas de la release spec, ejecutada bajo instruccion explicita en paralelo a `T02.1`/`T03`, tras una fase de auditoria previa contra el contrato real confirmado por el mantenedor de `mistergoliat/MS-pesaschile-catalog-service` en `main@147794b`/`feature@efc2f7e`) agrega extremos (mas caro/mas barato), top-N, rankings y exploracion filtrada del catalogo (`POST /v1/products/explore`) al pool fijo del Agent Tool Loop, expandiendolo de tres a cuatro tools sin eliminar ninguna existente (`AGENT_LOOP_TOOL_POOL`: `search_products`, `get_product_details`, `search_company_knowledge`, `explore_catalog`). El vocabulario de `availability`/filtros de `explore_catalog` es un sub-contrato deliberadamente independiente de `CatalogAvailabilityStatus` (usado por `search_products`/`get_product_details`) - nunca unificado, mismo criterio ya documentado para `search_company_knowledge`. La capability defaultea `availability` a `available` cuando el modelo no lo especifica, nunca `all`; rechaza `price.max < price.min` y `sort`/`limit` ausentes del lado del cliente antes de cualquier llamada HTTP; y construye el request mediante un mapper allowlisted explicito (`buildExploreRequestBody`), nunca reenviando argumentos crudos del modelo. Corrigio de paso dos defectos reales encontrados durante la implementacion, ninguno inventado: (1) el endpoint `explore` usa su propio vocabulario de error en snake_case minuscula (`invalid_limit`/`invalid_sort`/`invalid_request`), distinto de los codigos UPPER_SNAKE de `search`/`details`/`batch` - sin este fix, cualquier rechazo de `explore` caia al branch generico `invalid_response` (payload inesperado) en vez de `invalid_input` (argumento invalido, replanteable), degradando la senal que llega al agente; (2) el campo `productId` de `explore` ya viene como `string` en el contrato real (a diferencia de `search`/`details`/`batch`, donde es numerico) - un primer intento de parseo que asumia numero descartaba silenciosamente cada item de la respuesta.

Arquitectura: `lib/catalog/types.ts` (tipos `CatalogExploreInput`/`CatalogExploreResult`/etc., metodo `exploreCatalog` en `CatalogPort`), `lib/catalog/httpCatalogAdapter.ts` (parseo defensivo, mapeo de errores, mapper de request cerrado), `lib/brain/commercial/capability-gateway/registry.ts` (`exploreCatalogCapability`, registrada junto a las capabilities existentes, ninguna eliminada), `lib/brain/commercial/agent-loop/runAgentToolLoop.ts` (pool expandido), `lib/brain/commercial/agent-loop/buildToolObservation.ts` (proyeccion allowlisted, tope de 10 productos, conserva `stockScope`/`classificationSource`/`exhaustiveForScope`, nunca el payload crudo), `lib/brain/commercial/agent-loop/buildAgentStepPromptPackage.ts` (reglas nuevas en gathering y finalization: diferenciacion `search_products`/`explore_catalog`/`get_product_details`, lenguaje comercial gobernado por `exhaustiveForScope`, prohibicion de inventar `categoryId`/`categorySlug`, prohibicion de mencionar terminos internos al cliente). `lib/brain/commercial/agent-loop/recentCatalogContext.ts` (correccion de alcance tras revision) agrega `explore_catalog` como tercer `sourceTool` valido (junto a `search_products`/`get_product_details`, mismo `crm_capability_executions.capability_name IN (...)`, mismo `response_summary_json`, sin tabla ni migracion nueva) reutilizando el helper existente `productsFromSearchProducts` (ambos comparten la forma `{items: [{productId, name, ...}]}` - `combinationId`/`variantLabel` quedan ausentes para `explore_catalog` porque su contrato no los provee, nunca inventados) - conserva unicamente `position`/`productId`/`name` por producto, nunca `price`/`stockQuantity`/`stockScope`/`availability` (serian los primeros campos comerciales que este contexto cargaria si se agregaran, contradiciendo la regla de prompt ya existente "Never use historical RecentCatalogContext data as current price, stock, availability, or URL evidence" y el propio test `[...]` que ya afirma que el contrato nunca carga esos campos). De paso se agrego deduplicacion de `productId` entre interacciones (fila mas reciente gana; una fila cuyos candidatos quedan todos deduplicados se salta con un warning nuevo y distinto, `recent_catalog_context_all_candidates_deduped`, nunca confundido con `recent_catalog_context_no_valid_products`) - esta deduplicacion no existia antes de esta tarea (auditada y confirmada su ausencia en el codigo real) y se agrega como parte necesaria de esta integracion, no como refactor no solicitado. Cero migraciones, cero cambio a auth/onboarding/ordenes/shipping/Customer Profile/schema, cero modificacion a `MS-pesaschile-catalog-service` (fuera de alcance explicito de esta tarea).

Probado (39 tests nuevos - conteo exacto de declaraciones `test(...)` verificado por archivo con `git diff` contra `develop`, sin MariaDB - doble HTTP local o `dataAccess` inyectado, nunca el Catalog Service real): `tests/catalog/httpCatalogAdapter.test.ts` (+11: mapper allowlisted, ambos codigos de error corregidos, `stockScope`/`classificationSource`/`exhaustiveForScope`/`price: null`, timeout/retryabilidad), `tests/commercial/capabilityGateway.test.ts` (+7: default de disponibilidad, rechazo cliente de `price.max < price.min` sin llamada HTTP, mapeo de error del servicio, ausencia de secretos en la evidencia persistida), `tests/agent-loop/buildToolObservation.test.ts` (+4), `tests/agent-loop/buildAgentStepPromptPackage.test.ts` (+3), `tests/agent-loop/runAgentToolLoop.test.ts` (+9: extremo global, `productType=machine`, filtros de rango/disponibilidad, encadenamiento con `get_product_details` en el mismo turno, deduplicacion de tool-call, falla del catalogo, mas 2 escenarios nuevos de `RecentCatalogContext` con `sourceTool=explore_catalog` resolviendo "el enlace de la segunda" y "esa" con un unico producto), `tests/agent-loop/recentCatalogContext.test.ts` (+5: captura y orden de ranking, deduplicacion cross-interaccion, fila totalmente deduplicada con warning distinto, limites de interacciones/productos respetados, coexistencia sin regresion con `search_products`/`get_product_details`). Dos de los tests del adapter (vocabulario de error, payload malformado) iteran internamente sobre 3 casos cada uno dentro de una sola llamada `test()` - contribuyen 1 caso cada uno al conteo del test runner (node:test, invocado via `tsx --test`, no Vitest), aunque ejercitan 3 escenarios/6 asserts cada uno; esto explica cualquier conteo mayor si se cuentan iteraciones de loop como tests separados. `npx tsc --noEmit`, `npm run lint` y `npm run build` limpios. Regresion completa comparada contra el mismo baseline via `git stash` (unico entorno disponible, sin Docker/MariaDB local en esta sesion, misma limitacion documentada en cada tarea previa de esta release): baseline 1822 tests/1363 pass/459 fail; con esta rama 1861 tests/1398 pass/463 fail - el delta exacto (+39 tests, +35 pass, +4 fail) coincide test por test con los tests nuevos listados arriba; los +4 fail son exactamente los 4 tests nuevos de `capabilityGateway.test.ts` que requieren DB para leer de vuelta la fila de auditoria (mismo patron de fallo `ECONNREFUSED 127.0.0.1:3306` que los 4 tests preexistentes de ese archivo, verificado por nombre de test, no por conteo) - cero regresiones atribuibles a esta rama. Los tests DB-dependientes (`capabilityGateway.test.ts`, 8 de 12 tests del archivo) permanecen sin ejecutar en este entorno por falta de Docker/MariaDB local - se reportan como no ejecutados, no como validados indirectamente.

No se declara `operational: verified` (ver `CAPABILITY_MATRIX.md`): un smoke real contra la instancia desplegada del Catalog Service (puerto 4010, credenciales validas via `CATALOG_SERVICE_BASE_URL`/`CATALOG_SERVICE_API_KEY` ya configuradas en `.env`) encontro un incidente real de servicio, documentado como incidente externo (esperado: `400 invalid_price_range` o equivalente; observado: la instancia dejo de responder por completo - una llamada `explore` previamente exitosa paso a dar timeout, `netstat` mostro la conexion original todavia `ESTABLISHED` mas varias `CLOSE_WAIT`) - registrado como deuda del Catalog Service, no corregido aqui (`MS-pesaschile-catalog-service` fuera de alcance explicito de esta tarea). El guard cliente ya implementado (`price_range_invalid`) evita que el Sales Agent dispare ese caso en produccion, y un test dedicado confirma cero llamadas HTTP en ese camino. Se reintento el smoke minimo (`{"availability":"available","sort":{"by":"price","direction":"desc"},"limit":1}`, sin `price` invertido) en una sesion posterior de la misma tarea: la instancia seguia sin responder (timeout, mismo patron) - el smoke de `scripts/manual-test/catalog-service-smoke.ts` (extendido con una llamada real a `exploreCatalog`) permanece no ejecutado, no fallido, a la espera de que la instancia se recupere. Rama `feat/acs-r1-05-1-t02-6-explore-catalog-capability`, base `develop@ce02797`. **Actualizacion posterior**: PR #71 creado, revisado (CI/GitGuardian en verde, diff completo, 17 archivos, cero migraciones, `operational: not_verified` intacto, commit unico `6ee344b`) y mergeado a `develop` con merge commit `657db19` - ya no es "sin push, sin PR" como decia esta entrada originalmente. No modifica `current_task`/`current_task_status` de esta seccion (siguen en `ACS-R1-05.1-T02.1`) - misma logica de `T02.3A`-`T02.5`: tarea paralela.

`ACS-R1-05.1-T02.6.1` (`implemented_pending_real_smoke` - correccion real encontrada por el primer smoke productivo de `T02.6`; misma condicion de `T02.3A`-`T02.6`: tarea no listada en la tabla de tareas de la release spec, ejecutada bajo instruccion explicita en paralelo a `T02.1`/`T03`) corrige la causa raiz de un handoff real: `crm_capability_executions` registro `execution_status=invalid_arguments`/`error_code=sort_and_limit_required` porque el modelo envio `{orderBy, orderDirection, limit}` en vez del contrato canonico `{sort:{by, direction}, limit}` - el Catalog Service nunca fue invocado (el rechazo ocurrio antes, dentro de la capability). Auditoria confirmo la causa estructural, no un error puntual del modelo: `httpAgentLoopProvider.ts` (unico provider productivo) envia unicamente `response_format: {type:"json_object"}` sobre el endpoint clasico de OpenAI Chat Completions - nunca `tools`/`function_call` nativo, confirmado leyendo el request real construido (`lib/brain/commercial/agent-loop/providers/httpAgentLoopProvider.ts`); `AgentLoopToolDescription` (`buildAgentStepPromptPackage.ts`) y `buildToolDescriptions()` (`runAgentToolLoop.ts`) solo exponian `{name, description}`, nunca el shape de argumentos; `validateAgentStep.ts` valida unicamente que `AgentStep.arguments` sea "un objeto plano pequeno", nunca el schema especifico de cada tool. El modelo nunca tuvo forma de conocer el contrato exacto de `explore_catalog` mas alla de texto libre.

Correccion estructural (no solo un alias de compatibilidad): `CapabilityGatewayDefinition.inputSchema` (nuevo, opcional, JSON Schema como `Record<string, unknown>` opaco) es ahora la fuente canonica unica - definida junto a cada capability en `lib/brain/commercial/capability-gateway/registry.ts` (`search_products`, `get_product_details`, `explore_catalog` con el schema exacto especificado, exigiendo `sort`/`limit`, `limit` acotado a 1-10) y `companyKnowledgeCapability.ts` (`search_company_knowledge`) - nunca `batch_get_products`, que se mantiene deliberadamente fuera del pool del Agent Tool Loop. `runAgentToolLoop.ts#buildToolDescriptions()` (ahora exportada) lee `inputSchema` directo de la definicion, sin segunda copia; `buildAgentStepPromptPackage.ts` lo renderiza verbatim como JSON en el bloque "Available tools" de cada turno de gathering, mas una regla nueva explicita: una observacion `blocked` con `errorCode` de argumentos invalidos autoriza al modelo a corregir y reintentar una vez usando el schema mostrado, en vez de rendirse. `httpAgentLoopProvider.ts` no se toco (no hardcodea el contrato en ningun punto - lo recibe ya resuelto desde `CapabilityGatewayDefinition` via el prompt).

Compatibilidad temporal acotada (`registry.ts#asLegacySortAlias`, solo `explore_catalog`, solo cuando `input.sort` esta completamente ausente): `{orderBy, orderDirection}` se normaliza a `{sort:{by, direction}}` solo si ambos valores son validos (`price|stock|name` / `asc|desc`); un alias invalido o un `sort` malformado (no ausente) nunca se rescata - sigue `invalid_arguments`. Los campos alias nunca llegan al adapter HTTP (el `exploreInput` se construye por clave explicita, nunca por spread del input crudo); se registra `explore_catalog_legacy_sort_alias_used` en `CapabilityExecutionOutcome.warnings`. El contrato oficial sigue siendo `sort` anidado - este bridge cubre unicamente el shape ya observado en produccion, nunca reemplaza el fix estructural de arriba.

Manejo de `invalid_arguments` (`runAgentToolLoop.ts#processUseToolStep`): una llamada que la capability rechaza antes de tocar el Catalog Service ya no cuenta contra `maxToolExecutions` (antes si contaba, exactamente igual que una ejecucion real - eso era lo que dejaba al modelo sin presupuesto tras un solo rechazo y lo empujaba a `handoff` en finalizacion, sin ninguna rama de codigo que forzara el handoff explicitamente). `maxDecisions` sigue acotando el numero total de intentos (sin loops infinitos); la deduplicacion por `tool+arguments` sigue distinguiendo un request invalido de su version corregida (claves de canonicalizacion distintas por construccion). Warning nuevo `agent_loop_tool_invalid_arguments:<tool>:<errorCode>` para observabilidad.

**Correccion posterior (misma tarea, sin commit todavia)**: el warning `explore_catalog_legacy_sort_alias_used` (seccion 6, alias de compatibilidad) se perdia en silencio - `CapabilityExecutionOutcome.warnings` nunca se persistia (migracion 022, `crm_capability_executions`, no tiene columna `warnings_json`) ni se propagaba a la `ToolObservation` ni al arreglo de warnings del turno. Corregido sin migraciones: `ToolObservation` (`agentStepTypes.ts`) gana un campo `warnings?: string[]` opcional (codigos fijos y sanitizados unicamente); `buildToolObservation.ts` lo expone en las tres ramas (`completed`/`blocked`/`failed`) solo cuando la capability reporto al menos uno; `runAgentToolLoop.ts#processUseToolStep` los pliega ademas en el arreglo de warnings del turno (`agent_loop_tool_warning:<tool>:<warning>`), el mismo canal interno que ya usan `agent_loop_tool_blocked_unregistered`/`agent_loop_tool_invalid_arguments`. Persistencia en `crm_capability_executions` queda explicitamente fuera de alcance (exigiria una columna nueva, prohibido por esta tarea) - deuda registrada, no oculta. Tambien confirmado con test: `limit>10` es un guard real en tiempo de ejecucion (no solo una aserticion de test), `invalid_arguments` sigue generando fila de auditoria y `ToolObservation` igual que cualquier otro resultado, sigue sin contar para `maxToolExecutions` pero si para `maxDecisions`, y `orderBy`/`orderDirection` nunca llegan al adapter HTTP (verificado, `exploreInput` se construye por clave explicita).

Probado (18 tests nuevos en total desde `develop@657db19`, sin MariaDB - doble HTTP local o llamada directa a `capability.execute()`, nunca `executeGovernedCapability`/DB): `tests/commercial/exploreCatalogCapability.test.ts` (9, archivo nuevo - shape del schema, alias de produccion como test de regresion exacto, alias invalido nunca convertido, `sort` malformado nunca rescatado por el alias, `sort` bien formado tiene precedencia, propiedades desconocidas nunca llegan al adapter, `limit>10` rechazado), `tests/agent-loop/buildAgentStepPromptPackage.test.ts` (+3: schema renderizado verbatim, tool sin schema cae a solo-descripcion, regla de recuperacion presente en gathering y ausente en finalizacion), `tests/agent-loop/runAgentToolLoop.test.ts` (+3: regresion exacta del incidente real con `{orderBy, orderDirection}` responde y nunca hace handoff -ahora tambien afirma que el warning aparece en la observation y en `result.warnings`-, `invalid_arguments` por `limit` ausente no consume presupuesto y el modelo corrige dentro del mismo turno, rechazos repetidos en los 3 intentos de gathering nunca exceden `maxToolExecutions` ni loopean infinitamente), `tests/agent-loop/buildToolObservation.test.ts` (+3, correccion posterior: warning visible en una observation `completed`, ausencia del campo cuando no hay warnings, warning visible tambien en una observation `blocked`). `npx tsc --noEmit`, `npm run lint` y `npm run build` limpios. Regresion completa via `git stash` contra el mismo baseline (`develop@657db19`, sin Docker/MariaDB local en esta sesion): baseline 1861 tests/1398 pass/463 fail; con esta rama 1879 tests/1416 pass/463 fail - delta exacto +18 tests/+18 pass/+0 fail, coincide test por test con los 18 tests nuevos listados arriba (todos DB-free por diseno) - cero regresiones.

**Estado real de Catalog Service y MariaDB (correccion posterior, reconciliado)**. Diferencia de punto de acceso, ya reconciliada: `127.0.0.1` en esta sesion local de implementacion no es `127.0.0.1` en EC2 - esta sesion nunca tuvo acceso de red directo a los servicios de EC2, asi que un timeout probado desde aqui reflejaba esa falta de acceso, no el estado real del servicio. En EC2, MariaDB esta disponible via el contenedor `crm-customer-360-mariadb`, publicado en `127.0.0.1:3306`; el Catalog Service esta disponible en `127.0.0.1:4010`. El operador ejecuto el smoke real directamente alli y fue exitoso: `searchProducts` OK, `getProductDetails` OK, `exploreCatalog` OK - `totalMatched=833`, `exhaustiveForScope=true`, `productId=1532`. `T02.6.1` ya fue desplegado en EC2 desde entonces y el flujo real de WhatsApp entrante a traves del Agent Tool Loop ya se ejercito de punta a punta hasta `terminalReason=responded` alli (ver evidencia completa en la entrada de verificacion operacional mas abajo). Los tests DB-backed (`tests/commercial/capabilityGateway.test.ts`, 8 de 12) no corrieron en esta sesion local (sin MariaDB local) y no deben ejecutarse contra EC2 tampoco mientras ese archivo hardcodee credenciales de desarrollo local (`DB_PASSWORD=una_clave_local`) apuntando a `main_management` - correrlo tal cual contra EC2 escribiria filas de auditoria reales en la base real.

Rama `fix/acs-r1-05-1-t02-6-1-explore-catalog-tool-schema`, base `develop@657db19` (incluye el merge de `T02.6`). **Actualizacion posterior**: PR #72 creado, revisado y mergeado a `develop` (commit `1928d6c`, merge commit `9a581f0`) - ya no es "sin push, sin PR, sin commit" como decia esta entrada originalmente. No modifica `current_task`/`current_task_status` de esta seccion (siguen en `ACS-R1-05.1-T02.1`) - misma logica de `T02.3A`-`T02.6`: tarea paralela.

**Verificacion operacional real - `ACS-R1-05.1-T02.6` y `ACS-R1-05.1-T02.6.1`: `operational: verified`** (evidencia reportada por el operador desde la instancia EC2 desplegada; no re-ejecutada de forma independiente en esta sesion de implementacion, que no tiene acceso de red directo a EC2 - ver la nota de vantage point mas arriba). Flujo real de WhatsApp ejercitado de punta a punta en dos turnos:

Turno 1, "¿Cual es el producto mas caro de la pagina?" -> `explore_catalog` -> `execution_status=completed` -> `terminalReason=responded` -> Camara Hiperbarica ST801 1.5ATA \| O2Life, $8.599.990 CLP.

Turno 2, "¿Me puedes dar el link?" -> `RecentCatalogContext` resuelve la referencia anaforica ("esa"/el resultado del turno anterior) -> `get_product_details` -> `terminalReason=responded` -> enlace canonico real del producto.

Cadena completa confirmada: ranking global -> identificacion del producto -> referencia anaforica en el turno siguiente -> rehidratacion con `get_product_details` -> URL canonica. Esto es exactamente el flujo que `T02.6` (capability `explore_catalog`) y `T02.6.1` (schema/alias/presupuesto) fueron disenados para habilitar, verificado ahora con datos reales, no solo con tests scriptados.

La evidencia historica del defecto original (`execution_status=invalid_arguments`/`error_code=sort_and_limit_required`, handoff, `provider_unavailable`) documentada en las entradas de arriba se conserva intacta como antecedente del problema que `T02.6.1` corrigio - no se reescribe ni se elimina, y no representa el estado actual de la capability. `docs/CAPABILITY_MATRIX.md` actualizado en el mismo cambio: `explore_catalog` pasa de `not_verified`/`active_development` a `verified`/`accepted_with_debt` (deuda remanente: incidente de `price.max < price.min` en el Catalog Service, alias legacy como bridge temporal, warnings no persistidos en `crm_capability_executions` - ninguna de las tres bloquea el `verified` operacional, todas quedan documentadas explicitamente).

`ACS-R1-05.1-T02.6.2` (`implemented_pending_real_smoke` - reglas comerciales de presentacion, tras la validacion operacional real de `T02.6`/`T02.6.1`; misma condicion de `T02.3A`-`T02.6.1`: tarea no listada en la tabla de tareas de la release spec, ejecutada bajo instruccion explicita en paralelo a `T02.1`/`T03`) cierra dos gaps encontrados en el mismo smoke real: (1) un ranking por stock expuso productos no comerciales (`Servicio vendedor Pesas Chile`, `Costo logistico`); (2) no existia ninguna regla de cierre orientada a ofrecer el enlace, ni una politica que acotara la exposicion de `stockQuantity` exacto al cliente.

**Auditoria de productos no comerciales (decision: no corregir en CRM)**: `CatalogExploreItem` (`lib/catalog/types.ts`) no trae ningun campo (`active`, `visibility`, `commerciallySellable`, `productType`, `category`, `internalOnly`, `availableForOrder`) que permita distinguir un producto real de una linea de servicio/logistica - confirmado leyendo el tipo completo, no asumido. `productType`/`categoryId`/`categorySlug` existen solo como filtros de request en `CatalogExploreInput`, nunca vueltos en cada item de la respuesta. Auditado tambien `catalogRepository.ts#search()` del checkout local de `catalog-service-mvp` (desactualizado, sin el endpoint `/explore`, pero mismo repositorio/equipo): su unico filtro es `WHERE p.active = 1`, sin ninguna condicion de visibilidad u orden habilitado - el mismo gap estructural es la explicacion mas probable para `/explore`. Como el Catalog Service es quien calcula `totalMatched`/ordena/selecciona el top-N/decide `exhaustiveForScope` antes de que CRM vea el resultado, y CRM no recibe ninguna senal estable para filtrar despues, la exclusion debe vivir alli, no aqui - filtrar por nombre de producto en CRM seria exactamente el anti-patron a evitar (fragil, no es una `productId`/`SKU`/clasificacion estable). Cero cambios de codigo en CRM para este punto: evidencia + prompt de handoff completo en `docs/handoffs/catalog-service-exclude-non-commercial-products.md` (archivo durable de este repo, no un directorio temporal) - implementacion real pendiente en una tarea y rama separadas dentro de `MS-pesaschile-catalog-service`, nunca en esta rama.

**Politica de exposicion de stock** (`lib/brain/commercial/agent-loop/stockDisclosurePolicy.ts`, nuevo, funcion pura `describeStockDisclosure(stockQuantity)`): `<=0` -> "Sin stock disponible."; `=1` -> singular exacto ("Queda 1 unidad disponible."); `2-19` -> plural exacto ("Quedan N unidades disponibles."); `20-100` inclusive -> "Hay más de 20 unidades disponibles." (nunca el numero); `>100` -> "Hay stock disponible." (nunca el numero). `buildAgentStepPromptPackage.ts` importa esta misma funcion para generar los ejemplos literales de la regla de prompt (`STOCK_DISCLOSURE_RULE_LINES`), de modo que el texto que ve el modelo y la logica de limites testeada nunca puedan desalinearse. Presentacion pura: `stockQuantity`, `ToolObservation`, `response_summary_json` y `crm_capability_executions` quedan exactamente igual - verificado con test dedicado. Aplica a `explore_catalog`, `get_product_details`, `search_products`, comparaciones, recomendaciones, rankings y respuestas de uno o varios productos, en gathering y en finalizacion (donde el modelo realmente compone la respuesta final). **Limite explicito, sin ambiguedad**: esta politica se aplica exclusivamente mediante instrucciones inmutables del prompt - el runtime no valida ni reescribe deterministicamente la cantidad de stock (ni ninguna otra parte de la respuesta) que el LLM efectivamente dice. `stockDisclosurePolicy.ts` genera el texto canonico para instruir al modelo; no intercepta ni reescribe la respuesta final. No bloquea este cierre por no existir enforcement todavia, pero si exige un smoke real antes de declarar esta regla especifica verificada.

**Regla de cierre comercial** (`COMMERCIAL_CLOSING_RULE_LINES`, mismo archivo): un producto concreto identificado (productId resoluble desde `explore_catalog`, `search_products` o `get_product_details`), sin enlace entregado todavia, sin pedido explicito del cliente -> cierra con "¿Quieres que te envíe el link para revisarlo?"; varios productos -> "¿Quieres que te envíe el link de alguno de estos productos?" (formula neutra fija - no existe infraestructura de concordancia de genero gramatical en este sistema, asi que nunca se intenta). Excepciones explicitas (ocho, ninguna omitida): enlace ya entregado, cliente pidio el enlace explicitamente, sin producto concreto identificado, respuesta de aclaracion, tool fallida/bloqueada, handoff, el agente aun necesita pedir una precision, respuesta no comercial. **Correccion de diseno explicita**: `publicLink` deliberadamente NO es una de las excepciones - `explore_catalog`/`search_products` nunca devuelven `publicLink` (solo `get_product_details` lo hace), asi que exigirlo por adelantado haria la oferta irrealizable para un producto visto solo via `explore_catalog`. La oferta se autoriza con solo tener un `productId` resoluble; si existe o no un enlace publico se descubre recien cuando el cliente acepta/pide el enlace y corre `get_product_details`: si `publicLink.available=true` se entrega `canonicalUrl`; si no, se informa que no hay enlace disponible - nunca se inventa una URL, nunca se vuelve a preguntar. Nada hardcodeado dentro de `explore_catalog`/`get_product_details` - todo vive como regla inmutable del prompt, en ambas fases.

Arquitectura: solo `lib/brain/commercial/agent-loop/stockDisclosurePolicy.ts` (nuevo), `lib/brain/commercial/agent-loop/buildAgentStepPromptPackage.ts` (dos bloques de reglas nuevos, importados en ambas fases) y `docs/handoffs/catalog-service-exclude-non-commercial-products.md` (nuevo, documental, sin codigo). Cero cambios en `ToolObservation`, `buildToolObservation.ts`, `runAgentToolLoop.ts`, capabilities, Catalog Service, schema o migraciones.

Probado (17 tests nuevos, sin MariaDB - puros, sin HTTP, sin DB): `tests/agent-loop/stockDisclosurePolicy.test.ts` (10, archivo nuevo - los 8 limites exactos pedidos, singular=1 y plural=2 diferenciados explicitamente, mas negativo tratado como cero), `tests/agent-loop/buildAgentStepPromptPackage.test.ts` (+6: regla de stock con los mismos ejemplos que la funcion testeada en ambas fases incluyendo singular/plural y el disclaimer de no-enforcement, cierre de un producto (incluye que no requiere conocer `publicLink` de antemano), cierre de varios productos, las ocho excepciones (confirma que `publicLink` no esta entre ellas), entrega de URL con ambos resultados de `publicLink.available` al pedir el link, presencia tambien en finalizacion), `tests/agent-loop/buildToolObservation.test.ts` (+1: `stockQuantity=47171` sigue intacto en la observation). `npx tsc --noEmit`, `npm run lint` y `npm run build` limpios, `git diff --check` limpio.

No se declara `operational: verified` para este incremento especifico todavia (aunque `explore_catalog` en su conjunto ya lo esta, ver mas arriba): estas reglas de presentacion son nuevas, sin smoke real propio contra WhatsApp, y sin ningun mecanismo de enforcement en runtime (ver limite explicito arriba) - quedan `implemented_pending_real_smoke` hasta que el operador confirme que el cierre comercial y la politica de stock aparecen correctamente en una conversacion real. Rama `feat/acs-r1-05-1-t02-6-2-commercial-presentation-rules`, base `develop@9a581f0` (incluye el merge de `T02.6.1`). **Actualizacion posterior**: PR #73 creado, revisado y mergeado a `develop` (commit `4aef875`, merge commit `4381c96`) - ya no es "sin push, sin PR, sin commit" como decia esta entrada originalmente. No modifica `current_task`/`current_task_status` de esta seccion (siguen en `ACS-R1-05.1-T02.1`) - misma logica de `T02.3A`-`T02.6.1`: tarea paralela.

`ACS-R1-05.1-T02.7` (`implemented_pending_real_smoke` - continuidad conversacional de una accion pendiente de catalogo; misma condicion de `T02.3A`-`T02.6.2`: tarea no listada en la tabla de tareas de la release spec, ejecutada bajo instruccion explicita en paralelo a `T02.1`/`T03`, sobre `develop@4381c96` una vez mergeado `T02.6.2`) corrige un turno innecesario real, observado por WhatsApp: el agente ofrece el link de varios productos ("¿Quieres que te envie el link de alguno de estos productos?"), el cliente responde con una seleccion valida ("el del magnesio") y el sistema resuelve el producto correctamente (`RecentCatalogContext`, ya operativo) pero interpreta la respuesta como una consulta generica de producto y vuelve a preguntar si quiere el link, en vez de entregarlo directo.

**Causa raiz**: la oferta de enlace (`COMMERCIAL_CLOSING_RULE_LINES`, `T02.6.2`) vive unicamente como texto libre en la respuesta del turno anterior - no existia ninguna representacion estructurada de "el turno anterior dejo una accion de catalogo pendiente", asi que el turno siguiente dependia por completo de que el modelo releyera y reinterpretara correctamente su propio mensaje anterior dentro de `commercialContextSummary.recentMessages`. `RecentCatalogContext` (resuelve que producto, no que accion) ya funcionaba correctamente; lo que faltaba era la continuidad de la intencion (enviar el link) a traves del turno.

**Decision de persistencia** (tras auditar `RecentCatalogContext`, estado conversacional, contexto de agent loop, metadata de mensajes): ninguna tabla nueva. `PendingCatalogActionStep` (`actionType: "send_product_link"`, `candidateProductIds: string[]`) es un campo opcional nuevo del propio `AgentStep` tipo `respond` (`agentStepTypes.ts`) - el modelo lo declara explicitamente junto a su mensaje cuando ese mensaje ofrece el link (mismo patron ya usado para `arguments` de `use_tool`: el modelo emite JSON estructurado, el runtime valida forma y luego valida coherencia contra evidencia de catalogo, nunca semantica por texto visible). Antes de exponer `AgentLoopResult.finalPendingCatalogAction`, `runAgentToolLoop.ts` normaliza candidatos con `normalizePendingCatalogActionForEvidence`: trim, dedupe preservando orden, cap de 20 y cruce contra `allowedProductIds = RecentCatalogContext vigente U productIds de ToolObservations del turno` (`search_products`, `explore_catalog`, `get_product_details`). Si no queda ningun candidato valido, se omite solo la accion estructurada y se conserva intacto el mensaje visible. La accion solo se persiste dentro del `payload_json` del evento `agent_tool_loop_completed` cuando `dispatch.outboxWritten === true`; un provider exitoso, `terminalReason="responded"`, `dispatch.attempted` o texto final no bastan. `outboxWritten` significa "fila escrita/deduplicada en `brain_message_outbox`", no entrega efectiva por WhatsApp ni lectura del cliente. El turno siguiente lo recupera con un loader minimo de una sola fila (`pendingCatalogAction.ts`): `SELECT payload_json FROM commercial_event WHERE conversation_id = ? AND event_type = 'agent_tool_loop_completed' ORDER BY created_at DESC, id DESC LIMIT 1`.

**Runtime**: `pendingCatalogAction` viaja como quinto campo estructural (junto a `recentCatalogContext`) desde `runNativeAutonomousCycle.ts` (carga con `loadPendingCatalogAction`, punto de inyeccion de test nuevo `loadPendingCatalogAction?`, mismo patron que `loadRecentCatalogContext?`) hasta `buildAgentStepPromptPackage.ts`, en ambas fases (gathering y finalizacion). Un bloque de reglas inmutable (`PENDING_CATALOG_ACTION_RULE_LINES`) instruye: resolver+entregar directo ante seleccion inequivoca (nominal, ordinal, o "si" con un unico candidato) sin volver a preguntar ni re-presentar el producto; pedir precision y **renovar** `pendingCatalogAction` con candidatos sanitizados ante ambiguedad, sin adivinar; omitir el campo (dejarlo caducar) ante cambio de tema; y, si `get_product_details` falla o queda bloqueado en una accion `send_product_link`, no inventar URL, no reutilizar URL previa, informar indisponibilidad temporal, no volver a ofrecer automaticamente el enlace y consumir la accion. La resolucion de referencia (que producto es "el del magnesio"/"el segundo") sigue siendo responsabilidad del modelo via `RecentCatalogContext`, sin resolucion nominal/ordinal nueva en codigo - esta tarea gobierna unicamente que hacer una vez resuelta la referencia y como sobrevive la intencion hacia el siguiente turno. `validateAgentStep.ts` valida forma de manera tolerante; `pendingCatalogAction.ts` valida coherencia contra evidencia. `runAgentToolLoop.ts` expone `AgentLoopResult.finalPendingCatalogAction` y registra la transicion en warnings internos (`pending_catalog_action_active`/`_renewed`/`_consumed`, `pending_catalog_action_sanitized`, `pending_catalog_action_dropped_no_candidates`, `pendingCatalogAction_tool_failed_consumed`, `pendingCatalogAction_tool_blocked_consumed`, `pendingCatalogAction_model_renewal_suppressed`); ademas, el runtime fuerza consumo si una observation `failed` o `blocked` de `get_product_details` corresponde por `productId` normalizado a uno de los candidatos pendientes, aunque el modelo intente reemitir una continuidad valida. `runNativeAgentToolLoopCycle.ts` agrega `pending_catalog_action_dropped_no_outbox` cuando corresponde. Ninguna senal incluye mensaje completo ni PII.

**Vigencia y limites**: `pendingCatalogAction` representa unicamente continuidad inmediata hacia el siguiente turno del cliente. No es workflow persistente, accion multitur, estado durable hasta consumo, replacement de motor de intents pendientes, ni tabla de pending actions. El consumo sigue siendo implicito: ultimo evento cronologico con accion -> disponible; evento posterior sin accion -> consumida o expirada. Si `humanOwnerActive`/`aiBlocked` impide ejecutar el ciclo, no hay escritura nueva de `agent_tool_loop_completed`; una accion previa podria reaparecer al devolver control a IA si no existio un evento IA posterior que la invalide. Ese caso queda documentado como follow-up no bloqueante de takeover humano, no como resuelto por T02.7.

**Extensibilidad**: `actionType` es una union literal con un unico miembro implementado (`send_product_link`); `show_product_details`/`compare_products`/`add_product_to_quote`/`check_shipping`/`add_to_cart` quedan como adiciones futuras al tipo, sin logica funcional agregada ahora ni cambio de forma del contrato existente.

No se toco `MS-pesaschile-catalog-service` ni ningun contrato del Catalog Service; `get_product_details` sigue siendo la unica fuente de `publicLink`; no se agrego resolucion nominal/ordinal nueva, taxonomia, `resolve_catalog_scope` ni diccionario por nombre de producto (las referencias de ejemplo del ticket - "el del magnesio" - existen solo en tests, nunca en reglas de produccion).

Archivos nuevos: `lib/brain/commercial/agent-loop/pendingCatalogAction.ts`, `tests/agent-loop/pendingCatalogAction.test.ts`. Archivos modificados: `agentStepTypes.ts`, `validateAgentStep.ts`, `buildAgentStepPromptPackage.ts`, `runAgentToolLoop.ts`, `runNativeAgentToolLoopCycle.ts`, `native-cycle/runNativeAutonomousCycle.ts`, `events/types.ts`, `events/normalize.ts`, `agent-loop/index.ts`, mas tests extendidos en `validateAgentStep.test.ts`, `runAgentToolLoop.test.ts`, `buildAgentStepPromptPackage.test.ts`, `tests/commercial/commercial-events.test.ts`.

Probado: suites focalizadas nuevas/extendidas en verde con MariaDB local disponible: loader multievento real (`created_at DESC, id DESC`), invalidacion por evento posterior sin accion, desempate deterministico por `id`, round-trip de evento, gating por `outboxWritten`, sanitizacion de candidatos contra `RecentCatalogContext` y ToolObservations (`search_products`, `explore_catalog`, `get_product_details`), fallo de `get_product_details` sin URL inventada ni segundo CTA, y continuidad de confirmacion simple/multiple. `npx tsc --noEmit`, `npm run lint`, `npm run build` y `git diff --check` deben permanecer limpios antes de nueva auditoria.

No se declara `operational: verified`: sin smoke real propio contra WhatsApp todavia. Rama `feat/acs-r1-05-1-t02-7-pending-catalog-action-continuity`, base `develop@4381c96` (incluye el merge de `T02.6.2`) - sin push, sin PR, sin commit al momento de esta entrada. No modifica `current_task`/`current_task_status` de esta seccion (siguen en `ACS-R1-05.1-T02.1`) - misma logica de `T02.3A`-`T02.6.2`: tarea paralela.

Ambos incrementos (`ACS-R1-05` y `ACS-R1-05.1`):

- no cierran `ACS-R1-04`;
- no alteran el bloqueo de `ACS-R1-04-T08`;
- no activan `ACS-R1-04-T09`;
- pueden avanzar porque no dependen del Customer Service externo (`PAUSED_EXTERNAL`).

`ACS-R1-05.1` es, ademas, el camino critico activo hacia el primer vertical conversacional operativo (ver "Camino critico al piloto controlado" en `ROADMAP.md`); no reabre ni reescribe el cierre de `ACS-R1-05` — lo extiende con memoria comercial persistente y lo valida contra un piloto real con un unico `wa_id` allowlisted.

`ACS-R1-05-T01` (cerrada, commit `d3b07ca`; primer intento `ef9c5ca` fue rechazado por semantica incorrecta de historial/intentos/idempotencia) consolido `sales-consultative/repository.ts` sobre `follow-up-planner/planFollowUp.ts` como unica fuente de calculo de `attemptNumber`/`maxAttempts`/`scheduledFor` para filas `schedule_followup` (antes hardcodeadas `1`/`1`/`"allowed"`). El historial durable (`loadFollowUpActionHistory`) queda escopado estrictamente por `opportunity_id` cuando existe (nunca cae a `wa_id` compartido entre oportunidades distintas), o por `conversation_case_id` exacto cuando no; solo estados explicitos (`planned`/`requires_review`/`executing`) cuentan como activos, y solo `executing`/`executed`/`failed` consumen un intento comercial (`rejected`/`blocked`/`cancelled`/`expired` no agotan `maxAttempts`). Un retry exacto (mismo `planId`/`intent`/`attemptNumber`) reutiliza la fila activa; un plan distinto mientras una fila sigue activa devuelve `active_followup_exists` sin sobrescribir (T01 no implementa supersession); sin fila activa, un intento terminal habilita el siguiente legitimo. `policy_status` y `action.status` son mapeos independientes de `plan.status` (nunca se persiste crudo), y `maxAttempts` viene de una constante canonica nombrada (`COMMERCIAL_FOLLOW_UP_DEFAULT_MAX_ATTEMPTS`). Otros tipos de accion (`send_whatsapp_reply`, `prepare_quote_draft`, `take_over_case`, `pause_ai`, `mark_lost_candidate`, `create_internal_task`) conservan exactamente su persistencia previa. Probado con MariaDB real contra `crm_test` (`tests/commercial/salesConsultativeFollowUpRepository.test.ts`, 19/19) mas tests puros de planner/adapter (`followUpPlanAdapter.test.ts` 12/12, `followUpPlanner.test.ts` +1 regresion). Detalle completo, incluidos dos bugs pre-existentes de escritura real corregidos de paso, en la seccion "Evidencia de cierre" de la release spec.

`ACS-R1-05-T02` (cerrada) conecto `policy/evaluateCommercialPolicy.ts` como `follow_up_dispatch_policy` - un gate obligatorio, no un shadow-advisory, justo antes del `INSERT INTO crm_agent_actions` dentro de `upsertFollowUpActionRow` (`sales-consultative/repository.ts`). El adapter nuevo (`sales-consultative/followUpDispatchPolicy.ts`) llama al boundary puro de politica con un `SalesAgentResult` vacio (sin claims/actions/toolRequests/entityProposals) para ejercitar unicamente su gate de canal, sin copiar ninguna de sus reglas de bloqueo. Fuentes reales, nunca texto libre ni la timezone del servidor: `optOut` desde `crm_opportunities.signals_json` (estructurado, hoy siempre vacio - no existe aun una capa de opt-out capture en el repo, y T02 no la agrega); `quietHoursActive` desde hora+timezone explicitos (`America/Santiago`, ventana 21:00-09:00, constante nueva ya que el contrato no fijaba una); `humanOwnerActive`/`aiBlocked` desde `conversation.human_owner_active`/`conversation.ai_enabled` como dos senales independientes (nunca combinadas, para que "dueno humano activo sin IA deshabilitada" siga siendo revisable y no un bloqueo duro); `identityConflict` desde `crm_customer_onboarding_state.status = 'conflict'` (el estado nativo real de ACS-R1-04, nunca el `resolver_identity` legacy del shadow). Un `conversationId` sin fila real degrada a "sin senal adicional" (invariante T01 de fallback seguro preservado); solo una excepcion tecnica real de la fuente de senales falla cerrado. Mapeo: `allow` conserva `planned`/`requires_review` calculados por el plan; `require_review` fuerza `requires_review` aunque el plan proponga `planned` (nunca degradado a `planned` - cubre quiet hours y human-owner-active); `deny`/`failed_safe` no insertan fila (mismo patron `follow_up_plan_not_persisted:<status>` de T01, ahora `follow_up_dispatch_deny:<reasonCode>`/`follow_up_dispatch_failed_safe:<reasonCode>`). Gated por `BRAIN_COMMERCIAL_POLICY_ENABLED` (mecanismo tipado `readEnvFlag` existente, ya documentado en `.env.example`), nunca por flags `BRAIN_COMMERCIAL_SHADOW_*`; deshabilitada, el write falla cerrado (nunca `allowed` por defecto). Solo codigos de razon cortos y fijos se persisten en `policy_notes_json` (nunca el `CommercialPolicyResult` completo, PII ni texto libre del cliente). Los 19 tests de T01.1 siguen en verde sin cambiar sus aserciones (solo se fijo un `currentTime` deterministico para evitar flakiness por quiet hours, y se activo `BRAIN_COMMERCIAL_POLICY_ENABLED=true` en el harness). Probado con 14 tests puros nuevos (`tests/commercial/followUpDispatchPolicy.test.ts`) y 11 tests nuevos con MariaDB real contra `crm_test` (`salesConsultativeFollowUpRepository.test.ts`, `[T02-1]`..`[T02-10]`). Detalle completo en la seccion "Evidencia de cierre - ACS-R1-05-T02" de la release spec.

`ACS-R1-05-T03` (cerrada; primer intento `6558f29` corregido por `ACS-R1-05-T03.1` `59a536c`, y por `ACS-R1-05-T03.2` `af0718f`) endurecio el runtime canonico de follow-up (`sales-consultative` -> `crm_agent_actions` -> `runFollowupTick` -> `runNativeAutonomousCycle`) sin tocar el planner T01 ni la dispatch policy T02. `selectDueFollowUps` selecciona tres grupos disjuntos de candidatas: `planned` due; **toda** fila `executing` con lock vencido (`updated_at` mas antiguo que `FOLLOW_UP_STALE_EXECUTING_LOCK_SECONDS = 300`, constante en `followUpWorkerPolicy.ts`), con o sin intentos restantes; y `failed` con `attempt_number < max_attempts`. Recuperar una fila `executing` stale con intentos restantes **incrementa `attempt_number` exactamente una vez** dentro del propio `UPDATE` que gana el CAS (`claimStaleExecutingFollowUp`) - es un intento comercial nuevo, no la continuacion silenciosa del anterior; una fila `executing` stale que ya agoto `max_attempts` nunca se reclama, se **terminaliza** a `failed` con `failure_reason = 'follow_up_stale_execution_exhausted'` (`terminalizeExhaustedStaleFollowUp`, CAS idempotente, sin `cycleRunner`, sin nueva fila, sin incrementar intentos). `claimFailedFollowUpRetry` sigue incrementando `attempt_number` exactamente una vez. La revalidacion del estado comercial (`shouldCancelFollowUp`) ahora corre **siempre despues del claim**, de forma identica para los tres origenes (`planned`, `failed`, `executing` recuperado) - el primer intento (`6558f29`) revalidaba antes del claim para `planned`/`failed`, dejando una ventana donde el estado pudo cambiar entre la revalidacion y el claim; la correccion cierra esa ventana reclamando primero y revalidando sobre la fila ya propia. `cancelFollowUp` (`status IN ('planned', 'failed')`, retorna `{cancelled: boolean}`, P1-1) ya no se llama desde el bucle del worker - queda como punto de entrada externo, probado directamente; el worker aborta una fila ya reclamada exclusivamente via `abortClaimedFollowUp` (`status = 'executing'`). `ACS-R1-05-T03.2` corrigio ademas una mezcla de fuente horaria en los tres CAS de staleness (seleccion de `executing`, recuperacion, terminalizacion): escribian `updated_at = CURRENT_TIMESTAMP(3)` (reloj de sesion) pero calculaban el cutoff con `UTC_TIMESTAMP()` (reloj siempre-UTC) - dependian por accidente de que la sesion MariaDB corriera en UTC. Las tres consultas ahora usan `CURRENT_TIMESTAMP(3)` en ambos lados; verificado con un test que fuerza `SET time_zone = '-04:00'` en una conexion dedicada (`withConnection`) y confirma la semantica completa (reciente no se reclama, stale se recupera, intento incrementado exactamente una vez, agotada se terminaliza, recien reclamada no vuelve a verse stale) sobre esa misma sesion, restaurando la zona horaria antes de liberar la conexion. Probado con 31/31 tests en `tests/commercial/runFollowupTick.test.ts` contra MariaDB real (`main_management`, incluye el hook de sincronizacion explicita `onAfterClaim` para probar la ventana de revalidacion sin sleeps y el test de sesion no-UTC), mas 75/75 combinados con los tests de T01/T02 sin modificar sus aserciones, mas 106/106 en regresion adicional y el escenario E del harness E2E en `PASS`. Detalle completo, incluida la evidencia de concurrencia CAS para recuperacion y terminalizacion, en la seccion "Evidencia de cierre - ACS-R1-05-T03" de la release spec.

`ACS-R1-05-T06` (cerrada; primer intento SHA funcional `b992ff9` corregido por `ACS-R1-05-T06.1` SHA funcional `c2953e7`, rama `acs-r1-05-t06-operational-safety`) cerro los gaps P1-2 y P1-5 de la auditoria sin tocar `ACS-R1-05-T05` (aislamiento de codigo muerto/paralelo, diferida en ese momento de `in_progress` a `ready` para priorizar este incremento - cerrada despues, ver parrafo de T05 abajo) ni ejecutar envios reales a Meta. El primer intento (T06) elimino la auto-escalacion de `process.env` en los tres scripts CLI (`autonomous-followup-worker.ts`, `autonomous-outbox-worker.ts`, `commercial-live-autonomy-run.ts`, que antes escribian `process.env[key] = "true"` para cada flag ausente) y construyo un lector canonico fail-closed (`lib/brain/runtime/autonomousRuntimeConfig.ts`, acepta unicamente `"true"`/`"false"`) con el contrato de arranque del outbox worker (real send requiere los tres flags explicitos y `BRAIN_AUTONOMOUS_TEST_WA_IDS` no vacio, o rechaza arrancar), mas la redaccion compartida (`redactErrorMessage.ts`) conectada en los dos sitios de `crm_agent_actions.failure_reason` que persistian `error.message` crudo. La correccion `ACS-R1-05-T06.1` encontro que `BRAIN_AUTONOMOUS_TEST_WA_IDS` nunca se comparaba realmente contra un `wa_id`, salvo en la defensa final de `metaClient.ts` (ya fail-closed) y, con un bug de `wa_id` nulo, en `autonomousOutboxTick.ts` - el ciclo autonomo llamaba al LLM y `runFollowupTick.ts` reclamaba/consumia intentos para cualquier numero sin mirar la allowlist. Agrega un gate temprano en `runNativeAutonomousCycle.ts` (antes de cualquier LLM/DB), un gate pre-claim en `runFollowupTick.ts` (fila y `attempt_number` intactos para un numero no autorizado), consolida el guard del outbox worker sobre el mismo normalizador de digitos que la defensa final, extiende `redactErrorMessage.ts` con redaccion de email/telefono conectada tambien en los dos escritores restantes de `autonomousOutboxTick.ts`, un contrato de arranque nuevo para el follow-up worker (sus 5 flags estructurales deben ser todos `true` o todos `false`) y un script de preflight puro (`npm run preflight:autonomous`) sin DB ni Meta. Probado con 22 tests nuevos (config, redaccion, pilot isolation en las 4 capas) mas regresion completa sin fallos nuevos (17 preexistentes, uno menos que el baseline de T06 y no relacionado). El runtime de follow-up sigue sin `operational: verified`. Detalle completo en la seccion "Evidencia de cierre - ACS-R1-05-T06" de la release spec.

`ACS-R1-05-T04` (cerrada; primer intento `df6c41a` corregido por `ACS-R1-05-T04.1` `468fe7d`) consolido los dos escritores divergentes de `brain_message_outbox` en un unico writer canonico (`lib/brain/messaging/canonicalOutboxWriter.ts`) con una sola sentencia `INSERT IGNORE`, una sola normalizacion de columnas y una sola resolucion de `phone_number_id`. `T04.1` corrigio dos gaps que el primer intento dejaba abiertos: (1) los dos adapters seguian computando `dedupe_key` con esquemas distintos - ahora ambos llaman a la misma `buildCanonicalOutboxDedupeKey(channel, actionId, idempotencyKey, recipient, content)`, verificado con un test que invoca ambos adapters (secuencial y concurrente) con el mismo comando logico y confirma una sola fila; (2) la proyeccion de `crm_opportunities` (y, se encontro de paso, tambien `conversation_message`/`brain_message_outbox`) usaba un CAS de lectura-mas-`UPDATE` que no protegia contra dos escrituras concurrentes de estado distinto para el mismo mensaje - ahora las tres proyecciones son un unico `UPDATE` atomico cuyo `WHERE` aplica un ranking total (`sent < failed < delivered < read`) expresado en SQL, nunca una decision previa en TypeScript. `applyMetaDeliveryStatus` (`native-whatsapp/service.ts`) resuelve la oportunidad por `meta_payload_json.opportunity_id` del outbox o, en su defecto, por `crm_agent_actions.opportunity_id` - nunca por "la oportunidad activa actual de la conversacion" - sin tocar `status`/`stage`/`temperature`/`next_action_type` (migracion 025, cuatro columnas nuevas en `crm_opportunities`). Anadio idempotencia real a `crm_action_outcomes` via `outcome_dedupe_key` (`sha256("delivery|meta|<provider_message_id>|<outcome_type>")`, migracion 025) compartida entre el camino de exito HTTP y el webhook. El registro del outcome dejo de estar condicionado a que la proyeccion avance (historial append-only genuino) y la falla real de persistencia del outcome ya no se silencia con `.catch(() => void 0)`. Se agrego trazabilidad opcional de atribucion A/B desde el writer hasta `crm_action_outcomes.metadata_json`, sin asignador de variantes ni calculo estadistico. Probado con 36 tests nuevos con MariaDB real (`tests/commercial/canonicalOutboxWriter.test.ts` 12/12, `tests/commercial/deliveryOutcomeProjection.test.ts` 21/21, incluye pares concurrentes `read`/`delivered`, `read`/`sent`, `failed`/`delivered` y mensajes antiguo/nuevo concurrentes) mas 289/289 en regresion combinada, mas 30/30 iteraciones de un stress test SQL aislado sin ninguna falla. `npm run e2e:autonomous` corrido varias veces contra el commit base sin T04/T04.1 y contra el codigo final: el escenario `[H]` falla identico en ambos (preexistente); los escenarios `[A]`/`[B]` (dependientes de un LLM real) variaron entre `PASS`/`PARTIAL`/`FAIL` en corridas repetidas del MISMO commit en ambos lados, confirmando no-determinismo del proveedor LLM externo, no una regresion de T04/T04.1 - ningun otro escenario vario nunca. No toco `ACS-R1-04`; el runtime de follow-up sigue sin `operational: verified`. Detalle completo en la seccion "Evidencia de cierre - ACS-R1-05-T04" de la release spec.

`ACS-R1-05-T05` (cerrada y aceptada en `develop`, rama `acs-r1-05-t05-isolate-parallel-runtimes`, creada desde `develop` sin mezclar la rama congelada `acs-r1-05-t06-2-sales-turn-continuity`) aislo/elimino los runtimes de follow-up paralelos o muertos que la auditoria encontro (P2-1, P2-2, P2-5), sin agregar funcionalidad ni cambiar politica/cadencia/contenido/handoff/catalogo/envio. Elimino el scheduler/persistidor muerto de `multi-request/requestFollowups.ts` (`scheduleRequestFollowup`/`scheduleFollowupFromDefinition`/`runRequestFollowupTick`, cero callers productivos) conservando intacta su unica proyeccion de lectura real (`listPendingFollowupsForRequest`, consumida por `requestsView.ts` -> `app/api/conversations/[id]/requests/route.ts`). Corto la alcanzabilidad de la familia `autonomous-loop` (+ `follow-up-scheduling`/`follow-up-replanning`) y del modulo `lib/brain/messaging/outbox-worker/` (hyphenated) desde los barrels de produccion (`lib/brain/commercial/index.ts`, `lib/brain/messaging/index.ts`) - ambos son simuladores puramente en memoria (sin SQL, sin `fetch`, sin sender/claim/retry real) alcanzables unicamente desde `app/(hub)/dev/ai-sdr-simulator`, marcado "Legacy scenario simulator" en su propia UI; no se reescribieron para usar el runtime canonico via DI porque ya no ejecutan ningun side effect real, y hacerlo habria sido una remodelacion funcional fuera de alcance. Tras esta tarea, `follow-up-planner/planFollowUp.ts#planCommercialFollowUp` es la unica fuente productiva de calculo para `schedule_followup` y `policy/evaluateCommercialPolicy.ts` (conectado desde T02) el unico gate - ninguna configuracion permite elegir un planner alternativo. Probado con un test estatico nuevo (`tests/commercial/followUpRuntimeAuthority.test.ts`, 5 tests sin DB) que fija esta autoridad por import graph real, no por conteo de archivos; verificado que 3 de esos 5 tests fallan contra `develop` HEAD (`433aaab`) antes de la tarea. `npx tsc --noEmit` y `npm run build` limpios. Suite completa sin Docker/MariaDB disponible en este entorno (misma limitacion que la auditoria original): `develop` HEAD dio 1293 tests/959 pass/334 fail: esta rama da 1292 tests/964 pass/328 fail, con el conjunto de fallos un subconjunto estricto del de `develop` (cero fallos nuevos; los 6 que desaparecen son los del archivo de test eliminado, que fallaban por falta de DB, no por logica). No toco schema/migraciones, no toco `ACS-R1-05-T06.2` (rama separada, integrada despues en `ACS-R1-05-T07`, ver parrafo siguiente), no inicio `ACS-R1-05-T07`. Detalle completo en la seccion "Evidencia de cierre - ACS-R1-05-T05" de la release spec.

`ACS-R1-05-T06.2` (`done, accepted` - verificado por `ACS-R1-05-T07` contra MariaDB real, ver parrafo siguiente; rama original `acs-r1-05-t06-2-sales-turn-continuity`, pusheada, congelada, sin PR) corrigio un incidente real de piloto controlado en el que un cliente pidio recomendacion con presupuesto definido y el sistema no ejecuto la busqueda; en el turno siguiente, el sandbox bloqueo por censura lexical ("precios") una respuesta legitima del modelo, dejando la accion en `proposed` sin outbox y al cliente sin respuesta. Corrige cuatro bloques: (A) `processNativeWhatsAppInbound` ya no descarta el resultado del ciclo reactivo - `ensureAutonomousSalesTurnContinuity` (`lib/brain/commercial/continuity/`) lo captura, terminaliza via CAS una accion bloqueada (`status = "blocked"`, terminal existente, nunca sobrescribe `planned`/`executing`/`executed`/`cancelled`/`failed`) y despacha un fallback idempotente por el pipeline real (sandbox -> execution gate -> outbox canonico) cuando el turno debia responder al cliente y no pudo; ownership humano real nunca recibe un fallback de IA. (B) `runCommercialOperationalLoop` ahora usa `commercialPolicyResult.governedResult` en vez del resultado crudo del Sales Agent; `crm_agent_actions.policy_status` propaga la misma autoridad que ya escribe `crm_agent_decisions.policy_status` en vez de recalcularla de forma independiente; `recentCustomerReply` ya no fuerza revision sobre el propio turno que lo genera (sigue disponible para cancelar follow-up); `quiet_hours_active`/`manual_approval_required` se reportan con su propio codigo, nunca como `human_owner_active` falso. (C) `search_products` -> `batch_get_products` (nuevo, `POST /v1/products/batch` del Catalog Service real, sin modificar ese repositorio) -> ranking por presupuesto (economy/near_budget/stretch, `rankCatalogCandidatesByBudget.ts`) -> mensaje compuesto grounded, en una sola llamada batch nunca solicitada por el LLM. (D) `validateAutonomousReplyCandidate.ts` elimina la lista lexica `precio/stock/descuento/garantia/...` del sandbox (controles tecnicos reales - placeholders, credenciales, JSON crudo, allowlist, ownership real - intactos). Cero migraciones (reutiliza `crm_agent_actions`/`crm_agent_decisions`/`crm_opportunities`/`crm_sales_need_profiles`/`commercial_event` existentes); dos eventos nuevos sobre `commercial_event` (`autonomous_turn_disposition`, `autonomous_turn_continuity_failed`). Regresion completa comparada exactamente contra `develop` (mismo comando, MariaDB no disponible en el entorno de implementacion en ambos lados): 334 fallos preexistentes identicos en ambos, 6 fallos nuevos exclusivos de los tests DB-backed nuevos de esta tarea (mismo motivo de conexion), cero tests previamente verdes rotos. `npx tsc --noEmit` y `npm run build` limpios. No declara el runtime `operational: verified`; no reemplaza la validacion E2E formal de `ACS-R1-05-T07`. **Correccion posterior (dos pasadas mas, misma rama)**: una auditoria independiente encontro que el punto (D) quedo sin ningun reemplazo real para compromisos comerciales no respaldados, y una segunda auditoria encontro que ese reemplazo solo cubria patrones sintacticos especificos (no significado), que el grounding de claims operaba por tipo y no por instancia concreta, que `requires_review` nunca detenia realmente el envio, y una carrera real de idempotencia en `persistAgentAction.ts`. Ambos hallazgos quedan corregidos con evidencia `file:line` y tests - ver "Correccion posterior (segunda pasada)" y "Correccion posterior (tercera pasada)" en la seccion "Evidencia de cierre - ACS-R1-05-T06.2" de la release spec, unica fuente de ese detalle. Antes de `ACS-R1-05-T07`, el unico bloqueo pendiente era la falta de MariaDB real en el entorno de esa implementacion para verificar sus tests DB-backed (`continuityFallback.test.ts`, `buildCatalogGroundedMessageBudget.test.ts`, `ensureAutonomousSalesTurnContinuity.test.ts`, `continuityConcurrency.test.ts`) - nunca un hallazgo de logica abierto. `ACS-R1-05-T07` levanto MariaDB real y corrio esos 4 archivos (mas 2 adicionales de la misma tabla): 17/17 en verde: el bloqueo esta cerrado y `T06.2` queda `done, accepted`.

`ACS-R1-05-T07` (`done, accepted`, rama `acs-r1-05-t07-e2e-restart-recovery`, HEAD inicial `25f15ce`, creada desde `develop` con `T05` ya aceptada, merge `--no-ff` de la rama congelada `acs-r1-05-t06-2-sales-turn-continuity` como linea base) cierra `ACS-R1-05`. Levanto MariaDB real desechable (contenedor Docker local, `crm_test`, 24/24 migraciones desde cero, segunda pasada 24/24 skip sin conflicto de checksum) y ejecuto los 6 archivos DB-backed que T06.2 dejo pendientes: 17/17 tests en verde, tras corregir un fixture invalido y un bug real de proceso (un `await import` dinamico colgaba el proceso al final del archivo) en `ensureAutonomousSalesTurnContinuity.test.ts`. Agrego 17 tests E2E nuevos contra MariaDB real en 4 archivos (5+4+5+3, verificado por conteo de bloques `test()`, no por estimacion) (`tests/e2e/reactiveTurnRestartRecovery.e2e.test.ts`, `reactiveTurnFallbacks.e2e.test.ts`, `followUpRestartRecovery.e2e.test.ts`, `outboxDeliveryRestartRecovery.e2e.test.ts`) cubriendo turno reactivo normal/replay/concurrencia real (10 iteraciones)/restart entre accion y outbox/restart entre outbox y disposition/draft inseguro/proveedor LLM caido/catalogo caido/resultado de modelo invalido/follow-up normal/stale/agotado/cancelado por respuesta del cliente/bloqueado por ownership humano/delivery de outbox con sender fake y restart tras el claim. Estos escenarios encontraron y corrigieron dos defectos reales previamente invisibles sin MariaDB: (1) `persistAgentAction.ts` re-seleccionaba la fila ganadora de una carrera de `ER_DUP_ENTRY` *antes* de hacer rollback, lo que bajo REPEATABLE READ le impedia ver el commit ganador y resolvia incorrectamente a `"failed"`; (2) dos ejecuciones concurrentes del ciclo reactivo para el mismo inbound podian crear dos acciones con dos mensajes de outbox identicos (duplicado real), corregido con un match secundario opt-in por `(conversation_case_id, message_id, action_type)` protegido por un lock consultivo de MariaDB, activado unicamente en la ruta de respuesta primaria; (3) un fallo tecnico del proveedor LLM que hacia fallar el operational loop por completo (nunca seleccionaba `nextAction`) dejaba al cliente sin ninguna respuesta - `ensureAutonomousSalesTurnContinuity` no reconocia esa señal como merecedora de fallback. Regresion completa comparada contra el baseline real `25f15ce` (worktree separado, MariaDB real disponible en ambos lados, no solo documental): cero regresiones funcionales nuevas (el unico test que aparece solo en la rama final es una flakiness de aislamiento entre archivos preexistente en el repo, confirmada pasando 5/5 en ejecucion aislada); 5 fallos que existian en baseline quedan corregidos. `npx tsc --noEmit`, `npm run build` y `npm run e2e:autonomous -- --skip-llm` (9/10, el fallo restante es el escenario `[H]` ya documentado como preexistente en la evidencia de T04) limpios. `followUpRuntimeAuthority.test.ts` sigue 5/5 en verde: ningun writer/outbox/planner paralelo introducido. La rama local esta adelantada respecto de `origin/acs-r1-05-t07-e2e-restart-recovery` (que permanece en `25f15ce`, el merge base) - sin push, sin PR, sin merge a `develop` (fuera de alcance de esta tarea). Detalle completo, incluido el conteo exacto de commits locales pendientes de push, en la seccion "Evidencia de cierre - ACS-R1-05-T07" de la release spec.

Detalle de alcance, tareas y Definition of Done: [ACS-R1-05 - Autonomous Follow-up Runtime](releases/ACS-R1-05-autonomous-follow-up-runtime.md). Estado tecnico real del runtime (que existe, que esta conectado, gaps): [Follow-up runtime reconciliation](audits/follow-up-runtime-reconciliation.md).

Detalle de alcance, tareas y Definition of Done del siguiente incremento (camino critico activo): [ACS-R1-05.1 - Persistent Commercial Memory + Controlled WhatsApp Pilot](releases/ACS-R1-05.1-persistent-commercial-memory-controlled-whatsapp-pilot.md).

## Workstream paralelo autorizado (tercero) - `SALES-AGENT-R3`

- `SALES-AGENT-R3` - target: un `SalesAgentRuntime` generico (evento -> modelo -> tool -> observacion -> modelo... -> respuesta final), sin intent/objective/workflow fijo, como reemplazo eventual del acoplamiento actual con el Agent Tool Loop especifico de WhatsApp.
- status: `parallel_in_progress`, `SalesAgentRuntime` ya es alcanzable desde el camino real de WhatsApp (`V1.4`) pero detras de `BRAIN_SALES_AGENT_RUNTIME_ENABLED`/`BRAIN_SALES_AGENT_RUNTIME_WA_IDS` (default `false`/vacio) - cero trafico general todavia, piloto no abierto a ningun wa_id real.
- current_task: ninguna abierta - `V1.8`, `V1.8.1` y `V1.8.1a` cerrados completos. `V1.8.1` (`R3_V1_8_1_CONVERSATIONAL_TURN_SETTLING_VALIDATED_WITH_KNOWN_DEBT`) agrega un mecanismo de turn-settling configurable entre el webhook de WhatsApp y la cognicion R3: fragmentos inbound rapidos ("hola"/"como"/"estas" como entregas de webhook separadas) se debouncean por una ventana de silencio/maxima (`BRAIN_R3_INBOUND_TURN_SETTLE_DELAY_MS`, default `0` = comportamiento identico a pre-V1.8.1, sin fila pendiente, sin espera extra - rollback path) antes de ejecutar `ensureAutonomousSalesTurnContinuity` una sola vez con el contenido agregado. Nueva tabla `crm_inbound_turn_settlements` (migracion 035, unicidad de fila `PENDING` por conversacion via columna generada, mismo patron que `sales_agent_configurations.published_scope_key` de la migracion 026) mas un worker dedicado (`scripts/autonomous-turn-settle-worker.ts`, mismo patron poll-CAS-revalidar que `runFollowupTick`/`commercialWorkWorker`). `deriveMessages.ts` gana `additionalExcludedMessageIds` (aditivo, no-op para todo caller de un solo mensaje) para que los fragmentos de un turno asentado nunca dupliquen la historia persistente. `dispatchGovernedSalesAgentMessage.ts` gana un recheck de frescura opcional (`checkInboundFreshness`) en la MISMA transaccion que el recheck de ownership existente: si llego un inbound mas nuevo mientras el modelo razonaba, la respuesta se suprime (`SUPERSEDED`) y nunca se despacha - deliberadamente excluido de `dispatchSalesAgentHardHandoff` (una transferencia de ownership ya real no se revierte). Indicador de "escribiendo"/lectura de Meta Cloud API real (`postMetaWhatsAppTypingIndicator`, mismo endpoint `/messages`, sin Baileys) tras `BRAIN_WHATSAPP_TYPING_INDICATOR_ENABLED` (default `false`, independiente del delay), no bloqueante, se dispara solo despues de asentar el turno (nunca en el primer fragmento crudo). 22 tests nuevos (repositorio/CAS, `deriveMessages` puro, e2e con MariaDB real + proveedor LLM falso cubriendo T1/T3/T7/T8/T9/T12-T15), mas un benchmark real DeepSeek+MariaDB (`scripts/live-turn-settle-benchmark.ts`, 4/4 escenarios incluido el caso literal "hola"/"como"/"estas" -> una sola respuesta natural combinada). Suite completa (`npm test`, 12 lotes): 4212 tests (4190 previos + 22 nuevos), 4191 pass, 21 fail - los 21 fallos, en 12 archivos preexistentes no relacionados (`a13ConversationalReliabilityBenchmark`, `commercialWorkIdentityOnboarding`, `commercialWorkParallelExecution`, `createCustomerCapability`, `customerOnboardingPostPlanPrivacy`, `customerOnboardingPostPlanStage`, `customerSession`, `customerSessionPrivacy`, `linkExternalIdentityCapability`, `processInboundCommercialShadow`, `runCommercialOperationalLoop`, `customerIdentityOnboarding.e2e`), verificados como `PREEXISTING` (no `NEW_V1_8_1_REGRESSION`) corriendo los mismos 12 archivos contra `develop` limpio via `git stash push -u` (mismo conteo exacto: 205/184/21) antes de restaurar el stash. `npx tsc --noEmit`, `npm run build` y `npm run lint` (0 errores, 40 warnings, identico a D5.2/D6/D7/V1.8-FINAL) limpios. Deuda explicita: sin tope de reintentos en `crm_inbound_turn_settlements` (fila que falla siempre queda reclamable indefinidamente, decision deliberada); `dispatchSalesAgentHardHandoff` fuera del recheck de frescura (decision de alcance); sin smoke real de UI de Meta para el indicador de escritura (deferred, misma categoria que V1.8-FINAL); sin taxonomia de eventos estructurados `turn_pending/turn_settled/...` (solo logging por consola en esta entrega). Detalle completo, los 17 exit gates y la deuda restante: [SALES-AGENT-R3-V1.8.1 - Conversational Turn Settling](releases/SALES-AGENT-R3-V1.8.1-CONVERSATIONAL-TURN-SETTLING.md). Siguiente workstream recomendado (no implementado aqui): `R3-V1.9` - Self-Recovery / Tool-Failure Resilience.

`V1.8.1a` (`R3_V1_8_1A_HARNESS_SEMANTIC_DURABILITY_VALIDATED`) cierra dos brechas de durabilidad encontradas despues de V1.8.1: (1) un inbound podia commitear en `conversation_message` antes de que existiera su fila de `crm_inbound_turn_settlements` (ventana de crash entre ambos escritos, sin migracion nueva); (2) una fila `PROCESSING` reclamada tras un crash podia, en teoria, duplicar una mutacion ya completada. Fix 1: `upsertPendingTurn` (`lib/brain/commercial/turn-settlement/repository.ts`) ahora acepta una `PoolConnection` opcional, y `processNativeWhatsAppInbound` (`lib/brain/native-whatsapp/service.ts`) la invoca DENTRO de la misma transaccion que persiste el inbound canonico, lanzando y provocando rollback completo si falla - `BRAIN_R3_INBOUND_TURN_SETTLE_DELAY_MS=0` (default) sigue sin crear ninguna fila, sin cambio de comportamiento. Fix 2: auditoria de `createQuoteCapability.ts` confirma que su clave de idempotencia (`sha256(opportunityId + commercial_line_items.factId)`, ambos releidos de estado durable en cada intento, nunca de estado en memoria) ya sobrevive un crash-antes-de-`completeTurn()` + reclamo de fila `PROCESSING` obsoleta - verificado con una prueba determinista nueva contra MariaDB real (una mutacion real de `create_quote` completada, crash simulado, reclamo, segundo intento de cognicion, exactamente una llamada al Quote Service externo, exactamente una cotizacion persistida) en vez de asumirlo. 6 tests nuevos (`tests/native/inboundTurnSettling.e2e.test.ts`: atomicidad, rollback en ventana de crash, retry idempotente post-rollback, dos fragmentos concurrentes reales via el webhook, y el escenario de mutacion+crash+reclamo), mas auditoria de huerfanos read-only (cero filas huerfanas reales encontradas en `crm_test`/`main_management`, solo artefactos de fixtures de test ya explicados). Sin migracion nueva (migracion 035 ya alcanza). Suite completa (`npm test`, 12 lotes): 4217 tests (4212 previos + 5 nuevos), 4192 pass, 25 fail - clasificados `PREEXISTING`: 21 en los mismos 12 archivos ya documentados por V1.8.1 (los 4 de `customerIdentityOnboarding.e2e` reconfirmados identicos contra `develop` limpio via `git stash push -u`), 2 mas del mismo flake de timing ya documentado por V1.8-FINAL (`agentSessionStoreMariaDb`, `commercialWorkParallelExecution`), y 2 mas (`tests/domains/customerIdentityEvidence.test.ts`) verificados directamente contra el baseline limpio (18/18 pass en aislamiento) como el mismo cluster de aislamiento `DATABASE_NAME` entre archivos ya documentado - cero fallos nuevos causados por este cambio. `npx tsc --noEmit`, `npm run build` y `npm run lint` (0 errores, 40 warnings, identico a V1.8.1) limpios. Pilot readiness: `TURN_SETTLING_NOT_READY_FOR_PILOT` (deuda de V1.8.1 sin cerrar - tope de reintentos, smoke real de Meta, taxonomia de eventos). Detalle completo, los 14 exit gates y la deuda restante: [SALES-AGENT-R3-V1.8.1A - Harness-Semantic Durability Hardening](releases/SALES-AGENT-R3-V1.8.1A-HARNESS-SEMANTIC-DURABILITY-HARDENING.md). Siguiente workstream recomendado (no implementado aqui): `R3-V1.9` - Self-Recovery / Tool-Failure Resilience.

`V1.8-FINAL` (`R3_V1_8_PERSISTENT_MEMORY_ARCHITECTURE_VALIDATED_WITH_KNOWN_DEBT`, solo validacion/documentacion, cero codigo de produccion nuevo) prueba la cadena completa (escritura durable -> lectura persistente -> `deriveMessages` -> historial nativo del proveedor -> cognicion DeepSeek -> tool loop -> accion gobernada -> despacho -> continuacion de sesion -> compactacion -> resume en turnos posteriores) con DeepSeek real + MariaDB real, en un benchmark propio de 11 turnos/5 rondas de compactacion/1 ciclo cierre-reapertura ejecutado a proposito en **tres procesos `node` separados** (prueba literal de que la memoria sobrevive al limite de proceso/request, no solo un array en memoria) sobre la MISMA conversacion via identidad determinista `waId+phoneNumberId`. Encuentra y corrige en el propio script de benchmark (nunca en codigo de produccion) un bug de truncamiento de `public_id` (`providerMessageId.slice(0,12)` en `persistCanonicalOutboundMessage`) que colisionaba con IDs sinteticos mal construidos - deuda de la herramienta de validacion, no del sistema real. Canoniza la arquitectura final (12 terminos, incluido `compactedThroughSeq` como corte de `conversation_message.id`, nunca de `agent_session_events.seq`) y clasifica formalmente `deriveToolActivityObservations` como `KNOWN_INERT_CROSS_DOMAIN_DEBT`. Decision explicita (no implicita): `BRAIN_R3_SESSION_COMPACTION_ENABLED` se mantiene `false` por defecto hasta que exista evidencia real de piloto en conversaciones largas (todo benchmark a la fecha, D7 y este, bajo un umbral artificialmente bajado) - ver seccion 21 del doc de cierre. Suite completa (`npm test`, 12 lotes): 4190 tests, 4166 pass, 24 fail, los 24 mapeados 1:1 contra categorias ya documentadas como preexistentes por D6/D7 (benchmark A13, flake de `agentSessionStoreMariaDb` en el mismo milisegundo, timing de `commercialWorkParallelExecution`, cluster de aislamiento `DATABASE_NAME` entre archivos, `customerIdentityOnboarding.e2e` bloqueado por el Customer Service no desplegado) - cero fallos nuevos en ningun archivo de V1.8. `npx tsc --noEmit`, `npm run build` (27/27 paginas) y `npm run lint` (0 errores, 40 warnings, identico a D5.2/D6/D7) limpios. Detalle completo, los 20 exit gates y la deuda restante: [SALES-AGENT-R3-V1.8-FINAL - Persistent Memory Closure](releases/SALES-AGENT-R3-V1.8-FINAL-PERSISTENT-MEMORY-CLOSURE.md). Siguiente workstream recomendado (no implementado aqui): `R3-V1.9` - Self-Recovery / Tool-Failure Resilience. `V1.8-D7` (`R3_V1_8_D7_PERSISTENT_SESSION_COMPACTION_VALIDATED`, codigo de produccion real detras de `BRAIN_R3_SESSION_COMPACTION_ENABLED`), `V1.8-D6` (`R3_V1_8_D6_LEGACY_RECENTMESSAGES_RETIREMENT_VALIDATED`), `V1.8-D5.2` (`R3_V1_8_D5_2_PROVIDER_MESSAGE_ALTERNATION_REPAIR_VALIDATED`) y `V1.8-D5.1` (caracterizacion controlada previa, cero codigo de produccion, `R3_V1_8_D5_1_B06_PERSISTENT_SESSION_REGRESSION`) siguen documentadas integramente en sus propios release docs: [SALES-AGENT-R3-V1.8-D5.1 - B06 Handoff Reproducibility Check](releases/SALES-AGENT-R3-V1.8-D5.1-B06-HANDOFF-REPRODUCIBILITY.md), [SALES-AGENT-R3-V1.8-D5.2 - Persistent Session Provider Message Alternation Repair](releases/SALES-AGENT-R3-V1.8-D5.2-PERSISTENT-SESSION-PROVIDER-MESSAGE-ALTERNATION-REPAIR.md), [SALES-AGENT-R3-V1.8-D6 - Legacy recentMessages Retirement](releases/SALES-AGENT-R3-V1.8-D6-LEGACY-RECENTMESSAGES-RETIREMENT.md), [SALES-AGENT-R3-V1.8-D7 - Persistent Session Compaction](releases/SALES-AGENT-R3-V1.8-D7-PERSISTENT-SESSION-COMPACTION.md).

**Deuda de reconciliacion (igual que la ya documentada arriba para `T02.3A`-`T02.3E` de `ACS-R1-05.1`)**: todo el trabajo `SALES-AGENT-R3-A00` a `R3-V1.2` (arquitectura objetivo, `AgentSessionStore`, Generalized Identity Gate, `CommercialActionRequest`, superficies de tool read/action, `AgentRuntimeEvent`/`FOLLOWUP_WAKE`, Runtime Opportunity Resolver, wiring perezoso de oportunidad en acciones comerciales) se ejecuto en sesiones previas sin nunca agregarse a este archivo - primera mencion de `SALES-AGENT-R3` en `ACTIVE_RELEASE.md` es esta entrada, escrita al cerrar `R3-V1.3`. No se reconstruye aqui esa historia completa (fuera de alcance de esta tarea); cada release doc individual bajo `docs/releases/SALES-AGENT-R3-*.md` sigue siendo la fuente de verdad de su propio incremento. Al momento de esta entrada, `A00`-`A05` y `V1.1` estan commiteados en `develop`; `V1.2` (`docs/releases/SALES-AGENT-R3-V1.2-lazy-opportunity-wiring.md`, 656/656 tests), `V1.3` y este mismo `V1.4` estan implementados y probados pero **sin commitear** (working tree del repo al iniciar esta tarea de V1.4).

`SALES-AGENT-R3-V1.3` (`implemented_pending_commit` - First Sales Agent Runtime) agrega `lib/brain/commercial/sales-agent-runtime/` (`salesAgentRuntime.ts` + `index.ts`), una capa fina que envuelve el Agent Tool Loop (`runAgentToolLoop.ts`, sin modificarlo) detras de un limite provider-neutral que consume `AgentRuntimeEvent` (R3-A05) y normaliza el resultado a un `SalesAgentRuntimeResult` estructurado (`status`/`responseText`/`reason`/`modelSteps`/`toolCalls`/`readToolCalls`/`commercialActionCalls`/`resolvedOpportunityId`/`finalPendingCatalogAction`/`durationMs`/`inputTokens`/`outputTokens`/`warnings`). No agrega intent/objective/workflow fijo (confirmado por `grep`, cero coincidencias); no cambia el enrutamiento real de WhatsApp (`runNativeAutonomousCycle.ts`/`runAgentRuntimeEvent.ts` intactos); no agrega capabilities nuevas; no reemplaza ni retira el Agent Tool Loop. Cierra ademas un gap real documentado por `V1.2` (`resolvedOpportunityId` nunca expuesto por `AgentLoopResult`) componiendo el seam de inyeccion `ensureOpportunity` que `V1.2` ya dejaba preparado para esto, sin tocar los ~6 puntos de retorno terminal de `runAgentToolLoop.ts`. Encontro y documento (no corrigio, fuera de alcance) un gap real de continuidad: `RecentCatalogContext` entre turnos depende de un `commercial_event` (`agent_tool_loop_completed`) que solo escribe `runNativeAgentToolLoopCycle.ts` (acoplado a `sales-agent-configuration`, deliberadamente no reimplementado aqui) - un caller que use `SalesAgentRuntime` de forma standalone hoy solo tiene continuidad cruzada de turno via `finalPendingCatalogAction` (agregado por esta tarea), no via `RecentCatalogContext`. Probado con 16 tests nuevos (`tests/commercial/salesAgentRuntime.test.ts`, MariaDB real) mas 358/358 en las suites de regresion directamente relacionadas (`agent-loop`/`agentCapabilityExposure`/`readToolRequest`/`commercialActionRequest`/`ensureCommercialActionOpportunity`/`resolveRuntimeOpportunity`/`agentRuntimeEvent`/`followUpWake`/`capabilityGatewayIdentityGate`), mas `npx tsc --noEmit` limpio. La corrida completa del repo (`npm test`, 3988 tests) devolvio 53 fallos preexistentes en subsistemas no tocados por esta tarea (identity evidence, onboarding, `link_external_identity`, customer session privacy, operational loop, checks de integridad de schema) - confirmados no relacionados corriendo uno de esos archivos en aislamiento completo (falla identico sin ningun archivo de esta tarea presente); tambien se confirmo que `agent_sessions`/`agent_session_event` (migracion `033_agent_sessions.sql`, presente en el repo) no estan aplicadas en la base de desarrollo local `main_management` - los escritos "shadow" de sesion de esta tarea degradan a un warning, nunca bloquean el turno, exactamente como esta disenado. Detalle completo, incluida la tabla de auditoria Fase 1 componente-por-componente y la traza real capturada: [SALES-AGENT-R3-V1.3 - First Sales Agent Runtime](releases/SALES-AGENT-R3-V1.3-first-sales-agent-runtime.md). Sin commit ni push al momento de esta entrada (instruccion explicita de la tarea).

`SALES-AGENT-R3-V1.4` (`implemented_pending_commit` - Flagged Runtime Routing + Canonical Response Dispatch) hace que `SalesAgentRuntime` (V1.3) sea alcanzable desde el camino real de WhatsApp, detras de un flag + allowlist propios (`BRAIN_SALES_AGENT_RUNTIME_ENABLED` / `BRAIN_SALES_AGENT_RUNTIME_WA_IDS`, default `false`/vacio - cero trafico general). El punto de insercion real es `runNativeAutonomousCycle.ts` (Step 4/5), con prioridad **mas baja** entre las cuatro ramas de seleccion de runtime (despues de CommercialWork/multi-request/Agent Tool Loop) para que este piloto nuevo nunca le robe silenciosamente un wa_id ya asignado a un piloto mas maduro. Hallazgo de auditoria que corrige la propia nota de V1.3 ("proximo task: conectar via runAgentRuntimeEvent.ts"): `runAgentRuntimeEvent.ts` no esta en el camino real de WhatsApp (su unico caller de produccion es `runFollowupTick.ts`, solo para `FOLLOWUP_WAKE`) - `ensureAutonomousSalesTurnContinuity.ts` llama `runNativeAutonomousCycle` directamente, así que conectar ahi habria sido codigo muerto. Nuevo archivo `runSalesAgentRuntimeCycle.ts` (adaptador de despacho, fuera del modulo `SalesAgentRuntime` mismo) reutiliza sin modificar `dispatchAgentLoopResponse` (outbox canonico), reutiliza el resolver real `resolveSalesAgentConfiguration()` + `createHttpAgentLoopProvider` (mismo camino que ATL) y cierra el gap documentado de V1.3 (`RecentCatalogContext` entre turnos) reutilizando `recordAgentToolLoopCompletedCommercialEvent` con datos de configuracion reales, nunca fabricados - probado por test, no solo por inspeccion. `ensureAutonomousSalesTurnContinuity.ts` gano una rama nueva (`cycle.salesAgentRuntime`) espejando la existente de `cycle.agentLoop`. Probado con 14 tests nuevos (`tests/commercial/shouldRouteToSalesAgentRuntime.test.ts` 7, `tests/commercial/runSalesAgentRuntimeCycle.test.ts` 7, MariaDB real) mas 242/242 en las suites de regresion directamente relacionadas (R3 completo, Agent Tool Loop, multi-request/multi-intent, CommercialWork, continuity, native-whatsapp), mas `npx tsc --noEmit` limpio, mas una llamada real a DeepSeek a traves de la nueva ruta (Fase 18: consulta de solo lectura "que barras olimpicas tienen?", respuesta correctamente grounded, outbox canonico escrito, cero envio real a Meta - el Catalog Service real no estaba accesible en este entorno, se uso un mock HTTP local solo para esa pieza). Deuda explicita: `toolsUsed`/`stepsSummary` quedan vacios (`[]`) en el evento `agent_tool_loop_completed` de R3 (SalesAgentRuntimeResult no expone detalle por tool por diseno de V1.3); `agent_sessions`/`agent_session_event` siguen sin aplicarse en la base de desarrollo local (gap preexistente, no bloqueante). Detalle completo: [SALES-AGENT-R3-V1.4 - Flagged Runtime Routing](releases/SALES-AGENT-R3-V1.4-flagged-runtime-routing.md). Sin commit ni push al momento de esta entrada (instruccion explicita de la tarea).

`SALES-AGENT-R3-V1.5` (`implemented_pending_commit` - Native Response Dispatch) responde a evidencia de produccion real (mensaje inbound 172 del piloto WhatsApp): un turno `terminalReason=responded` de `SalesAgentRuntime` con `finalMessage` presente terminaba en `sales_agent_runtime_dispatch_failed:responded:agent_tool_loop_gate_disabled` porque `dispatchAgentLoopResponse.ts` (reutilizado sin cambios por `V1.4`) exige el stack completo de accion R1 (`crm_agent_actions`/`autonomy-sandbox`/`execution-gate`) incluso para una respuesta conversacional que no es una mutacion comercial. Esta tarea agrega un nuevo despachador nativo, `lib/brain/commercial/sales-agent-runtime/dispatchSalesAgentResponse.ts`, usado por `runSalesAgentRuntimeCycle.ts` **solo** cuando `terminalReason === "responded"` (todo otro terminal reason sigue exactamente igual por `dispatchAgentLoopResponse.ts`, sin modificar - Scope Guard explicito de la tarea). El despachador nuevo nunca crea una fila `crm_agent_actions`: aplica gobernanza deterministica propia (mensaje no vacio, `humanOwnerActive`/`aiBlocked` del snapshot de inicio de turno, `BRAIN_AUTONOMOUS_RESPONSES_ENABLED`, el mismo allowlist de piloto `isWaIdAuthorizedForPilot`/`BRAIN_AUTONOMOUS_TEST_WA_IDS` que ya usan las rutas hermanas, `waId` valido) y despues, **dentro de la misma transaccion que el insert**, relee `conversation.human_owner_active`/`ai_enabled`/`status` con `FOR UPDATE` antes de escribir - cierra la carrera real que la propia tarea exigio como invariante obligatorio (un operador toma control mientras el modelo razona no debe dejar encolada una respuesta obsoleta), probado con dos tests dedicados (`[D9]`/`[D9b]`) donde el snapshot de inicio de turno dice `false` pero la fila real en DB ya dice lo contrario. Reutiliza `writeCanonicalOutboxMessage` (`lib/brain/messaging/canonicalOutboxWriter.ts`, ya documentado como el unico escritor permitido de `brain_message_outbox`, con `INSERT IGNORE` sobre `dedupe_key` unico) pasandole la conexion abierta del despachador - nunca una segunda cola/tabla de salida. Clave de dedupe nueva y legible: `sales-agent-r3:{conversationId}:{inboundMessageId}:responded`. Agrega un tipo de `commercial_event` nuevo, `sales_agent_runtime_response_dispatched` (un evento por intento de despacho, exito o skip gobernado, mismo saneador que ya rechaza claves de razonamiento/cadena de pensamiento). Probado con 12 tests nuevos (`tests/commercial/dispatchSalesAgentResponse.test.ts`, MariaDB real, incluye `[D10]`: una corrida real de `runOutboxTick({dryRun:true})` sobre la fila escrita confirma que el worker existente la procesa sin cancelarla) mas actualizacion de 3 tests existentes que asumian una fila `crm_agent_actions` para un turno `responded` (`[RC1]`/`[RC5]`/`[RC7]` en `tests/commercial/runSalesAgentRuntimeCycle.test.ts`, ahora verifican la fila de outbox canonico directamente y `cero` filas `crm_agent_actions`) - **105/105 tests en verde** (12 nuevos + 8 modificados/no modificados de ese archivo + 85 de regresion dirigida: `dispatchAgentLoopResponse`/outbox/ATL/CommercialWork/continuity/native-whatsapp, todos sin cambios de comportamiento), mas `npx tsc --noEmit`, `npm run build` y `npm run lint` limpios (0 errores; 39 warnings preexistentes ajenos a esta tarea). Verdict: `R3_V1_5_NATIVE_RESPONSE_DISPATCH_VALIDATED`. Detalle completo: [SALES-AGENT-R3-V1.5 - Native Response Dispatch](releases/SALES-AGENT-R3-V1.5-native-response-dispatch.md). Sin commit ni push al momento de esta entrada (instruccion explicita de la tarea).

`SALES-AGENT-R3-V1.7` (`implemented` - Runtime Dependency & Legacy Isolation Audit, no production code changed) es una tarea de auditoria, no una feature ni un refactor amplio: reconstruye el grafo real de llamadas/imports desde `app/api/integrations/whatsapp/webhook/route.ts` hasta `brain_message_outbox`, enumera las cuatro ramas de seleccion de runtime dentro de `runNativeAutonomousCycle.ts` (CommercialWork -> multi-request -> Agent Tool Loop -> SalesAgentRuntime, en ese orden estricto, con el pipeline legacy shadow/loop/bridge como fallback final) y confirma que `SalesAgentRuntime`/`runSalesAgentRuntimeCycle.ts`/el terminal dispatch boundary de R3 no tienen dependencia real (directa ni transitiva en tiempo de ejecucion) hacia `action-queue/**`, `autonomy-sandbox/**` (logica de evaluacion), `execution-gate/**`, `dispatchAgentLoopResponse`, CommercialWork, multi-request, multi-intent o el motor legacy `sales-consultative`. Encuentra y documenta (no corrige, no es un bug de routing) una unica excepcion real y acotada: dos archivos de despacho de R3 (`dispatchGovernedSalesAgentMessage.ts`/`dispatchSalesAgentHardHandoff.ts`) importan `normalizeWaIdDigits` desde el barrel `autonomy-sandbox` - un helper puro de normalizacion de digitos, sin persistencia, sin logica de ruteo, sin gate legado - clasificado `KEEP_SHARED` segun la propia excepcion de la tarea para helpers puros compartidos, y ahora blindado por un test dedicado (`[ISO3]`) que falla si esa importacion crece mas alla de exactamente ese simbolo en exactamente esos dos archivos. Clasifica cada modulo relevante como `R3_NATIVE`/`KEEP_SHARED`/`LEGACY_ONLY`/`DEAD_CANDIDATE` (`UNKNOWN` no fue necesario), construye la matriz completa de feature flags (R3, legacy, y el stack de despacho R1) con el set exacto recomendado para el piloto EC2, y agrega dos archivos de test nuevos: `tests/commercial/salesAgentR3RuntimeIsolationAuthority.test.ts` (5 tests estaticos, sin DB - escanea todo el directorio `sales-agent-runtime/**`, no solo los 5 archivos que V1.6 ya cubria, contra una lista ampliada de identificadores prohibidos, mas el orden de precedencia de las cuatro ramas y la prueba estructural de que la rama R3 de `ensureAutonomousSalesTurnContinuity.ts` nunca alcanza `dispatchFallbackAction`) y `tests/commercial/salesAgentR3PilotRoutingAuthority.test.ts` (3 tests contra MariaDB real `crm_test`, sin red ni Meta - prueban que con CommercialWork/multi-request/ATL/multi-intent/legacy apagados y R3 encendido+allowlisted, `runNativeAutonomousCycle` enruta exclusivamente a `SalesAgentRuntime`, incluso con el stack completo de despacho R1 apagado, con cero filas `crm_agent_actions` creadas). No se encontro ningun defecto de routing real; no se corrigio ni se modifico codigo de produccion. Deuda nueva registrada (no bloqueante): `BRAIN_MULTI_REQUEST_RUNTIME_ENABLED`/`BRAIN_AGENT_TOOL_LOOP_ENABLED` son flags globales sin allowlist propio (a diferencia de CommercialWork/R3) - dejarlos en `true` en produccion precederia a R3 para toda conversacion, sin evidencia de que esto ocurra hoy fuera de tests. Tambien se confirmo, reproducido en aislamiento contra el arbol limpio, un fallo preexistente no relacionado (`agentSessionStoreMariaDb.test.ts`, ordenamiento de eventos en el mismo milisegundo). Validado con `npx tsc --noEmit` limpio, `npm run build` limpio, `npm run lint` (0 errores, 39 warnings preexistentes identicos a V1.5/V1.6, ninguno en archivos tocados por esta tarea) y regresion dirigida (297 tests en las suites de native WhatsApp/native-cycle/SalesAgentRuntime/R3 dispatch/AgentSessionStore/ReadToolGateway/CommercialActionRequest/Capability Gateway/canonical outbox/outbox ownership/CommercialWork/ATL/legacy routing authority, 296/297 en verde - el unico fallo es el preexistente ya mencionado, no relacionado con esta tarea) mas los 8 tests nuevos propios (8/8 en verde). Verdict: `R3_V1_7_RUNTIME_ISOLATION_VALIDATED`. Detalle completo: [SALES-AGENT-R3-V1.7 - Runtime Dependency & Legacy Isolation Audit](releases/SALES-AGENT-R3-V1.7-runtime-dependency-legacy-isolation-audit.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-A` (`implemented` - Conversational Context Input Audit, no production code changed) responde la pregunta binaria planteada por la tarea - "¿R3 hoy reconstruye una conversacion, o solo reconstruye estado operacional alrededor del mensaje actual?" - trazando en codigo, no por inferencia, exactamente que recibe el modelo en cada llamada al provider DeepSeek (`buildAgentStepPromptPackage.ts` -> `httpAgentLoopProvider.ts`, mensajes `system`+`user` reconstruidos desde cero en cada llamada, nunca un array de chat acumulado, cero mensajes `role:"assistant"`). Verdict: `R3_CONTEXT_CONTINUITY_PARTIAL`. Hallazgo central: existen cuatro mecanismos reales de continuidad entre turnos - una cola de los ultimos 5 mensajes crudos (`direction`+`body` solamente, sin rol explicito, recortada de una lectura SQL sin `LIMIT` de `conversation_message` en dos pasos de corte fijo por cantidad, `COMMERCIAL_CONTEXT_MAX_RECENT_MESSAGES=12` luego `.slice(-5)`), `RecentCatalogContext` (identidad/posicion de producto, nunca precio/stock/evidencia comercial), `pendingCatalogAction` (la unica primitiva de continuidad estructurada activamente exigida por el prompt y el evidence-gate de `runAgentToolLoop.ts`) y el estado comercial durable (`opportunity`/`needProfile`/`shippingDestination`/`commercialLineItems`, siempre releido fresco desde SQL) - pero `AgentSessionStore`, el unico componente cuyo nombre y esquema prometen memoria semantica de sesion, es escritura pura: `loadSummary`/`rebuildSummary`/`loadRecentEvents` existen y estan probados pero tienen cero callers de produccion (confirmado por grep exhaustivo sobre `lib/`, `app/`, `scripts/`), asi que `agent_sessions.summary_json` permanece `NULL` no por un bug puntual del conversation_id=83 citado en la tarea sino porque ninguna ruta de codigo real jamas invoca la funcion que lo poblaria. Root cause del caso turno 180->182 clasificado explicitamente por nivel de evidencia (PROVEN/LIKELY/NOT PROVEN, nunca mezclados): PROVEN que no existe ningun campo de "topic/intent anchor" explicito en el payload del modelo; LIKELY (no verificable sin logs reales de proveedor) que el texto crudo de los turnos 178/180 seguia dentro de la ventana de 5 mensajes en el turno 182 y el problema fue de saliencia/ponderacion (enterrado en un JSON grande, sin rol ni recencia explicita) frente a una frase de cliente que ampliaba el alcance ("varias opciones para home gym"), no una perdida por truncamiento. Explica ademas, como hallazgo lateral pedido por la tarea, por que `agent_tool_loop_completed` muestra `toolsUsed=[]`/`stepsSummary=[]` para turnos R3 pese a que `agent_session_events` sí muestra ejecucion real de tools: `SalesAgentRuntimeResult` (V1.3, decision de diseno) solo expone conteos agregados, nunca el detalle por tool, mientras `agent_session_events` lo escribe desde dos rutas independientes (`read-tool-request/sessionEvents.ts`, `commercial-action-request/sessionEvents.ts`) que sí tienen el nombre de la tool en alcance. Cuatro recomendaciones minimas para un futuro `V1.8-B` quedan documentadas (nunca implementadas aqui): dar mayor saliencia/rol explicito a la cola de 5 mensajes; agregar un "topic hint" advisory de una linea derivado de datos ya durables; conectar `rebuildSummary` para que `summary_json` deje de ser codigo muerto; no tocar `pendingCatalogAction`/`RecentCatalogContext`, que ya funcionan como fueron disenados - ninguna introduce workflow engine, maquina de estados de intent, ni constraints conversacionales obligatorias (confirmado explicitamente como non-goal). Validado con `npx tsc --noEmit` limpio y regresion dirigida contra MariaDB real: `tests/commercial/salesAgentRuntime.test.ts`+`runSalesAgentRuntimeCycle.test.ts` 26/26, `tests/agent-loop/runAgentToolLoop.test.ts` 108/108, suite de `AgentSessionStore` 57/58 (el unico fallo es el mismo flake preexistente de ordenamiento por milisegundo en `agentSessionStoreMariaDb.test.ts` que V1.7 ya documento, no relacionado, no corregido aqui). No se agregaron tests nuevos (la suite existente ya prueba cada ruta trazada, incluido un escenario dedicado de continuidad "la segunda" via `pendingCatalogAction`). No se cambio codigo de produccion, schema, flag ni prompt. Verdict: `R3_V1_8_A_CONTEXT_AUDIT_COMPLETE`. Detalle completo: [SALES-AGENT-R3-V1.8-A - Conversational Context Input Audit](releases/SALES-AGENT-R3-V1.8-A-CONVERSATIONAL-CONTEXT-INPUT-AUDIT.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-B` (`implemented` - Harness Native Session Continuity Audit, no production code changed) responde la pregunta central planteada por la tarea - "¿R3 usa el Harness oficial (`@deepseek-ai/dsh`) como sesion persistente multi-turn, o solo reutiliza su tool/reasoning loop de forma stateless?" - con un hallazgo que reencuadra la pregunta: **R3 no usa el Harness en absoluto hoy**, ni bien ni mal - cero imports de `@deepseek-ai/*` en `lib/`/`app/`, cero entrada en el `package.json`/`package-lock.json` del repo principal (confirmado por grep exhaustivo). El Harness existe unicamente dentro de `experiments/deepseek-harness/`, un sandbox aislado (su propio `package.json`/`node_modules`, excluido explicitamente de `tsconfig.json`) construido para el bake-off `SALES-AGENT-R2-A13-H0` (`docs/architecture/A13-H0-deepseek-harness-bakeoff.md`), cuyo propio veredicto (`HYBRIDIZE_WITH_HARNESS`) ya habia decidido adoptar el patron (loop iterativo de tools + sesion persistente + prompt caching) y no el paquete, precisamente porque `runAgentToolLoop.ts` ya existia como el runtime nativo equivalente. Verdict de este audit: `HARNESS_SESSION_CONTINUITY_NATIVE_FULL` para la capacidad nativa del paquete instalado (`@deepseek-ai/dsh@0.1.1-rc.2`, developer preview), probado por codigo fuente - no inferido -: `dsh-session-persistence-jsonl` y `dsh-session-checkpoint-policy` vienen montados por defecto en el propio `cordis.patch.yml` de `dsh-base`, y `AgentRegistry.resume({resumeSessionId})` es un metodo publico documentado que carga una sesion persistida en un proceso nuevo - LEVEL 4 de durabilidad (el maximo de la rubrica de la tarea), aunque nunca invocado en vivo ni en este audit ni en `A13-H0` (ambos deshabilitaron esa capa para sus propios sandboxes; queda marcado explicitamente como "probado por fuente, no por ejecucion propia"). Evidencia en vivo nueva, no solo lectura de tipos: se agrego un escenario de caracterizacion (`V18B-01`, unico agregado aditivo a `scenarios/bakeoff-scenarios.json`, las 20 escenarios `H0-*` existentes intactas) con los 4 turnos exactos pedidos por la tarea (barra olimpica 20kg -> "muestrame varias opciones" -> colchonetas -> "volvamos a la barra anterior"), corrido de verdad contra la API real de DeepSeek (`deepseek-v4-flash`, Node 24.14.0 via el cache `nvm4w` ya presente en la maquina, sin tocar el Node por defecto del shell). Resultado real: el turno 2 (analogo directo al turno 182 de produccion que V1.8-A investigo) se mantuvo anclado al tema barra olimpica (presento ambas opciones 15kg/20kg) en vez de ampliar a equipamiento generico de home gym, y el turno 4 recordo ambas opciones sin volver a buscar, tras un desvio real a un topico no relacionado (colchonetas, cero resultados, cero alucinacion) - prueba directa de que el patron de sesion nativa del Harness evita exactamente el modo de falla documentado en V1.8-A. Evidencia de tokens/cache (`cacheReadTokens` creciendo monotonicamente 640->3328 a lo largo de 8 llamadas reales, `request/header`/`request/context` emitidos una sola vez en las 4 turnos) confirma que la sesion se sirve como un prefijo cacheable que crece de forma incremental, nunca reconstruido desde cero como hace `buildAgentStepPromptPackage.ts` hoy. Comparacion con `AgentSessionStore` de R3: **no hay duplicacion** - `AgentSessionStore` nunca fue diseñado para reconstruir contexto de modelo (su propio docstring: "nunca una segunda fuente de verdad... nunca registra texto de mensaje"), es correctamente clasificado **D: util solo como adaptador de persistencia/audit trail**, con su propio trabajo mas angosto ya sub-realizado (hallazgo de V1.8-A, no relacionado con esta comparacion). Direccion recomendada para un futuro `V1.8-C` (no implementada aqui, dos ejes independientes): Eje 1, no adoptar el paquete externo en produccion (re-afirma `A13-H0` - problemas operacionales de un developer preview de 2 dias de antiguedad no re-verificados); Eje 2, sí diseñar un objeto de sesion propio de R3 (dentro de `runAgentToolLoop`/`salesAgentRuntime`) que crezca turno a turno y del cual el modelo lea incrementalmente, en vez de reconstruir el prompt desde cero cada llamada - inspirado en el patron ya validado del Harness (log append-only + `deriveMessages()`-style derivacion + compaccion solo cuando se necesite), nunca en el paquete mismo. Confirma explicitamente que nada de esto requiere `currentTopic`/`currentIntent`, workflow engine, ni un segundo sistema de summary paralelo a `AgentSessionStore` (extender el `AgentSessionSummary` ya existente, si acaso). Validado con `npx tsc --noEmit` limpio (el sandbox `experiments/deepseek-harness/**` esta excluido de `tsconfig.json`, cero impacto en el build principal); no se re-corrio la suite de regresion de R3 (cero codigo de produccion cambiado). Verdict: `R3_V1_8_B_HARNESS_AUDIT_COMPLETE`. Detalle completo: [SALES-AGENT-R3-V1.8-B - Harness Native Session Continuity Audit](releases/SALES-AGENT-R3-V1.8-B-HARNESS-NATIVE-SESSION-CONTINUITY-AUDIT.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-C0` (`implemented` - Decision Gate: Harness Production Adoption Re-evaluation, no production code changed) responde la pregunta que `V1.8-B` dejo abierta explicitamente para un futuro `V1.8-C`: dado que R3 no usa el Harness en absoluto hoy y que su patron de sesion nativa ya demostro valor en vivo (`V18B-01`), ¿corresponde ahora adoptar el paquete externo `@deepseek-ai/dsh` en produccion (Opcion B), ponerlo detras de un adapter interno (Opcion C), o seguir construyendo la sesion propia de R3 sobre el patron ya validado (Opcion A)? A diferencia de `A13-H0`/`V1.8-B`, esta tarea no se apoyo solo en el snapshot de `node_modules` ya leido: consulto en vivo el registro publico de npm y GitHub el mismo dia de la tarea (2026-09-01) y encontro evidencia nueva, fechada y decisiva: la API exacta sobre la que `V1.8-B` construyo toda su evidencia en vivo (`agent.session.events`) ya fue reemplazada por una API on-demand (`seq`/`eventAt()`/`snapshotEvents()`) en `@deepseek-ai/dsh-agent@0.1.2-alpha.4`, publicada el mismo dia de esta re-evaluacion, apenas diez dias despues de la version instalada en el sandbox (`0.1.1-rc.2`, 21-ago-2026) - cuatro releases en cinco dias solo en el canal `alpha`, que ademas corre adelante del canal `next`/`rc` con cambios incompatibles (orden invertido respecto de una progresion de estabilizacion normal). El tag `latest` de npm sigue roto exactamente como `A13-H0` ya documento (resuelve a `0.0.1-rc.1`, sin fix). Tambien se leyo el codigo real de la frontera de produccion que un adapter tendria que respetar: `AgentLoopProvider` (`lib/brain/commercial/agent-loop/agentLoopProviderTypes.ts`) ya es provider-neutral por diseno (mensajes `{role,content}` planos, sin concepto DeepSeek/Cordis), y `CommercialActionRequestSource` (`lib/brain/commercial/commercial-action-request/types.ts:16`) ya reserva el literal `"sales_agent_harness"` desde el propio diseno de `R3-A03` - sin ningun emisor en produccion (grep exhaustivo) - evidencia de que la frontera ya fue disenada pensando en una futura fuente basada en Harness, sin nunca comprometerse a construirla. Verdict primario: `CONTINUE_NATIVE_R3_HARNESS_PATTERN` (mantener `runAgentToolLoop`/`AgentLoopProvider`/Capability Gateway, disenar a continuacion el objeto de sesion cruzada-de-turno que `V1.8-B` ya dejo scoped en su Eje 2); verdict secundario: `DEFER_HARNESS_ADOPTION_UNTIL_STABLE` (reabrir solo si el paquete llega a un release estable con `latest` funcional y sin romper `Agent`/`Session` por 4-6 semanas). La matriz de decision completa (16 dimensiones x 3 opciones, `STRONG`/`GOOD`/`ACCEPTABLE`/`WEAK`/`POOR` con justificacion) identifica la Opcion C (adapter) como la de mayor riesgo real de terminar construyendo un segundo framework dentro de PesasChile, porque un adapter creible hoy no puede retirar su fallback nativo (dado el churn de API) y por lo tanto duplica superficie de mantenimiento en vez de reemplazarla. Dos brechas quedan documentadas como explicitamente no probadas (ni por esta tarea ni por las anteriores): (1) una mutacion tipo `CommercialActionRequest` nunca fue expuesta como tool del Harness ni rechazada por un Capability Gateway simulado - las cuatro tools de los bake-offs previos son deliberadamente de solo lectura; (2) `AgentRegistry.resume()` nunca fue invocado en ningun bake-off/audit - la durabilidad "LEVEL 4" de `V1.8-B` sigue siendo prueba por fuente, no por ejecucion, y ahora con el agravante de que el propio formato de persistencia del paquete ya rompio compatibilidad una vez (SQLite, `0.1.2-alpha.1`) antes de que se rompiera tambien la API del log de eventos. Ambas quedan explicitamente fuera de alcance de esta tarea (decision ya resuelta sin necesitarlas, gastar cupo real de API de DeepSeek para probarlas ahora seria alcance fuera de lo que pide este gate) y nombradas como los dos primeros spikes a correr si la Opcion B/C se reabre alguna vez. No se instalo, modifico ni ejecuto codigo de produccion; no se toco `runAgentToolLoop`, Capability Gateway, ni ningun store de dominio. Verdict: `R3_V1_8_C0_HARNESS_REEVALUATION_COMPLETE`. Detalle completo: [SALES-AGENT-R3-V1.8-C0 - Harness Production Adoption Re-evaluation](releases/SALES-AGENT-R3-V1.8-C0-HARNESS-PRODUCTION-ADOPTION-REEVALUATION.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-C` (`implemented` - Native Persistent Agent Session / Memory Design, solo diseno, no implementacion, no codigo de produccion) responde el mandato que `V1.8-C0` dejo como siguiente paso: disenar como trasladar la semantica de sesion del Harness (persistente, append-only, resumible, derive-on-read, cache-friendly, compaction-capable) a la topologia real de R3 (webhook stateless, MariaDB, Capability Gateway) sin reimplementar el paquete ni construir una maquina de estados conversacional. El hallazgo central, verificado leyendo el codigo real (no asumido): R3 ya tiene casi todo el sustrato necesario. `agent_sessions`/`agent_session_events` (migracion `033_agent_sessions.sql`, `SALES-AGENT-R3-A01`) ya son una identidad de sesion estable 1:1 con `conversation_id`, un log append-only con `seq` monotonico real, dedupe idempotente a nivel de base de datos y correlation/causation tracking - exactamente la forma que este task pedia disenar desde cero. Lo que falta es mas angosto de lo que el brief sugeria: (a) un lado de lectura que no existe hoy (nada deriva mensajes de proveedor desde este log), (b) dos campos de payload hoy retenidos por un limite de diseno explicito y todavia correcto (texto de mensaje, detalle de tool) que este diseno extiende sin violar, y (c) un artefacto genuinamente nuevo (cache de compactacion, tres columnas nuevas en `agent_sessions`, nunca reutilizando `summary_json` que ya tiene su propio trabajo mas angosto). Decision arquitectonica explicita: extender la estructura existente (Opcion D del propio brief), nunca un store nuevo ni un motor de persistencia paralelo (ADR-009). La pieza nueva mas importante es `deriveMessages()` (Seccion 7 del documento): siete slots ordenados (system estable, identidad/config, prefijo compactado, historial append-only con mensajes `role:"assistant"` reales por primera vez - hallazgo central de `V1.8-A` finalmente resuelto -, contexto comercial fresco en su propio slot nunca enterrado en mensajes viejos, turno actual, observaciones de tools del turno actual) que reemplaza el blob JSON unico y mutable que `buildAgentStepPromptPackage.ts` reconstruye hoy desde cero en cada llamada (`V1.8-A` seccion 4) - el mecanismo concreto detras de la ganancia de cache que `V18B-01` ya midio en vivo. Decision notable que el propio documento justifica y no da por sentada: se evaluo agregar `TURN_STARTED`/`TURN_COMPLETED`/`SESSION_RESUMED` como tipos de evento nuevos (candidatos del propio brief) y se descarto - `USER_MESSAGE_RECEIVED` (reubicado antes del loop, no despues como hoy) y `ASSISTANT_MESSAGE_SENT` (ya escrito siempre, cualquier resultado) ya cumplen ese rol sin duplicar vocabulario, y `SESSION_RESUMED` queda como metrica de observabilidad, nunca como evento durable, porque en este diseno todo turno "resume" por construccion (nunca existe un objeto `Agent` en memoria entre requests, a diferencia del Harness) - la simplificacion mas importante de este diseno frente al propio modelo del Harness, y la que evita de raiz el blocker de topologia de produccion que `V1.8-C0` seccion 9 identifico. Concurrencia: reutiliza sin modificar el patron ya probado de `work/sequencing.ts` (`GET_LOCK` + `FOR UPDATE` + contador monotonico), nunca un mecanismo de lock nuevo, con una salvedad explicita registrada como riesgo (verificar si `ensureAutonomousSalesTurnContinuity` ya serializa por conversacion antes de agregar el lock). Checkpointing: los cinco checkpoints pedidos por el brief mapean a escrituras ya idempotentes y ya atomicas individualmente (dedupe key existente por `inboundMessageId`/`stepIndex`/`tool`/`requestId`), sin necesitar ninguna transaccion distribuida nueva - misma logica que `ACS-R1-05-T07` ya valido para el pipeline reactivo hermano. `RecentCatalogContext`/`pendingCatalogAction`/`conversation_message`/Capability Gateway quedan explicitamente `UNCHANGED`, con la comparacion de las cuatro opciones de relacion con `conversation_message` documentada (Seccion 15): se elige referenciar por id y materializar texto en tiempo de lectura de `deriveMessages()`, nunca duplicar. Plan de migracion aditivo de siete slices (`D1` contratos/persistencia hasta `D7` compactacion), cada uno con rollback propio, sin reemplazo big-bang, con `D6` (remover la cola de ultimos 5 mensajes) explicitamente condicionado a evidencia real de `D5`, nunca asumido de antemano. Backfill: ninguno global - `ensureSession()` ya es perezoso/idempotente, y las conversaciones preexistentes reciben una compactacion inicial acotada derivada de `conversation_message` en su primer turno post-diseno, nunca un job masivo. Cinco riesgos quedan registrados explicitamente, ninguno bloqueante: semantica de fallo de escritura de sesion debe cambiar (hoy degrada a warning silencioso, aceptable solo mientras nada leia el log de vuelta); verificacion pendiente de si ya existe un lock de conversacion upstream; umbrales de compactacion genuinamente desconocidos (ninguna conversacion real observada en las auditorias previas fue lo bastante larga para dispararla); campo de cache tokens del proveedor no verificado en `httpAgentLoopProvider.ts`; lifecycle de cierre de `conversation` no leido en esta tarea. No se creo ninguna migracion, modulo `.ts`, flag ni test - documento de diseno unicamente, sin tocar Capability Gateway, stores de dominio, ni el escritor de `conversation_message`. Verdict: `R3_NATIVE_PERSISTENT_SESSION_DESIGN_READY`. Detalle completo: [SALES-AGENT-R3-V1.8-C - Native Persistent Agent Session / Memory Design](releases/SALES-AGENT-R3-V1.8-C-NATIVE-PERSISTENT-AGENT-SESSION-MEMORY-DESIGN.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-D0` (`implemented` - Persistent Session Implementation Preflight, solo auditoria, no implementacion, no codigo de produccion) cierra las cuatro incertidumbres operacionales que `V1.8-C` dejo explicitamente abiertas antes de implementar `D1`, mas dos adicionales que el propio `V1.8-C` habia registrado como riesgo (semantica de fallo de escritura de sesion, viabilidad de backfill) - las seis con evidencia de codigo real, sin ninguna quedar "a decidir en implementacion". Concurrencia: se leyo completo `ensureAutonomousSalesTurnContinuity.ts` (sin ningun lock) y las tres unicas implementaciones reales de `GET_LOCK` en el repo (`resolveRuntimeOpportunity.ts`, `persistAgentAction.ts`, `work/sequencing.ts`) - las tres comparten una propiedad estructural que `V1.8-C` no habia verificado: ninguna se sostiene durante una llamada al proveedor, todas se liberan tras una transaccion corta solo-DB. Un mensaje duplicado de Meta ya esta completamente resuelto rio arriba (`processNativeWhatsAppInbound` chequea `loadConversationMessageByProviderMessageId` antes de crear cualquier fila, respaldado por `UNIQUE KEY uq_provider_message`) - pero dos mensajes reales y distintos para la misma conversacion llegando casi simultaneos NO estan serializados hoy alrededor de la llamada al LLM (el webhook corre sincrono, sin cola). Clasificacion: `PARTIAL_SERIALIZATION_NEEDS_SESSION_GUARD`. Correccion real al diseno de `V1.8-C` seccion 12: sostener un `GET_LOCK` durante una llamada LLM de 10-30+ segundos seria un patron sin precedente en este repo (riesgo real de agotamiento del pool de conexiones, timeout de 10s de todo lock existente muy por debajo de esa duracion) - la recomendacion corregida es un lock corto (mismo shape que `resolveRuntimeOpportunity`) solo alrededor de "resolver sesion + derivar el prefijo de este turno", liberado antes de invocar el modelo, confiando en el mecanismo de append idempotente por dedupe key (ya implementado y probado) como red de seguridad para el interleaving residual. Lifecycle de `conversation`: se leyo completo `lib/domains/conversations/control.ts` (unico escritor real de `conversation.status` en todo el repo, confirmado por grep) - exactamente dos estados reales escritos (`open`/`closed`), independientes de `ai_enabled`/`human_owner_active`, con `reopen` siempre reutilizando la misma fila/mismo `id` (nunca crea una segunda `conversation`) - lo que fuerza por evidencia, no por preferencia conceptual, que reabrir una conversacion debe reusar la misma `agent_session`, nunca crear otra. Politica seleccionada: `SESSION_MIRRORS_CONVERSATION_STATUS` (agregar `UPDATE agent_sessions SET status=...` dentro de las mismas transacciones ya existentes de `close`/`reopen` en `control.ts`, sin tabla ni lock nuevo). Sanitizer: se ejecuto literalmente (Node, no solo lectura manual) el regex real de `events/normalize.ts` (`SENSITIVE_KEY_PATTERN`) y el de `agent-session/sanitizer.ts` (`AGENT_SESSION_PII_KEY_PATTERN`) contra los cuatro campos propuestos por `V1.8-C` - `outboundMessagePublicId`/`fromSeq`/`toSeq` pasan ambas capas, pero **`summaryTokenEstimate` es rechazado** (contiene la subcadena prohibida `token`) - hallazgo real que `V1.8-C` no habia verificado pese a haberlo marcado explicitamente como pendiente de verificar. Nombre alternativo aprobado y verificado: `summaryEstimatedSize` (seguido de la convencion ya existente en este mismo repo: `effectiveMaxOutputSize`, nunca `...Token(s)`). Cache de DeepSeek: en vez de asumir, se hizo una llamada real y aislada (script descartable en scratchpad, nunca agregado al repo, sin loggear la API key) contra el endpoint real configurado en `.env` - el objeto `usage` real expone `prompt_cache_hit_tokens`/`prompt_cache_miss_tokens` (nombres exactos que el propio brief de la tarea sugeria) ademas de la forma anidada equivalente `prompt_tokens_details.cached_tokens` - ninguno de los dos leido hoy por `httpAgentLoopProvider.ts`. Clasificacion: `CACHE_METRICS_AVAILABLE`; campos recomendados para agregar a `AgentLoopProviderResponse`: `cacheReadTokens`/`cacheMissTokens`, leidos de los campos top-level (mas simples que la forma anidada). Fallo de escritura de sesion: se leyo el call site real (`salesAgentRuntime.ts:241-254`) - un fallo de `appendEvent` hoy solo agrega un string a `warnings`, el turno puede terminar `responded` igual (`status` se computa de `loop.terminalReason`, nunca de `shadowResult.ok`), sin retry, sin tipo de error mas alla de `AgentSessionForbiddenPayloadError`. Recomendacion para `D2`/`D5`: `RETRY_THEN_DEGRADE` asimetrico - degradar a contexto legacy en fallo de lectura (nunca `FAIL_CLOSED`, justificado con evidencia repetida en este repo de que ningun turno debe terminar en silencio), un retry corto y acotado antes de degradar-y-continuar en fallo de escritura (cambio real respecto de hoy, que no reintenta nunca). Backfill: viable (`BOUNDED_BOOTSTRAP_VIABLE`) pero con una correccion concreta - la query de historial ya existente (`loadNativeConversationDetailByPublicId`) es sin `LIMIT` (full scan), nunca debe reusarse para el bootstrap; usar en su lugar una query acotada por `LIMIT` con el mismo shape ya probado de `loadRecentEvents` (`ORDER BY ... DESC LIMIT N` + reverse en JS), sobre el indice ya existente `idx_message_conversation_created`. No se creo ninguna migracion, modulo `.ts`, flag ni test en el repo - solo un script descartable fuera del repo para la llamada aislada al proveedor. Verdict: `R3_PERSISTENT_SESSION_PREFLIGHT_READY_WITH_CONSTRAINTS`. Detalle completo: [SALES-AGENT-R3-V1.8-D0 - Persistent Session Implementation Preflight](releases/SALES-AGENT-R3-V1.8-D0-PERSISTENT-SESSION-IMPLEMENTATION-PREFLIGHT.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-D1` (`implemented` - Persistent Session Schema + Contracts, primera tarea de la subserie `V1.8` que toca codigo de produccion real) implementa exclusivamente los contratos/schema que `V1.8-D0` dejo listos para D1, sin tocar prompt, `deriveMessages()`, lectura de historial hacia el modelo, locks, compaction, backfill ni Capability Gateway. Migracion nueva `migrations/034_agent_sessions_compaction_columns.sql` (numero elegido inspeccionando `migrations/`, no asumido) agrega tres columnas NULLABLE a `agent_sessions` (`compacted_prefix_json`/`compacted_through_seq`/`compacted_prefix_updated_at`), idempotente via el mismo patron `information_schema` + SQL dinamico ya establecido en la migracion 006 (nunca uno nuevo) - aplicada y re-verificada idempotente en vivo contra `crm_test` real (a diferencia de `V1.8-D0`, esta sesion SI tuvo MariaDB local alcanzable). `AgentSession` (tipo) y ambos stores (`mariaDbAgentSessionStore.ts`/`inMemoryAgentSessionStore.ts`) quedan extendidos con los tres campos nuevos, siempre `null` hasta que una futura tarea de compactacion (`D7`) los escriba - la interfaz `AgentSessionStore` (`store.ts`) no necesito ningun cambio, exactamente como `V1.8-D0` seccion G anticipaba. Se agrego `SESSION_COMPACTED` a la taxonomia de eventos (aditivo puro sobre el const array) con su propio contrato de payload tipado (`{fromSeq, toSeq, summaryEstimatedSize}` + `isValidAgentSessionCompactedPayload`) y su dedupe key builder (`buildSessionCompactedDedupeKey`, mismo patron que cada otro tipo de evento ya tiene) - explicitamente sin agregar `TURN_STARTED`/`TURN_COMPLETED`/`SESSION_RESUMED` como tipos durables, documentado ahora directamente en el codigo (no solo en el doc de diseno). `ASSISTANT_MESSAGE_SENT` gano un contrato de payload extendido (`AgentSessionAssistantMessagePayload`, con `outboundMessagePublicId` opcional) sin tocar el call site real de `shadowRecorder.ts` (sigue emitiendo exactamente el shape de hoy, sin el campo nuevo) - probado explicitamente para no romper. `AgentLoopProviderResponse`/`httpAgentLoopProvider.ts` ganan `cacheReadTokens`/`cacheMissTokens`, parseados de `usage.prompt_cache_hit_tokens`/`usage.prompt_cache_miss_tokens` (los nombres reales confirmados en vivo por `V1.8-D0`) sin cambiar el request body en absoluto (verificado byte-a-byte via los tests `[HP11]`-`[HP13b]` re-corridos sin cambios). Hallazgo nuevo de esta tarea, no anticipado por `V1.8-D0`: propagar esos mismos nombres (`cacheReadTokens`/`cacheMissTokens`) hacia `AgentToolLoopLlmCallSummary`/`llmMetrics` (el contrato que SI pasa por el sanitizer de `commercial_event`) seria rechazado por el sanitizer real - ese modulo ya usa `inputSize`/`outputSize` en vez de `...Tokens` por la misma razon exacta que `V1.8-D0` encontro para `summaryTokenEstimate` - **decision deliberada de esta tarea: no propagar mas alla del provider response**, dejado como item pendiente documentado con el nombre correcto (`cacheReadSize`/`cacheMissSize`) para cuando corresponda, porque hacerlo bien requeriria tocar `runAgentToolLoop.ts` (fuera de alcance explicito de D1). Sanitizer sin modificar (confirmado por diff), con 4 tests nuevos incluyendo uno que prueba explicitamente que `summaryTokenEstimate` sigue siendo rechazado (regresion permanente contra reintroducir el nombre incorrecto). Validado con `npx tsc --noEmit` limpio, `npm run build` limpio, y 359 tests corridos en total en tres tandas (sanitizer+store+summary+provider: 93, 92 pass/1 fail; shadow recorder+agent tool loop completo: 116/116; regresion mas amplia R3-adyacente - Capability Gateway/identity gate/CommercialActionRequest/ReadToolRequest/SalesAgentRuntime/dispatch/routing: 150/150) - el unico fallo (`agentSessionStoreMariaDb.test.ts`, ordenamiento por milisegundo) se confirmo identico contra el baseline exacto pre-D1 via `git stash` (mismo test, mismo sintoma, sin ningun archivo de esta tarea presente) - el mismo flake preexistente que `V1.7`/`V1.8-A`/`V1.8-B` ya documentaron, no corregido aqui por instruccion explicita de la tarea. No se corrio la suite completa no dirigida de `tests/commercial/` (161 archivos, incluye dominios que este cambio no puede tocar) - juzgado no rentable dado que cada archivo realmente tocado o adyacente ya corrio limpio. Cero cambios de comportamiento visible para el cliente (verificado explicitamente contra las 8 afirmaciones de compatibilidad hacia atras del propio brief de la tarea). Verdict: `R3_V1_8_D1_SCHEMA_CONTRACTS_VALIDATED`. Detalle completo: [SALES-AGENT-R3-V1.8-D1 - Persistent Session Schema + Contracts](releases/SALES-AGENT-R3-V1.8-D1-PERSISTENT-SESSION-SCHEMA-CONTRACTS.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-D2` (`implemented` - Persistent Session Write-Side Wiring, codigo de produccion real, alcance acotado exclusivamente al lado de escritura) implementa exactamente lo que `V1.8-D0` dejo listo para D2: `USER_MESSAGE_RECEIVED` movido a pre-loop, `ASSISTANT_MESSAGE_SENT` referenciando el outbound canonico, lifecycle mirroring en `control.ts`, y retry/degrade acotado - sin `deriveMessages()`, sin leer historial de sesion hacia el modelo, sin cambiar el prompt del proveedor, sin compaction, sin backfill, sin lock nuevo, sin tocar `recentMessages` legacy. Hallazgo central de esta tarea, no anticipado por `V1.8-D0`: se trazaron los cuatro dispatchers R3-native reales (`dispatchSalesAgentResponse`/`dispatchSalesAgentFallback`/`dispatchSalesAgentHardHandoff`, todos via `dispatchGovernedSalesAgentMessage.ts`) y **ninguno crea ni tiene acceso a una fila `conversation_message` de forma sincronica** - todos escriben exactamente una fila a `brain_message_outbox` (`status:'planned'`) y devuelven un `outboxId` (id de esa tabla, nunca un `conversation_message.public_id`). La fila canonica outbound solo se crea despues, de forma asincrona, cuando el worker de outbox (`outboundMessages.ts#persistCanonicalOutboundMessage`, llamado desde `autonomousOutboxTick.ts`/`outboxWorker.ts`) confirma el envio real via Meta - en un call stack completamente separado, mucho despues de que `ASSISTANT_MESSAGE_SENT` ya se habria escrito. Consecuencia implementada honestamente, no evitada: `outboundMessagePublicId` queda `null` para los tres outcomes reales (`responded`/`handoff`/fallback tecnico) hoy - no por wiring incompleto, sino porque el valor genuinamente no existe todavia en ese punto del call stack; cerrar esa brecha (persistencia canonica sincronica, o un evento nuevo posterior emitido por el worker) queda fuera de alcance de D2 (la tarea prohibe explicitamente cambiar semantica de outbox/delivery) y registrado como item pendiente. `USER_MESSAGE_RECEIVED` se movio de `shadowRecorder.ts` (post-loop) a `salesAgentRuntime.ts`, escrito antes de invocar `runAgentToolLoop`, reutilizando el dedupe key existente (`session:{sessionId}:user_message:{inboundMessageId}`) sin duplicar texto del cliente. `ASSISTANT_MESSAGE_SENT` se movio de `salesAgentRuntime.ts` (pre-dispatch, arquitectonicamente incorrecto ya que corria antes de conocer el resultado real del dispatch) a `runSalesAgentRuntimeCycle.ts` (post-dispatch, el boundary que realmente conoce el outcome) - exactamente la correccion que la propia Seccion I de la tarea pedia ("one event, one clear writer"). El escritor compartido pre-D2 (`recordAgentToolLoopSessionShadowEvents`) queda **sin cambios de comportamiento** para su unico caller real, `runNativeAgentToolLoopCycle.ts` (el runtime ATL/legado, explicitamente fuera de alcance de toda la serie `V1.8`) - se extrajo una funcion nueva y mas angosta (`recordAgentToolLoopToolActivityEvents`, solo tool events) para que `SalesAgentRuntime` no dependa de la funcion completa, ambas comparten una sola implementacion interna (`appendToolActivityEvents`) para no duplicar el mapeo de gobernanza/dedupe. Retry: nuevo helper pequeno y de proposito unico (`agent-session/appendWithRetry.ts#appendAgentSessionEventWithRetry`) - un intento, un reintento acotado de 50ms, degrada con warning sin bloquear el turno; un fallo de sanitizer (identificado por el prefijo estable `agent_session_forbidden_payload:` del mensaje de error, sin necesitar cambiar el tipo `AppendEventResult`) nunca se reintenta. Lifecycle mirroring: `agent_sessions.status` se actualiza dentro de las mismas transacciones ya existentes de `close`/`reopen` en `control.ts`; una conversacion sin fila `agent_sessions` nunca crea una solo por la accion de lifecycle (UPDATE simple, cero filas afectadas); reabrir siempre reutiliza el mismo id de sesion. Validado con `npx tsc --noEmit` limpio, `npm run build` limpio, y 401 tests corridos en cinco tandas (nuevo archivo en memoria para ordering/retry/degrade/replay: 6/6; `runSalesAgentRuntimeCycle` existente + 6 tests nuevos de referencia/replay: 15/15; `conversationControl` existente + 4 tests nuevos de lifecycle: 10/10; suite core de agent-session/provider/agent-loop/SalesAgentRuntime: 224/225; ATL/legado + regresion R3-adyacente mas amplia: 145/145) - el unico fallo es el mismo flake preexistente de ordenamiento por milisegundo (`agentSessionStoreMariaDb.test.ts`) que `V1.7`/`V1.8-A`/`V1.8-B`/`V1.8-D1` ya documentaron identicamente, no tocado aqui. Verdict: `R3_V1_8_D2_SESSION_WRITE_SIDE_VALIDATED`. Detalle completo: [SALES-AGENT-R3-V1.8-D2 - Persistent Session Write-Side Wiring](releases/SALES-AGENT-R3-V1.8-D2-PERSISTENT-SESSION-WRITE-SIDE-WIRING.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-D3` (`implemented` - Persistent Session Read Side + `deriveMessages()`, codigo de produccion real, alcance acotado exclusivamente al lado de lectura, shadow-only) implementa exactamente lo que `V1.8-D0`/`V1.8-D2` dejaron listo para D3: un lector acotado de eventos (reutiliza `AgentSessionStore.loadRecentEvents` sin cambios), un lector acotado nuevo de `conversation_message` (`conversationTranscriptReader.ts`, mismo shape `DESC + LIMIT` luego reverse en JS ya probado por `loadRecentEvents`, sobre el indice ya existente `idx_message_conversation_created` - nunca la query sin `LIMIT` `loadNativeConversationDetailByPublicId` que `V1.8-D0` ya habia descartado), `deriveMessages()` (proyeccion pura, cero I/O) y el `GET_LOCK` corto de lectura (`loadPersistentSessionContext.ts`, mismo shape de `resolveRuntimeOpportunity.ts`, nunca sostenido durante una llamada al proveedor porque este modulo no importa ningun proveedor) - sin alimentar el modelo con estos mensajes derivados todavia, sin reemplazar `buildAgentStepPromptPackage.ts`, sin remover `recentMessages`, sin compaction, sin routing D4/D5 en vivo, sin tocar Capability Gateway ni semantica de tools. Estrategia de merge confirmada exactamente como `V1.8-C`/`V1.8-D0` preferian (Opcion C del propio brief): `conversation_message` es la unica fuente canonica de transcript humano (`inbound -> role "user"`, `outbound -> role "assistant"`, filtrando la tercera direccion real que el schema si tiene - `'system'`, las filas de timeline de `lib/domains/conversations/control.ts`), `agent_session_events` es evidencia operacional pura (actividad de tools, nunca texto) - las dos nunca se mezclan en un solo arreglo. Hallazgo real no anticipado por el brief: dado que `conversation_message` es *siempre* la fuente (nunca solo un fallback), no existe un "camino de bootstrap" separado - una conversacion pre-D2 con cero `agent_session_events` simplemente tiene `events: []` y el lector de transcript igual devuelve sus turnos reales; `bootstrapUsed` queda como una señal de observabilidad pura (`events.length === 0 && transcriptMessages.length > 0`), nunca una rama de codigo. La exclusion del mensaje del turno actual (Seccion K del brief, el defecto estructural exacto que `V1.8-A` encontro) se implemento en `deriveConversationMessages()` mismo (no en el lector de sesion, que no tiene nocion de "cual turno es el actual") comparando por `conversation_message.id` numerico - confirmado como el mismo identificador que `inboundMessageId`/`AgentRuntimeEvent.messageId` ya usan, trazado directamente contra `native-whatsapp/service.ts`. `AgentLoopProviderMessage.role` se extendio de `"system"|"user"` a `"system"|"user"|"assistant"` (nunca `"tool"`, sin evidencia real de necesitarlo hoy) - verificado en vivo contra el proveedor HTTP real (`[D3-HP1]`), y `AgentSessionEvent` gano un campo `seq` real (columna `agent_session_events.seq` ya existente desde la migracion 033, usada hasta ahora solo dentro de `ORDER BY`, nunca expuesta en el contrato) necesario para que `deriveToolActivityObservations()` excluya eventos ya cubiertos por una futura compactacion (`compactedThroughSeq`) sin inventar un mapeo que no existe. Lectura de compactacion (columnas de la migracion 034): valida ambas columnas juntas, degrada solo ese slot con warning ante metadata invalida/parcial, nunca revienta el turno completo - probado contra MariaDB real con un fixture valido y uno deliberadamente invalido. El `GET_LOCK` de lectura se probo con exclusividad real (una conexion separada sostiene la misma lock nombrada, la llamada concurrente espera el timeout completo y degrada), liberacion tras exito y liberacion tras excepcion, las tres contra MariaDB real, nunca mockeadas. Probado con 419 tests corridos en total esta tarea (66 nuevos o extendidos): `deriveMessages()` puro 13/13, read-side + concurrencia contra MariaDB real 10/10, serializacion de roles contra el proveedor HTTP real 42/42 (incluye el test nuevo), regresion core de `agent-session`/`SalesAgentRuntime`/`conversationControl` 98/99, regresion R3-adyacente mas amplia 124/124, `runAgentToolLoop`/multi-intent/`semanticIntentAdapter` (los tres call sites del tipo ampliado) 132/132 - el unico fallo es el mismo flake preexistente de ordenamiento por milisegundo (`agentSessionStoreMariaDb.test.ts`) que `V1.7`/`V1.8-A`/`V1.8-B`/`V1.8-D1`/`V1.8-D2` ya documentaron identicamente, no tocado aqui. `npx tsc --noEmit` limpio, `npm run build` limpio. Cero cambio de comportamiento visible para el cliente; cero call site de produccion agregado para este read side todavia. Verdict: `R3_V1_8_D3_SESSION_READ_SIDE_VALIDATED`. Detalle completo: [SALES-AGENT-R3-V1.8-D3 - Persistent Session Read Side + deriveMessages](releases/SALES-AGENT-R3-V1.8-D3-PERSISTENT-SESSION-READ-SIDE-DERIVE-MESSAGES.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-D4` (`implemented` - Persistent Session Shadow Integration, codigo de produccion real, alcance acotado exclusivamente a wiring shadow-only dentro del turno real) conecta el read-side de `V1.8-D3` (`loadPersistentSessionContext()`/`deriveMessages()`, sin cambios) al turno real de `SalesAgentRuntime`, sin que el resultado derivado alcance nunca `loopInput`/`buildAgentStepPromptPackage.ts`/el request real al proveedor - probado por ejecucion (test de identidad byte-a-byte del request real con el shadow apagado vs. encendido), no solo por inspeccion. Punto de insercion exacto: `salesAgentRuntime.ts`, dentro de `runSalesAgentRuntime()`, inmediatamente despues de construir `loopInput` y de la escritura pre-loop de `USER_MESSAGE_RECEIVED` (D2), inmediatamente antes de `runAgentToolLoop(loopInput)` - el unico punto donde `conversationId`/`inboundMessageId`/`commercialContextSummary`/`sessionStore`/el mecanismo de warnings ya existente (`preLoopWarnings`) estan todos en alcance sin cargar nada nuevo. Flag nuevo `BRAIN_R3_PERSISTENT_SESSION_SHADOW_ENABLED` (default `false`, `buildPersistentSessionShadowFeatureFlags()` en `commercialCycleConfig.ts`) leido una sola vez en `runNativeAutonomousCycle.ts` (nunca dentro de `salesAgentRuntime.ts`, que mantiene su disciplina existente de cero lecturas de `process.env`) y enhebrado sin cambios via `RunSalesAgentRuntimeCycleInput`/`SalesAgentRuntimeInput` como `persistentSessionShadowEnabled` - sin allowlist propio, porque `shouldRouteToSalesAgentRuntime` ya acota cada turno real que podria llegar a este shadow. Dos modulos nuevos: `agent-session/persistentSessionShadowComparison.ts` (puro, cero I/O - `PersistentSessionShadowComparison`, comparacion legacy-vs-persistente por conteos/booleanos, nunca `activeTopic`/`currentIntent`/score sintetico, verificado directamente contra las keys del objeto real) y `agent-session/runPersistentSessionShadow.ts` (el unico orquestador de I/O - carga, deriva, compara, emite un `commercial_event` nuevo `persistent_session_shadow_compared` con solo conteos/timings/booleanos, nunca texto de mensaje, dentro de un unico `try`/`catch` que garantiza que ningun fallo de esta pieza puede alterar el turno real). El "real incident replay" pedido por la tarea (barra olimpica 20kg -> respuesta -> "varias opciones para home gym") corre contra MariaDB real (`crm_test`) y confirma que la sesion persistente recupera ambos turnos previos incluso cuando la cola legacy narrow solo carga la respuesta del asistente. El benchmark de continuidad completo (C01-C12 del brief) esta implementado y en verde, la mayoria como tests puros sobre `deriveConversationMessages()` real (D3, sin cambios) mas dos fixtures reales contra MariaDB (C08 eventos-de-sesion-ausentes, C10 fallo-de-lectura). Aislamiento de fallas probado en tres capas independientes (fallo de lectura de sesion, dedupe key faltante, y de punta a punta en el boundary del runtime con un store que solo falla en la lectura del shadow) - las tres degradan a warning o a un evento marcado `degraded:true`, nunca un throw, nunca un resultado de turno alterado. Latencia real medida (no estimada): mediana de 20 iteraciones controladas contra MariaDB real, `readMs=7 deriveMs=0 shadowTotalMs=7` - tres ordenes de magnitud por debajo de una llamada real a DeepSeek. Probado con 25 tests nuevos (16 puros en `persistentSessionShadowComparison.test.ts`, 6 reales contra MariaDB en `runPersistentSessionShadow.test.ts`, 3 nuevos en `salesAgentRuntime.test.ts` - identidad de request/aislamiento de fallas/flag-off) mas regresion: 32/32 de D3 (`deriveMessages`/`loadPersistentSessionContext`/`agentSessionStoreMariaDb`), 298/298 de regresion R3-adyacente mas amplia (14 archivos), y la suite completa del repo (`npm test`, 4127 tests: 4105 pass/22 fail) - los 22 fallos confirmados preexistentes reproduciendo identicos contra el baseline `develop` limpio via `git stash` (identidad/onboarding, benchmark A13 ya documentado con deuda propia, timing de `CommercialWork` paralelo, y el mismo flake preexistente de ordenamiento por milisegundo que `V1.7`-`V1.8-D3` ya documentaron), ninguno relacionado con esta tarea. `npx tsc --noEmit`, `npm run build` y `npm run lint` (0 errores, 39 warnings preexistentes ajenos) limpios. Nota de entorno local (sin cambio de codigo, no commiteada): la base de datos local dev/test tenia 9 migraciones pendientes (`026`-`034`, incluidas `033`/`034` de las que D3/D4 dependen) bloqueadas por drift preexistente de checksum en `schema_migrations` (misma categoria que `docs/operations/local-migration-checksum-drift-009-010.md` ya documenta para `009`/`010`, aqui extendido a `001`-`025`) y dos migraciones (`027`/`028`) cuyo DDL ya estaba aplicado fuera de banda sin fila de bookkeeping - ambas corregidas localmente (rebaseline de checksum al contenido actual del archivo; backfill de bookkeeping sin re-ejecutar DDL), con aprobacion explicita del usuario antes de cada paso, sin tocar ningun archivo de migracion ni codigo de producto. Verdict: `R3_V1_8_D4_PERSISTENT_SESSION_SHADOW_VALIDATED`; D5 readiness: `READY_FOR_D5_OWNER_ONLY`, explicitamente acotado a ensamblaje completo de mensajes al proveedor + flag/allowlist propio + contexto de sesion realmente llegando a DeepSeek + fallback a legacy en fallo de lectura + benchmark de continuidad real contra el proveedor + metricas de cache reales - nunca redisenar el foundation/schema/store de D1-D3. Detalle completo: [SALES-AGENT-R3-V1.8-D4 - Persistent Session Shadow Integration](releases/SALES-AGENT-R3-V1.8-D4-PERSISTENT-SESSION-SHADOW-INTEGRATION.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-D5` (`implemented` - Owner-Only Persistent Session Cognition, codigo de produccion real, primera tarea de la subserie que cambia lo que el modelo realmente ve para trafico real, acotado a un allowlist owner-only) hace que la sesion persistente reemplace `commercialContextSummary.recentMessages` en el request real al proveedor, exclusivamente cuando `BRAIN_R3_PERSISTENT_SESSION_COGNITION_ENABLED=true` Y el `wa_id` del cliente esta en `BRAIN_R3_PERSISTENT_SESSION_COGNITION_WA_IDS` (ambos vacios/`false` por defecto - cero cambio de comportamiento para cualquier turno fuera de ese allowlist explicito). Gate deliberadamente independiente del allowlist de `SalesAgentRuntime` y del flag de shadow de `V1.8-D4` (`shouldEnablePersistentSessionCognition`, nuevo en `commercialCycleConfig.ts`, mismo patron fail-closed que `shouldRouteToSalesAgentRuntime`), resuelto una sola vez en `runNativeAutonomousCycle.ts` y enhebrado como booleano ya compuesto - `salesAgentRuntime.ts` nunca lee `process.env`. Punto de insercion: `resolvePersistentSessionCognitionContext()` (nuevo, `agent-session/`) corre ANTES de construir `loopInput` (a diferencia del shadow de D4, que corre despues y descarta) porque una lectura exitosa cambia dos campos reales de `loopInput`: `commercialContextSummary` (con `recentMessages` removido via `stripRecentMessagesForPersistentSessionContext`, funcion pura de 2 lineas, nunca muta el objeto original) y el nuevo `persistentSessionHistoricalMessages`, cargado una sola vez por turno y enhebrado sin cambios a traves de `runAgentToolLoop.ts` hacia sus dos call sites de `buildAgentStepPromptPackage` (gathering y finalization), manteniendo el prefijo historico estable entre iteraciones del mismo turno. `buildAgentStepPromptPackage.ts` gano exactamente una rama nueva (nunca duplico la logica de instrucciones de sistema existente, reutilizada tal cual para ambas ramas): sistema estable+identidad -> historial real `user`/`assistant` de `deriveMessages()` (D3, sin cambios) -> bloque de contexto fresco (`commercialContext` ya sin `recentMessages` + `RecentCatalogContext` + `pendingCatalogAction`) -> mensaje del turno actual + observaciones de tools de este turno. La rama legacy es literalmente el mismo codigo de antes de esta tarea, alcanzado siempre que `persistentSessionHistoricalMessages` sea `null`/`undefined` - probado byte-identico por construccion y por test. Fallback (`resolvePersistentSessionCognitionContext`, contrato de union limpia `active:true|false`, nunca un prompt persistente parcial): un fallo de lectura de sesion, timeout de lock, excepcion en `deriveMessages()` (probado con un evento deliberadamente malformado), o ausencia de `inboundMessageId` degradan siempre a `active:false` mas un warning `persistent_session_cognition_fallback:` no sensible, nunca un throw, nunca un resultado de turno alterado - probado tanto a nivel del resolver (MariaDB real, `crm_test`) como en el limite completo del runtime (`main_management`), incluyendo el caso donde el shadow de D4 Y la cognicion de D5 fallan simultaneamente con el mismo store y ambos degradan de forma independiente sin fugarse entre si. Benchmark real de continuidad contra el endpoint real de DeepSeek configurado en este repo (mismo tecnica "provider real, inbound simulado, cero Meta real" que `V1.4` Fase 18/`V1.8-B` ya establecieron): 6 de los 7 escenarios nombrados (B01/B02/B03/B05/B06/B07; B04 deliberadamente no re-corrido en vivo porque ejercita continuidad de `RecentCatalogContext`/`pendingCatalogAction`, explicitamente sin cambios por D5 y ya cubierta en vivo por la suite existente), 14 turnos reales por modo (legacy/persistente), reportado con honestidad total incluyendo un hallazgo no resuelto: en B03 (retorno de tema) el modo persistente resolvio explicitamente la referencia ("Entiendo que quieres retomar la barra olimpica") mientras que legacy pidio aclaracion pese a tener la misma informacion en su ventana de 5 mensajes - evidencia real y concreta; en B06 (correccion de despacho) el modo persistente produjo un `handoff` inesperado en el turno 1 (unico entre 14 turnos), reportado como anomalia real sin causa raiz confirmada, no oculta ni minimizada. Metricas de cache reales (nunca estimadas): tasa de acierto de cache observada 96.7% en modo persistente vs 88.8% en legacy sobre el mismo benchmark (240,729 vs 243,010 tokens de entrada totales, volumen casi identico) - el prefijo de sistema estatico (~5,888 tokens) cachea de forma confiable en ambos modos; el sufijo historico/contexto creciente NO mostro cacheo incremental turno a turno en esta medicion, reportado honestamente como limitacion, no vendido de mas. Metricas de cache deliberadamente NO propagadas a ningun `commercial_event` en esta tarea (unicamente observadas via el wrapper del script de benchmark) - un evento nuevo SI se agrego para observabilidad de produccion (`persistent_session_cognition_applied`, emitido solo para turnos elegibles, nunca para trafico ordinario, evita ruido) con campos puramente estructurales (`active`/`fallbackReason`/`historyMessageCount`), nunca texto de mensaje. Smoke real de WhatsApp (Section T del brief) diferido por decision explicita del usuario en esta sesion (memoria de 63 dias registra que el webhook de Meta de este entorno no apunta a esta instancia local; solo el usuario puede confirmar si sigue siendo cierto y solo el usuario puede enviar mensajes reales desde el telefono owner) - no simulado como si se hubiera corrido. Regresion de gobernanza probada explicitamente (`Capability Gateway`/`CommercialActionRequest`/dispatch/outbox sin cambios, un turno elegible con `select_products` sigue resolviendo exactamente una oportunidad real) y por la suite completa. Probado con 28 tests nuevos (7 en `buildAgentStepPromptPackage.test.ts`, 8 en `resolvePersistentSessionCognitionContext.test.ts` nuevo, 7 en `shouldEnablePersistentSessionCognition.test.ts` nuevo, 6 en `salesAgentRuntime.test.ts`) mas regresion: 501/502 en toda la suite tocada (1 flake preexistente de milisegundo ya documentado), y la suite completa del repo (`npm test`, 4154 tests: 4129 pass/25 fail) - los 25 fallos confirmados preexistentes reproduciendo identicos contra el baseline `develop` limpio via `git stash` (verificado dos veces: identidad/onboarding/e2e, y separadamente dos tests de `sales-agent-configuration` afectados por contaminacion de datos preexistente en la base compartida `main_management`, dominio completamente ajeno a esta tarea), ninguno relacionado. `npx tsc --noEmit`, `npm run build` y `npm run lint` (0 errores, 39 warnings preexistentes ajenos) limpios. Verdict: `R3_V1_8_D5_OWNER_ONLY_SESSION_COGNITION_VALIDATED_WITH_KNOWN_DEBT` (deuda honesta: anomalia de B06 sin causa raiz, G18 WhatsApp diferido, ningun escenario en vivo supero la ventana de 5 mensajes de legacy). D6 readiness: `READY_FOR_D6_LEGACY_TAIL_RETIREMENT` condicional a cerrar esa deuda (repetir B06 para verificar reproducibilidad; completar G18 o aceptar explicitamente la evidencia de benchmark como suficiente) - nunca asumido, la propia `V1.8-C` ya condiciono retirar la cola legacy a "evidencia real de D5, nunca asumida". Detalle completo: [SALES-AGENT-R3-V1.8-D5 - Owner-Only Persistent Session Cognition](releases/SALES-AGENT-R3-V1.8-D5-OWNER-ONLY-PERSISTENT-SESSION-COGNITION.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-D5.2` (`implemented` - Persistent Session Provider Message Alternation Repair, codigo de produccion real, fix minimo y unico sobre el defecto estructural que `V1.8-D5.1` diagnostico sin corregir) reemplaza los dos mensajes `user` consecutivos que la rama persistente de `buildAgentStepPromptPackage.ts` (`V1.8-D5`) emitia (contexto fresco + turno actual, sin ningun `assistant` entre medio) por exactamente un mensaje `user` final que lleva todos los campos de ambos (`commercialContext`, `RecentCatalogContext`, `pendingCatalogAction`, `priorStepsThisTurn`, `currentTime`, `customerMessage`, `question`) - cero mensaje `assistant` sintetico insertado, cero campo perdido o duplicado. Forma resultante: historial cero -> `[system, user]` (nunca `[system, user, user]`, el defecto exacto de `V1.8-D5.1`); con historial real -> `[system, ...historial user/assistant real de deriveMessages() sin cambios, user(turno actual)]`, alternancia limpia en toda la conversacion, probado incluso en la iteracion 5 de un benchmark real de 5 turnos (Seccion 11 del release doc). La rama legacy (alcanzada siempre que `persistentSessionHistoricalMessages` sea `null`/`undefined`) no fue tocada por este diff - probada byte-identica por test antes y despues (`[D5-G2/T11/T12]`). Revalidacion en vivo del escenario B06 exacto de `V1.8-D5.1` (mismo DeepSeek real configurado en este repo, misma MariaDB real `main_management`, misma falla real y controlada de `set_shipping_destination` via `LOGISTICS_DB_ENABLED=false`, 10 repeticiones frescas por modo, nunca reutilizando una conversacion): el handoff en modo persistente bajo de **60% (6/10, medicion pre-fix de `V1.8-D5.1`) a 10% (1/10, post-fix)** - una reduccion de 6x bajo la misma falla real. **Hallazgo honesto, no ocultado**: en esta misma corrida legacy midio 50% (5/10) de handoff, frente al 0/10 que `V1.8-D5.1` habia medido - un movimiento real en un codigo que es provablemente identico byte a byte entre ambas mediciones, atribuido a deriva/no-determinismo real de DeepSeek entre sesiones de benchmark (dias distintos, mismo alias de modelo), nunca a este fix. Lo que si es atribuible al fix es la comparacion propia de persistente (antes/despues, misma falla real) y la comparacion dentro de esta misma sesion (persistente 10% no fue peor que legacy 50%, revirtiendo el hallazgo de `V1.8-D5.1` de "persistente materialmente peor que legacy"). Controles adicionales, todos en vivo contra DeepSeek/MariaDB reales: exito deterministico de envio (resolver de comuna fake inyectado via `setCommuneResolverForTests`, seam de test ya existente, nunca produccion nueva) dio 0/10 handoffs en ambos modos, con "San Bernardo" superseded correctamente a "Maipú" en las 20 repeticiones; una segunda falla de herramienta independiente (`search_products` contra el Catalog Service real de este entorno, confirmado inalcanzable, sin seam inyectado) dio 0/5 en ambos modos; continuidad B01/B02/B03/B05 mas un escenario nuevo B07 (pregunta lateral sobre despacho + retorno al producto) dieron 0/10 escenarios con handoff; un escenario nuevo de 5 turnos que excede la ventana de 5 mensajes de `recentMessages` de legacy (Seccion M del brief, nunca antes corrido en vivo) demostro que el modo persistente retiene el detalle "20kg" que legacy ya habia evictado de su ventana - visible incluso en los argumentos reales de la llamada `search_products` del modelo (persistente reintento con `"barra olimpica 20kg"` las 3 veces; legacy busco solo `"barra olimpica"`), no solo en el texto final de la respuesta - 0 handoffs en ambos modos. Metricas de cache reales sobre 352 llamadas reales al proveedor (170 legacy/182 persistente) mostraron ~97% de acierto en ambos modos sin diferencia material - distinto del hallazgo de `V1.8-D5` (96.7% persistente vs 88.8% legacy), reportado sin reconciliar (esta corrida es mas amplia y con mas tipos de escenario). Probado con 4 tests de `buildAgentStepPromptPackage.test.ts` actualizados (forma nueva, misma disciplina de aserciones) mas 4 nuevos (`[D5.2-T1]`, `[D5.2-G5/G6/G7/T4/T5/T6/T7/T8]`, `[D5.2-T3]`, `[D5.2-T10]`), suite objetivo completa (22 archivos: `buildAgentStepPromptPackage`/`runAgentToolLoop`/`salesAgentRuntime`/`resolvePersistentSessionCognitionContext`/gating de sesion persistente/shadow de `V1.8-D4`/lectura-escritura de `AgentSessionStore`/Capability Gateway/`CommercialActionRequest`/`ReadToolRequest`/dispatch-routing) **441/441 en verde**, y la suite completa del repo con solo fallas preexistentes ya documentadas (identidad/onboarding por aislamiento de `DATABASE_NAME` entre archivos de test, benchmark A13 con deuda propia ya registrada, timing de `CommercialWork` paralelo, contaminacion de datos compartida en `sales-agent-configuration`, flake preexistente de milisegundo) - confirmado sin relacion via `git stash` contra el baseline limpio para dos archivos representativos. `npx tsc --noEmit`, `npm run build` y `npm run lint` (0 errores, 40 warnings preexistentes ajenos) limpios. El benchmark real completo (62 escenarios, 124 turnos de cliente, 352 llamadas reales al proveedor) se ejecuto via un script scratchpad (`d52-benchmark.ts`, raiz del repo, nunca commiteado, eliminado tras su uso - mismo precedente que los scripts de `V1.8-D5`/`V1.8-D5.1`). No se implemento `D6` (instruccion explicita de la tarea); este documento no declara `D6` `GO`/`NO-GO` por si mismo, solo remueve el bloqueo especifico de `V1.8-D5.1` y deja la inestabilidad real del handoff de legacy entre sesiones (0% a 50%, real, no explicada, ver Seccion 18 del release doc) como deuda honesta para que una futura tarea `D6` decida su propio criterio de cierre. Deuda de handoff estructurado (`AgentStepHandoff.reason` free-text) documentada desde `V1.6`, no tocada por `V1.7`/`V1.8-A`/`V1.8-B`/`V1.8-C0`/`V1.8-C`/`V1.8-D0`/`V1.8-D1`/`V1.8-D2`/`V1.8-D3`/`V1.8-D4`/`V1.8-D5`/`V1.8-D5.1`/`V1.8-D5.2`. Verdict: `R3_V1_8_D5_2_PROVIDER_MESSAGE_ALTERNATION_REPAIR_VALIDATED`. Detalle completo: [SALES-AGENT-R3-V1.8-D5.2 - Persistent Session Provider Message Alternation Repair](releases/SALES-AGENT-R3-V1.8-D5.2-PERSISTENT-SESSION-PROVIDER-MESSAGE-ALTERNATION-REPAIR.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-D6` (`implemented` - Legacy `recentMessages` Retirement, codigo de produccion real cambiado, alcance de routing/config unicamente - `buildAgentStepPromptPackage.ts`, el armado real de mensajes que `V1.8-D5.2` ya habia corregido, no fue tocado por esta tarea) hace que la memoria conversacional de sesion persistente sea el default para TODO turno ya elegible de `SalesAgentRuntime`, retirando la responsabilidad #1 (memoria NORMAL) de `recentMessages` legacy y dejando solo la #2 (fallback). Cambio de configuracion: `BRAIN_R3_PERSISTENT_SESSION_COGNITION_ENABLED` invierte su default a `true` (ahora es la palanca de rollback: `=false` revierte todo turno R3 al camino legacy, sin tocar ningun allowlist); el allowlist independiente de `V1.8-D5` (`BRAIN_R3_PERSISTENT_SESSION_COGNITION_WA_IDS`) queda retirado por completo (`loadPersistentSessionCognitionAllowlist` eliminado de `autonomousRuntimeConfig.ts`, sin otro caller); `BRAIN_SALES_AGENT_RUNTIME_WA_IDS` sigue siendo la unica frontera real de rollout del piloto (sigue vacia por defecto - cero trafico real todavia). `shouldEnablePersistentSessionCognition()` ya no recibe `waId`. `recentMessages` sigue siendo construido por `buildMinimalCommercialContextSummary` en cada turno (no se elimino), simplemente se descarta cuando la sesion persistente resuelve `active:true` y solo se usa cuando esta cae a fallback (fallo de lectura/derive, o el flag de rollback explicito) - contrato identico al que ya probaba `V1.8-D5`. Validado en vivo contra DeepSeek real + MariaDB real (`main_management`), esta vez a traves del punto de entrada real (`runNativeAutonomousCycle`, sin bypass de proveedor, a diferencia del benchmark de `V1.8-D5.2`): conversacion nueva sin historial (3/3 turnos `cognitionActive=true`/`historyMessageCount=0`, sin `BRAIN_R3_PERSISTENT_SESSION_COGNITION_WA_IDS` configurado - prueba que el allowlist retirado ya no hace falta); rollback en vivo (`BRAIN_R3_PERSISTENT_SESSION_COGNITION_ENABLED=false`, 2/2 turnos sin ningun evento `persistent_session_cognition_applied`, ambos respondieron normalmente via legacy); reejecucion del control de falla de `set_shipping_destination` (B06, `LOGISTICS_DB_ENABLED=false`) en modo default: 0/5 handoffs (referencia post-fix de `V1.8-D5.2`: 1/10); control independiente de falla de `search_products` (Catalog Service inalcanzable): 0/3 handoffs; escenario de continuidad de 5 turnos de `V1.8-D5.2` repetido 3 veces en vivo bajo el routing default: en las 3 corridas el conteo de historial crecio correctamente (0->2->4->6->8) y la sesion persistente nunca cayo a fallback; el argumento real de la llamada `search_products` del turno 5 (capturado desde `crm_capability_executions.request_summary_json`, no solo el texto final) conservo `"barra olimpica 20kg"` en 2/2 reejecuciones instrumentadas; el texto final de la respuesta conservo "20kg" en 2/3 corridas (la primera corrida omitio el numero en su redaccion final pese a que el mecanismo subyacente fue correcto en las tres - variacion real y honesta de DeepSeek entre sesiones, no una regresion, reportada sin ocultar siguiendo el mismo criterio que `V1.8-D5.2` ya establecio para el hallazgo analogo de legacy). Decision D4: `KEEP_AS_DIAGNOSTIC` (el shadow de `V1.8-D4` sigue siendo un diagnostico independiente de lectura/derive, sin tocar codigo). Probado con `tests/commercial/shouldEnablePersistentSessionCognition.test.ts` reescrito (5/5, semantica sin allowlist/default-true) mas `tests/commercial/salesAgentR3PersistentSessionDefaultRouting.test.ts` nuevo (4/4, real MariaDB, modelado sobre las convenciones de `salesAgentR3PilotRoutingAuthority.test.ts`); suite objetivo de archivos no tocados por esta tarea re-corrida en verde (`buildAgentStepPromptPackage`/`runAgentToolLoop`/`salesAgentRuntime`/`resolvePersistentSessionCognitionContext`/`runPersistentSessionShadow`/`salesAgentR3PilotRoutingAuthority`/`salesAgentR3RuntimeIsolationAuthority`, 228/228) mas gobernanza/dispatch (`capabilityGateway*`/`commercialActionRequest`/`readToolRequest`/`dispatchSalesAgent*`/`runSalesAgentRuntimeCycle`/`agentSession*`, 184/185 - la unica falla es el flake preexistente de orden por milisegundo de `agentSessionStoreMariaDb.test.ts`, reproducido identico contra el baseline pre-D6 via `git stash`); suite completa del repo 4134/4160, las 26 fallas (8 archivos unicos) confirmadas preexistentes y ajenas - dos de ellas (`continuityConcurrency`/`runCommercialOperationalLoop`) re-verificadas en aislamiento contra el baseline pre-D6 reproduciendo el mismo error `Missing DATABASE_NAME` de contaminacion de entorno entre archivos, sin ningun codigo de `V1.8-D6` presente; el resto coincide exactamente con la deuda ya documentada por `V1.8-D5.2` (benchmark A13, timing de `CommercialWork` paralelo, dependencia externa de Customer Service en identity/onboarding). `npx tsc --noEmit`, `npm run build` y `npm run lint` (0 errores, 40 warnings preexistentes identicos a `V1.8-D5.2`, ningun archivo de esta tarea) limpios. Sin migracion de base de datos. `D7`: `READY_FOR_D7_SESSION_COMPACTION`. Verdict: `R3_V1_8_D6_LEGACY_RECENTMESSAGES_RETIREMENT_VALIDATED`. Detalle completo: [SALES-AGENT-R3-V1.8-D6 - Legacy recentMessages Retirement](releases/SALES-AGENT-R3-V1.8-D6-LEGACY-RECENTMESSAGES-RETIREMENT.md). Sin commit ni push al momento de esta entrada.

`SALES-AGENT-R3-V1.8-D7` (`implemented` - Persistent Session Compaction, codigo de produccion real nuevo, alcance acotado a compactacion durable sobre el foundation ya default de `V1.8-D6` - `conversationTranscriptReader.ts` sin cambios, sin migracion) implementa la pieza que faltaba para conversaciones largas: el historial antiguo se compacta en un resumen evidencial durable (`agent_sessions.compacted_prefix_json`, columna ya reservada por `V1.8-D1`) mientras `conversation_message` permanece intacto y completo, y el historial reciente sigue llegando al proveedor como turnos `user`/`assistant` nativos, nunca como texto re-serializado. Hallazgo de dominio (no un rediseño de schema, seccion propia del release doc): `compacted_through_seq` (migracion 034) quedo documentado en `V1.8-D1` como un limite de `agent_session_events.seq` - una suposicion especulativa anterior a que `V1.8-D3` confirmara `conversation_message` como el unico transcript canonico. `V1.8-D7` puebla esa columna con un `conversation_message.id` real (la unidad real sobre la que opera la compactacion) - seguro porque la columna nunca se habia poblado en produccion y su otro lector (`deriveToolActivityObservations`) no tiene ningun consumidor real en el prompt hoy (confirmado contra `buildAgentStepPromptPackage.ts`). Trigger: `shouldTriggerSessionCompaction` (una sola condicion, `BRAIN_R3_SESSION_COMPACTION_MAX_RAW_MESSAGES`, default 40, clampeado a 80 - deja margen bajo el hard cap existente de 100 de `V1.8-D3`) mas `BRAIN_R3_SESSION_COMPACTION_TARGET_RECENT_MESSAGES` (default 20) para decidir el split ventana-vieja-a-compactar vs. cola-reciente-cruda; compactacion incremental probada contra MariaDB real (`[D7-O4]`): una segunda ronda construye sobre el resumen previo sin releer nunca el rango ya compactado. Invariante sin-solapamiento/sin-hueco probado de punta a punta contra MariaDB real (`[D7-O3]`): `deriveConversationMessages()` excluye toda fila `id <= compactedThroughSeq`, y `loadPersistentSessionContext.ts` amplia su propia ventana de lectura al hard cap existente solo cuando ya existe un prefijo compactado valido (cero cambio de comportamiento en el camino sin compactacion, byte-identico a `V1.8-D3`-`V1.8-D6`). Persistencia atomica con guardia de avance monotono (`AgentSessionStore.persistCompactedPrefix`, un solo `UPDATE` con `WHERE compacted_through_seq IS NULL OR compacted_through_seq < ?`, sin lock sostenido durante la llamada al modelo) - probada con una carrera real de dos escrituras concurrentes (`[D7-S4]`, MariaDB real) y con la carrera realista de Section K (`[D7-O7]`: un proveedor que ejecuta una escritura competidora *mientras* esta corrida sigue esperando su propia respuesta del modelo) - la escritura perdedora se descarta de forma segura, nunca un error, nunca una escritura parcial. Aislamiento de fallas probado en vivo con un turno real (`[D7-CC3]`, MariaDB real, proveedor de compactacion forzado a fallar via env): el turno igual despacha normalmente (`status: responded`, `outboxWritten: true`), solo se agrega un warning `session_compaction_failed:` a `runtime.warnings` - la compactacion nunca puede fallar el turno del cliente. Politica de contenido (instruccion de sistema dedicada en `compactAgentSessionHistory.ts`, nunca reutilizando el prompt principal): resume QUE PASO (metas, preferencias, categorias, correcciones, cambios de tema, compromisos, preguntas sin resolver), nunca QUE HACER DESPUES (sin intent/next-step/stage), nunca precios/stock/envio/cotizacion como verdad actual. Benchmark real en vivo (DeepSeek real + MariaDB real `main_management`, a traves del punto de entrada real de produccion `processNativeWhatsAppInbound -> ensureAutonomousSalesTurnContinuity -> runNativeAutonomousCycle -> runSalesAgentRuntimeCycle`, sin bypass de ninguna capa, script scratchpad `d7-benchmark.ts` en raiz, nunca commiteado, eliminado tras su uso): conversacion de 10 turnos (barra olimpica 20kg -> 8 preguntas de categorias no relacionadas -> "volvamos a la barra olimpica del principio, la de 20kg") disparo compactacion 4 veces, `compacted_through_seq` avanzo monotonicamente con `conversation_message.id` reales, el resumen final conservo explicitamente el retorno de tema ("...Luego volvió a preguntar por la disponibilidad de la barra olimpica de 20kg") y declino explicitamente afirmar hechos de negocio actuales ("No se mencionaron precios, stock ni detalles de envío como hechos actuales") - el turno 10 realmente despachado sigue refiriendose a "la barra olímpica de 20kg" con la misma especificidad del turno 1, a traves de 3 rondas de compactacion y 8 turnos intermedios, con el Catalog Service real de este entorno inalcanzable (misma condicion ambiental que `V1.8-D5`-`V1.8-D6`) - sin ningun handoff ni fallback estructural en ninguno de los 10 turnos. Hallazgo real de este benchmark (documentado en el release doc, no anticipado por `V1.8-D5`/`V1.8-D6`): tres allowlists independientes (`BRAIN_WHATSAPP_TEST_WA_IDS`, `BRAIN_AUTONOMOUS_TEST_WA_IDS`, `BRAIN_SALES_AGENT_RUNTIME_WA_IDS`) deben coincidir con el mismo `wa_id` antes de que un turno alcance `SalesAgentRuntime`/compactacion, algo facil de pasar por alto al manejar el punto de entrada real de produccion directamente. Probado con 22 tests nuevos puros/de integracion (`sessionCompactionPolicy.test.ts` 8, `compactAgentSessionHistory.test.ts` 7, `runSessionCompaction.test.ts` 7 contra MariaDB real) mas 5 nuevos en `agentSessionStoreMariaDb.test.ts` (atomicidad/concurrencia de `persistCompactedPrefix`) mas 2 nuevos en `runSalesAgentRuntimeCycle.test.ts` (wiring post-dispatch), mas regresion completa de `V1.8-D3`-`V1.8-D6` (118/118) y gobernanza/dispatch/Capability Gateway (290/290) sin ninguna falla nueva - la unica falla en toda la corrida es el mismo flake preexistente de milisegundo de `agentSessionStoreMariaDb.test.ts` que `V1.7`-`V1.8-D6` ya documentaron identicamente, confirmado sin relacion via `git stash` contra el baseline `develop` limpio (incluye tres archivos de la suite completa del repo cuyas fallas por `Missing DATABASE_NAME` reprodujeron identicas sin ningun codigo de `V1.8-D7` presente). `npx tsc --noEmit`, `npm run build` y `npm run lint` (0 errores, 40 warnings preexistentes identicos a `V1.8-D6`, ningun archivo de esta tarea) limpios. Sin migracion de base de datos. `BRAIN_R3_SESSION_COMPACTION_ENABLED` default `false` (palanca de rollback propia, independiente de la de `V1.8-D6`), adicionalmente condicionada a `persistentSessionCognitionEnabled` activo este turno (decision propia, evita compactar sesiones que nadie lee durante un rollback de D6). V1.8 final readiness: `READY_FOR_R3_V1_8_FINAL_PERSISTENT_MEMORY_CLOSURE`. Verdict: `R3_V1_8_D7_PERSISTENT_SESSION_COMPACTION_VALIDATED`. Detalle completo: [SALES-AGENT-R3-V1.8-D7 - Persistent Session Compaction](releases/SALES-AGENT-R3-V1.8-D7-PERSISTENT-SESSION-COMPACTION.md). Sin commit ni push al momento de esta entrada.

Este tercer workstream no cierra `ACS-R1-04` ni `ACS-R1-05`/`ACS-R1-05.1`, no altera su bloqueo/estado, y no depende del Customer Service externo (`PAUSED_EXTERNAL`).

## Commit aceptado

- `last_accepted_commit`: `102fc5e`
- `t06_1_sha`: `0c51419`
- `t06_2_sha`: `72cb9c3`
- `t07_sha`: `cd9317e`
- `t08_1_sha`: `102fc5e`

## Release spec

- [ACS-R1-04 - Customer Identity Resolution + Onboarding](releases/ACS-R1-04-customer-identity-onboarding.md)

## Required reading

- [Autonomous Commerce PRD](product/autonomous-commerce-prd.md)
- [ROADMAP](ROADMAP.md)
- [MVP execution map](product/MVP_EXECUTION_MAP.md)
- [ACS-R1-04 release spec](releases/ACS-R1-04-customer-identity-onboarding.md)
- [Customer onboarding and identity contract](data/customer-onboarding-identity-contract.md)
- [Customer creation, linking and interest authority contract](data/customer-creation-linking-authority-contract.md)
- [Customer Service capability](capabilities/customer-service-capability.md)
- [Customer Service HTTP contract](integrations/customer-service-http-contract.md)
- [CAPABILITY_MATRIX](CAPABILITY_MATRIX.md)
- [Follow-up runtime reconciliation](audits/follow-up-runtime-reconciliation.md)
- [ACS-R1-05.1 release spec](releases/ACS-R1-05.1-persistent-commercial-memory-controlled-whatsapp-pilot.md)

## Nota operativa

`ACS-R1-04-T08` ejecuto pruebas end-to-end (cliente nuevo, cliente antiguo, conflicto) contra el flujo de identidad/onboarding ya conectado (`T06`/`T06.1`) y ahora instrumentado (`T07`). No reabrio autoridad ni la frontera de Customer 360. Queda `blocked: true` unicamente por la verificacion operacional real contra un Customer Service desplegado (inexistente en este repo/entorno) - la integracion completa (inbound -> `runNativeAutonomousCycle` -> `resolveNativeCustomerSession` -> `CustomerOnboardingService` -> Capability Gateway -> adapter HTTP -> `commercial_event` T07 -> estado final) esta probada y en verde contra una base `crm_test` desechable.

`ACS-R1-04-T08.1` (cerrada, dos incrementos) reconcilio el `customerMasterId` entre Customer Service y ACS: contrato HTTP v2.0.0 (breaking, `customerId` -> `customerMasterId` en los tres resultados exitosos), adapter fail-closed ante respuestas invalidas/incompletas, y un gate centralizado (`completeOnboardingWithVerifiedCustomer`/`verifyCustomerMasterProjection`) que verifica la proyeccion local `master_customer` antes de completar onboarding - sin eliminar la FK, sin que ACS inserte/actualice `master_customer`, sin tabla ni migracion nueva. Un segundo incremento (commit `102fc5e`) agrego la recuperacion runtime real: `resolveNativeCustomerSession` ahora tambien intenta `resolve_customer` de nuevo cuando el onboarding esta en `temporarily_unavailable` (no solo `required`/`collecting`), y un helper centralizado nuevo (`ensureResolving`, `onboardingTransitions.ts`) conecta el metodo de dominio `retryResolution` (existente desde T03, sin callers hasta ahora) - nunca un segundo `create_customer`, verificado con un turno real end-to-end (`tests/e2e/customerIdentityOnboarding.e2e.test.ts`, "T08-A6"/"T08-A7"), nunca llamando las funciones del gate directamente. Esto cierra el gap estructural que T08 encontro; el unico bloqueo restante de T08 es la falta de un Customer Service desplegado para el smoke operacional. Ver la release spec para el detalle completo.

`ACS-R1-04-T07` (cerrada, commit `cd9317e`) persistio evidencia durable de resolucion de identidad (local/externa), transiciones efectivas de onboarding, business outcomes de `resolve_customer`/`create_customer`/`link_external_identity` (separados del status tecnico del Gateway) y warnings estructurados, todo sobre `commercial_event` existente - sin tabla nueva, sin duplicar `crm_capability_executions`, sin cambiar autoridad ni el orden pre-plan/post-plan. Tambien redacto `request_summary_json`/`response_summary_json` de las tres capabilities de identidad en `crm_capability_executions` (antes texto crudo con telefono/email/wa_id). Ver evidencia completa en la release spec.

`ACS-R1-04-T06.2` (previa a T07, ya cerrada) reconcilio `resolveOrCreateNativeCustomer` (renombrada `resolveOrPersistNativeExternalIdentity`) y elimino el dominio paralelo `lib/domains/customer-identity-onboarding` introducido fuera de secuencia por `PR #43`.

## Regla de actualizacion

Cuando la tarea actual termine:

1. Cambiar su estado a `done` en la release spec.
2. Mover la siguiente tarea a `in_progress`.
3. Actualizar `current_task` y `next_task` aqui y en la release spec.
4. Actualizar `last_accepted_commit`.
5. Actualizar `docs/CAPABILITY_MATRIX.md` si cambio la implementacion real.
6. No crear una auditoria por cada tarea; crear auditoria solo al cerrar una release.
